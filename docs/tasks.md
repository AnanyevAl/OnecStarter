# Бэклог

Статусы: `TODO` · `WIP` · `DONE` · `BLOCKED`

---

## T-01. Доменные скилы проекта — `DONE`

Написаны через `writing-skills` по циклу RED-GREEN-REFACTOR.

| # | Задача | Статус |
| --- | --- | --- |
| T-01.1 | Материал из первоисточников (ИТС, Приложения 3 и 7, версии 8.3.18…8.5.4) → `docs/research/` | DONE |
| T-01.2 | RED: baseline-сценарии на субагентах без скилов | DONE |
| T-01.3 | GREEN: `.claude/skills/v8i-format/` | DONE |
| T-01.4 | GREEN: `.claude/skills/platform-launch/` | DONE |
| T-01.5 | Проверка сценариями на субагентах + REFACTOR | DONE |

Соглашения по фикстурам и сборке скилами не стали — по правилу `writing-skills` они
project-specific и живут в `CLAUDE.md`.

### Что дал цикл

RED зафиксировал уверенные ошибки агента без скилов: UTF-8 «с BOM» с выдуманным обоснованием;
«секций-групп в формате нет» с изобретённым sidecar-файлом под несуществующую проблему;
«`DefaultVersion` — не ключ `.v8i`, скорее всего ручная правка»; разрядность «выбирается деревом
установки»; `OrderInList` как целое с предложением пересчитать в плотную нумерацию.

GREEN закрыл все эти ошибки.

REFACTOR вскрыл дефект в самом скиле: утверждение «маска разрешается в максимальную установленную
с этим префиксом» было моим выводом, а не фактом из ИТС — там сказано «максимальный номер
установленной версии» без оговорки про префикс. Понижено до непроверенного с обеими трактовками.

---

## T-02. Эксперименты на реальной платформе — `DONE`

Протокол и результаты — [research/t02-protocol.md](research/t02-protocol.md).
Проведено 31.07.2026 (две сессии) на платформе 8.3.25.1633 (установлены 8.3.10…8.3.27.2214).

| # | Статус | Итог |
| --- | --- | --- |
| T-02.1 | DONE | Маска → максимум **с этим префиксом** (8.3.25 → 8.3.25.1633) |
| T-02.2 | DONE | Точная `Version` > `DefaultVersion` > маска; ИТС опровергнут |
| T-02.3 | DONE | `Folder` группы = путь родителя; мастер стартера создаёт группы, вложенность поддерживается |
| T-02.4 | DONE | Блокировки файла нет ни при открытом стартере, ни при работающем клиенте |
| T-02.5 | DONE | Посторонний ключ `XTest=1` переживает перезапись (уезжает в конец секции); перезапись — только при запуске из окна стартера, CLI-запуск файл не трогает |
| T-02.6 | DONE | Файловая база: и `App=Auto`, и явный `App=ThinClient` → `1cv8c.exe`; при явном `App` стартер не передаёт `/AppAutoCheckMode` |
| T-02.7 | DONE* | Стартер не передаёт `/WA`; при `/F` ключи `/WA+`, `/WA-` и их отсутствие дают одинаковый диалог (на базе без ОС-аутентификации); кейс с настроенной ОС-аутентификацией не проверен |
| T-02.8 | DONE | Несуществующая версия → молча максимум вообще; `DefaultVersion` пишется **запрошенный** |
| T-02.9 | DONE | Пробелы вокруг `=` ломают распознавание ключа: база деградирует до группы, платформа при перезаписи удаляет её базовые ключи |

Результаты возвращены в скилы `v8i-format` и `platform-launch` с метками **[Ф]**.

### Исходная постановка

Открытые вопросы, которые меняют архитектуру, а не детали. Закрыть до написания кода запуска.

| # | Вопрос | Метод |
| --- | --- | --- |
| T-02.1 | Чем разрешается неполная версия: максимум с префиксом или максимум вообще | **[Ф]** база с `Version=8.3.25` в списке уже есть — запустить, снять фактический путь |
| T-02.2 | Приоритет `DefaultVersion` над `Version` на практике | Конфликтующие значения → снять `ExecutablePath` |
| T-02.3 | Семантика `Folder` у секции-группы; вложенные группы | Создать группу второго уровня в штатном стартере, снять diff файла |
| T-02.4 | Держит ли `1cestart.exe` `ibases.v8i` заблокированным | Открыть стартер, попытаться записать |
| T-02.5 | Полный набор ключей, которые платформа дописывает сама | Менять свойства базы в стартере, снимать diff |
| T-02.6 | `1cv8c.exe` или `1cv8s.exe` для тонкого клиента файловой базы | Запустить, снять `ExecutablePath` |
| T-02.7 | Как воспроизвести `WA=1` («пытаться, потом спросить») ключами командной строки | Эксперимент с недоступной ОС-аутентификацией |
| T-02.8 | Поведение при `Version=8.3.99.1` и `AppAutoInstallLastVersion=0` | Эксперимент |

Инструмент проверки:

```powershell
Get-CimInstance Win32_Process -Filter "Name='1cv8.exe' OR Name='1cv8c.exe'" |
  Select-Object ProcessId, ExecutablePath, CommandLine
```

Результаты возвращаются в скилы: подтверждённое повышается до **[Ф]**, опровергнутое исправляется.

> T-02.7 стоит отдельно: `WA=1` в `.v8i` означает «пытаться аутентификацию ОС, при неудаче
> спросить логин/пароль», а `/WA+` означает «обязательная». Точного эквивалента в командной строке
> нет — откат придётся реализовывать самим, и неясно, как детектировать неуспех у GUI-клиента.

---

## T-03. Требования — `DONE`

Собраны в сессии brainstorming 30.07.2026: [requirements.md](requirements.md) заполнен,
дизайн v1 утверждён — [superpowers/specs/2026-07-30-v1-core-design.md](superpowers/specs/2026-07-30-v1-core-design.md).

Ключевые решения: публичный open source (MIT/Apache-2.0) для специалистов 1С;
роадмап v1 (ядро запуска ИБ) → v2 (серверы/консоль) → v3 (конструктор параметров
CLI) → v4+ (ТЖ, публикации, EDT); форма — рабочее место с боковой навигацией,
тёмный стиль по мотивам developer.1c.ru.

---

## T-04. Планы реализации v1 — `WIP`

Реализация разбита на серию планов, каждый даёт работающий тестируемый результат.

| # | План | Статус | Блокеры |
| --- | --- | --- | --- |
| T-04.1 | [Слой `config`: v8i round-trip, атомарная запись, 1cestart.cfg](superpowers/plans/2026-07-31-v1-plan1-config-core.md) | DONE | нет |
| T-04.2 | [`platform_1c` + `domain`: реестр версий, обнаружение, выбор версии, командная строка](superpowers/plans/2026-07-31-v1-plan2-platform-domain.md) | DONE | нет (T-02 закрыт) |
| T-04.3 | [`services`: сборка модели, наши данные в `%APPDATA%`, координатор](superpowers/plans/2026-08-04-v1-plan3-services.md) | DONE | нет (T-02.5 закрыт) |
| T-04.4 | [Операции над группами и дедупликация источников](superpowers/plans/2026-08-04-v1-plan4-groups-and-dedupe.md) | DONE | нет |
| T-04.5a | [UI «Базы»: просмотр и запуск](superpowers/plans/2026-08-07-v1-plan4a-ui-bases.md) | DONE 08.08.2026 — слито в `master` линейной историей (32 коммита). Открыт дефект показа контекстного меню, задача 15 плана: чиним вместе с 4b по решению заказчика | нет |
| T-04.5b | [UI «Базы»: правки списка, витрина размещения, светлая тема, `F3`/`F4`](superpowers/plans/2026-08-08-v1-plan4b-ui-edit.md) | **DONE 10.08.2026** — слито в `master` линейной историей (116 коммитов, fast-forward). 1000 тестов, `ruff` и `mypy` коды 0. Ручные smoke №1 (08.08) и №2 (09.08) пройдены, замечания разнесены и закрыты. Задача 16 снята из v1 → v2, задача 20 добавлена по итогам smoke №2. Финальное ревью ветки на самой сильной модели нашло не сломанный код, а тесты, зелёные на сломанной реализации; закрыто двумя волнами, обе с точечным ре-ревью. Долг — раздел «Долг, вынесенный финальным ревью ветки 4b» ниже | нет |
| T-04.6 | Поставка: сборка exe, установщик, публикация на GitHub; диагностика молчаливого отказа старта ([спека](superpowers/specs/2026-08-16-v1-delivery-design.md), [план](superpowers/plans/2026-08-16-v1-delivery.md)) | **DONE 16.08.2026.** Старт перестроен (окно до фонового обнаружения, потоки-демоны, отменяемый прогресс у ярлыков), молчаливый отказ исключён (лог, faulthandler, перехват с MessageBoxW, консольный exe), PyInstaller one-dir + smoke собранного экземпляра + Inno Setup per-user; 1083 теста, ruff/mypy чисты; сборка 0.1.0: dist 123 МБ, zip 53 МБ, setup 35 МБ. Единый фирменный значок — замечание заказчика с контрольной точки. Публикация на GitHub — свежий корень, той же датой | T-04.5b |
| T-04.7 | Функции настроек: закрытие в трей, глобальный хоткей, лимит «Недавних», автозапуск ([спека](superpowers/specs/2026-08-19-settings-functions-design.md), [план](superpowers/plans/2026-08-19-settings-functions.md)) | **DONE 20.08.2026.** Четыре настройки в разделе «Настройки»: закрытие в трей (тумблер), глобальный хоткей (поле захвата сочетания, перерегистрация на лету `GlobalHotkey.rebind`, единый писатель `SettingsStore`, `Alt+F4` запрещён решением заказчика), лимит «Недавних» (0–50, асимметричный клэмп — решение заказчика), автозапуск при входе в Windows (`HKCU\...\Run`, истина только в реестре, спека §3.1). 1214 тестов (было 1083 на T-04.6, +131 в этой ветке), `ruff` и `mypy` коды 0. Защитные тесты вехи мутационно проверены: мутации ставили не авторы тестов — раздел «Закрытие находок финального ревью ветки» ниже; кириллическая раскладка и цикл установщика с реестром — непроверены честно, см. там же. Финальное ревью всей ветки нашло девять пунктов (два теста, зелёных на сломанной реализации; условие показа окна читало не тот признак; `Alt+F4` не запрещён; асимметрия клэмпа не решена; тумблер автозапуска не досинхронизировался; докстринг `is_enabled` обещал самолечение; `dispose()` не снимал нативный фильтр) — закрыты одной волной, раздел ниже | T-04.6 |

### Долг, вынесенный финальным ревью ветки 4b (10.08.2026)

Ревью нашло не сломанный код, а **тесты, зелёные на сломанной реализации**, —
и доказало это мутациями. Основное закрыто двумя волнами; ниже то, что
осознанно вынесено за их пределы. Слияние не блокирует.

| # | Долг | Почему вынесено |
| --- | --- | --- |
| 1 | **Второй рубеж валидации размещения в `services`.** `build_connect(FILE, file_path="")` в обход диалога вернёт `File="";`, и `add_infobase` примет. У имени рубежа два (`validate_section_name` в services), у размещения — один, и тот в UI, куда инвариант 1 логику не пускает | **Закрыт (18.08.2026):** `validate_connect` в `services/model.py`, вызывается из `apply_patch`: ADD требует `Connect` (секция без него — группа, [Ф] T-05.6), UPDATE проверяет новое значение; пустой фрагмент размещения **вида записи** (`PLACEMENT_FRAGMENTS[classify_connect]`) отвергается, отсутствующий или чужого вида — не требуется (уточнение волны финального ревью, см. абзац ниже); дубли ключей изменений в разном регистре отвергаются |
| 2 | **Десять директив `# noqa: …, RUF100`** прикрывают устаревшие `RUF001`/`RUF002` вместо их удаления. Остаток восстановления через `ruff --add-noqa` | **Закрыт (18.08.2026):** сняты все директивы с `RUF100` (по факту их было 11), `ruff` чист без них |
| 3 | **Спека §3.2:** обоснование «диалог только для чтения потребовал бы второго режима» устарело — механизм построен задачей I8; и цена завышена, путь подключения записи общего списка виден в `ConnectionPanel` | **Закрыт (18.08.2026):** в §3.2 датированная правка — цена сужена до таблицы прочих ключей, довод про «второй режим» снят, неактивные «Свойства…» зафиксированы как осознанный выбор |
| 4 | **Три копии обходчика дерева** в `tests/ui/test_bases_view.py` | **Закрыт (18.08.2026):** к этому моменту копий стало семь (рестайл и поставка добавили свои) — все сведены к одному генератору `_iter_tree` + `_index_of_key` |
| 5 | **CRLF в части файлов**, `.gitattributes` нет. Диф `view.py` из-за этого раздувался с 88 строк до 2912, blame по файлу испорчен | **Закрыт T-04.6 (16.08.2026):** `.gitattributes` + перенормализация 19 файлов; фикстуры защищены `tests/fixtures/** -text` и остались байт-в-байт |
| 6 | **Коммит `4121853` хранит в истории фикстуру с настоящим SID** пользователя. Remote нет, в `master` коммита нет, наружу не ушло | **Закрыт T-04.6 (16.08.2026):** решение заказчика — публикация со свежим корнем; полная история осталась локальной веткой `archive/pre-publish-history`, которая не пушится никогда |
| 7 | **Шаг 8 задачи 17** — ярлык на собранном `OneCStarter.exe` машинно не проверен: ветка `frozen=True` под тестами не исполняется, `build/` в репозитории нет | **Закрыт T-04.6 (16.08.2026):** smoke каждой сборки (`build/build.ps1`) исполняет frozen-ветку и сверяет цель ярлыка штатным читателем `.lnk` |

Закрытие долгов №1–4 и миноров T-04.6 (ветка `chore/2026-08-18-review-debts`,
18.08.2026) прошло финальное ревью ветки на самой сильной модели. Ревью
подтвердило два дефекта первой редакции рубежа №1 — обход дублем ключа
изменений в разном регистре (воспроизведён исполнением: валидировалось первое
совпадение, писались все, `V8iSection.set` сливает «последний побеждает»)
и запирание правки живой записи пустым фрагментом чужого вида, — оба закрыты
волной правок с точечным ре-ревью. Мутационные проверки ставил отдельный
агент, не автор тестов: рубеж `validate_connect`/`apply_patch` — 4/4 убиты
до волны и 6/6 после неё, сторож «текст исключения не в лог» — 3/3.

### Открытые вопросы T-04.6 (16.08.2026)

| # | Вопрос | Что делать |
| --- | --- | --- |
| 1 | **`.lnk` с классами `0x35`/`0x36` не разрешается штатным `IShellLink` на Windows Server** (github-runner, en-US): все четыре поля ярлыка пусты, тогда как десктопная Windows 11 читает и запускает ([Ф] 16.08.2026, докстринг `_path_item` в `config/shell_link.py`). Причина не установлена: версия оболочки или кодовая страница | Эксперимент на en-US десктопной Windows; если дело в кодовой странице — пользователи латинских локалей получат нерабочие ярлыки, и формат придётся дополнить ANSI-именем + extension block. На CI сторож исключён маркером `requires_windows_shell`, локальные прогоны гоняют его всегда |
| 2 | Ручной чек-лист контрольной точки (пп. 1–5, 7: холодный старт после перезагрузки, ярлык, консольный exe, удаление/переустановка) заказчик отложил — публикация прошла без него | **Закрыт (19.08.2026):** все пункты пройдены заказчиком чисто — установка per-user без UAC; холодный старт после перезагрузки (окно сразу, версии по мере обнаружения, лог с фазами — инцидент 15.08 не воспроизводится); тёплый старт; ярлык создаёт и запускает нужную базу и версию; консольный exe дублирует лог; удаление сохраняет `%APPDATA%\OneCStarter` и `ibases.v8i`, переустановка подхватывает. Находок нет |

### Отклонённый параллельный дизайн 4b — ветка `archive/2026-08-09-parallel-4b-design`

09.08.2026 сессия, не сверившая состояние репозитория, провела brainstorming 4b заново
и написала второй дизайн и второй план — при том, что 4b к тому моменту был реализован
до задачи 15 из 19. Три коммита сняты с `master` в архивную ветку; **сливать её никогда
не нужно.**

Она оставлена не как альтернатива, а как след: если кто-то найдёт в ней полный дизайн 4b,
это не забытая работа, а дубль. Отклонён по существу — в нём вырезание учётных данных
лежит в `services/`, что против инварианта 5, а разбор строки соединения свален
в `services/display.py` вместо отдельного модуля.

Причина сбоя и что с ней делать: оба дизайна писались прямо в `master`, поэтому
столкновение вскрылось только через сутки. Проектные документы 4c и дальше держать
на ветке своего плана — тогда расхождение видно сразу.

Вторая причина, и она тяжелее первой: статус T-04.5b в таблице выше до 09.08.2026
гласил «реализация не начата» — при 74 коммитах в ветке и работе, доведённой
до задачи 15. Сессия 09.08 сверилась с документом, документ соврал, и дальше она
действовала последовательно. Ровно тот случай, о котором правило `CLAUDE.md`
«документ, разошедшийся с кодом, врёт следующему исполнителю»: цена оказалась —
день на дубль и полдня на разбор. Там же не были проставлены 102 чекбокса плана 4b
при выполненных задачах 1–15. Оба хвоста приведены к факту 09.08.2026.
Вывод для процесса: статус плана обновляется тем же коммитом, что и переход
к следующей задаче, а не «потом одним махом» — иначе он не документ, а помеха.

### Замечания заказчика по ручному smoke 4a (07.08.2026)

Первый запуск на рабочей машине: 7 версий платформы, 64 записи, 6 групп, приложение
работает. Четыре замечания, все по показу — подробно в [спеке UI, §9](superpowers/specs/2026-08-05-v1-ui-bases-design.md).

| # | Замечание | Куда |
| --- | --- | --- |
| 1 | Контекстное меню базы отрисовано скомканно: подсказка сочетания налезает на название. Offscreen не воспроизводится ни в одном из 4 стилей Qt — причину снимать на реальном экране | задача 15 плана 4a → переезжает в 4b ([§7](superpowers/specs/2026-08-08-v1-plan4b-ui-edit-design.md)) |
| 2 | Не видно, файловая база или серверная — нужен значок вида размещения (`ConnectKind` уже различает FILE/SERVER/WEB/UNKNOWN, показа нет) | 4b |
| 3 | Панель свойств выделенной записи с путём подключения: копировать, открыть каталог для файловой — как в штатном стартере. Решено 07.08.2026: показывается **только путь подключения, без дополнительных параметров**; для веб-базы из URL вырезается `user:pass@` | 4b |
| 4 | `gui-scripts` на Windows проглатывает любую ошибку старта (нет консоли у `pythonw.exe`) — нужен лог или консольный entry point | T-04.6 |
| 5 | Переключение на светлую тему. Не только палитра: цвета запечены в `QBrush` элементов модели и в иконку трея, смена на лету требует пересборки обеих ([спека §10](superpowers/specs/2026-08-05-v1-ui-bases-design.md)) | 4b |

T-04.4 закрывает долги плана 3 (пункты 1 и 3 ниже) и три дефекта того же класса,
найденных при разборе: смена `Folder` у секции-группы разрушает дерево молча, удаление
группы оставляет сирот, создать группу нельзя ни одним вызовом. Идёт до UI, чтобы план UI
не упирался в отсутствующие операции.

### Обязательства из финального ревью плана 2 (01.08.2026)

| # | Пункт | Статус |
| --- | --- | --- |
| 1 | Guard секретов в `build_arguments` шире одного `Pwd` | DONE (03.08.2026) |
| 2 | `try/except OSError` на обход корня в `discover_installations` | DONE (03.08.2026) |
| 3 | При первом реальном запуске по `/IBConnectionString` проверить форму присоединения значения к ключу и вернуть результат в скил `platform-launch` (допущение решения 1 плана 2) | TODO → T-05.1 (форма ключа) и T-05.2 (запуск из общих списков) |

### Обязательства из финального ревью плана 3 (04.08.2026)

Полный перечень — раздел 10 [дизайна плана 3](superpowers/specs/2026-08-03-v1-plan3-services-design.md);
там же раздел 11 фиксирует, чем реализация отличается от первоначальных формулировок дизайна
и почему. Кратко, к исполнению:

| # | Пункт | Кому |
| --- | --- | --- |
| 1 | Каскадное переименование секции-группы: сейчас переименование группы запрещено, потому что `Folder` потомков не переписывался и операция молча разрушала дерево | DONE (T-04.4) |
| 2 | `PPasswd` (зашифрованный пароль прокси, ключ секции) не распознаётся `security.secrets.is_secret_key`. Сегодня недостижим — функция применяется только к фрагментам `Connect`; станет достижим, когда UI покажет свойства записи | План UI |
| 3 | Запись, присутствующая и в пользовательском списке, и в общем, выдаётся `items()` дважды: дедупликации по ключу привязки между источниками нет, политика в дизайне не задана | DONE (T-04.4) |
| 4 | Эксперименты: поведение `/IBName` при совпадении имён без учёта регистра; трактовка `Connect=` с пустым значением штатным стартером | T-05.3, T-05.6 |
| 5 | Эксперименты, добавленные дизайном T-04.4: регистр при сопоставлении `Folder` с именем группы; видит ли стартер группу, созданную без `OrderInTree`; что стартер делает с содержимым при удалении группы | T-05.7, T-05.8, T-05.9 |

Пункт 1: список ключей из ревью (`DBPwd`, `SPwd`, `wsppwd`) оказался неполным — по справочнику
`platform-launch` пароль несёт и `wsp` (пользователь веб-сервера). Знание о том, что является
секретом, вынесено в `security/secrets.py` (инвариант 5 `CLAUDE.md`): там же оно понадобится
для показа командной строки в сообщениях об ошибке без секретов (спека §4). Сверка идёт
по полному имени ключа — `wsp` секрет, `wspuser`/`wspsrv`/`wspport` нет; дополнительно
отклоняется любое неизвестное имя на `pwd`.

### Мутационные проверки вехи «Функции настроек» (20.08.2026)

Правило проекта (`CLAUDE.md`): тест, зелёный на пустышке, хуже отсутствующего — он создаёт
ложную уверенность; мутацию ставит **не автор теста**. По перечисленным ниже тестам
мутация поставлена агентом, не писавшим этот тест, результат воспроизведён и мутация
откачена; дерево после каждой мутации возвращено в чистое состояние (`git status --short`
пуст), полный прогон снова зелёный.

**Исправлено 20.08.2026.** Прежняя формулировка («по каждому защитному тесту вехи»)
завышала охват — опровергнута находкой 1 финального ревью ветки (раздел ниже,
«Закрытие находок финального ревью ветки»): у `test_disabled_hotkey_sets_the_plain_tooltip`
мутации не ставил никто, и тест был бессилен — совпадение значения тултипа по умолчанию
с тем, что ставит `create_tray` при создании трея, маскировало отсутствие проверки.
Пункт 6 того же ревью — второй пробел того же рода (`blockSignals` в `settings_view.py`
без единого теста). Формулировка сужена до факта: не «каждый», а «перечисленные».

Четыре мутации из исходной таблицы плана (`task-11-brief.md`):

| № | Тест | Мутация | Кто ставил | Результат дословно |
| --- | --- | --- | --- | --- |
| 1 | `tests/ui/test_settings_store.py::test_update_keeps_other_fields` | `SettingsStore.update`: `replace(self._settings, **changes)` → `Settings(**changes)` | ревьюер Task 5 | «1 failed, 5 passed»; `AssertionError: assert True is False + where True = Settings(theme=<ThemeMode.DARK: 'dark'>, close_to_tray=True, hotkey='Ctrl+Alt+B', recent_limit=10).close_to_tray`». Откат чист, 6/6 |
| 2 | `tests/ui/test_hotkey.py::test_rebind_to_none_disables` | `GlobalHotkey.rebind` (`src/onecstarter/ui/hotkey.py`): убран ранний выход `if spec is None: return True` | исполнитель Task 11 | Плановая формулировка ожидала «падение на непустом `calls["register"]`»; фактически тест падает раньше — исключением `AttributeError: 'NoneType' object has no attribute 'modifiers'` на `hotkey.py:59` (`spec.modifiers` при `spec is None`), возбуждённым из `assert hotkey.rebind(None) is True` (`test_hotkey.py:96`). Коллатерально падают ещё два теста в `test_app.py`, использующие настоящий `GlobalHotkey`: `test_disabled_hotkey_is_not_registered`, `test_disabled_hotkey_sets_the_plain_tooltip` — та же ошибка. Итог по всему сьюту: 3 failed, 1204 passed. Откат чист, 1207/1207 |
| 3 | `tests/ui/test_settings_view.py::test_autostart_writes_registry_not_settings_file` | `_choose_autostart` + поле `autostart` в `Settings` + `store.update(autostart=checked)` | исполнитель Task 8 (самоотчёт, обе половины лично), ревьюер воспроизвёл | Плановая редакция теста (проверка только отсутствия ключа в JSON) мутацию **не поймала** — «1 passed» (дефект плана, см. «Главный урок» ниже). После усиления теста сравнением `store.settings == Settings()`: «AssertionError: assert Settings(them...utostart=True) == Settings(them...tostart=False); Differing attributes: ['autostart']; autostart: True != False». Откат чист, 1191 passed |
| 4 | `tests/ui/test_app.py::test_disabled_hotkey_is_not_registered` | `apply_hotkey`: `parse_hotkey(text)` → `parse_hotkey(text) or parse_hotkey(DEFAULT_HOTKEY)` | исполнитель Task 9, дословно воспроизвёл ревьюер | «assert [(3, 66)] == []» |

Оговорка к мутации №2: тест ловит её не своими `assert`-утверждениями, а самим фактом
краша. Исключение `AttributeError` возникает при вычислении `spec.modifiers` — ДО вызова
`self._register(...)`. Если бы это исключение было где-то подавлено, `calls["register"]`
остался бы пустым списком, и `assert calls["register"] == []` из теста прошёл бы. Сигнал
не слабее упавшего `assert` — необработанное исключение валит тест так же надёжно, — но
природа другая: тест поймал мутацию случайностью реализации (`None` не имеет атрибутов),
а не проверкой того поведения, которое он формально описывает.

Сверх исходной таблицы — мутации, поставленные не авторами соответствующих тестов (правило
требует независимости именно для тестов отказа операции и сохранности пользовательских
данных). Атрибуция по каждой — тем же принципом прозрачности, что и в строке 3 выше: где
исполнитель задачи прогнал мутацию сам (обычно как часть самоотчёта об исправлении находки
ревью) и это отдельно воспроизвёл ревьюер, названы обе стороны.

- **Task 3**, ревьюер напрямую (в самоотчёте исполнителя мутация не упоминается):
  `test_write_failure_reaches_caller`, обёртка enable/disable в
  `try/except OSError: pass` → «Failed: DID NOT RAISE OSError», остальные 7 тестов файла прошли.
- **Task 6**, ревьюер напрямую, поставлена и снята лично: пропуск `unregister` при
  совпадающем `spec` в `GlobalHotkey.rebind` → «assert [] == [41244]» (0xA11C = `HOTKEY_ID`).
- **Task 7**, ревьюер напрямую, три независимые мутации, каждая уронила ровно свой тест:
  стирание при непригодном нажатии → `test_widget_ignores_unusable_press`; Backspace/Delete
  не очищают → `test_delete_clears_to_disabled`; собственная проверка модификаторов вместо
  `parse_hotkey` → `test_shift_only_is_rejected`.
- **Task 9**, исполнитель Task 9 сам поставил и прогнал каждую (самоотчёт «Круг исправлений 1»
  в `task-9-report.md`), ревьюер затем воспроизвёл лично: тихий старт (условие заменено на
  `if False:`) → «assert [1] == []»; безусловная перестройка дерева → «assert 2 == 1», и
  обратная сторона (перестройка снята совсем) → «assert 0 == 1» — тест ловит обе стороны;
  провайдер лимита подменён на `lambda: 10` → «assert 10 == 17».
- **Task 10**, исполнитель Task 10 сам поставил и прогнал обе (самоотчёт «Круг исправлений» в
  `task-10-report.md`), ре-ревьюер воспроизвёл дословно: деинсталляция/smoke, `_log.info` →
  `_log.debug` и смена текста строки лога — тест упал на каждой из двух мутаций.

**Task 4**, вне рамки правила, обе стороны: исполнитель Task 4 прогнал мутацию (слайс
`[:recent_limit]` → `[:10]` над `display_forest`) сам, post-hoc вместо строгого красного
шага TDD — «assert False» на тесте-канарейке и «assert 5 == 2» на тесте обрезания, откат
чист. Ревьюер Task 4 независимо поставил ту же мутацию и получил те же два падения, откат
тоже чист. Правило проекта её не требовало: `display_forest` — чистая функция, тесты
табличные, а на них независимость не обязательна (`CLAUDE.md`); обе стороны прогнали её
сверх обязательного, каждая по своей инициативе.

Дополнительно, сверх задания Task 11 (тест не входил в исходную таблицу плана, проверка
по собственной инициативе исполнителя Task 11):
`tests/unit/test_settings.py::test_old_file_without_new_keys_reads_with_defaults` —
тест сохранности: файл настроек
прошлой версии (только `schema` и `theme`, без новых ключей) не должен уезжать в `.bad`.
Мутация в `load_settings` (`src/onecstarter/services/settings.py`) — схема отказа расширена
требованием всех пяти ключей (`required = {"schema", "theme", "close_to_tray", "hotkey",
"recent_limit"}`, `required.issubset(payload)` в условии `raise ValueError`), имитируя
реальную ошибку «забыли про обратную совместимость» — ровно тот сценарий, о котором
предупреждает докстринг модуля. Результат дословно:

```text
tests\unit\test_settings.py:122: in test_old_file_without_new_keys_reads_with_defaults
    assert load_settings(path) == Settings(theme=ThemeMode.LIGHT)
E   AssertionError: assert Settings(them...cent_limit=10) == Settings(them...cent_limit=10)
E     Differing attributes:
E     ['theme']
E       theme: <ThemeMode.AUTO: 'auto'> != <ThemeMode.LIGHT: 'light'>
```

Коллатерально в файле падают ещё 7 тестов (`test_unknown_theme_value_falls_back_to_auto`,
`test_empty_hotkey_means_disabled_not_default`, четыре кейса `test_recent_limit_is_clamped`) —
итого 8 failed, 23 passed в файле. Откат чист, 1207/1207.

### Непроверенное честно (веха «Функции настроек»)

Два пункта веха оставляет с меткой достоверности «не проверено» — не потому что забыто,
а потому что для проверки нужны условия, которых на этой машине нет:

| # | Пункт | Почему не проверено | Что нужно |
| --- | --- | --- | --- |
| 1 | Секция `[Registry]` установщика Inno Setup (запись автозапуска через инсталлятор) | На машине нет `ISCC.exe` — секция написана, но ни разу не скомпилирована и не прогнана | Машина с Inno Setup: цикл установка → включить автозапуск в приложении → удаление, проверить, что значение `OneCStarter` пропадает из `HKCU\...\Run` |
| 2 | Поведение захвата хоткея при кириллической раскладке (Ctrl+Alt+Б и т.п.) | Офскрин-тесты строят `QKeyEvent` с явным `Qt.Key`, в обход нативной трансляции скан-кодов Windows — драйвер клавиатуры не участвует | Живая машина с русской раскладкой: нажать сочетание с кириллической буквой, свериться с ожиданием «`parse_hotkey` вернёт `None` → отказ без стирания уже введённого» |

Логическая проверка для пункта 2 проведена (Task 7): любой кириллический код на входе
даёт `parse_hotkey() is None`, то есть деградация приемлема — но что физически посылает
драйвер Windows при таком сочетании, показывает только реальное железо.

### Главный урок вехи «Функции настроек»

Мутация №3 в исходной редакции таблицы плана оказалась **бессильной**: тест сравнивал
JSON-файл настроек до и после, то есть проверял сериализатор, а не то, тронуло ли
изменение состояние программы по существу. `save_settings` перечисляет ключи явным
списком — лишнее поле в `Settings` никогда не протекло бы в файл, и тест остался бы
зелёным на сломанной реализации сколько угодно долго. Тест усилен прямым сравнением
`store.settings == Settings()` — и та же мутация поймана мгновенно. Урок: мутационная
проверка обязана убить мутацию на прямом сравнении состояния, а не на побочном
артефакте вроде формата файла на диске — иначе она проверяет не то, что заявлено.

### Закрытие находок финального ревью ветки (20.08.2026)

Финальное ревью всей ветки `settings-functions` (HEAD `7a98892`, 1207 тестов) вынесло
вердикт «нельзя сливать без правок», девять пунктов. Все закрыты одной волной; полный
отчёт — `.superpowers/sdd/2026-08-19-settings-functions/final-review-fix-report.md`
(в репозиторий не идёт, леджерный документ приватного разбора). Здесь — мутационные
проверки защитных тестов этой волны, требуемые правилом проекта.

| № | Тест | Мутация | Результат дословно |
| --- | --- | --- | --- |
| 1 | `tests/ui/test_app.py::test_clearing_a_busy_hotkey_resets_the_tooltip_to_plain` (переименован и переписан из `test_disabled_hotkey_sets_the_plain_tooltip` — прежняя версия сравнивала тултип со значением по умолчанию, которое `create_tray` ставит ещё до логики хоткея, и была бессильна, см. абзац выше) | `apply_hotkey` (`ui/app.py:405-408`): убрана строка `_set_tray_tooltip(tray, None)` в ветке выключенного хоткея | `AssertionError: assert 'OneCStarter — Ctrl+Alt+B занято другим приложением' == 'OneCStarter'` (тултип не вернулся к чистому после очистки поля). Откат чист, 1214/1214 |
| 2 | `tests/ui/test_app.py::test_main_start_hidden_with_tray_and_close_to_tray_off_keeps_the_window_hidden` (новый — четвёртая комбинация условия показа окна) | `main()` (`ui/app.py`): условие показа окна возвращено к `if start_hidden and window.close_to_tray:` (было исправлено на `window.tray_available`) | `AssertionError: скрываться было чем — окно не показывается; assert [1] == []` (окно показалось, хотя трей жив). Откат чист |
| 3а | `tests/ui/test_app.py::test_build_main_window_disposes_hotkey_and_removes_filter_together_on_quit` (заменяет `test_main_disposes_the_hotkey_on_quit` — тот сравнивал подключённый слот с `hotkey.dispose` напрямую, что перестало быть верным после обёртки `dispose_hotkey`) | `_build_main_window` — `aboutToQuit` подключён напрямую к `hotkey.dispose` (обёртка `dispose_hotkey` убрана целиком) | `StopIteration` — среди подключённых слотов не нашлось функции с именем `dispose_hotkey`. Откат чист |
| 3б | (тот же тест) | `dispose_hotkey` оставлена, но без строки `application.removeNativeEventFilter(hotkey)` | `AssertionError: assert [] == [<_FakeHotkey ...>]` (`removeNativeEventFilter` не вызван). Откат чист |
| 4 | `tests/ui/test_app.py::test_run_smoke_disposes_the_hotkey_before_returning` (новый) | `run_smoke` (`ui/app.py`): тело `finally` заменено на `pass` | `AssertionError: assert False is True` (`window.global_hotkey.disposed` осталось `False`). Откат чист |
| 5 | `tests/ui/test_settings_view.py::test_autostart_toggle_resyncs_on_show` (новый) | `SettingsView.showEvent` — убран вызов `self._sync_autostart()` | `AssertionError: assert True is False` (тумблер не подхватил внешнюю правку реестра при показе). Откат чист |
| 6а | (тот же тест) | `_set_autostart_state` — сняты `blockSignals(True)`/`blockSignals(blocked)` вокруг `self._autostart.setChecked(checked)` | `AssertionError: assert ['OneCStarter'] == []` (приведение тумблера к прочитанному факту само вызвало `registry.delete` — ровно риск, который глушение сигналов предотвращает). Откат чист |
| 6б | `tests/ui/test_settings_view.py::test_sync_does_not_bounce_external_settings_changes_back_into_the_store` (новый) | `_sync` — сняты оба `blockSignals` вокруг `_tray.setChecked`/`_recent.setValue` | `AssertionError: ... Left contains 2 more items, first extra item: {'close_to_tray': False}` (один явный `store.update` породил три вызова вместо одного — `changed` → `update` → `changed` эхом). Откат чист |

Мутации на пунктах 3 и 4 спеки (Alt+F4, границы «Недавних») — табличные тесты чистых
функций, правило `CLAUDE.md` независимую мутацию не требует. Прогнаны сверх обязательного
по собственной инициативе для очистки совести:

- `services/hotkeys.py::parse_hotkey` — снята проверка `key == _FORBIDDEN_KEY and modifiers
  == _FORBIDDEN_MODIFIERS`: `tests/unit/test_hotkeys.py::test_rejects_invalid_combinations[Alt+F4]`
  падает — `AssertionError: assert HotkeySpec(modifiers=1, vk=115, key='F4') is None`. Откат чист.
- `services/settings.py::_recent_of` — возвращена прежняя формула `max(RECENT_MIN,
  min(RECENT_MAX, value))`: `tests/unit/test_settings.py::test_recent_limit_is_clamped[-3-10]`
  падает — `AssertionError: assert 0 == 10`. Откат чист.

Пункты, закрытые не так, как предписывала находка, — с обоснованием:

- **Пункт 9** заменил тест `test_main_disposes_the_hotkey_on_quit` новым, а не починил
  на месте: старая проверка сравнивала подключённый к `aboutToQuit` слот с `hotkey.dispose`
  через `disconnect` — сравнение стало неверным, как только слотом стала обёртка
  `dispose_hotkey`. Новый тест сильнее прежнего (доказывает и снятие регистрации, и
  `removeNativeEventFilter` вместе, а не только факт подписки) — не ослабление, замена.
- Остальные восемь пунктов закрыты предписанным способом.

---

## T-05. Эксперименты на платформе, сессия 2 — `WIP` (блок В не проводился; T-05.12/13 → v2, добавлен T-05.14)

Проверка допущений слоя запуска и формата, накопленных ревью планов 2–4, до плана UI.
Решение заказчика 05.08.2026: сначала эксперименты, потом brainstorming и план UI
(запуск процессов 1С в рамках протокола согласован тогда же). Протокол с шагами
и картой «куда возвращается результат» — [research/t05-protocol.md](research/t05-protocol.md).

Блок А блокирует T-04.5: исходы меняют код запуска, а не детали. Блок Б правит
формат/группы. Блок В — опциональный, план UI не блокирует.

Блок А проведён агентом 05.08.2026, блок Б — 05.08.2026 сессией «заказчик у стартера,
агент снимает diff'ы» (автоматизация окна выбора ненадёжна, см. журнал T-05.5).
Результаты в протоколе. Блок В (T-05.10–T-05.12) не проводился — план UI не блокирует.

| # | Вопрос | Статус | Итог |
| --- | --- | --- | --- |
| T-05.1 | Форма присоединения значения к `/IBConnectionString` | DONE | **[Ф]** наша форма верна (значение прижато, кавычки удвоены), путь с пробелом работает; код без правок |
| T-05.2 | Находит ли `/IBName` базу из общего списка (`CommonInfoBases`) | DONE | **[Ф]** находит; клиент материализует запись в `ibases.v8i` с `External=1` полной перезаписью; ненайденное имя → окно выбора, не ошибка |
| T-05.3 | `/IBName` при дублях имён; регистр имени | DONE | **[Ф]** «Не уникальное имя…» при дублях, в т.ч. различающихся только регистром; поиск имени регистронезависим. **Код: casefold в `_reject_ambiguous_name`** |
| T-05.4 | Держит ли `/AppAutoCheckVersion-` нашу версию при иной `Version` секции | DONE | **[Ф]** держит; с включённой проверкой платформа перезапускает клиента версией секции. Модель запуска подтверждена |
| T-05.5 | `DefaultVersion` вне маски `Version` (остаток T-02.2) | DONE | **[Ф]** `DefaultVersion` побеждает маску даже вне её; неустановленный → молча максимум вообще. **Код: `resolve_version` расходится с платформой в обоих случаях** |
| T-05.6 | Пустой `Connect=` — группа или база | DONE | **[Ф]** группа; перезапись довершает превращение (удаляет `Connect=` и `Version`). **Код: `is_group` учитывает пустое значение — сделано** |
| T-05.7 | Регистр при сопоставлении `Folder` с именем группы | DONE | **[Ф]** регистрозависимо; несовпавший путь → неявный узел без секции, регистр не нормализуется. **Код: `group_binding_key` с точным регистром — сделано** |
| T-05.8 | Видит ли стартер группу без `OrderInTree` | DONE | **[Ф]** видна и безвредна, `OrderInTree` платформа дописывает сама; `groups._create` без правок |
| T-05.9 | Что стартер делает с содержимым при удалении группы | DONE | **[Ф]** молчаливый каскад; подтверждение «Удалить группу "имя"?» без слова о содержимом. Тексты для плана UI |
| T-05.10 | Локальный кэш базы (опц., под фичу очистки кэша) | TODO | опционально |
| T-05.11 | Форма URL веб-базы из `ws` (опц.) | TODO | опционально |
| T-05.12 | `/WA+` при настроенной ОС-аутентификации (опц., остаток T-02.7) | → v2 | не на чем ставить: ни одна база заказчика не входит по учётной записи Windows |
| T-05.13 | Как заставить платформу **проигнорировать сохранённые `Usr`/`Pwd` и `WA`** и спросить логин ([спека 4b, §6.2](superpowers/specs/2026-08-08-v1-plan4b-ui-edit-design.md)) | шаг 0 DONE 09.08.2026, остальное → v2 | **[Ф]** 58 записей баз: с `Usr` — 0, с паролем — 0, с `WA=1` — все 58. Игнорировать нечего, `WA` инертен. Задача 16 плана 4b снята из v1 решением заказчика |
| T-05.14 | Изолируется ли процесс 1С подменой `APPDATA` | DONE 09.08.2026 | **[Ф]** нет: путь к списку платформа берёт не из переменной окружения. Снято дважды — `1cestart.exe` переписал живой файл вместо копии; клиент по `/IBName` не нашёл имя, существующее только в копии. Плюс **[Ф]** запуск по `/IBName` меняет `mtime` живого `ibases.v8i`, не меняя содержимого. Возвращено в скил `platform-launch`, факты 12 и 13 |

T-05.4 и T-05.5 добавлены при сверке протокола с кодом: первый — потому что вся модель
запуска (версию разрешаем сами + `/AppAutoCheckVersion-`) держится на неснятом [Д],
второй — открытый остаток T-02.2, который `resolve_version` сейчас решает экстраполяцией.

### Обязательства из блока А (код и скилы)

| # | Пункт | Кому |
| --- | --- | --- |
| 1 | `_reject_ambiguous_name`: сравнение имён на casefold + тест (платформа считает дублями имена, различающиеся регистром) | DONE 05.08.2026 |
| 2 | `resolve_version`: `DefaultVersion` вне маски и неустановленный `DefaultVersion` — привести `fallback` к фактическому поведению платформы (наш выбор версии может отличаться осознанно, но подсказка «что запустил бы штатный стартер» врать не должна) + две строки в табличные тесты | решение принято 05.08.2026: гибрид ([спека UI, §4](superpowers/specs/2026-08-05-v1-ui-bases-design.md)), реализация в плане 4a |
| 3 | Снять допущение-комментарий у `/IBConnectionString` в `build_arguments`; в докстринге `services/launch.py` убрать «[не проверено]» про общие списки | DONE 05.08.2026 |
| 4 | План UI: дубль «пользовательская + общая» после первого запуска общей базы — штатное состояние (`External=1`), дедупликация T-04.4 по `ID` обязательна; отказ платформы при ненайденном имени — окно выбора, поэтому диагностика «имя не найдено» — наша | план UI |

### Обязательства из блока Б (код и скилы)

| # | Пункт | Кому |
| --- | --- | --- |
| 1 | `is_group`: пустое значение `Connect=` — группа ([Ф] T-05.6), тест перевёрнут с опровергнутого [Р] | DONE 05.08.2026 |
| 2 | `group_binding_key`: ключ группы без ID — путь с точным регистром ([Ф] T-05.7); расхождение `binding_key`/`paths` из ревью плана 4 закрыто в пользу регистрозависимости | DONE 05.08.2026 |
| 3 | План UI: подтверждение `GroupRemoval.RECURSIVE` обязано перечислять содержимое или его количество — платформа каскадит молча («Удалить группу "имя"?»), быть не хуже недостаточно, надо лучше | план UI |
| 4 | План UI: секция с пустым `Connect=` в списке — это группа, которую первая же перезапись платформой «канонизирует» с потерей `Version`; показывать как группу, править как группу | план UI |
| 5 | План UI: висячий `Folder` платформа рисует неявным узлом-группой без секции ([Ф] T-05.7), наш `build_tree` кладёт запись в корень с пометкой orphan (решение 3 плана 3) — решить: повторить платформу или оставить корень+пометку с объяснением расхождения. Если рисуем неявный узел — у него нет секции и ключа, операции над ним невозможны | план UI |

---

## T-06. Рестайл UI по мокапу 4b — `DONE`

Заказчик закрепил новый целевой вид документом 15.08.2026: «новый дизайн приятнее»
([мокап](superpowers/specs/assets/2026-08-08-v1-plan4b-mockup.html)). Дизайн-дельта —
[спека](superpowers/specs/2026-08-15-v1-restyle-design.md), реализация по семи задачам —
[план](superpowers/plans/2026-08-15-restyle.md), ветка `feature/2026-08-15-restyle`
(13 коммитов от `master`, линейная история, без слияний).

Светлая палитра инвертирует иерархию поверхностей (приподнятое — белое на сером,
а не серое на белом) и добавляет роли `accent_fill`/`selection`; тёмная не менялась.
Рельса разделов — 76 px, рисованные значки, «Настройки» сразу под «Базами». Панель
свойств — вариант B (заголовок + вид словом + моноширинный путь + кнопки), витрина
вынесена чистой функцией в `services` (инвариант 1). Новые формы значков размещения —
полки для серверной, пунктирный круг для неразобранной. Раздел «Настройки» — заголовок,
живой путь к файлу настроек, группа «ВНЕШНИЙ ВИД», сегментный переключатель темы.

Задача 7: четыре оффскрин-скриншота (тёмная/светлая тема × «Базы»/«Настройки»)
отданы заказчику. Финальное ревью ветки на самой сильной модели нашло два Important
одного класса «цвет мимо палитры»: подсказки панели красились системным цветом
placeholder (2,15–2,49:1 при пороге 4,5:1), путь подключения приглушался правилом
`QLineEdit:read-only` вопреки мокапу — закрыто волной правок со сторожами и точечным
ре-ревью, спека §4 дополнена вслед за находкой. Итоговая верификация — `pytest` 1039
прошли, `ruff` и `mypy` коды 0. Слияние в `master` — решение заказчика.
