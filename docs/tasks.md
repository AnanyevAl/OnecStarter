# Бэклог

Статусы: `TODO` · `WIP` · `DONE` · `BLOCKED`

---

## T-01. Доменные скилы проекта — `DONE`

Написаны через `writing-skills` по циклу RED-GREEN-REFACTOR.

| # | Задача | Статус |
| --- | --- | --- |
| T-01.1 | Материал из первоисточников (ИТС, Приложения 3 и 7, версии 8.3.18…8.5.4) → `docs/research/` | DONE |
| T-01.2 | RED: baseline-сценарии на субагентах без скилов | DONE |
| T-01.3 | GREEN: `.claude/skills/v8i-format/` | DONE |
| T-01.4 | GREEN: `.claude/skills/platform-launch/` | DONE |
| T-01.5 | Проверка сценариями на субагентах + REFACTOR | DONE |

Соглашения по фикстурам и сборке скилами не стали — по правилу `writing-skills` они
project-specific и живут в `CLAUDE.md`.

### Что дал цикл

RED зафиксировал уверенные ошибки агента без скилов: UTF-8 «с BOM» с выдуманным обоснованием;
«секций-групп в формате нет» с изобретённым sidecar-файлом под несуществующую проблему;
«`DefaultVersion` — не ключ `.v8i`, скорее всего ручная правка»; разрядность «выбирается деревом
установки»; `OrderInList` как целое с предложением пересчитать в плотную нумерацию.

GREEN закрыл все эти ошибки.

REFACTOR вскрыл дефект в самом скиле: утверждение «маска разрешается в максимальную установленную
с этим префиксом» было моим выводом, а не фактом из ИТС — там сказано «максимальный номер
установленной версии» без оговорки про префикс. Понижено до непроверенного с обеими трактовками.

---

## T-02. Эксперименты на реальной платформе — `DONE`

Протокол и результаты — [research/t02-protocol.md](research/t02-protocol.md).
Проведено 31.07.2026 (две сессии) на платформе 8.3.25.1633 (установлены 8.3.10…8.3.27.2214).

| # | Статус | Итог |
| --- | --- | --- |
| T-02.1 | DONE | Маска → максимум **с этим префиксом** (8.3.25 → 8.3.25.1633) |
| T-02.2 | DONE | Точная `Version` > `DefaultVersion` > маска; ИТС опровергнут |
| T-02.3 | DONE | `Folder` группы = путь родителя; мастер стартера создаёт группы, вложенность поддерживается |
| T-02.4 | DONE | Блокировки файла нет ни при открытом стартере, ни при работающем клиенте |
| T-02.5 | DONE | Посторонний ключ `XTest=1` переживает перезапись (уезжает в конец секции); перезапись — только при запуске из окна стартера, CLI-запуск файл не трогает |
| T-02.6 | DONE | Файловая база: и `App=Auto`, и явный `App=ThinClient` → `1cv8c.exe`; при явном `App` стартер не передаёт `/AppAutoCheckMode` |
| T-02.7 | DONE* | Стартер не передаёт `/WA`; при `/F` ключи `/WA+`, `/WA-` и их отсутствие дают одинаковый диалог (на базе без ОС-аутентификации); кейс с настроенной ОС-аутентификацией не проверен |
| T-02.8 | DONE | Несуществующая версия → молча максимум вообще; `DefaultVersion` пишется **запрошенный** |
| T-02.9 | DONE | Пробелы вокруг `=` ломают распознавание ключа: база деградирует до группы, платформа при перезаписи удаляет её базовые ключи |

Результаты возвращены в скилы `v8i-format` и `platform-launch` с метками **[Ф]**.

### Исходная постановка

Открытые вопросы, которые меняют архитектуру, а не детали. Закрыть до написания кода запуска.

| # | Вопрос | Метод |
| --- | --- | --- |
| T-02.1 | Чем разрешается неполная версия: максимум с префиксом или максимум вообще | **[Ф]** база с `Version=8.3.25` в списке уже есть — запустить, снять фактический путь |
| T-02.2 | Приоритет `DefaultVersion` над `Version` на практике | Конфликтующие значения → снять `ExecutablePath` |
| T-02.3 | Семантика `Folder` у секции-группы; вложенные группы | Создать группу второго уровня в штатном стартере, снять diff файла |
| T-02.4 | Держит ли `1cestart.exe` `ibases.v8i` заблокированным | Открыть стартер, попытаться записать |
| T-02.5 | Полный набор ключей, которые платформа дописывает сама | Менять свойства базы в стартере, снимать diff |
| T-02.6 | `1cv8c.exe` или `1cv8s.exe` для тонкого клиента файловой базы | Запустить, снять `ExecutablePath` |
| T-02.7 | Как воспроизвести `WA=1` («пытаться, потом спросить») ключами командной строки | Эксперимент с недоступной ОС-аутентификацией |
| T-02.8 | Поведение при `Version=8.3.99.1` и `AppAutoInstallLastVersion=0` | Эксперимент |

Инструмент проверки:

```powershell
Get-CimInstance Win32_Process -Filter "Name='1cv8.exe' OR Name='1cv8c.exe'" |
  Select-Object ProcessId, ExecutablePath, CommandLine
```

Результаты возвращаются в скилы: подтверждённое повышается до **[Ф]**, опровергнутое исправляется.

> T-02.7 стоит отдельно: `WA=1` в `.v8i` означает «пытаться аутентификацию ОС, при неудаче
> спросить логин/пароль», а `/WA+` означает «обязательная». Точного эквивалента в командной строке
> нет — откат придётся реализовывать самим, и неясно, как детектировать неуспех у GUI-клиента.

---

## T-03. Требования — `DONE`

Собраны в сессии brainstorming 30.07.2026: [requirements.md](requirements.md) заполнен,
дизайн v1 утверждён — [superpowers/specs/2026-07-30-v1-core-design.md](superpowers/specs/2026-07-30-v1-core-design.md).

Ключевые решения: публичный open source (MIT/Apache-2.0) для специалистов 1С;
роадмап v1 (ядро запуска ИБ) → v2 (серверы/консоль) → v3 (конструктор параметров
CLI) → v4+ (ТЖ, публикации, EDT); форма — рабочее место с боковой навигацией,
тёмный стиль по мотивам developer.1c.ru.

---

## T-04. Планы реализации v1 — `DONE` (23.08.2026, все планы закрыты)

Реализация разбита на серию планов, каждый даёт работающий тестируемый результат.

| # | План | Статус | Блокеры |
| --- | --- | --- | --- |
| T-04.1 | [Слой `config`: v8i round-trip, атомарная запись, 1cestart.cfg](superpowers/plans/2026-07-31-v1-plan1-config-core.md) | DONE | нет |
| T-04.2 | [`platform_1c` + `domain`: реестр версий, обнаружение, выбор версии, командная строка](superpowers/plans/2026-07-31-v1-plan2-platform-domain.md) | DONE | нет (T-02 закрыт) |
| T-04.3 | [`services`: сборка модели, наши данные в `%APPDATA%`, координатор](superpowers/plans/2026-08-04-v1-plan3-services.md) | DONE | нет (T-02.5 закрыт) |
| T-04.4 | [Операции над группами и дедупликация источников](superpowers/plans/2026-08-04-v1-plan4-groups-and-dedupe.md) | DONE | нет |
| T-04.5a | [UI «Базы»: просмотр и запуск](superpowers/plans/2026-08-07-v1-plan4a-ui-bases.md) | DONE 08.08.2026 — слито в `master` линейной историей (32 коммита). Открыт дефект показа контекстного меню, задача 15 плана: чиним вместе с 4b по решению заказчика | нет |
| T-04.5b | [UI «Базы»: правки списка, витрина размещения, светлая тема, `F3`/`F4`](superpowers/plans/2026-08-08-v1-plan4b-ui-edit.md) | **DONE 10.08.2026** — слито в `master` линейной историей (116 коммитов, fast-forward). 1000 тестов, `ruff` и `mypy` коды 0. Ручные smoke №1 (08.08) и №2 (09.08) пройдены, замечания разнесены и закрыты. Задача 16 снята из v1 → v2, задача 20 добавлена по итогам smoke №2. Финальное ревью ветки на самой сильной модели нашло не сломанный код, а тесты, зелёные на сломанной реализации; закрыто двумя волнами, обе с точечным ре-ревью. Долг — раздел «Долг, вынесенный финальным ревью ветки 4b» ниже | нет |
| T-04.6 | Поставка: сборка exe, установщик, публикация на GitHub; диагностика молчаливого отказа старта ([спека](superpowers/specs/2026-08-16-v1-delivery-design.md), [план](superpowers/plans/2026-08-16-v1-delivery.md)) | **DONE 16.08.2026.** Старт перестроен (окно до фонового обнаружения, потоки-демоны, отменяемый прогресс у ярлыков), молчаливый отказ исключён (лог, faulthandler, перехват с MessageBoxW, консольный exe), PyInstaller one-dir + smoke собранного экземпляра + Inno Setup per-user; 1083 теста, ruff/mypy чисты; сборка 0.1.0: dist 123 МБ, zip 53 МБ, setup 35 МБ. Единый фирменный значок — замечание заказчика с контрольной точки. Публикация на GitHub — свежий корень, той же датой | T-04.5b |
| T-04.7 | Функции настроек: закрытие в трей, глобальный хоткей, лимит «Недавних», автозапуск ([спека](superpowers/specs/2026-08-19-settings-functions-design.md), [план](superpowers/plans/2026-08-19-settings-functions.md)) | **DONE 20.08.2026.** Четыре настройки в разделе «Настройки»: закрытие в трей (тумблер), глобальный хоткей (поле захвата сочетания, перерегистрация на лету `GlobalHotkey.rebind`, единый писатель `SettingsStore`, `Alt+F4` запрещён решением заказчика), лимит «Недавних» (0–50, асимметричный клэмп — решение заказчика), автозапуск при входе в Windows (`HKCU\...\Run`, истина только в реестре, спека §3.1). 1214 тестов (было 1083 на T-04.6, +131 в этой ветке), `ruff` и `mypy` коды 0. Защитные тесты вехи мутационно проверены: мутации ставили не авторы тестов — раздел «Мутационные проверки вехи» ниже (волны закрытия находок поставлены и воспроизведены ре-ревьюером, все шесть); кириллическая раскладка и цикл установщика с реестром — непроверены честно, см. раздел «Непроверенное честно» ниже. Финальное ревью всей ветки нашло девять пунктов (два теста, зелёных на сломанной реализации; условие показа окна читало не тот признак; `Alt+F4` не запрещён; асимметрия клэмпа не решена; тумблер автозапуска не досинхронизировался; докстринг `is_enabled` обещал самолечение; `dispose()` не снимал нативный фильтр) — закрыты одной волной, раздел ниже | T-04.6 |
| T-04.8 | Завершение v1: клиент по умолчанию и очистка кэша ([спека](superpowers/specs/2026-08-23-v1-completion-design.md), [план](superpowers/plans/2026-08-23-v1-completion.md)) | **DONE 23.08.2026.** Шестая настройка «Клиент по умолчанию» (Тонкий/Толстый, дефолт тонкий — поведение существующих установок не меняется; приоритет: принудительный выбор → `App` записи → настройка; смена на лету через `store.changed`, доезжает и до запуска по ярлыку — `build_runtime` читает `settings.json`). Очистка кэша: подменю «Очистить кэш» в контекстном меню записи, адрес по `ID` с GUID-защитой (§5.1 — при невалидном `ID` адреса нет вовсе), рекурсивный обход не следует за junction/симлинками (§5.2, интеграционный тест на настоящем reparse point), не останавливается на первой ошибке, подтверждение всегда — с именем, размером до удаления и дефолтом «Нет», сводка — два числа, вторичные отказы не показываются. 1342 теста (было 1280, +62 в этой ветке), `ruff` и `mypy` коды 0. Конфликт спеки §3.4/§4 (доступность пунктов против запрета диска при построении меню) разрешён правкой правила §4 — по правилу «переписывается правило, а не подгоняется код». Мутационная стадия — раздел «Мутационные проверки вехи „Завершение v1“» ниже (восемь применений, набор убил все; независимый агент). Финальное ревью на самой сильной модели: два Important (мёртвый сторож `CORE` для нового модуля; ложная сводка «файлы заняты» при гонке «каталог исчез») — закрыты одной волной с ре-ревью; остальной долг — раздел ниже | T-04.7 |

### Долг, вынесенный финальным ревью вехи «Завершение v1» (23.08.2026)

Финальное ревью оценило ветку «With fixes»: оба Important закрыты волной до слияния,
перечисленное ниже ревьюер явно пометил «чинить желательно, не блокирует» — выносится
пакетом после слияния. Слияние не блокирует.

Пункты 1–9 закрыты пакетом 23.08.2026 (ветка `chore/2026-08-23-completion-debts`,
по образцу `chore/2026-08-18-review-debts`); мутации по защитным тестам ставил
независимый агент, не автор тестов — результаты в аннотациях пунктов. Пункт 10
остаётся принятым ограничением v1 (решение заказчика 23.08.2026).

1. **Позиция подменю «Очистить кэш» не закреплена тестом** (§3.2: выше разделителя
   перед «Удалить из списка…») — проверка «индекс „Очистить кэш“ < индекса
   „Удалить из списка…“» в `_build_menu`. **Закрыт (23.08.2026):**
   test_cache_submenu_sits_above_removal_item; мутация «подменю в конец меню» убита.
2. **Запись общего списка в подменю кэша не покрыта** — пункт осознанно активен
   (кэш локален, к источнику записи отношения не имеет); тест закрепил бы, что
   дизейбл COMMON-записей его не зацепил. **Закрыт (23.08.2026):**
   test_cache_submenu_stays_enabled_for_common_entry; мутация «дизейбл COMMON зацепил
   кэш» убита.
3. **Сценарий `clear_cache` проходит только вид PROGRAM** — параметризовать один
   тест по `CacheKind`, чтобы перепутанный вид на втором пункте ловился. **Закрыт
   (23.08.2026):** test_clear_cache_clears_exactly_requested_kind (оба вида, оба корня
   наполнены); мутация «kind подменён на PROGRAM» убита.
4. **Выходы `item is None` / `path is None` в `clear_cache` без тестов** — ветки
   молчаливого выхода держатся на рассуждении ревью. **Закрыт (23.08.2026):**
   test_clear_cache_ignores_unknown_key, test_clear_cache_exits_when_address_is_gone;
   мутации: «снять guard item» убита, «снять обе предварительные проверки, положившись
   на measure» убита; «снять только guard path» на фейке бессильна — guard защищает от
   падения настоящей Path.is_dir(None), зафиксировано в отчёте стадии.
5. **`measure` на ссылке не тестирован** — «ссылка = запись без размера» согласована
   с `clear`, но не закреплена. **Закрыт (23.08.2026):**
   test_link_counts_as_entry_without_size_and_is_not_walked; мутация «обойти ссылку как
   каталог» убита.
6. **`format_size`: значения <10 округляются, ≥10 усекаются** — косметика вопроса
   подтверждения (206,94 МБ → «206 МБ», но 9,96 ГБ → «10 ГБ»). **Закрыт (23.08.2026):**
   округление единообразное (банковское, round-half-to-even) в обеих ветках с переносом единицы (1023,6 КБ → «1 МБ»);
   три новые строки таблицы. Табличный тест чистой функции — без мутационной проверки
   (правило проекта).
7. **`NO_CACHE_ROOT_NOTE` без теста** (пункт неактивен, когда в окружении нет
   корня кэша). **Закрыт (23.08.2026):** test_cache_item_disabled_without_cache_root;
   мутация «снять ветку path is None» убита.
8. **`FakeCacheOps.list_dir` на отсутствующем пути даёт `KeyError`, не
   `FileNotFoundError`** — расходится с настоящим `os.scandir`; защитный тест гонки
   ловит мутацию, но через незапланированный сигнал (зафиксировано в комментарии
   самого теста). **Закрыт (23.08.2026):** FakeCacheOps.list_dir даёт FileNotFoundError,
   как os.scandir; мутация «снять is_dir перед замером» перепроверена — тест гонки
   падает через запланированный сигнал.
9. **Дефолтный `cache_ops` в помощнике UI-тестов — настоящая ФС** (read-only
   `is_dir` по фиктивным GUID; безвредно, но фейк по умолчанию снял бы класс
   проблем целиком). **Закрыт (23.08.2026):** _view подаёт свежий FakeCacheOps() по
   умолчанию; прямые конструирования BasesView вне помощника меню записей не строят и
   ФС не трогают.
10. **Очистка блокирует GUI-поток** — на измеренном кэше заказчика (2,9 ГБ) замер
    и удаление идут синхронно в главном потоке; спека синхронна, для v1 принято
    как известное ограничение.

### Долг, вынесенный финальным ревью ветки 4b (10.08.2026)

Ревью нашло не сломанный код, а **тесты, зелёные на сломанной реализации**, —
и доказало это мутациями. Основное закрыто двумя волнами; ниже то, что
осознанно вынесено за их пределы. Слияние не блокирует.

| # | Долг | Почему вынесено |
| --- | --- | --- |
| 1 | **Второй рубеж валидации размещения в `services`.** `build_connect(FILE, file_path="")` в обход диалога вернёт `File="";`, и `add_infobase` примет. У имени рубежа два (`validate_section_name` в services), у размещения — один, и тот в UI, куда инвариант 1 логику не пускает | **Закрыт (18.08.2026):** `validate_connect` в `services/model.py`, вызывается из `apply_patch`: ADD требует `Connect` (секция без него — группа, [Ф] T-05.6), UPDATE проверяет новое значение; пустой фрагмент размещения **вида записи** (`PLACEMENT_FRAGMENTS[classify_connect]`) отвергается, отсутствующий или чужого вида — не требуется (уточнение волны финального ревью, см. абзац ниже); дубли ключей изменений в разном регистре отвергаются |
| 2 | **Десять директив `# noqa: …, RUF100`** прикрывают устаревшие `RUF001`/`RUF002` вместо их удаления. Остаток восстановления через `ruff --add-noqa` | **Закрыт (18.08.2026):** сняты все директивы с `RUF100` (по факту их было 11), `ruff` чист без них |
| 3 | **Спека §3.2:** обоснование «диалог только для чтения потребовал бы второго режима» устарело — механизм построен задачей I8; и цена завышена, путь подключения записи общего списка виден в `ConnectionPanel` | **Закрыт (18.08.2026):** в §3.2 датированная правка — цена сужена до таблицы прочих ключей, довод про «второй режим» снят, неактивные «Свойства…» зафиксированы как осознанный выбор |
| 4 | **Три копии обходчика дерева** в `tests/ui/test_bases_view.py` | **Закрыт (18.08.2026):** к этому моменту копий стало семь (рестайл и поставка добавили свои) — все сведены к одному генератору `_iter_tree` + `_index_of_key` |
| 5 | **CRLF в части файлов**, `.gitattributes` нет. Диф `view.py` из-за этого раздувался с 88 строк до 2912, blame по файлу испорчен | **Закрыт T-04.6 (16.08.2026):** `.gitattributes` + перенормализация 19 файлов; фикстуры защищены `tests/fixtures/** -text` и остались байт-в-байт |
| 6 | **Коммит `4121853` хранит в истории фикстуру с настоящим SID** пользователя. Remote нет, в `master` коммита нет, наружу не ушло | **Закрыт T-04.6 (16.08.2026):** решение заказчика — публикация со свежим корнем; полная история осталась локальной веткой `archive/pre-publish-history`, которая не пушится никогда |
| 7 | **Шаг 8 задачи 17** — ярлык на собранном `OneCStarter.exe` машинно не проверен: ветка `frozen=True` под тестами не исполняется, `build/` в репозитории нет | **Закрыт T-04.6 (16.08.2026):** smoke каждой сборки (`build/build.ps1`) исполняет frozen-ветку и сверяет цель ярлыка штатным читателем `.lnk` |

Закрытие долгов №1–4 и миноров T-04.6 (ветка `chore/2026-08-18-review-debts`,
18.08.2026) прошло финальное ревью ветки на самой сильной модели. Ревью
подтвердило два дефекта первой редакции рубежа №1 — обход дублем ключа
изменений в разном регистре (воспроизведён исполнением: валидировалось первое
совпадение, писались все, `V8iSection.set` сливает «последний побеждает»)
и запирание правки живой записи пустым фрагментом чужого вида, — оба закрыты
волной правок с точечным ре-ревью. Мутационные проверки ставил отдельный
агент, не автор тестов: рубеж `validate_connect`/`apply_patch` — 4/4 убиты
до волны и 6/6 после неё, сторож «текст исключения не в лог» — 3/3.

### Открытые вопросы T-04.6 (16.08.2026)

| # | Вопрос | Что делать |
| --- | --- | --- |
| 1 | **`.lnk` с классами `0x35`/`0x36` не разрешается штатным `IShellLink` на Windows Server** (github-runner, en-US): все четыре поля ярлыка пусты, тогда как десктопная Windows 11 читает и запускает ([Ф] 16.08.2026, докстринг `_path_item` в `config/shell_link.py`). Причина не установлена: версия оболочки или кодовая страница | **Осознанно перенесён за 1.0 решением заказчика (23.08.2026)** — не забыт. Эксперимент на en-US десктопной Windows; если дело в кодовой странице — пользователи латинских локалей получат нерабочие ярлыки, и формат придётся дополнить ANSI-именем + extension block. На CI сторож исключён маркером `requires_windows_shell`, локальные прогоны гоняют его всегда |
| 2 | Ручной чек-лист контрольной точки (пп. 1–5, 7: холодный старт после перезагрузки, ярлык, консольный exe, удаление/переустановка) заказчик отложил — публикация прошла без него | **Закрыт (19.08.2026):** все пункты пройдены заказчиком чисто — установка per-user без UAC; холодный старт после перезагрузки (окно сразу, версии по мере обнаружения, лог с фазами — инцидент 15.08 не воспроизводится); тёплый старт; ярлык создаёт и запускает нужную базу и версию; консольный exe дублирует лог; удаление сохраняет `%APPDATA%\OneCStarter` и `ibases.v8i`, переустановка подхватывает. Находок нет |

### Отклонённый параллельный дизайн 4b — ветка `archive/2026-08-09-parallel-4b-design`

09.08.2026 сессия, не сверившая состояние репозитория, провела brainstorming 4b заново
и написала второй дизайн и второй план — при том, что 4b к тому моменту был реализован
до задачи 15 из 19. Три коммита сняты с `master` в архивную ветку; **сливать её никогда
не нужно.**

Она оставлена не как альтернатива, а как след: если кто-то найдёт в ней полный дизайн 4b,
это не забытая работа, а дубль. Отклонён по существу — в нём вырезание учётных данных
лежит в `services/`, что против инварианта 5, а разбор строки соединения свален
в `services/display.py` вместо отдельного модуля.

Причина сбоя и что с ней делать: оба дизайна писались прямо в `master`, поэтому
столкновение вскрылось только через сутки. Проектные документы 4c и дальше держать
на ветке своего плана — тогда расхождение видно сразу.

Вторая причина, и она тяжелее первой: статус T-04.5b в таблице выше до 09.08.2026
гласил «реализация не начата» — при 74 коммитах в ветке и работе, доведённой
до задачи 15. Сессия 09.08 сверилась с документом, документ соврал, и дальше она
действовала последовательно. Ровно тот случай, о котором правило `CLAUDE.md`
«документ, разошедшийся с кодом, врёт следующему исполнителю»: цена оказалась —
день на дубль и полдня на разбор. Там же не были проставлены 102 чекбокса плана 4b
при выполненных задачах 1–15. Оба хвоста приведены к факту 09.08.2026.
Вывод для процесса: статус плана обновляется тем же коммитом, что и переход
к следующей задаче, а не «потом одним махом» — иначе он не документ, а помеха.

### Замечания заказчика по ручному smoke 4a (07.08.2026)

Первый запуск на рабочей машине: 7 версий платформы, 64 записи, 6 групп, приложение
работает. Четыре замечания, все по показу — подробно в [спеке UI, §9](superpowers/specs/2026-08-05-v1-ui-bases-design.md).

| # | Замечание | Куда |
| --- | --- | --- |
| 1 | Контекстное меню базы отрисовано скомканно: подсказка сочетания налезает на название. Offscreen не воспроизводится ни в одном из 4 стилей Qt — причину снимать на реальном экране | задача 15 плана 4a → переезжает в 4b ([§7](superpowers/specs/2026-08-08-v1-plan4b-ui-edit-design.md)) |
| 2 | Не видно, файловая база или серверная — нужен значок вида размещения (`ConnectKind` уже различает FILE/SERVER/WEB/UNKNOWN, показа нет) | 4b |
| 3 | Панель свойств выделенной записи с путём подключения: копировать, открыть каталог для файловой — как в штатном стартере. Решено 07.08.2026: показывается **только путь подключения, без дополнительных параметров**; для веб-базы из URL вырезается `user:pass@` | 4b |
| 4 | `gui-scripts` на Windows проглатывает любую ошибку старта (нет консоли у `pythonw.exe`) — нужен лог или консольный entry point | T-04.6 |
| 5 | Переключение на светлую тему. Не только палитра: цвета запечены в `QBrush` элементов модели и в иконку трея, смена на лету требует пересборки обеих ([спека §10](superpowers/specs/2026-08-05-v1-ui-bases-design.md)) | 4b |

T-04.4 закрывает долги плана 3 (пункты 1 и 3 ниже) и три дефекта того же класса,
найденных при разборе: смена `Folder` у секции-группы разрушает дерево молча, удаление
группы оставляет сирот, создать группу нельзя ни одним вызовом. Идёт до UI, чтобы план UI
не упирался в отсутствующие операции.

### Обязательства из финального ревью плана 2 (01.08.2026)

| # | Пункт | Статус |
| --- | --- | --- |
| 1 | Guard секретов в `build_arguments` шире одного `Pwd` | DONE (03.08.2026) |
| 2 | `try/except OSError` на обход корня в `discover_installations` | DONE (03.08.2026) |
| 3 | При первом реальном запуске по `/IBConnectionString` проверить форму присоединения значения к ключу и вернуть результат в скил `platform-launch` (допущение решения 1 плана 2) | TODO → T-05.1 (форма ключа) и T-05.2 (запуск из общих списков) |

### Обязательства из финального ревью плана 3 (04.08.2026)

Полный перечень — раздел 10 [дизайна плана 3](superpowers/specs/2026-08-03-v1-plan3-services-design.md);
там же раздел 11 фиксирует, чем реализация отличается от первоначальных формулировок дизайна
и почему. Кратко, к исполнению:

| # | Пункт | Кому |
| --- | --- | --- |
| 1 | Каскадное переименование секции-группы: сейчас переименование группы запрещено, потому что `Folder` потомков не переписывался и операция молча разрушала дерево | DONE (T-04.4) |
| 2 | `PPasswd` (зашифрованный пароль прокси, ключ секции) не распознаётся `security.secrets.is_secret_key`. Сегодня недостижим — функция применяется только к фрагментам `Connect`; станет достижим, когда UI покажет свойства записи | План UI |
| 3 | Запись, присутствующая и в пользовательском списке, и в общем, выдаётся `items()` дважды: дедупликации по ключу привязки между источниками нет, политика в дизайне не задана | DONE (T-04.4) |
| 4 | Эксперименты: поведение `/IBName` при совпадении имён без учёта регистра; трактовка `Connect=` с пустым значением штатным стартером | T-05.3, T-05.6 |
| 5 | Эксперименты, добавленные дизайном T-04.4: регистр при сопоставлении `Folder` с именем группы; видит ли стартер группу, созданную без `OrderInTree`; что стартер делает с содержимым при удалении группы | T-05.7, T-05.8, T-05.9 |

Пункт 1: список ключей из ревью (`DBPwd`, `SPwd`, `wsppwd`) оказался неполным — по справочнику
`platform-launch` пароль несёт и `wsp` (пользователь веб-сервера). Знание о том, что является
секретом, вынесено в `security/secrets.py` (инвариант 5 `CLAUDE.md`): там же оно понадобится
для показа командной строки в сообщениях об ошибке без секретов (спека §4). Сверка идёт
по полному имени ключа — `wsp` секрет, `wspuser`/`wspsrv`/`wspport` нет; дополнительно
отклоняется любое неизвестное имя на `pwd`.

### Мутационные проверки вехи «Функции настроек» (20.08.2026)

Правило проекта (`CLAUDE.md`): тест, зелёный на пустышке, хуже отсутствующего — он создаёт
ложную уверенность; мутацию ставит **не автор теста**. По перечисленным ниже тестам
мутация поставлена агентом, не писавшим этот тест, результат воспроизведён и мутация
откачена; дерево после каждой мутации возвращено в чистое состояние (`git status --short`
пуст), полный прогон снова зелёный.

**Исправлено 20.08.2026.** Прежняя формулировка («по каждому защитному тесту вехи»)
завышала охват — опровергнута находкой 1 финального ревью ветки (раздел ниже,
«Закрытие находок финального ревью ветки»): у `test_disabled_hotkey_sets_the_plain_tooltip`
целевой мутации не ставил никто, и тест был бессилен — совпадение значения тултипа по умолчанию
с тем, что ставит `create_tray` при создании трея, маскировало отсутствие проверки (под мутацию №2 падает коллатерально, что как раз вскрывает бессилие).
Пункт 6 того же ревью — второй пробел того же рода (`blockSignals` в `settings_view.py`
без единого теста). Формулировка сужена до факта: не «каждый», а «перечисленные».

Четыре мутации из исходной таблицы плана (`task-11-brief.md`):

| № | Тест | Мутация | Кто ставил | Результат дословно |
| --- | --- | --- | --- | --- |
| 1 | `tests/ui/test_settings_store.py::test_update_keeps_other_fields` | `SettingsStore.update`: `replace(self._settings, **changes)` → `Settings(**changes)` | ревьюер Task 5 | «1 failed, 5 passed»; `AssertionError: assert True is False + where True = Settings(theme=<ThemeMode.DARK: 'dark'>, close_to_tray=True, hotkey='Ctrl+Alt+B', recent_limit=10).close_to_tray`». Откат чист, 6/6 |
| 2 | `tests/ui/test_hotkey.py::test_rebind_to_none_disables` | `GlobalHotkey.rebind` (`src/onecstarter/ui/hotkey.py`): убран ранний выход `if spec is None: return True` | исполнитель Task 11 | Плановая формулировка ожидала «падение на непустом `calls["register"]`»; фактически тест падает раньше — исключением `AttributeError: 'NoneType' object has no attribute 'modifiers'` на `hotkey.py:59` (`spec.modifiers` при `spec is None`), возбуждённым из `assert hotkey.rebind(None) is True` (`test_hotkey.py:96`). Коллатерально падают ещё два теста в `test_app.py`, использующие настоящий `GlobalHotkey`: `test_disabled_hotkey_is_not_registered`, `test_disabled_hotkey_sets_the_plain_tooltip` — та же ошибка. Итог по всему сьюту: 3 failed, 1204 passed. Откат чист, 1207/1207 |
| 3 | `tests/ui/test_settings_view.py::test_autostart_writes_registry_not_settings_file` | `_choose_autostart` + поле `autostart` в `Settings` + `store.update(autostart=checked)` | исполнитель Task 8 (самоотчёт, обе половины лично), ревьюер воспроизвёл | Плановая редакция теста (проверка только отсутствия ключа в JSON) мутацию **не поймала** — «1 passed» (дефект плана, см. «Главный урок» ниже). После усиления теста сравнением `store.settings == Settings()`: «AssertionError: assert Settings(them...utostart=True) == Settings(them...tostart=False); Differing attributes: ['autostart']; autostart: True != False». Откат чист, 1191 passed |
| 4 | `tests/ui/test_app.py::test_disabled_hotkey_is_not_registered` | `apply_hotkey`: `parse_hotkey(text)` → `parse_hotkey(text) or parse_hotkey(DEFAULT_HOTKEY)` | исполнитель Task 9, дословно воспроизвёл ревьюер | «assert [(3, 66)] == []» |

Оговорка к мутации №2: тест ловит её не своими `assert`-утверждениями, а самим фактом
краша. Исключение `AttributeError` возникает при вычислении `spec.modifiers` — ДО вызова
`self._register(...)`. Если бы это исключение было где-то подавлено, `calls["register"]`
остался бы пустым списком, и `assert calls["register"] == []` из теста прошёл бы. Сигнал
не слабее упавшего `assert` — необработанное исключение валит тест так же надёжно, — но
природа другая: тест поймал мутацию случайностью реализации (`None` не имеет атрибутов),
а не проверкой того поведения, которое он формально описывает.

Сверх исходной таблицы — мутации, поставленные не авторами соответствующих тестов (правило
требует независимости именно для тестов отказа операции и сохранности пользовательских
данных). Атрибуция по каждой — тем же принципом прозрачности, что и в строке 3 выше: где
исполнитель задачи прогнал мутацию сам (обычно как часть самоотчёта об исправлении находки
ревью) и это отдельно воспроизвёл ревьюер, названы обе стороны.

- **Task 3**, ревьюер напрямую (в самоотчёте исполнителя мутация не упоминается):
  `test_write_failure_reaches_caller`, обёртка enable/disable в
  `try/except OSError: pass` → «Failed: DID NOT RAISE OSError», остальные 7 тестов файла прошли.
- **Task 6**, ревьюер напрямую, поставлена и снята лично: пропуск `unregister` при
  совпадающем `spec` в `GlobalHotkey.rebind` → «assert [] == [41244]» (0xA11C = `HOTKEY_ID`).
- **Task 7**, ревьюер напрямую, три независимые мутации, каждая уронила ровно свой тест:
  стирание при непригодном нажатии → `test_widget_ignores_unusable_press`; Backspace/Delete
  не очищают → `test_delete_clears_to_disabled`; собственная проверка модификаторов вместо
  `parse_hotkey` → `test_shift_only_is_rejected`.
- **Task 9**, исполнитель Task 9 сам поставил и прогнал каждую (самоотчёт «Круг исправлений 1»
  в `task-9-report.md`), ревьюер затем воспроизвёл лично: тихий старт (условие заменено на
  `if False:`) → «assert [1] == []»; безусловная перестройка дерева → «assert 2 == 1», и
  обратная сторона (перестройка снята совсем) → «assert 0 == 1» — тест ловит обе стороны;
  провайдер лимита подменён на `lambda: 10` → «assert 10 == 17».
- **Task 10**, исполнитель Task 10 сам поставил и прогнал обе (самоотчёт «Круг исправлений» в
  `task-10-report.md`), ре-ревьюер воспроизвёл дословно: деинсталляция/smoke, `_log.info` →
  `_log.debug` и смена текста строки лога — тест упал на каждой из двух мутаций.

**Task 4**, вне рамки правила, обе стороны: исполнитель Task 4 прогнал мутацию (слайс
`[:recent_limit]` → `[:10]` над `display_forest`) сам, post-hoc вместо строгого красного
шага TDD — «assert False» на тесте-канарейке и «assert 5 == 2» на тесте обрезания, откат
чист. Ревьюер Task 4 независимо поставил ту же мутацию и получил те же два падения, откат
тоже чист. Правило проекта её не требовало: `display_forest` — чистая функция, тесты
табличные, а на них независимость не обязательна (`CLAUDE.md`); обе стороны прогнали её
сверх обязательного, каждая по своей инициативе.

Дополнительно, сверх задания Task 11 (тест не входил в исходную таблицу плана, проверка
по собственной инициативе исполнителя Task 11):
`tests/unit/test_settings.py::test_old_file_without_new_keys_reads_with_defaults` —
тест сохранности: файл настроек
прошлой версии (только `schema` и `theme`, без новых ключей) не должен уезжать в `.bad`.
Мутация в `load_settings` (`src/onecstarter/services/settings.py`) — схема отказа расширена
требованием всех пяти ключей (`required = {"schema", "theme", "close_to_tray", "hotkey",
"recent_limit"}`, `required.issubset(payload)` в условии `raise ValueError`), имитируя
реальную ошибку «забыли про обратную совместимость» — ровно тот сценарий, о котором
предупреждает докстринг модуля. Результат дословно:

```text
tests\unit\test_settings.py:122: in test_old_file_without_new_keys_reads_with_defaults
    assert load_settings(path) == Settings(theme=ThemeMode.LIGHT)
E   AssertionError: assert Settings(them...cent_limit=10) == Settings(them...cent_limit=10)
E     Differing attributes:
E     ['theme']
E       theme: <ThemeMode.AUTO: 'auto'> != <ThemeMode.LIGHT: 'light'>
```

Коллатерально в файле падают ещё 7 тестов (`test_unknown_theme_value_falls_back_to_auto`,
`test_empty_hotkey_means_disabled_not_default`, четыре кейса `test_recent_limit_is_clamped`) —
итого 8 failed, 23 passed в файле. Откат чист, 1207/1207.

### Непроверенное честно (веха «Функции настроек») — ОБА ПУНКТА ЗАКРЫТЫ 21.08.2026

Веха оставила два пункта с меткой достоверности «не проверено» — не потому что забыто,
а потому что для проверки нужны были условия, которых на машине разработки не нашлось.
Ручной прогон по протоколу
[t04-7-settings-protocol.md](research/t04-7-settings-protocol.md) закрыл оба, и **ни один
не подтвердился в том виде, в каком его ожидала веха**: пункт 1 вскрыл дефект установщика,
пункт 2 оказался поставлен не про ту клавишу. Метка «не проверено» оправдала себя дважды.

Шаг В7 (поведение тумблера при внешней правке — выключение через «Диспетчер задач»)
закрыт 22.08.2026 повторным циклом установки и вскрыл третий дефект прогона:
у Windows истина об автозапуске не одна, см. раздел ниже. Непроверенного
по этой вехе не осталось.

Ниже — исходная таблица с проставленными исходами:

| # | Пункт | Почему не проверено | Что нужно |
| --- | --- | --- | --- |
| 1 | Секция `[Registry]` установщика Inno Setup (запись автозапуска через инсталлятор) | **ЗАКРЫТ 21.08.2026 полным циклом блока А.** По дороге метка сработала: установщик СОЗДАВАЛ в `Run` пустое значение (`dontcreatekey` относится к ключу, а ключ `Run` есть всегда) — исправлено, см. раздел «Пустое значение в Run» ниже. После правки: значение при установке не создаётся, тумблер пишет верную строку, тихий старт при входе работает, **деинсталляция значение удаляет**, контрольный вход проходит в тишине | — |
| 2 | Поведение захвата хоткея при кириллической раскладке (Ctrl+Alt+Б и т.п.) | **ЗАКРЫТ 20.08.2026** ручным прогоном (блок Б протокола), но не так, как ожидала веха, — см. абзац о двух классах клавиш ниже | — |

Логическая проверка для пункта 2 была проведена ещё вехой (Task 7): любой кириллический
код на входе даёт `parse_hotkey() is None`, то есть деградация приемлема. **Прогон
20.08.2026 показал, что вопрос был поставлен неверно, и обоснование устарело**: клавиши
русской раскладки делятся на два класса, а веха видела только один.

- **Буква с латинским эквивалентом по позиции** («И» на клавише `B`, «Ц» на `W`
  и так далее). Драйвер шлёт обычный `VK_*` буквы, и сочетание обязано **приниматься**:
  оно же и срабатывает при любой раскладке. До правки не принималось — это и был дефект
  шага В1. Кириллический код теперь до `parse_hotkey` не доходит вовсе, так что прежнее
  обоснование («любой кириллический код даёт `None`») к этому классу больше неприменимо.
- **Буква без латинского эквивалента** («Ё» на `` ` ``, «Ж» на `;`, **«Б» на `,`**).
  Драйвер шлёт `VK_OEM_*`, в наборе такого нет, и отказ остаётся отказом — здесь
  обоснование Task 7 верно.

Веха (и первая редакция протокола) называла проверочным случаем `Ctrl+Alt+Б`, полагая
«Б» буквой клавиши `B`. Это ошибка: в ЙЦУКЕН на клавише `B` стоит «И», а «Б» — на
запятой. То есть проверялся класс, который и должен отвергаться, а класс, где сидел
настоящий дефект, не проверялся ни разу. Найдено финальным ревью ветки (п. Б-2),
исправлено в спеке §4.1, протоколе (шаги Б1, Б2) и тестах.

**Протокол ручной проверки готов (20.08.2026):**
[docs/research/t04-7-settings-protocol.md](research/t04-7-settings-protocol.md) — блок А
закрывает пункт 1 (сборка установщика, `dontcreatekey` при установке, форма значения в
`Run`, деинсталляция, контрольный вход; кандидаты на замену, если `uninsdeletevalue`
не удаляет значение, созданное не установщиком), блок Б — пункт 2, блок В — общий прогон
четырёх настроек. Шаг А1 **выполнен агентом** (компиляция — не запуск GUI и не установка):
1214 тестов, `smoke: OK`, ISCC 6.7.3, `dist\OneCStarter-0.1.0-setup.exe` 33,2 МБ, код
выхода 0.

**Прогон 20.08.2026 (заказчик): блоки Б и В закрыты целиком, блок А2–А7 и шаг В7
отложены его решением** — они требуют установки, двух выходов из системы и
деинсталляции. Итог прогона:

- **пункт 2 «Непроверенного честно» закрыт** — кириллическая раскладка ведёт себя
  как предписано (таблица выше);
- **найден и исправлен настоящий дефект** — захват сочетания при русской раскладке
  (шаг В1, раздел «Правка захвата сочетания» ниже). Пришёл он не из блока Б, который
  специально искал проблемы с раскладкой и дал «всё по плану», а из соседнего шага
  про перерегистрацию: сценарий «верните как было» проходит по тем же путям,
  но в состоянии, которого автор не предусматривал;
- **один шаг протокола опровергнут как нерабочий** — `Win+L` не годится источником
  занятости, Windows перехватывает его ниже уровня приложения; взамен описан метод
  второго экземпляра с подменённым `APPDATA`;
- **дважды устный ответ разошёлся с фактами на диске** (шаги Б3 и В2) — оба раза
  расхождение снято контрольным замером (соседняя раскладка; лог и `settings.json`
  изолированного каталога), и ни одно не попало в документы как «проверено».
  Правило на будущее: ручной замер, особенно отрицательный, ставится парой
  «рабочий случай рядом с проверяемым», а его результат сверяется с артефактами
  на диске, а не только со словами.

Остальное — за заказчиком: запуск GUI собранного приложения и установщика агентам
запрещён всю веху. Результаты пишутся в тот же файл, подтверждённое поднимает
метки в спеке §3.5/§4.1 и в комментарии `build/installer.iss`.

Протокол добавил **третий** вопрос, которого веха не задавала (шаг Б3): срабатывает ли
уже назначенное `Ctrl+Alt+B` при активной **русской раскладке**. Ни тесты (живой
`RegisterHotKey` не трогают), ни эксперимент §7 (синтез `keybd_event` при латинской
раскладке) этого не проверяли, а раскладка у пользователя основная — если не срабатывает,
глобальный вызов не работает большую часть рабочего дня. Это дороже обоих пунктов выше
и, в отличие от них, правится не меткой, а спекой §4.

### Главный урок вехи «Функции настроек»

Мутация №3 в исходной редакции таблицы плана оказалась **бессильной**: тест сравнивал
JSON-файл настроек до и после, то есть проверял сериализатор, а не то, тронуло ли
изменение состояние программы по существу. `save_settings` перечисляет ключи явным
списком — лишнее поле в `Settings` никогда не протекло бы в файл, и тест остался бы
зелёным на сломанной реализации сколько угодно долго. Тест усилен прямым сравнением
`store.settings == Settings()` — и та же мутация поймана мгновенно. Урок: мутационная
проверка обязана убить мутацию на прямом сравнении состояния, а не на побочном
артефакте вроде формата файла на диске — иначе она проверяет не то, что заявлено.

### Закрытие находок финального ревью ветки (20.08.2026)

Финальное ревью всей ветки `settings-functions` (HEAD `7a98892`, 1207 тестов) вынесло
вердикт «нельзя сливать без правок», девять пунктов. Все закрыты одной волной; полный
отчёт — `.superpowers/sdd/2026-08-19-settings-functions/final-review-fix-report.md`
(в репозиторий не идёт, леджерный документ приватного разбора). Здесь — мутационные
проверки защитных тестов этой волны, требуемые правилом проекта.

| № | Тест | Мутация | Результат дословно |
| --- | --- | --- | --- |
| 1 | `tests/ui/test_app.py::test_clearing_a_busy_hotkey_resets_the_tooltip_to_plain` (переименован и переписан из `test_disabled_hotkey_sets_the_plain_tooltip` — прежняя версия сравнивала тултип со значением по умолчанию, которое `create_tray` ставит ещё до логики хоткея, и была бессильна, см. абзац выше) | `apply_hotkey` (`ui/app.py:405-408`): убрана строка `_set_tray_tooltip(tray, None)` в ветке выключенного хоткея | `AssertionError: assert 'OneCStarter — Ctrl+Alt+B занято другим приложением' == 'OneCStarter'` (тултип не вернулся к чистому после очистки поля). Откат чист, 1214/1214 |
| 2 | `tests/ui/test_app.py::test_main_start_hidden_with_tray_and_close_to_tray_off_keeps_the_window_hidden` (новый — четвёртая комбинация условия показа окна) | `main()` (`ui/app.py`): условие показа окна возвращено к `if start_hidden and window.close_to_tray:` (было исправлено на `window.tray_available`) | `AssertionError: скрываться было чем — окно не показывается; assert [1] == []` (окно показалось, хотя трей жив). Откат чист |
| 3а | `tests/ui/test_app.py::test_build_main_window_disposes_hotkey_and_removes_filter_together_on_quit` (заменяет `test_main_disposes_the_hotkey_on_quit` — тот сравнивал подключённый слот с `hotkey.dispose` напрямую, что перестало быть верным после обёртки `dispose_hotkey`) | `_build_main_window` — `aboutToQuit` подключён напрямую к `hotkey.dispose` (обёртка `dispose_hotkey` убрана целиком) | `StopIteration` — среди подключённых слотов не нашлось функции с именем `dispose_hotkey`. Откат чист |
| 3б | (тот же тест) | `dispose_hotkey` оставлена, но без строки `application.removeNativeEventFilter(hotkey)` | `AssertionError: assert [] == [<_FakeHotkey ...>]` (`removeNativeEventFilter` не вызван). Откат чист |
| 4 | `tests/ui/test_app.py::test_run_smoke_disposes_the_hotkey_before_returning` (новый) | `run_smoke` (`ui/app.py`): тело `finally` заменено на `pass` | `AssertionError: assert False is True` (`window.global_hotkey.disposed` осталось `False`). Откат чист |
| 5 | `tests/ui/test_settings_view.py::test_autostart_toggle_resyncs_on_show` (новый) | `SettingsView.showEvent` — убран вызов `self._sync_autostart()` | `AssertionError: assert True is False` (тумблер не подхватил внешнюю правку реестра при показе). Откат чист |
| 6а | (тот же тест) | `_set_autostart_state` — сняты `blockSignals(True)`/`blockSignals(blocked)` вокруг `self._autostart.setChecked(checked)` | `AssertionError: assert ['OneCStarter'] == []` (приведение тумблера к прочитанному факту само вызвало `registry.delete` — ровно риск, который глушение сигналов предотвращает). Откат чист |
| 6б | `tests/ui/test_settings_view.py::test_sync_does_not_bounce_external_settings_changes_back_into_the_store` (новый) | `_sync` — сняты оба `blockSignals` вокруг `_tray.setChecked`/`_recent.setValue` | `AssertionError: ... Left contains 2 more items, first extra item: {'close_to_tray': False}` (один явный `store.update` породил три вызова вместо одного — `changed` → `update` → `changed` эхом). Откат чист |

Мутации на пунктах 3 и 4 спеки (Alt+F4, границы «Недавних») — табличные тесты чистых
функций, правило `CLAUDE.md` независимую мутацию не требует. Прогнаны сверх обязательного
по собственной инициативе для очистки совести:

- `services/hotkeys.py::parse_hotkey` — снята проверка `key == _FORBIDDEN_KEY and modifiers
  == _FORBIDDEN_MODIFIERS`: `tests/unit/test_hotkeys.py::test_rejects_invalid_combinations[Alt+F4]`
  падает — `AssertionError: assert HotkeySpec(modifiers=1, vk=115, key='F4') is None`. Откат чист.
- `services/settings.py::_recent_of` — возвращена прежняя формула `max(RECENT_MIN,
  min(RECENT_MAX, value))`: `tests/unit/test_settings.py::test_recent_limit_is_clamped[-3-10]`
  падает — `AssertionError: assert 0 == 10`. Откат чист.

Пункты, закрытые не так, как предписывала находка, — с обоснованием:

- **Пункт 9** заменил тест `test_main_disposes_the_hotkey_on_quit` новым, а не починил
  на месте: старая проверка сравнивала подключённый к `aboutToQuit` слот с `hotkey.dispose`
  через `disconnect` — сравнение стало неверным, как только слотом стала обёртка
  `dispose_hotkey`. Новый тест сильнее прежнего (доказывает и снятие регистрации, и
  `removeNativeEventFilter` вместе, а не только факт подписки) — не ослабление, замена.
- Остальные восемь пунктов закрыты предписанным способом.

### Долг, вынесенный вехой «Функции настроек» (20.08.2026)

Леджер `.superpowers/sdd/2026-08-19-settings-functions/progress.md` не коммитится
и будет удалён. Правило проекта (находка ревью 18.08.2026, `CLAUDE.md`): результат
такой работы обязан жить в коммитуемом документе. Ниже — миноры вехи, которые
финальное ревью ветки осознанно решило не закрывать перед слиянием; девять пунктов
из раздела выше и три находки ре-ревью здесь не повторяются — они уже закрыты.
Ни один пункт слияние не блокирует.

| # | Долг | Почему вынесено |
| --- | --- | --- |
| 1 | **`parse_hotkey`** (`services/hotkeys.py`) не покрыт тестами на трёх краевых входах: `"+++"`, `"Ctrl+Alt+Ctrl+B"`, `"Ctrl+Alt+F 5"` | Таблица чистой функции — три строки `parametrize` в `tests/unit/test_hotkeys.py`; вручную проверено, что все три дают `None`. Дешевле закрыть, чем описывать, но разбор ни на одном из них не ломается |
| 2 | Косметика Task 1: в `tests/unit/test_hotkeys.py` у случая `"Ctrl+Alt+F13"` нет поясняющего комментария (в отличие от соседнего `"Ctrl+Alt+Щ"`); эталонный код-блок плана/брифа Task 1 не несёт `# noqa: RUF002`/`RUF003` для кириллицы, которых требует `ruff` везде в проекте | Текст плана и комментарий в тесте — не код и не поведение |
| 3 | **Дубль тестов.** `tests/unit/test_settings.py`: `test_schema_is_written` и `test_all_fields_are_written` почти дублируют друг друга после правки состава payload | Удалить один при первой же правке файла; оба сейчас зелёные, дублирование не создаёт ложной уверенности — просто лишнее |
| 4 | **`autostart_command`** (`services/autostart.py:78`) не документирует и не тестирует случай уже закавыченного пути на входе — даст `'""C:\...""'` | Недостижимо на практике: единственный источник аргумента — `sys.executable`, платформа его не кавычит |
| 5 | **Магическое число в тестах.** `tests/ui/test_bases_view.py`: десять мест строят `BasesView` с `recent_limit=lambda: 10` вместо константы `DEFAULT_RECENT_LIMIT` | Только тесты; при смене дефолта лимита эти вызовы не поедут синхронно с ним — тест продолжит проверять число 10, а не факт «дефолт» |
| 6 | **Фикстура тянет `qapp`.** `tests/ui/test_app.py`: автоиспользуемая фикстура уборки нативных фильтров (`autouse=True`) поднимает `qapp` для каждого теста файла, включая тесты чистой логики, которым Qt не нужен | Эффект сейчас нейтральный — направление стоит иметь в виду при росте файла, не дефект сегодняшнего дня |
| 7 | **Двойной `_sync()`.** `ui/settings_view.py`: `_sync` подключён и к `controller.changed`, и к `store.changed` — смена темы шлёт оба сигнала и приводит виджеты к состоянию дважды подряд | Идемпотентно, лишняя работа без наблюдаемого эффекта |
| 8 | **`run_smoke` читает боевой реестр машины сборки.** `_build_main_window` (общая для `run_launch` и `run_smoke`) строит настоящий `GlobalHotkey` и настоящий `autostart.WindowsRegistry()`; в frozen-сборке конструктор `SettingsView` сразу читает `HKCU\...\Run` той машины, на которой идёт smoke (`_sync_autostart` вызывается из `__init__`) | Отдельно проверено: волна финального ревью (девять пунктов выше) это не закрыла — её находки про тултип, показ окна, `dispose_hotkey`/`removeNativeEventFilter`, ресинк по `showEvent` и двойной `blockSignals`, ни одна не про подмену реестра в смоуке. Чтение, не запись — вреда сегодня нет, но `build/smoke.py` стоило бы подать фейковую `Registry` при следующей правке файла |
| 9 | **`_build_main_window` перегружена.** `ui/app.py` (на 22.08.2026 — строки 327-513) — около 170 строк на момент вехи, 185 после разбора долга и девять замыканий (`on_theme_changed`, `on_file_changed`, `favorites`, `dispose_hotkey`, `apply_close_to_tray`, `apply_hotkey`, `rebuild_if_recent_limit_changed`, `on_installations`, `on_common`), часть зависит от порядка объявления (`apply_close_to_tray`/`apply_hotkey`/`dispose_hotkey` обязаны стоять после `create_tray`/`GlobalHotkey`) | Финальное ревью ветки назвало это последним дешёвым моментом для разреза — вынести проводку настроек в отдельную `_wire_settings(...)`. Не блокер: поведение не меняется, тесты идут через саму функцию целиком |
| 10 | **Нет трея + сочетание занято на старте — пользователь не узнаёт ничего.** `apply_hotkey`/`_set_tray_tooltip` (`ui/app.py`) шлют балун и меняют тултип только при живом `tray`; без трея остаётся одна строка в разделе «Настройки», а программа открывается на «Базах» | Спека §4.3 перечисляет три канала (балун, тултип, строка в «Настройках») — без трея все три вырождаются до одного, который сразу не виден. Узкий случай (систрей отключён политикой/окружением), не регрессия — так было и раньше |
| 11 | **Отчёты ревьюеров не пишутся в файлы.** Единственный письменный след работы ревьюера — леджер вроде этого, гитигнорный и per-branch; агент, читающий только `task-N-report.md`, видит одну сторону и рискует принять «не нашёл» за «не существует» | Процессная заметка Task 11 (сама стала таким случаем при разборе Task 4: спор решил контроллер, приведя дословную цитату ревью из своего контекста). На следующую веху: либо писать отчёты ревью в файлы, либо явно предупреждать документирующего агента об этой границе источников |

### Правка захвата сочетания: раскладка (20.08.2026, ветка `fix/2026-08-20-hotkey-capture-layout`)

Дефект найден **ручным прогоном** (шаг В1 протокола
[t04-7-settings-protocol.md](research/t04-7-settings-protocol.md)), а не тестами
и не ревью: сменив сочетание на `Ctrl+Alt+G`, заказчик не смог вернуть
`Ctrl+Alt+B` — при русской раскладке поле захвата молча не реагировало.
`spec_from_event` строила имя клавиши из `QKeyEvent.key()`, раскладко-зависимого
(для клавиши позиции `B` при русской раскладке он отдаёт «И», код `0x418`), тогда
как срабатывание живёт на драйверном `nativeVirtualKey` и потому от раскладки
не зависит вовсе (шаг Б3 того же прогона). UI был строже системы, и молча.

Правка: имя клавиши берётся из `nativeVirtualKey` через новую чистую функцию
`key_name_for_vk` (`services/hotkeys.py`), построенную **из** `_VK_BY_KEY`, а не
набранную второй таблицей. Драйверный код пришёл — судит только таблица, и его
отсутствия в ней достаточно для отказа; `key()` остаётся запасным путём **только
при нулевом коде** (события, синтезированные программно). Тестов 1240 (было 1214).
Спека §4.1 переписана до кода — её утренняя редакция объявляла нормой то, что
прогон опроверг (правило проекта: переписывается правило, а не подгоняется код).

**Уточнение внесено финальным ревью ветки (п. Б-1), и оно поймало настоящий
дефект первой редакции правки.** Пока запасной путь срабатывал на любом коде вне
таблицы, NumPad 5 (`VK_NUMPAD5`, `0x65`) принимался именем главного ряда: поле
показывало `Ctrl+Alt+5`, `RegisterHotKey` подписывался на `0x35`, и клавиша,
которой сочетание назначали, не поднимала окно никогда. Ревьюер доказал разрыв
не рассуждением, а тем, что подменил тело функции на версию, описанную в
докстринге, — и все тесты остались зелёными: фактическое поведение не было
закреплено ни одним утверждением. Дефект не был регрессией (на `master` то же
самое), но ветка декларировала инвариант «читаем ровно то, на что подпишется
регистрация» и его не держала.

**[Проверено, 20.08.2026, машина заказчика]** Правка проверена не только тестами:
на живой клавиатуре при активной русской раскладке `Ctrl+Alt+B` встал в поле,
чего до неё сделать было нельзя (шаг В3 протокола). Гипотеза о `nativeVirtualKey`
подтверждена железом — драйвер шлёт `0x42` для клавиши позиции `B` в обеих
раскладках.

Мутации ставил отдельный агент, не автор тестов; девять мутаций в первом круге,
три в подтверждающем. **Таблица ниже снята с первой редакции `_key_name`, которой
в коде уже нет** (её сузила находка ревью Б-1, см. абзац выше): строки 1 и 2
описывают тогдашний широкий запасной путь, а не сегодняшнее поведение. Оставлена
как есть намеренно — она документирует, что именно проверялось на том шаге.
Существенное — не в успешных мутациях, а в **бессильной**:

| № | Мутация | Результат дословно |
| --- | --- | --- |
| 1 | `_key_name`: приоритет снят — сначала `key()`, драйвер запасным | `AssertionError: assert 'G' == 'B'` (победил раскладко-зависимый источник) плюс `assert None is not None` в двух тестах. Убито 3 |
| 2 | `_key_name`: запасной путь удалён (`return ""`) | `assert None is not None` в `test_captures_modifier_combination`. Убито 5 |
| 3 | `key_name_for_vk`: `_KEY_BY_VK.get(vk, "B")` — неизвестный код получает **поддержанное** имя | `AssertionError: assert HotkeySpec(modifiers=3, vk=66, key='B') is None` в тесте отказа. Убито 8 |
| 4 | `_KEY_BY_VK[0x4D] = "M1"` — прямая и обратная таблицы разъехались | `assert None is not None` в `test_key_name_for_vk_agrees_with_parse_hotkey`. Убито 1 — единственный сторож этого класса |
| 5 | `keyPressEvent`: фильтр нелатинских кодов (правдоподобное **неправильное** исправление той же находки) | `assert [] == ['Ctrl+Alt+B']`. Убито 1 — только тест виджета целиком; тесты `spec_from_event` идут мимо и остаются зелёными |
| 6 | **`_key_name`: `...toString()[:1]`** | **Не убила ни одного теста из 1235.** См. ниже |

**Мутация 6 была бессильной, и это главный результат разбора.** Обрезка имени до
первого символа молча превращает `Ctrl+Alt+F5` в `Ctrl+Alt+F` — не отказ, а
подмену на другое рабочее сочетание, — и весь набор из 1235 тестов этого не видел.
Причина: запасной путь испытывался ровно одним стимулом, латинской `B`, тем же,
что и в соседнем тесте; у неё имя односимвольное, и обрезка её не задевает.
Тест параметризован тремя формами имени — буква, цифра, F-клавиша, — после чего
мутация 6 убивается (`AssertionError: assert 'F' == 'F5'`), а контрольная мутация
того же класса (`"".join(ch for ch in ... if ch.isalpha())`) убивается двумя
параметрами сразу: цифра падает на `assert None is not None`, F-клавиша — на
подмене имени. Мутация 2 после параметризации убивает 5 тестов вместо 3 —
сигнал расширился, не размылся.

Урок в копилку к «Главному уроку вехи» выше: **бессильной мутацию делает не
слабое утверждение, а бедный стимул.** Утверждение `spec.key == "B"` безупречно;
негодным его сделало то, что единственный вход теста был симметричен относительно
поломки. Проверяя функцию, которая переводит одно представление в другое, стимулы
надо брать из разных классов формы, иначе тест доказывает лишь то, что код
работает на одном примере.

**Вторая волна мутаций (после правок ревью) нашла ту же дыру на соседнем пути —
и это продолжение урока, а не его повторение.** Мутация
`(key_name_for_vk(vk) or "")[:1]` пережила все 1240 тестов: `F5` и `F12`
превращались в `Ctrl+Alt+F` — снова тихая подмена рабочего сочетания другим.
Причина — асимметрия, возникшая ровно потому, что первую находку лечили точечно:

| Путь | Когда работает | Чем был закреплён |
| --- | --- | --- |
| запасной (`vk == 0`) | только синтезированные события | тремя формами имени: `B`, `1`, `F5` |
| драйверный (`vk != 0`) | **каждое реальное нажатие** | только односимвольными: `B`, `2` |

То есть лечение получил редкий путь, а основной остался открытым. Закрыто
параметризацией драйверного пути теми же тремя формами
(`test_driver_code_names_every_form_of_key`, тестов стало 1242) со сверкой
не только имени, но и `spec.vk == native_vk` — именно она ловит случай,
когда на экране одно, а регистрация уходит на другую клавишу.

Урок: **найдя дыру, закрывать её на всех путях сразу, а не на том, где споткнулись.**
Точечная правка оставляет соседний путь без покрытия и создаёт ложное чувство,
что класс дефектов закрыт.

**Продолжение того же урока, третий круг.** Даже после симметричной правки
мутация `[:2]` пережила набор: `F10`, `F11`, `F12` превращались в `Ctrl+Alt+F1` —
снова подмена рабочего сочетания. Причина мельче прежней: многосимвольную форму
имени представлял **двухсимвольный** `F5`, и усечение до двух символов его не
задевало. Обе параметризации переведены на `F12`: трёхсимвольное имя доминирует
двухсимвольное как представитель класса — убивает и `[:1]`, и `[:2]`, — а число
тестов при этом не растёт. **Представителя класса выбирают самым сильным
из доступных, а не первым попавшимся:** слабый представитель закрывает класс
только на бумаге.

**Четвёртый круг закрыл спор о представителях по существу — переходом на перебор.**
После замены `F5` на `F12` набор пробила мутация `.replace("0", "")`: она пережила
все 1242 теста, превращая `Ctrl+Alt+F10` в `Ctrl+Alt+F1` (регистрация уходит
на чужую клавишу), а `Ctrl+Alt+0` — в молчаливый отказ. `0` и `F10` — обычные
поддержанные клавиши, до которых не дотягивался ни один UI-тест, потому что
представителями были выбраны `B`, `2` и `F12`.

Третья волна подряд отвечала на вопрос «закрыт ли класс» одинаково — «добавьте
ещё представителя». Это не сходящийся процесс. Добавлен
`test_every_supported_driver_code_names_itself`: перебор всех кодов `0x00`–`0xFF`,
где поддержанность каждого спрашивается у самой `key_name_for_vk`, а не берётся
из перечня диапазонов, — значит расширение таблицы попадёт под перебор
автоматически. `key()` на каждой итерации задан заведомо не тем (кириллическая
«Б»), поэтому все 48 итераций заодно закрепляют приоритет драйверного кода
над раскладкой. Тестов 1245.

Дублирования с `test_key_name_for_vk_agrees_with_parse_hotkey` нет, хотя
перебирается то же множество кодов: тот тест живёт в `services` и проверяет
round-trip двух таблиц, а мутации сидели в `ui/hotkey_edit.py`, куда сервисный
перебор не заглядывает вовсе.

Урок, который стоит всей ветки: **представительный набор доказывает, что код
работает на выбранных примерах, а не что он работает.** Пока стоимость перебора
сопоставима со стоимостью спора о том, достаточно ли представителей, — перебор
строго лучше: он превращает утверждение «мы взяли удачные примеры» в «каждый
поддержанный вход обрабатывается собой».

Пятая волна мутаций проверяла уже сам перебор, и он выдержал: **ни одна мутация
драйверного пути не выжила**, включая две атаки на оракул. Опустошённая таблица
(`key_name_for_vk` возвращает `None` на всё) сделала бы тест самосогласованным
и зелёным ни на чём — ловит счётчик `supported == 48`. Самосогласованно сдвинутая
(`get(vk + 1)`) проходит и по именам, и по счётчику — ловит сверка `spec.vk == vk`.
Занятно, что та же строка в соседнем параметризованном тесте различающей силы
не имеет: там имя проверяется абсолютной константой, здесь — самосогласованным
оракулом, и единственным абсолютным остаётся round-trip через `parse_hotkey`.
Одинаковый код в двух тестах играет разные роли — записано в докстрингах обоих.

Две границы зафиксированы честно, а не умолчаны:

- **Перебор слеп к согласованной перестановке таблицы.** Поменяв в `_VK_BY_KEY`
  два кода вместе с именами, round-trip не сломаешь. Ловят это тесты `services`
  с абсолютными значениями и вписанные константы `VK_B`/`"B"` в UI-тестах —
  то есть их нельзя удалять как «дублирующие перебор». Записано прямо
  в докстринге перебора: иначе следующий исполнитель уберёт якоря.
- **Запасной путь оставлен на представителях сознательно** — перебор там
  заставил бы тест повторять вычисление кода (имена даёт Qt, а не наша таблица).
  Цена сделки измерена: мутация `.replace("9", "")` переживает набор, ломая
  цифру `9` и `F9`. Риск принят — путь срабатывает только при
  `nativeVirtualKey == 0`, живая клавиатура туда не приходит; сделка записана
  в докстринге, чтобы не превратиться в забытое место.

Удалён `test_cyrillic_be_lives_on_another_key_and_is_rejected` как **строго
избыточный**: его стимул совпадал с итерацией перебора буква в букву, убить один,
не убив другой, невозможно. Знание из него (почему «Б» — это класс отказа, и как
первая редакция тестов утверждала физически невозможное событие) перенесено
в докстринг «принимаемой» половины пары. Строго избыточный тест не просто не
бесплатен — он вводит в заблуждение: выглядит рубежом, не будучи им. Тестов 1244.

Там же снята и переформулирована громкая, но неверная формулировка в докстринге:
сверка `spec.vk == native_vk` объявлялась тем, «что ловит подмену клавиши»,
а собственной различающей силы у неё нет — `spec.vk` выводится из имени, а не
из события, и при верном `spec.key` совпадение следует автоматически. Доказано
мутацией: сдвиг `vk` роняет сразу 14 тестов, из которых 13 — не этот. Настоящий
сторож — сплошной перебор в `test_key_name_for_vk_agrees_with_parse_hotkey`.
Утверждение оставлено (записывает инвариант в точке применения), докстринг
приведён к факту.

### Пустое значение в Run: чем обернулась метка «не проверено» (21.08.2026)

Шаг А3 блока А — первый же шаг после установки — **опроверг** ожидание вехи.
Установщик 0.1.0 создавал в `HKCU\...\Run` значение `OneCStarter` длиной
0 символов. Замер на живой машине: имя в `GetValueNames()`, `GetValueKind` —
`String`, данные — пустая строка.

Причина в неверно понятом флаге. Веха считала, что `dontcreatekey` не даёт
создать **значение**; **[из документации Inno Setup]** он относится к **ключу**
(«Setup will not attempt to create the key or any value if the key did not
already exist»), а ключ `Run` существует на любой машине всегда. При
`ValueType: string` без `ValueData` Inno записывает пустую строку.

**Дефект — не мусор в реестре, а ложь о состоянии системы.** `is_enabled`
возвращал `True` на любом существующем значении, поэтому раздел «Настройки»
показывал автозапуск включённым сразу после установки, хотя выполнять при
входе было нечего. Хуже того, расхождение самонеизлечимо ровно по причине,
описанной в докстринге самой `is_enabled`: пользователь, видящий «включено»,
не станет включать, а значит не перезапишет запись верными данными. И формально
нарушалась §1 спеки — «автозапуск никогда не включается молча».

Правка парная (ветка `fix/2026-08-21-installer-empty-run-value`):

- `build/installer.iss` — `ValueType: none` вместо `string`. **[Из документации
  Inno Setup]** «If none (the default setting) is specified, Setup will create
  the key but _not_ a value»: запись остаётся в секции исключительно ради
  `uninsdeletevalue`. Флаг `dontcreatekey`, сохранённый было «на случай,
  если ключа `Run` нет в экзотическом окружении», **снят 22.08.2026** финальным
  ревью ветки: при отсутствующем ключе Setup пропустил бы строку целиком,
  а значит (вывод из документации, не её текст) не зарегистрировал бы и удаление
  значения — тогда как приложение всё равно создало бы и ключ, и значение своим
  `enable()`;
- `services/autostart.py` — `is_enabled` считает включённым только непустое
  значение (`bool(value and value.strip())`). Нужно и само по себе: у всех,
  кто уже поставил 0.1.0, пустая запись в реестре **уже лежит**, и правка
  установщика её не уберёт.

Почему это стоит записать отдельно, а не строкой в таблице: метка достоверности
«из документации, не проверено» на этом шаге сработала ровно так, как правило
проекта и предполагает. Утверждение из документации было прочитано добросовестно,
звучало разумно, прошло три круга ревью вехи — и оказалось неверным на живой
машине с первой попытки. **Документация отвечает на вопрос, который задал
читатель, а не на тот, который он имел в виду**: там было написано про ключ,
а прочитано — про значение.

### У Windows истина об автозапуске не одна (22.08.2026, находка шага В7)

Спека §3.1 объявляла истиной значение в `HKCU\...\Run`. Прогон показал, что
Windows считает автозапуск включённым по **паре**: значение в `Run` **и** метка
в `HKCU\...\Explorer\StartupApproved\Run`, которую ставит «Диспетчер задач».

Замер после «Отключить» в Диспетчере: значение в `Run` на месте (79 символов),
метка появилась (первый байт `0x01`), Диспетчер показывает «Отключено». Наш
раздел при этом уверенно показывает «включено» — по своему единственному
источнику он прав, а по факту врёт.

**Ловушка подтверждена отдельным экспериментом**, и она хуже простой лжи:
выключение и повторное включение тумблером перезаписали значение в `Run`,
а метка осталась. То есть после отключения снаружи раздел «Настройки»
бесполезен — что бы пользователь ни делал, автозапуск не заработает.

Кодировку метки решено **не использовать**: формат не документирован, а на
одной и той же машине у соседних отключённых записей первый байт `0x03`,
у нашей — `0x01`. Правило «нечётный = отключено» с наблюдениями согласуется,
но строить показ состояния на нём — врать увереннее, чем сейчас.

Решение заказчика 22.08.2026 — честный минимум: постоянная строка под тумблером
(`AUTOSTART_ROW_NOTE`, `ui/settings_view.py`), сообщающая, что отключённый
в Диспетчере автозапуск включается только там же. Тупик размыкается, расхождение
остаётся; его устранение — отдельное решение и отдельный эксперимент над
недокументированным форматом.

Тест `test_autostart_row_warns_about_task_manager` проверяет подпись **своей
строки** через аксессор `row_note("Запускать при входе в Windows")`, а не
значение константы и не наличие текста где-нибудь в разделе. Первая редакция
искала текст по `findChildren(QLabel)` — и мутационная проверка показала, что
так переживают сразу две подмены: подсказку можно скрыть (`setVisible(False)`)
и повесить под соседнюю настройку. Второе хуже отсутствия подсказки: она
начинает указывать не на ту настройку. Парный аксессор `row_control` сторожит
третий случай — заголовок и подпись на местах, а переключатель под ними чужой.

**Метка переживает деинсталляцию** — выяснилось контрольным циклом того же дня.
После полного удаления программы (каталог установки исчез, значение из `Run`
удалено) запись `StartupApproved\Run\OneCStarter` осталась в реестре. Значит
переустановка от проблемы не спасает: свежая программа запишет значение в `Run`,
а Windows по-прежнему будет считать автозапуск отключённым, и ни в одном нашем
файле следов этого не будет. Довод за подсказку усиливается: без неё у
пользователя нет ни одного способа догадаться, почему автозапуск молчит.

**Подсказка упиралась в ту же стену на шаг дальше — исправлено 22.08.2026.**
В «Диспетчере задач» Windows показывает `FileDescription` из метаданных exe,
а он гласил «Запуск информационных баз 1С:Предприятие». То есть подсказка звала
пользователя туда, где он искал бы «OneCStarter» и не нашёл. Находка финального
ревью ветки; решение заказчика — имя в описание: теперь
`OneCStarter — запуск информационных баз 1С:Предприятие`
(`build/onecstarter.spec`, проверено на пересобранном exe через `VersionInfo`).

### Урок шести волн мутаций: правило проверяют не везде, где оно действует (21–22.08.2026)

Ветка `fix/2026-08-21-installer-empty-run-value` прошла **шесть** волн мутационной
проверки и два круга финального ревью. Дыры находились каждый раз, и все — одного рода,
который стоит назвать прямо: **проверку ставят там, где нашлась мутация, а не
там, где действует правило.**

| Волна | Что было закрыто | Что осталось открытым рядом |
| --- | --- | --- |
| 1 | пустое значение = выключено | тест «включено» подавал одно значение — любое условие на содержимое проходило |
| 2 | формы значения (флаг, кавычки, длина) | чтение не пишет — проверено только в ветке пустого значения |
| 3 | подсказка на своей строке | орган управления строки не привязан к ней ничем |
| 4 | пустое значение видит и вьюха | тот же возврат в ветке `showEvent`; доступность тумблера при успешном чтении |
| 5 | `showEvent` и доступность тумблера | **четвёртое** место того же правила — откат при отказе записи; чистота чтения во вьюхе проверена только на изменении содержимого |
| 6 | откат при отказе, чистота во вьюхе | показ раздела при **нормальном** значении — обе проверки `view.show()` шли на отсутствующем и на пустом |

Каждая следующая волна закрывала предыдущую находку **точечно** — и тут же
обнажала соседний путь, где то же правило не проверялось. Приём, который
в итоге сработал: закрыв находку, спрашивать не «убита ли мутация», а
**«где ещё действует это правило и проверено ли оно там»**. Конкретно на этой
ветке: правило «включено = непустое значение» действует в трёх местах —
чистая функция, конструктор вьюхи, `showEvent`; правило «чтение не пишет» —
в обеих ветках `is_enabled`; правило «строка — это заголовок, подсказка
и орган вместе» — во всех пяти строках раздела.

Практический вывод дешевле, чем кажется: перечислить места действия правила
занимает минуту, а каждая необнаруженная дыра стоила отдельной волны мутаций
и отдельного круга правок.

Пятая волна проверяла уже сам приём — и он сработал: получив задание «назови
все места, где действует правило», мутатор нашёл **четвёртое**, о котором
не помнил никто, включая автора правила. Откат тумблера при отказе записи
(`_choose_autostart`) идёт через тот же `_sync_autostart`, и тест отказа
строил реестр без значений — на пустом реестре старая и новая редакции
отвечают одинаково. Живой сценарий: в `Run` пустая строка от установщика,
запись падает по политике, раздел откатывается в «включено» на пустом месте.

Шестая волна дала последнюю находку — и она про другую ось. Мест применения
правила действительно четыре, пятого вызова нет; дыра была в том, что **путь
`showEvent` ни разу не проходили с нормальным значением**: оба теста с
`view.show()` работали на отсутствующем или пустом. Переживали две мутации
сразу — «показ всегда снимает галку» (раздел врёт о включённом автозапуске,
только в другую сторону) и «показ пишет в реестр» (запись в `HKCU` при каждом
открытии раздела на рабочей машине). То есть перебирать надо не только места
применения правила, но и **входы на каждом из них**, включая самый обычный:
проверки естественно скапливаются на краевых случаях, а нормальный вход
остаётся непокрытым именно потому, что он «очевидно работает».

Итоговый счёт правила «включено = непустое значение»: четыре места действия
(чистая функция, конструктор вьюхи, `showEvent`, откат при отказе), все
закрыты и на пустом, и на нормальном входе. Правило «чтение не пишет»: обе ветки `is_enabled` плюс вьюха, где
сторожа стояли на изменении содержимого и на удалённом значении — запись
«той же строкой» при каждом открытии раздела проходила незамеченной.

### Разбор долга вехи (22.08.2026, ветка `chore/2026-08-22-debt`)

Пункты долга разобраны после релиза 0.2.0. **Закрыто шесть, оставлено пять** —
и оставлено не молчанием, а с обоснованием: долг, который просто висит, через
месяц читается как «забыли», а не как «решили не делать».

| № | Что сделано |
| --- | --- |
| 1 | Три краевых входа `parse_hotkey` (`"+++"`, `"Ctrl+Alt+Ctrl+B"`, `"Ctrl+Alt+F 5"`) стали строками параметризации — раньше проверялись руками |
| 3 | `test_all_fields_are_written` удалён как дубль `test_schema_is_written`: разница была в одном поле темы, утверждение одно |
| 5 | Десять мест с `recent_limit=lambda: 10` переведены на `DEFAULT_RECENT_LIMIT` |
| 8 | `run_smoke` больше не читает живой реестр: `_build_main_window` принимает `autostart_registry`, самопроверка получает `NullRegistry` |
| 10 | Без трея занятое на старте сочетание открывает окно на разделе «Настройки» — там уже стоит строка отказа |
| новый | `WindowsRegistry` покрыт тестами через подмену функций `winreg`; живой `HKCU` по-прежнему неприкосновенен |

**Оставлено осознанно:**

- **№2** (нет комментария у случая `"Ctrl+Alt+F13"`, в эталонном блоке плана
  не проставлены `# noqa` для кириллицы) — текст плана и комментарий в тесте
  не влияют ни на код, ни на поведение.
- **№4** (`autostart_command` с уже закавыченным путём) — недостижимо:
  единственный источник аргумента `sys.executable`, платформа его не кавычит.
  Тест зафиксировал бы поведение на входе, которого не бывает.
- **№6** (автоиспользуемая фикстура тянет `qapp` в тесты чистой логики) —
  эффект нейтральный; трогать общую фикстуру ради этого дороже, чем терпеть.
- **№7** (двойной `_sync`: подписан и на `controller.changed`, и на
  `store.changed`) — **не трогаем, и это отдельное решение.** Разделение
  на `_sync_theme` и `_sync` убрало бы лишний вызов, но наблюдаемого эффекта
  у правки нет, а риск есть: сейчас `_sync` при любой правке настроек приводит
  и кнопки темы, то есть чинит возможный рассинхрон; после разделения этой
  подстраховки не станет. Правка без проверяемого выигрыша, но с новым
  краевым случаем — плохой размен.
- **№9** (`_build_main_window` перегружена) — **не закрыт, и ветка сделала его
  больше.** Функция получила параметр `autostart_registry` и ветку показа
  раздела: было около 170 строк, стало 185. Разрез на `_wire_settings` остаётся
  разумным, но делать его вместе с шестью правками поведения значило бы
  смешать рефакторинг с содержательными изменениями в одной ветке — а тогда
  ревью не отличит одно от другого. Пункт назван здесь прямо: раздел, который
  сам осуждает молчание, в первой редакции о №9 умолчал (находка финального
  ревью ветки 22.08.2026).
- **№11** (отчёты ревьюеров не пишутся в файлы) — процессный пункт, не код.
  Прогон 20–22.08 показал рабочий обходной путь: контроллер приводит дословную
  цитату ревью из своего контекста, а находки закрепляются в коммитуемых
  документах сразу. Отдельного механизма это не требует.

### Мутационные волны разбора долга (22.08.2026)

Две волны, десять правок тестов. Записано здесь, потому что докстринги тестов
видит только тот, кто их открыл, а урок общий.

**Самое важное — не убитые мутации, а две бессильные.**

1. **Тест долга №8 в первой редакции был вакуумно зелёным.** Он проверял, что
   `run_smoke` *передал* в `_build_main_window` объект класса `NullRegistry`,
   то есть намерение вызывающего. Мутация «`_build_main_window` игнорирует
   переданный реестр и всегда берёт настоящий» пережила весь набор — долг
   откатывался бесшумно. Хуже того, под pytest `sys.frozen` отсутствует,
   `_sync_autostart` уходит по короткой ветке, и настоящий реестр от поддельного
   был наблюдаемо неотличим: тест не проверял вообще ничего.
   Переписан на факт: `winreg` подменяется запрещающими заглушками до вызова,
   `sys.frozen` включается намеренно, утверждается `touched == []`.
2. **Позитивный контроль оказался обязателен.** Мутация «`SettingsView` получает
   жёсткий `frozen=False`» разом убивала тумблер автозапуска в собранном exe
   и обнуляла тест, который эту фичу сторожит. Добавлен парный тест: с
   `sys.frozen=True` и настоящим реестром обращение к `winreg` обязано
   состояться. Пара утверждает то, что нужно: **в сборке реестр читается,
   а самопроверка сборки — нет.** Ни один из двух тестов поодиночке этого
   не утверждает.

Прочее, закрытое волнами: подделка `winreg` молча принимала любой куст, тип
значения и флаг доступа (`HKLM` вместо `HKCU`, `REG_DWORD` вместо `REG_SZ`,
`KEY_READ` вместо `KEY_SET_VALUE` — всё это переживало набор); `delete` глушил
`PermissionError` наравне с `FileNotFoundError`, хотя для `read` тот же класс
уже был закрыт; аксессор `current_section_index`, заведённый ради одного теста,
делал его зелёным при заглушке `return 1` — удалён, тест смотрит наблюдаемое
состояние окна.

**Страховка от разрушения.** Подделка перехватывает пять функций `winreg`;
мутация, позвавшая неперехваченную (`DeleteKey` разрушителен), ушла бы в живой
`HKCU` машины разработчика. Мутатор такую мутацию ставить отказался именно
из-за цены ошибки — и это правильный отказ. Остальные функции модуля теперь
запираются явно.

**Наблюдение без правки.** Долг №10 вернул в самопроверку зависимость от
машины — того же класса, что закрытый долг №8, только слабее: под
`QT_QPA_PLATFORM=offscreen` трея нет, поэтому занятое на машине сборщика
сочетание уводит окно smoke на «Настройки». На код выхода и на проверки
`build/smoke.py` (лог, `frozen=True`, цель ярлыка) это не влияет, поэтому
правка не делалась — но знать стоит.

### Принятый риск: `[UninstallDelete]` и чужой `_internal` (22.08.2026)

Секция удаляет `{app}\_internal` целиком (`filesandordirs`), потому что состав
каталогов внутри задаёт PyInstaller и перечень `dirifempty` по уровням протух бы
молча при смене версий PySide6. Находка финального ревью ветки: `_internal` —
родовое имя **любой** one-dir сборки PyInstaller, а страница выбора каталога
в установщике не отключена. Поставив программу в чужой непустой каталог, где
уже лежит `_internal` другой такой программы, пользователь получит удаление
чужого дерева при нашей деинсталляции.

Решение заказчика 22.08.2026 — **риск принят, выбор каталога сохранён**.
`DisableDirPage=yes` закрыл бы дыру полностью, но отнял бы установку на другой
диск. Заслон остаётся один: штатное предупреждение Inno «папка уже существует».
Записано и в комментарии `build/installer.iss` — там, где стоит сама секция,
чтобы следующий исполнитель не принял утверждение «`_internal` целиком наш»
за безусловное: оно верно только для каталога по умолчанию.

### Нарушенное правило процесса (20.08.2026)

Правило 7 вводной («параллельные агенты не работают в одном worktree») нарушено:
мутационная проверка и ре-ревью ветки запускались **одновременно в одном рабочем
дереве**. Ревьюер трижды видел в дереве чужие мутации (`M src/...`, а в один
момент `parse_hotkey('Ctrl+Alt+Shift+2')` возвращал `vk=51` вместо `0x32`) и
спасся тем, что стал перепроверять всё на чистой копии `git archive HEAD` —
и сам же указал на это в отчёте. Цена ошибки: ревью на подложном состоянии кода
могло дать ложные находки или, хуже, ложное «чисто». Правило существует не для
красоты: мутатор по своей природе держит дерево сломанным бо́льшую часть времени.
Впредь — либо последовательно, либо ревьюеру отдельный worktree.

Две мутации той же волны выжили, но находками не являются, и различать это
обязательно: снятие охранника `if not key_name: return None` и снятие быстрого
пути `if key in _MODIFIER_KEYS` — эквивалентные мутанты, наблюдаемого поведения
не меняют (`parse_hotkey` отвергает и пустой хвост, и имена `Control`/`Meta`;
проверено замером). Второе прямо подтверждает докстринг «быстрый путь, а не
рубеж» — выживание мутации здесь доказывает утверждение кода, а не вскрывает
дыру. Пережить набор мало: мутация обязана менять поведение, иначе её выживание
ничего не говорит о тестах.

Уточнён и докстринг теста отказа `test_cyrillic_key_without_latin_counterpart_is_still_rejected`:
он обещал ловить «захват принял то, что `parse_hotkey` отвергнет», а ловит более
узкое и более важное — «неподдержанный код переведён в имя ИЗ набора». Мусорное
имя на выходе таблицы безопасно, его добивает `parse_hotkey`; подстановка годного
имени превращает промах в чужое рабочее сочетание, и остановить это уже некому.

### Мутационные проверки вехи «Завершение v1» (23.08.2026)

Правило проекта (`CLAUDE.md`): тест, зелёный на пустышке, хуже отсутствующего — он
создаёт ложную уверенность; мутацию ставит **не автор теста**. Ветка
`feat/2026-08-23-v1-completion` (настройка «Клиент по умолчанию» и очистка кэша,
спека [2026-08-23-v1-completion-design.md](superpowers/specs/2026-08-23-v1-completion-design.md),
план [2026-08-23-v1-completion.md](superpowers/plans/2026-08-23-v1-completion.md)),
семь мутаций из плана по защитным тестам `services/cache.py` и
`ui/bases/view.py::clear_cache` (одна — №6 — в двух вариантах, адаптер `WindowsCacheOps`
и обходчик `clear`, итого восемь применений). Мутация ставлена независимым агентом,
результат воспроизведён, мутация откачена; дерево после каждой возвращено в чистое
состояние (`git status --short` пуст), полный прогон снова зелёный (1342 passed).
Подробный лог с диффами каждой мутации — леджерный документ
`.superpowers/sdd/2026-08-23-v1-completion/mutation-report.md` (в репозиторий не идёт).

| № | Тест | Мутация | Кто ставил | Результат дословно |
| --- | --- | --- | --- | --- |
| 1 | `tests/unit/test_cache.py::TestCachePath::test_invalid_id_gives_no_address` | `cache_path`: снята проверка `is_valid_cache_id`, `section_id=None` → `""` (код исполняется, а не выходит) | независимый агент мутационной стадии (не автор тестов) | «8 failed, 1 passed» — упали все 8 параметризованных случаев, включая ГЛАВНЫЙ РИСК §5.1 (пустой ID): `AssertionError: assert WindowsPath('...Roaming/1C/1Cv8/a1b2c3d4-...') is None`. Откат чист |
| 1 (UI) | `tests/ui/test_bases_view.py::test_cache_items_disabled_without_id` | та же мутация | тот же | **ПЕРЕЖИЛА** — «1 passed». См. разбор ниже |
| 2 | `tests/unit/test_cache.py::TestCachePath::test_invalid_id_gives_no_address` | `is_valid_cache_id`: `_GUID.fullmatch(section_id)` → `re.match(_GUID.pattern + "$", section_id)` | независимый агент мутационной стадии (не автор тестов) | «1 failed, 7 passed» — падает ровно кейс `GUID+"\n"`, предсказанный докстрингом теста ($ пропускает завершающий перевод строки). Откат чист |
| 3 | `tests/ui/test_bases_view.py::test_clear_cache_without_confirmation_deletes_nothing` | `clear_cache`: подтверждение больше не гейтит удаление (`confirm_cache_clear` вызывается, но результат игнорируется) | независимый агент мутационной стадии (не автор тестов) | «1 failed» — `Failed: сводка без удаления` (`show_cache_report` вызван вопреки отказу пользователя). Откат чист |
| 4 | `tests/unit/test_cache.py::TestClear::test_busy_file_does_not_stop_walk_and_secondary_is_not_counted` + `tests/ui/test_bases_view.py::test_clear_cache_reports_busy_files_once` | `clear`: вторичный отказ (ветка `else` при незавершённом каталоге) тоже увеличивает `failed` | независимый агент мутационной стадии (не автор тестов) | «2 failed» — unit: `failed: 2 != 1`; UI-сводка: «Не удалось удалить 2» вместо «1». Откат чист |
| 5 | `tests/unit/test_cache.py::TestClear::test_busy_file_does_not_stop_walk_and_secondary_is_not_counted` | `clear_dir`: после первого `failed += 1` на файле — `return False` (обход прерывается) | независимый агент мутационной стадии (не автор тестов) | «1 failed, 5 passed» — `deleted: 2 != 3` (`b.bin`, идущий после занятого `a.bin`, не удалён). Откат чист |
| 6а | `tests/unit/test_cache.py::TestWindowsCacheOpsIntegration::test_junction_is_removed_without_touching_target` | `WindowsCacheOps.list_dir`: снята проверка `entry.is_symlink() or entry.is_junction()` (junction классифицируется по `lstat` как обычный каталог) | независимый агент мутационной стадии (не автор тестов) | «1 failed» на настоящем reparse point (`tmp_path`, `_winapi.CreateJunction`) — падает РАНЬШЕ, чем предсказывал план: `assert entries["junction"] is EntryKind.LINK` → `EntryKind.DIR`, ещё до попытки удаления цели. Откат чист |
| 6б | `tests/unit/test_cache.py::TestClear::test_link_is_removed_as_link_and_never_walked` | `clear`/`clear_dir`: ссылка (`EntryKind.LINK`) обрабатывается как каталог — рекурсия вместо `remove_link` | независимый агент мутационной стадии (не автор тестов) | «1 failed» — падает на первой же проверке: `assert link in ops.removed_links` → `[]` (ссылка ушла через `remove_dir`, не `remove_link`). Откат чист |
| 7 | `tests/ui/test_bases_view.py::test_clear_cache_silently_exits_when_directory_disappeared` | `clear_cache`: снята перепроверка `self._cache_ops.is_dir(path)` перед `measure` | независимый агент мутационной стадии (не автор тестов) | «1 failed» — воспроизведено дословно по докстрингу теста: `KeyError` внутри `FakeCacheOps.list_dir` (вызван из `measure`), а не `pytest.fail`; обе заглушки-ловушки (`confirm_cache_clear`, `show_cache_report`) не вызваны. Откат чист |

Оговорки к мутациям 6а и 6б: точка падения в обоих случаях раньше, чем называл план
(«чужое.txt существует» и «`list_dir` на ссылке» соответственно) — тесты ловят
дефект на первой же относящейся к делу проверке, а не на последней. Сигнал сильнее
предсказанного, не слабее; отклонение от плана записано ради точности, а не потому
что это находка.

Финальная проверка после отката всех восьми мутаций: `uv run pytest -q` — 1342 passed;
`uv run ruff check .` — код 0; `uv run mypy` — код 0, «Success: no issues found in
132 source files».

**Пережившая мутация (находка) — мутация 1 против UI-теста.**
`test_cache_items_disabled_without_id` не поймал снятие GUID-проверки в `cache_path`,
хотя план это предписывал. Причина — не слабый `assert`, а структура кода:
`_add_cache_menu` (`ui/bases/view.py`) проверяет `cache.is_valid_cache_id(item.section_id)`
и делает `continue` (пункт неактивен) **раньше**, чем вызывает `cache.cache_path(...)`:

```python
if not cache.is_valid_cache_id(item.section_id):
    action.setEnabled(False)
    action.setToolTip(NO_CACHE_ID_NOTE)
    continue
path = cache.cache_path(self._cache_env, kind, item.section_id)
```

`is_valid_cache_id` мутацией не тронута. Для записи без `ID` UI никогда не доходит
до мутированной ветки `cache_path` — решение «неактивно» принимается раньше и
независимо от того, что делает `cache_path` при `None`/пустом значении. Это
защита-в-глубину (defense in depth), а не дыра: сам риск §5.1 (пустой `ID` → адрес
корня кэша) закрыт **полностью и независимо** unit-тестом `test_invalid_id_gives_no_address`
(мутация 1, unit — «8 failed», включая случай `""`). UI-тест проверяет другой,
тоже настоящий инвариант (пункт меню неактивен без `ID`) через `is_valid_cache_id`,
а не через `cache_path`.

Чем усилить (без правки самого теста, только описание стимула): независимая проверка
UI-слоя именно для GUID-барьера `cache_path` структурно недостижима, пока
`_add_cache_menu` держит собственный ранний барьер `is_valid_cache_id` — тест не может
подать в `cache_path` значение, которое пройдёт мимо `is_valid_cache_id`, но упадёт
внутри `cache_path` (это буквально одна и та же проверка). Единственный стимул,
который сделал бы UI-тест независимо чувствительным к контракту самой `cache_path`, —
тест, вызывающий `cache.cache_path` напрямую в обход `_add_cache_menu` (что и делает
`test_invalid_id_gives_no_address`) или, при рефакторинге, снятие дублирующей проверки
из `_add_cache_menu` в пользу единственного источника истины — `cache_path`
(изменение продуктового кода, не только теста, и с потерей защиты-в-глубину — решение
не мутационной стадии). Правка предписания плана: докстринг
`test_cache_items_disabled_without_id` («ЗАЩИТНЫЙ ТЕСТ пары к GUID-проверке §5.1»,
`tests/ui/test_bases_view.py`) стоит уточнить — он не является независимой парой
именно к GUID-проверке `cache_path` из-за раннего барьера `is_valid_cache_id`; тест
защищает свой собственный, соседний инвариант.

---

## T-05. Эксперименты на платформе, сессия 2 — `WIP` (блок В не проводился; T-05.12/13 → v2, добавлен T-05.14)

Проверка допущений слоя запуска и формата, накопленных ревью планов 2–4, до плана UI.
Решение заказчика 05.08.2026: сначала эксперименты, потом brainstorming и план UI
(запуск процессов 1С в рамках протокола согласован тогда же). Протокол с шагами
и картой «куда возвращается результат» — [research/t05-protocol.md](research/t05-protocol.md).

Блок А блокирует T-04.5: исходы меняют код запуска, а не детали. Блок Б правит
формат/группы. Блок В — опциональный, план UI не блокирует.

Блок А проведён агентом 05.08.2026, блок Б — 05.08.2026 сессией «заказчик у стартера,
агент снимает diff'ы» (автоматизация окна выбора ненадёжна, см. журнал T-05.5).
Результаты в протоколе. Блок В (T-05.10–T-05.12) не проводился — план UI не блокирует.

| # | Вопрос | Статус | Итог |
| --- | --- | --- | --- |
| T-05.1 | Форма присоединения значения к `/IBConnectionString` | DONE | **[Ф]** наша форма верна (значение прижато, кавычки удвоены), путь с пробелом работает; код без правок |
| T-05.2 | Находит ли `/IBName` базу из общего списка (`CommonInfoBases`) | DONE | **[Ф]** находит; клиент материализует запись в `ibases.v8i` с `External=1` полной перезаписью; ненайденное имя → окно выбора, не ошибка |
| T-05.3 | `/IBName` при дублях имён; регистр имени | DONE | **[Ф]** «Не уникальное имя…» при дублях, в т.ч. различающихся только регистром; поиск имени регистронезависим. **Код: casefold в `_reject_ambiguous_name`** |
| T-05.4 | Держит ли `/AppAutoCheckVersion-` нашу версию при иной `Version` секции | DONE | **[Ф]** держит; с включённой проверкой платформа перезапускает клиента версией секции. Модель запуска подтверждена |
| T-05.5 | `DefaultVersion` вне маски `Version` (остаток T-02.2) | DONE | **[Ф]** `DefaultVersion` побеждает маску даже вне её; неустановленный → молча максимум вообще. **Код: `resolve_version` расходится с платформой в обоих случаях** |
| T-05.6 | Пустой `Connect=` — группа или база | DONE | **[Ф]** группа; перезапись довершает превращение (удаляет `Connect=` и `Version`). **Код: `is_group` учитывает пустое значение — сделано** |
| T-05.7 | Регистр при сопоставлении `Folder` с именем группы | DONE | **[Ф]** регистрозависимо; несовпавший путь → неявный узел без секции, регистр не нормализуется. **Код: `group_binding_key` с точным регистром — сделано** |
| T-05.8 | Видит ли стартер группу без `OrderInTree` | DONE | **[Ф]** видна и безвредна, `OrderInTree` платформа дописывает сама; `groups._create` без правок |
| T-05.9 | Что стартер делает с содержимым при удалении группы | DONE | **[Ф]** молчаливый каскад; подтверждение «Удалить группу "имя"?» без слова о содержимом. Тексты для плана UI |
| T-05.10 | Локальный кэш базы (под фичу очистки кэша) | **DONE 23.08.2026** | Семь шагов из девяти измерены, четыре — без запуска 1С. Кэшей два: программный `%LOCALAPPDATA%CCv8\<ID>` (`Config`, `ConfigSave`, `SICache`, у заказчика 2,9 ГБ) и пользовательский `%APPDATA%CCv8\<ID>` (профили форм, словарь, история ввода, 341 МБ). Имя каталога = `ID` записи (58 совпадений из 66). Вид базы на состав не влияет. Удаление при открытой базе частичное. Удаление программного кэша безобидно — база запускается как обычно. Протокол — [t05-protocol.md](research/t05-protocol.md) |
| T-05.11 | Форма URL веб-базы из `ws` (опц.) | TODO (опционален, за пределами v1 — решение заказчика 23.08.2026) | опционально, релиз 1.0 не блокирует |
| T-05.12 | `/WA+` при настроенной ОС-аутентификации (опц., остаток T-02.7) | → v2 | не на чем ставить: ни одна база заказчика не входит по учётной записи Windows |
| T-05.13 | Как заставить платформу **проигнорировать сохранённые `Usr`/`Pwd` и `WA`** и спросить логин ([спека 4b, §6.2](superpowers/specs/2026-08-08-v1-plan4b-ui-edit-design.md)) | шаг 0 DONE 09.08.2026, остальное → v2 | **[Ф]** 58 записей баз: с `Usr` — 0, с паролем — 0, с `WA=1` — все 58. Игнорировать нечего, `WA` инертен. Задача 16 плана 4b снята из v1 решением заказчика |
| T-05.14 | Изолируется ли процесс 1С подменой `APPDATA` | DONE 09.08.2026 | **[Ф]** нет: путь к списку платформа берёт не из переменной окружения. Снято дважды — `1cestart.exe` переписал живой файл вместо копии; клиент по `/IBName` не нашёл имя, существующее только в копии. Плюс **[Ф]** запуск по `/IBName` меняет `mtime` живого `ibases.v8i`, не меняя содержимого. Возвращено в скил `platform-launch`, факты 12 и 13 |

T-05.4 и T-05.5 добавлены при сверке протокола с кодом: первый — потому что вся модель
запуска (версию разрешаем сами + `/AppAutoCheckVersion-`) держится на неснятом [Д],
второй — открытый остаток T-02.2, который `resolve_version` сейчас решает экстраполяцией.

### Обязательства из блока А (код и скилы)

| # | Пункт | Кому |
| --- | --- | --- |
| 1 | `_reject_ambiguous_name`: сравнение имён на casefold + тест (платформа считает дублями имена, различающиеся регистром) | DONE 05.08.2026 |
| 2 | `resolve_version`: `DefaultVersion` вне маски и неустановленный `DefaultVersion` — привести `fallback` к фактическому поведению платформы (наш выбор версии может отличаться осознанно, но подсказка «что запустил бы штатный стартер» врать не должна) + две строки в табличные тесты | решение принято 05.08.2026: гибрид ([спека UI, §4](superpowers/specs/2026-08-05-v1-ui-bases-design.md)), реализация в плане 4a |
| 3 | Снять допущение-комментарий у `/IBConnectionString` в `build_arguments`; в докстринге `services/launch.py` убрать «[не проверено]» про общие списки | DONE 05.08.2026 |
| 4 | План UI: дубль «пользовательская + общая» после первого запуска общей базы — штатное состояние (`External=1`), дедупликация T-04.4 по `ID` обязательна; отказ платформы при ненайденном имени — окно выбора, поэтому диагностика «имя не найдено» — наша | план UI |

### Обязательства из блока Б (код и скилы)

| # | Пункт | Кому |
| --- | --- | --- |
| 1 | `is_group`: пустое значение `Connect=` — группа ([Ф] T-05.6), тест перевёрнут с опровергнутого [Р] | DONE 05.08.2026 |
| 2 | `group_binding_key`: ключ группы без ID — путь с точным регистром ([Ф] T-05.7); расхождение `binding_key`/`paths` из ревью плана 4 закрыто в пользу регистрозависимости | DONE 05.08.2026 |
| 3 | План UI: подтверждение `GroupRemoval.RECURSIVE` обязано перечислять содержимое или его количество — платформа каскадит молча («Удалить группу "имя"?»), быть не хуже недостаточно, надо лучше | план UI |
| 4 | План UI: секция с пустым `Connect=` в списке — это группа, которую первая же перезапись платформой «канонизирует» с потерей `Version`; показывать как группу, править как группу | план UI |
| 5 | План UI: висячий `Folder` платформа рисует неявным узлом-группой без секции ([Ф] T-05.7), наш `build_tree` кладёт запись в корень с пометкой orphan (решение 3 плана 3) — решить: повторить платформу или оставить корень+пометку с объяснением расхождения. Если рисуем неявный узел — у него нет секции и ключа, операции над ним невозможны | план UI |

---

## T-06. Рестайл UI по мокапу 4b — `DONE`

Заказчик закрепил новый целевой вид документом 15.08.2026: «новый дизайн приятнее»
([мокап](superpowers/specs/assets/2026-08-08-v1-plan4b-mockup.html)). Дизайн-дельта —
[спека](superpowers/specs/2026-08-15-v1-restyle-design.md), реализация по семи задачам —
[план](superpowers/plans/2026-08-15-restyle.md), ветка `feature/2026-08-15-restyle`
(13 коммитов от `master`, линейная история, без слияний).

Светлая палитра инвертирует иерархию поверхностей (приподнятое — белое на сером,
а не серое на белом) и добавляет роли `accent_fill`/`selection`; тёмная не менялась.
Рельса разделов — 76 px, рисованные значки, «Настройки» сразу под «Базами». Панель
свойств — вариант B (заголовок + вид словом + моноширинный путь + кнопки), витрина
вынесена чистой функцией в `services` (инвариант 1). Новые формы значков размещения —
полки для серверной, пунктирный круг для неразобранной. Раздел «Настройки» — заголовок,
живой путь к файлу настроек, группа «ВНЕШНИЙ ВИД», сегментный переключатель темы.

Задача 7: четыре оффскрин-скриншота (тёмная/светлая тема × «Базы»/«Настройки»)
отданы заказчику. Финальное ревью ветки на самой сильной модели нашло два Important
одного класса «цвет мимо палитры»: подсказки панели красились системным цветом
placeholder (2,15–2,49:1 при пороге 4,5:1), путь подключения приглушался правилом
`QLineEdit:read-only` вопреки мокапу — закрыто волной правок со сторожами и точечным
ре-ревью, спека §4 дополнена вслед за находкой. Итоговая верификация — `pytest` 1039
прошли, `ruff` и `mypy` коды 0. Слияние в `master` — решение заказчика.

---

## T-07. Эксперименты серверной механики (v2) — `TODO`

Блокирует план реализации T-08: дизайн v2 опирается на непроверенные факты
о `ragent`, `regsvr32`/`radmin.dll` и MMC-консоли. Веха v2 стартовала сессией
brainstorming 26.08.2026 (заказчик пропустил кандидатов v1.1 и пошёл к
роадмапу §5): спека — [2026-08-26-v2-servers-design.md](superpowers/specs/2026-08-26-v2-servers-design.md)
(девять решений заказчика в §2, перечень непроверенных фактов в §10),
протокол — [research/t07-protocol.md](research/t07-protocol.md).

Правила: серверные процессы запускает заказчик или агент по явной команде
на каждый запуск; эксперименты — только на одноразовых каталогах кластера
в `e:\tmp\t07\` и сдвинутых портах (2540+), рабочие кластеры не трогаются.

| # | Вопрос | Статус |
| --- | --- | --- |
| T-07.1 | Семантика и дефолты ключей `ragent`; квотирование пути в `-d` | DONE 26.08.2026 (кроме дефолтов) — [Ф] слэши обоих видов; путь с пробелом в кавычках; ключи регистронезависимы; дефолты отложены — порт 1540 занят рабочим сервером |
| T-07.2 | Создаёт ли `ragent` каталог `-d` сам; состав каталога | DONE 26.08.2026 — [Ф] создаёт; реестр кластера в `reg_<regport>`, смена regport = новый пустой реестр |
| T-07.3 | Поведение при занятом порту (код, лог, висит ли) | DONE 26.08.2026 — [Ф] умирает молча, но оставляет живого `rmngr`-сироту на regport; спека §8 правлена |
| T-07.4 | Корректная остановка; судьба `rmngr`/`rphost`; целостность кластера | DONE 26.08.2026 — [Ф] TerminateProcess не убивает детей → остановка деревом; кластер переживает жёсткое убийство (тот же clstid); спека §4.2 правлена |
| T-07.5 | Порождает ли `ragent` детей; достаточность скана по имени | DONE 26.08.2026 — [Ф] дети: `rmngr`, `rphost`, `dbda` (все напрямую); `rphost`/`dbda` без `-d` — для статуса профиля скана по `ragent.exe` достаточно, для остановки нужен снимок дерева |
| T-07.6 | Видимость argv служб (SYSTEM) без повышения | WIP — [Ф] 26.08.2026: WMI без повышения не видит `CommandLine`/путь exe SYSTEM-процессов (106 проверено), спека §3.1/§3.3/§6.3 правлены вслед; осталось подтвердить на настоящей службе агента |
| T-07.7 | Признак службы в командной строке | TODO |
| T-07.8 | `regsvr32 radmin.dll`: повышение, `/u`, коды возврата | DONE 26.08.2026 — [Ф] перезапись поверх без `/u`; CLSID стабильны → текущая версия читается из HKLM без UAC; спека §3.4/§7 правлены; код отказа без повышения не снят (в проде не нужен) |
| T-07.9 | MMC: нужен ли `ras`; несовпадение версий; определение текущей | DONE 26.08.2026 — [Ф] ras не нужен (mmc → порт агента напрямую); версии консоль/сервер обязаны совпадать до сборки; текущая — из HKLM без UAC; спека §3.4 правлена |
| T-07.10 | Наличие `ragent.exe`/`radmin.dll` по установленным версиям | DONE 26.08.2026 — [Ф] все 7 версий несут `ragent`/`radmin`/`ras`, `.msc` в `common` один; оговорка: клиентской установки без серверных компонент на машине нет, вопрос о ней не снят |
| T-07.11 | Куда `ragent` пишет логи | DONE 26.08.2026 (с оговоркой) — [Ф] по умолчанию никуда: консоль пуста, в `-d` и `%LOCALAPPDATA%\1C` лога нет; полный поиск по диску не делался |
| T-07.12 | Пересечение `-range` двух серверов | DONE 26.08.2026 — [Ф] безвредно, процессы разводятся по свободным портам; запрещать только дубли `-port`/`-regport` |
| T-07.13 | Механизм сканирования процессов (psutil/WMI/WinAPI): вес, антивирусы, argv, стоимость | DONE 27.08.2026 — [Ф] psutil выбран (~90 мс против ~0,8 с WMI; путь exe SYSTEM-процессов виден); вес: dist 118,1 МБ с psutil (0.1.0 был 123 МБ — прирост растворился в обновлении зависимостей), сборка+smoke прошли без реакции Касперского; АВ при spawn ragent из собранного exe — пункт ручного чек-листа вехи |
| T-07.14 | Поднимает ли ragent нового патча кластер старого (и наоборот) — судьба маски в профиле | DONE 26.08.2026 — [Ф] внутри префикса 8.3.25 совместим в обе стороны (тот же clstid); маска остаётся с предупреждением; межминорная совместимость не мерилась |

Результаты возвращаются в новый доменный скил `.claude/skills/server-launch/`
(через `writing-skills`, baseline до написания — Iron Law) и в спеку v2;
опровергнутое переписывает правило спеки, а не подгоняет код.

---

## T-08. Реализация v2 «Серверы» — `WIP` (код готов, идёт финальное ревью ветки)

Спека: [2026-08-26-v2-servers-design.md](superpowers/specs/2026-08-26-v2-servers-design.md)
(провалидирована экспериментами T-07 — 12 из 14 закрыто 26.08.2026).
План: [2026-08-26-v2-servers.md](superpowers/plans/2026-08-26-v2-servers.md).

**Реализация завершена 27.08.2026** (ветка `feat/2026-08-26-v2-servers`,
субагентный цикл: свежий исполнитель на задачу + ревью каждой задачи):
все 17 задач приняты, из них пять прошли через круги исправлений
(задача 7 — позиция группы по спеке §3.5; задача 9 — сужение except;
задача 11 — UnknownItemError; задача 14 — два круга: рескан после
удаления + перенос удаления в контекстное меню, затем ленивое меню
без утечки; задача 15 — неприкосновенность каталога в for_edit (§3.2)
и окраска предупреждений; задача 16 — привязка проверки §8 к свежему
снимку, ложное «сервер умер» устранено). **1586 тестов** (было 1342,
+244), `ruff` и `mypy` — коды 0. Сборка: dist 118,1 МБ, setup 33,4 МБ,
smoke собранного экземпляра прошёл, Касперский не вмешался.
**Мутационная стадия: 10/10 предписанных мутаций убиты** независимым
агентом (не автором тестов), лог — леджерный
`.superpowers/sdd/2026-08-26-v2-servers/mutation-report.md`. Спека §3.2
и план (types-psutil) правлены вслед за находками ревью.

**Финальное ревью всей ветки (27.08.2026, самая сильная модель):
вердикт «With fixes», закрыто одной волной с ре-ревью.** Два Critical
(голые исключения spawn/terminate/startfile мимо ловцов UI — включая
ожидаемый АВ-сценарий; `ServersUnavailableError` ронял старт без окна)
и пять Important (работающий сервер при неразрешённой версии показывался
«версия не установлена» и был неуправляем — против решения заказчика 5;
слепое окно до первого скана ломало §6.4 — теперь первый скан немедленный;
«Погасить» мог убить rmngr чужого живого кластера; после add/update
не было рескана и пересопоставления; докстринг console.py искажал [Ф] Г2).
Все семь закрыты волной из 4 коммитов, ре-ревью волны чистое; 15 новых
защитных тестов, их мутации ставил независимый агент — **10/10 убиты**
(итого по вехе 20/20). Финальный гейт: **1601 тест**, `ruff`/`mypy` — 0.
Спека §3.1/§4.4 правлены вслед за находками.

### Долг вехи T-08 (вынесен финальным ревью и задачными ревью, слияние не блокирует)

Триаж финального ревьюера: всё перечисленное едет долгом осознанно.

1. Дубль `server_convention_for`/`convention_for` — общий помощник при
   третьем типе соглашений; двойное чтение `registry.toml`.
2. `server_store`: тест порядка нескольких профилей в round-trip;
   отдельный защитный тест ветки `UnicodeDecodeError`.
3. `process_scan`: мёртвый `except (AccessDenied, NoSuchProcess)` вокруг
   `process.info` (psutil гасит сам) — убрать при следующей правке;
   докстринг огрубляет [Ф] («недоступна» вместо «видна у 10/107»).
4. `elevation`: возвраты `WAIT_FAILED`/`GetExitCodeProcess` не проверяются —
   при NULL hProcess код соврёт «успех»; проверяется живым UAC-прогоном
   ручного чек-листа. Гонка `NoSuchProcess` в `children()` без теста
   (нужен мок psutil).
5. `stop_orphans` не снимает `children()` сироты — по [Ф] Т-07.5 дети
   у rmngr-сироты не наблюдались, риск принят; при жалобе — гасить
   деревом симметрично `stop` (решение зафиксировано здесь, обещание
   «в спеку при закрытии» выполнено этой строкой).
6. UI-мелочи: dim-окраска подписи каталога в диалоге; палитра для
   бейджей `ConsoleDialog` (и бейдж «работает» строить от фактической
   версии exe процесса, не от resolved); `_parse_int` для «-5» даёт
   неточное сообщение; «Открыть» консоль при current вне installed —
   тихий no-op (`.msc` один на все версии [Ф] Г1 — можно открывать
   от корня любой установки); регресс-тест «QMenu не копятся после
   N rebuild»; `spy_open` теста реестра не различает источник чтения;
   `rebuild` каждые 5 с пересобирает карточки целиком и читает HKLM —
   кандидат на дифф-обновление при жалобах на мерцание.

**Ручной чек-лист T-08 пройден 29.08.2026** вместе с чек-листом T-10
(итоги и находки — в T-10): запуск/остановка профиля, консоль с UAC
в обе стороны (раскладка `SHELLEXECUTEINFOW` подтверждена живым
повышением), антивирус на spawn из собранного exe молчит. «Погасить»
гасит только rmngr — находка 6 в T-10. **Осталось до закрытия T-08:
слияние решением заказчика** после волны исправлений по находкам
чек-листа.

### Находка ручного чек-листа (28.08.2026): россыпь консольных окон

Первый живой запуск профиля из собранного приложения открыл по
консольному окну на каждого ребёнка ragent (rmngr, rphost, dbgs, dbda,
java из dbgs). Механизм ([Р], эксперимент T-09.4 подтвердит):
`DETACHED_PROCESS` лишает ragent консоли — дети, не найдя её, заводят
свои. В T-07 окон не было: ragent запускался из PowerShell заказчика,
дети наследовали его консоль. Решение заказчика — не «спрятать окна»,
а встроенная консоль профиля с реверсом решения 3 (спека §12): серверы
становятся дочерними, выход лаунчера их завершает (с подтверждением),
крах — тоже (Job Object), вывод — в файл с tail-панелью и историей
прошлого запуска. Реализация — T-10 после экспериментов T-09.

---

## T-09. Эксперименты: консоль профиля и дочерний жизненный цикл — `DONE` (28.08.2026)

Блокер T-10 снят: все решающие вопросы закрыты за один день двумя
сессиями «заказчик запускает / агент снимает» + подставными деревьями. Протокол — [research/t09-protocol.md](research/t09-protocol.md);
формат сессии — T-07 (запуски ragent — заказчик или агент по явной
команде; подставные python-процессы — агент сам). Решения заказчика
28.08.2026 — спека §12.

| # | Вопрос | Статус |
| --- | --- | --- |
| T-09.1 | Буферизация вывода ragent при редиректе (задержка строк) | DONE 28.08.2026 (слабая база) — [Ф] единственная ловимая строка (dbgs) пришла в файл в первые секунды, блочной задержки не видно |
| T-09.2 | Наследуют ли дети файловый stdout (строки детей в одном файле) | DONE 28.08.2026 — **[Ф] ОПРОВЕРГНУТО**: баннеры rmngr/rphost существуют только в их собственных окнах, не достаются ни унаследованной консоли (видимой/скрытой), ни редиректу; ловится только dbgs. Панель переосмыслена в «Журнал профиля» (спека §12.1/§12.5, решение заказчика) |
| T-09.3 | Кодировка перенаправленного вывода | Не проверено — пойманные строки ASCII, кириллица в выводе платформы за весь эксперимент и всю разработку T-10 так и не встретилась. Снятие отложено до первого появления факта: перенесено в долг вехи T-10 (см. ниже), тест `tail` уже покрыт `errors="replace"` на этот случай |
| T-09.4 | Скрытая консоль: ни одного окна у всего дерева | DONE 28.08.2026 — [Ф] окон нет, дерево живо; корень дефекта чек-листа подтверждён |
| T-09.5 | Job Object kill-on-close гасит дерево; ragent в Job штатен; кластер цел | DONE 28.08.2026 — [Ф] живое дерево (ragent+rmngr+rphost+dbgs+dbda+conhost) погашено целиком закрытием handle; кластер после перезапуска цел; Касперский цепочку python→ragent заказчика не тронул |
| T-09.7 | Дети наследуют Job (нет breakaway) | DONE 28.08.2026 — [Ф] все пять детей живого дерева умерли с Job |

Решения заказчика 28.08.2026 по итогам А1: панель — «Журнал профиля»
(наши события + пойманный stdout), реверс жизненного цикла остаётся
(дочерние серверы, подтверждение при выходе, Job).

---

## T-10. Реализация: журнал профиля и дочерние серверы — `WIP` (код готов, ждёт финального ревью ветки и ручного чек-листа)

Спека: [2026-08-26-v2-servers-design.md](superpowers/specs/2026-08-26-v2-servers-design.md)
§12 (решения заказчика 28.08.2026, провалидированы экспериментами T-09).
План: [2026-08-28-v2-servers-journal.md](superpowers/plans/2026-08-28-v2-servers-journal.md).
Строится поверх ветки T-08 (`feat/2026-08-26-v2-servers`), сливаются вместе.

**Реализация завершена 28.08.2026**, субагентный цикл SDD (свежий
исполнитель на задачу + ревью каждой задачи, леджер —
`.superpowers/sdd/2026-08-28-v2-servers-journal/progress.md`): все
8 задач приняты. Задача 1 — Job Object (`platform_1c/job.py`,
находка по ходу: Job не поглощает уже существующих потомков, только
рождённых после `assign` — тест и план исправлены на месте). Задача 2 —
серверный spawn без окон, редирект в файл, `CREATE_NO_WINDOW` вместо
`DETACHED_PROCESS` ([Ф] А3 T-09: `DETACHED_PROCESS` и был причиной
дефекта чек-листа — россыпи консольных окон у детей). Задача 3 —
журнал профиля, чистые функции ротации и записи событий. Задача 4 —
координатор: `start`/`stop`/`stop_orphans` ротируют и пишут события
до/после эффекта. Задача 5 — панель «Журнал профиля» (tail, 500 строк,
1000 мс, автопрокрутка) и выделение карточки в разделе «Серверы».
Задача 6 — подтверждение выхода при работающих серверах (§12.3,
дефолт «Отмена»), диалог инъецируется, чтобы не подвесить тесты
безусловным `QMessageBox`. Задача 7 — проводка `app.py`: настоящий
`ServerJob` вместо временного шима задачи 4, `NullJob` только
в `run_smoke`. Задача 8 — мокап и спека синхронизированы с кодом.
Помимо задачных исправлений по ходу ревью, находок, доживших до
финального гейта, нет.

**Финальный гейт (задача 9, 28.08.2026, независимый агент — не автор
тестов вехи): 1664 теста** (было 1601 на закрытии T-08, +63), `ruff`
и `mypy` — коды 0. Сборка (`build/build.ps1`, с установщиком):
smoke собранного экземпляра — `OK` (окно офскрин, обе фоновые задачи,
ярлык на запущенный exe, `sys.frozen=True` в логе, `--autostart`
завершается штатно); dist 118,1 МБ, portable-zip 49,1 МБ, setup 33,4 МБ
— вес не сдвинулся от T-08 (изменения T-10 не тянут новых тяжёлых
зависимостей).

**Мутационная стадия: 16/16 предписанных мутаций убиты** — 6/6 независимым
агентом (не автором тестов вехи) на гейте задачи 9, полный протокол
(правка → целевой тест → дословный результат → откат → чистое дерево) —
в `.superpowers/sdd/2026-08-28-v2-servers-journal/task-9-report.md`;
ещё 3/3 (строки 7–9) — отдельной волной независимого агента по трём
защитным утверждениям, добавленным правками финального ревью ветки
(best-effort ротация не должна прерывать запуск, событие «порождён PID»,
событие «работает · PID» в `_check_pending_confirmation`), тот же
протокол — в
`.superpowers/sdd/2026-08-28-v2-servers-journal/final-mutation-report.md`;
ещё 7/7 (строки 10–16) — отдельным независимым агентом по находкам волны
финального ревью ручного чек-листа T-10/T-08 (держатель хендла журнала
ребёнка, утечка Win32-хендла, дефолт часового пояса `now`, адресность
`log_shutdown`, событие выхода в `ui/app.py`, дефолтная кнопка диалога
подтверждения), тот же протокол — в
`.superpowers/sdd/2026-08-28-v2-servers-journal/checklist-mutation-report.md`.

| # | Мутация | Файл | Тест | Результат |
| --- | --- | --- | --- | --- |
| 1 | `assign` не кладёт процесс в Job (`AssignProcessToJobObject` снят) | `platform_1c/job.py` | `test_close_kills_parent_and_grandchild` | УПАЛ — `assert not psutil.pid_exists(parent.pid)`: `AssertionError: assert not True`, дерево осталось живым; `finally` теста прибрал подставные родителя и внука (после теста живых процессов не осталось) |
| 2 | `rotate_journal` удаляет текущий журнал вместо переноса | `services/server_journal.py` | `test_current_becomes_previous` | УПАЛ — `previous_journal_path(...).read_text(...)`: `FileNotFoundError: [Errno 2] No such file or directory: '...\\p1.1.log'`, `.1.log` не создан |
| 3 | `start` не ротирует и не пишет событие перед `server_spawn` | `services/servers.py` | `test_start_rotates_journal_and_logs_command` | УПАЛ — `previous.read_text(...)`: `FileNotFoundError` на `....1.log`, ротации не было |
| 4 | `except (OSError, JobError)` в `start` не пишет «отказ запуска» | `services/servers.py` | `test_failed_spawn_logs_the_refusal` | УПАЛ — `assert "отказ запуска" in content`: строки нет, в журнале осталась только запись `запуск: …`; сам `ServerError` при этом поднимается штатно |
| 5 | `closeEvent` игнорирует отказ `confirm_quit`, закрывает всегда | `ui/shell.py` | `test_close_with_running_servers_and_declined_confirmation_keeps_window` | УПАЛ — `assert window.isVisible()`: `AssertionError: assert False`, окно закрылось несмотря на отказ подтверждения |
| 6 | `run_smoke` не подставляет `server_job=NullJob()` (уходит дефолтный `ServerJob()`) | `ui/app.py` | `test_run_smoke_uses_null_job` | УПАЛ — фейковый конструктор `ServerJob` (`bomb`) сработал: `AssertionError: smoke не должен создавать ServerJob` |
| 7 | Отказ ротации журнала (`except OSError`) снова поднимает `ServerError`, прерывая запуск | `services/servers.py` | `test_start_survives_rotation_failure_when_previous_journal_is_locked` | УПАЛ — незапойманный `ServerError: Не удалось запустить сервер: [WinError 32] Процесс не может получить доступ к файлу... '...bdbdbdbdbdbdbdbdbdbdbdbdbdbdbdbd.log' -> '...bdbdbdbdbdbdbdbdbdbdbdbdbdbdbdbd.1.log'`; `1 failed in 0.35s` |
| 8 | `start` не пишет событие `порождён PID <pid>` после успешного `server_spawn` | `services/servers.py` | `test_start_rotates_journal_and_logs_command`, `test_start_survives_rotation_failure_when_previous_journal_is_locked` | УПАЛИ оба — `assert f"порождён PID {pid}" in content`: строки нет ни в обычном журнале, ни в журнале после best-effort ротации; `2 failed in 0.62s` |
| 9 | `_check_pending_confirmation` не пишет `работает · PID <pids>` при положительном исходе подтверждающего скана | `ui/servers/view.py` | `test_confirmed_running_is_also_written_to_the_journal` | УПАЛ — `assert "работает · PID 4242" in journal_text`: строки нет, в журнале только `запуск: …` и `порождён PID 4242`; `1 failed in 0.29s` |
| 10 | Хендл журнала ребёнку — `GENERIC_WRITE` вместо `FILE_APPEND_DATA` (обычный указатель записи вместо атомарной дозаписи в конец) | `platform_1c/server_spawn.py` (`_open_append_shared`) | `test_parent_event_survives_child_write` | УПАЛ — `assert "событие координатора" in content`: строка родителя затёрта дочерней записью (`'xxxx…\n'`); `1 failed in 1.80s` |
| 11 | Из режима расшаривания `CreateFileW` убран `FILE_SHARE_DELETE` | `platform_1c/server_spawn.py` (`_open_append_shared`) | `test_rotation_succeeds_while_child_holds_journal` | УПАЛ — `PermissionError: [WinError 32] Процесс не может получить доступ к файлу…` на `log_path.replace(previous)`; `1 failed in 0.20s` |
| 12 | `CloseHandle(handle)` убран из ветки `except OSError` вокруг `open_osfhandle` | `platform_1c/server_spawn.py` (`_open_append_shared`) | `test_open_append_shared_closes_handle_when_open_osfhandle_fails` | УПАЛ — `assert after <= before`: `AssertionError: хендл журнала утёк: было 217, стало 218`; `1 failed in 0.16s` |
| 13 | Дефолт `now` конструктора вернут на `lambda: datetime.now(UTC)` | `services/servers.py` (`ServersWorkspace.__init__`) | `TestDefaultNow::test_default_now_is_local_time` | УПАЛ — `assert actual.utcoffset() == expected_offset`: `AssertionError: assert datetime.timedelta(0) == datetime.timedelta(seconds=18000)`; `1 failed in 0.26s` |
| 14 | `log_shutdown` пишет событие ВСЕМ профилям (снята проверка `_matched_processes`) | `services/servers.py` (`log_shutdown`) | `TestLogShutdown::test_writes_only_to_running_profiles_and_returns_their_count` | УПАЛ — `assert count == 1`: `assert 2 == 1` (записано и простаивающему профилю); `1 failed in 0.27s` |
| 15 | `servers_workspace.log_shutdown()` убран из `_build_confirm_quit` при согласии | `ui/app.py` (`_build_confirm_quit`) | `test_confirm_quit_true_logs_shutdown_event_for_running_profile` | УПАЛ — `FileNotFoundError` на `journal_path(...).read_text(...)`: журнал профиля не создан, событие выхода не записано; `1 failed in 0.45s` |
| 16 | Дефолтная кнопка `ask_confirmation` — «Да» вместо «Нет» | `ui/dialogs/buttons.py` (`ask_confirmation`) | `test_ask_confirmation_sets_no_as_default_button` | УПАЛ — `assert captured["default_text"] == "Нет"`: `AssertionError: assert 'Да' == 'Нет'`; `1 failed in 0.12s` |

Каждая мутация внесена, прогнан только названный тест, откат
`git checkout --`, `git status --short` пуст — протокол CLAUDE.md
(«Мутационная проверка тестов») соблюдён по всем шестнадцати пунктам.
Мутация 1 в новой тройке (строка 7) — держатель файла подставной
python-процесс (`time.sleep(60)`), после прогона проверено, что он не
остался жить (`Get-CimInstance Win32_Process | Where-Object { $_.CommandLine
-match 'time.sleep\(60\)' }` — пусто). Мутации 10 и 11 (волна ручного
чек-листа) тоже поднимают подставные python-процессы
(`time.sleep(1.5)`/`time.sleep(3)`) — после каждого прогона тем же
способом подтверждено отсутствие живого держателя. Полный протокол волны
10–16 (правка → целевой тест → дословный результат → откат → чистое
дерево) — в
`.superpowers/sdd/2026-08-28-v2-servers-journal/checklist-mutation-report.md`.

### Ручной чек-лист T-10/T-08 — пройден 29.08.2026, шесть находок

Собранный экземпляр (пересобран с HEAD `a39bf29` перед проходом — dist от
28.08 не содержал волны финального ревью), профиль 8.3.25.1633 с реальным
кластером, формат — как T-07/T-09: заказчик действует, агент снимает
процессы, журнал и реестр после каждого шага. Полный протокол с PID и
временем — `.superpowers/sdd/2026-08-28-v2-servers-journal/manual-checklist.md`.

| Пункт | Итог |
| --- | --- |
| 1. Окон нет | ПРОЙДЕН — ни одного окна у ragent/rmngr/rphost/dbgs/dbda; ragent — прямой ребёнок `OneCStarter.exe` [Ф] |
| 2. Журнал наполняется | ПРОЙДЕН с оговоркой — `запуск: …` и ТРИ баннера платформы (Server Agent, Search server, Debug Server — больше, чем в T-09 А1); но события ПОСЛЕ spawn потеряны — находка 1 |
| 3. Панель — журнал выбранного профиля | ПРОЙДЕН (скриншот: рамка на карточке, заголовок, строки) |
| 4. Прошлый журнал в `<id>.1.log` | ПРОЙДЕН — вместе с событием `остановка по команде пользователя` [Ф] |
| 5. Выход с подтверждением | ПРОЙДЕН — крестик при `close_to_tray` не спрашивает; трей → «Выход» → вопрос с «Нет» по умолчанию; «Нет» — всё живо; «Да» — дерево погашено целиком, порт свободен [Ф] |
| 6. Крах лаунчера (taskkill) | ПРОЙДЕН — дерево погашено так же, без участия кода [Ф] Job kill-on-close |
| 7. Снять ragent, dbgs жив, «Запустить» | ПРОЙДЕН со второй попытки — `ротация журнала не удалась ([WinError 32] …)` с текстом ОС, запуск прошёл; первая попытка: окно мелькнуло, в журнал ничего не записалось — НЕ ВОСПРОИЗВЕДЕНО |
| T-08: остановка / «Погасить» | остановка гасит дерево [Ф]; «Погасить» гасит rmngr, но не rphost/dbgs — находка 6 |
| T-08: консоль MMC с UAC | ПРОЙДЕН в обе стороны — согласие (перерегистрация 1633→1560→1633 поверх без `/u`, MMC открылась; живая проверка раскладки `SHELLEXECUTEINFOW`) и отказ (информационное окно, не ошибка) [Ф] |
| T-08: антивирус при spawn из exe | ПРОЙДЕН — Касперский молчал все четыре запуска |

**Находки 1–4 закрыты волной исправлений 29.08** (коммиты `7ff6ca4`…`21d228b`,
мутации 10–16 в таблице выше) **и подтверждены живым прогоном на пересобранном
экземпляре**: в журнале `порождён PID …` и `работает · PID …` между баннерами
платформы уцелели, время локальное, событие выхода записано, кнопки «Да»/«Нет».
Находки 5–6 закрыты вехой T-12 «Job на профиль» (решение заказчика
29.08.2026, задачи 1–5 реализации — 30.08.2026; финальный гейт и
мутации 1–16 выполнены задачей 7, `d8efd8e`, 30.08.2026 — см. раздел T-12).

**Находки (исходный список):**

1. **Critical — события координатора после spawn затираются ребёнком.**
   `порождён PID` и `работает · PID` в журнале нет. Механизм [Ф]
   (воспроизведён подставным python-ребёнком): ребёнок унаследовал хендл,
   открытый `open("ab")`, со своим файловым указателем на конце файла
   в момент spawn; наши `append_event` пишут отдельным хендлом в конец,
   а следующая запись ребёнка идёт по его указателю — поверх нашей
   строки. Баннер длиннее события → строка исчезает без следа. Уцелевает
   только записанное после смерти дерева. Лекарство [Ф] (проверено на
   подставном процессе): хендл ребёнку через `CreateFileW(FILE_APPEND_DATA
   | SYNCHRONIZE, FILE_SHARE_READ|WRITE|DELETE, OPEN_ALWAYS)` +
   `msvcrt.open_osfhandle` — запись по такому хендлу всегда в конец
   независимо от указателя; заодно `FILE_SHARE_DELETE` снимает долг
   ротации (`Path.replace` при живом держателе прошёл, ребёнок дописывает
   уже в `.1.log`). Мутации 8–9 были зелёными на фейке: `FakeServerSpawn`
   не пишет в файл — тест с настоящим ребёнком-писателем обязателен.
2. **Important — время событий в UTC** (`[05:30:03]` при 10:30 локальных):
   `now=lambda: datetime.now(UTC)` из задачи 4, `app.py` `now` не передаёт.
   Журнал читает человек рядом с часами Windows — время локальное.
3. **Minor — кнопки диалога выхода `Yes`/`No`** по-английски:
   `QMessageBox.question` со стандартными кнопками; остальные диалоги —
   `russian_button_box()`.
4. **Minor — выход не оставляет следа в журнале** (дерево гасит ОС, кода
   нет — §12.4): писать событие после согласия, до `quit()`.
5. **Important — запуск при живых сиротах даёт полумёртвый сервер с видом
   «работает».** Под новым ragent поднялись только java и dbda: порты
   1541 (rmngr) и 1550 (dbgs) держали сироты старого дерева; карточка —
   «работает», красная строка о сиротах исчезла (сироты стали «частью
   профиля», раз ragent есть). `start()` сирот не проверяет. Решение
   заказчика: (а) отказ «сначала „Погасить“» или (б) `start()` сам гасит
   сирот профиля перед запуском с событием в журнале. **Закрыто T-12,
   30.08.2026** (задача 3, вариант б): `start()` сам закрывает остатки
   Job профиля ДО spawn и пишет событие «погашены остатки прошлого
   запуска: PID …».
6. **Important — «Погасить» оставляет rphost/dbgs** (T-08 §6: сироты =
   менеджеры; здесь rphost/dbgs — не дети rmngr, а братья): dbgs держит
   порт 1550 и журнал, следующий запуск снова без Debug Server и с
   пропуском ротации. Сопоставить dbgs с профилем по argv нельзя
   (`--port=1550`, без `-d`). Корневое решение — рекомендация финального
   ревью, теперь подтверждённая живым сценарием: **Job Object на профиль**
   вместо одного на лаунчер; «Остановить»/«Погасить» = закрыть хендл Job
   профиля, ОС гасит всё дерево без сопоставления; ragent, снятый извне,
   оставляет процессы в Job → лаунчер видит «ragent нет, Job не пуст».
   Kill-on-close при выходе сохраняется. Это редизайн §12.4 — решение
   заказчика, brainstorming. **Закрыто T-12, 30.08.2026** (задачи 1, 3):
   `stop()`/«Погасить» закрывают Job профиля целиком — `rphost`/`dbgs`
   гасятся вместе с `rmngr`, сопоставление по argv не нужно. Ограничение
   [Р], спека T-12 §4: чужой `dbgs`, не входящий ни в один наш Job
   (например, остаток дерева, снятого извне ДО перехода на T-12), по
   argv всё ещё не опознаётся — остаётся долгом вехи T-12.

### Долг вехи T-10 (не блокирует слияние)

1. T-09.3 (кодировка перенаправленного вывода платформы) не закрыт:
   за весь эксперимент T-09 и всю разработку T-10 кириллица в
   перехваченном stdout `ragent`/детей ни разу не встретилась (ловится
   практически только ASCII-строка dbgs, [Ф] А1 T-09) — снимать факт
   не с чего. `tail`-чтение панели журнала уже защищено
   `errors="replace"` на этот случай заранее. Действие: снять кодировку
   экспериментом при первом реальном появлении кириллицы в журнале
   (жалоба пользователя или собственное наблюдение), не раньше.
2. Мелкие находки задачных ревью, отложенные осознанно (детали —
   `.superpowers/sdd/2026-08-28-v2-servers-journal/progress.md`):
   `sleep(1)` вместо детерминированного барьера в тесте Job (риск
   CI-флейка низкий, 3/3 стабильно); ~~`Job(Protocol)` задачи 1 пока не
   используется как аннотация (`spawn_server` типизирован закрытой
   унией `ServerJob | NullJob`)~~ **закрыто T-12, 30.08.2026** (задача 1:
   протокол `Job` расширен методами Job Object, `spawn_server` принимает
   `Job`); `except JobError` в `spawn_server` не
   подавляет `ResourceWarning` брошенного `Popen` (асимметрия
   с happy-path); `mkdir`-ветки `rotate_journal`/`append_event` не
   тестируются (в тестах `tmp_path` всегда уже существует);
   `_ask_quit_confirmation` без прямого теста (тот же класс, что
   `_default_confirm_removal` в T-08); допуск `_SCROLL_TOLERANCE`
   (≤ 2 строки) в тесте автопрокрутки панели журнала — офф-бай-уан
   offscreen Qt, задокументирован в тесте.
3. Находки финального ревью ветки, отложенные осознанно (не блокируют
   слияние — волна исправлений закрыла Important 1/2 и fix-before-merge,
   подробности — `.superpowers/sdd/2026-08-28-v2-servers-journal/
   final-fix-report.md`):
   - ~~`FILE_SHARE_DELETE` при открытии журнала в `spawn_server`
     (`platform_1c/server_spawn.py`) — снимает саму причину пропуска
     ротации, а не только её best-effort обработку.~~ **Закрыто волной
     исправлений по ручному чек-листу T-10, 29.08.2026** (находка 1,
     Critical — `.superpowers/sdd/2026-08-28-v2-servers-journal/
     checklist-fix-report.md`): `_open_append_shared` открывает хендл
     ребёнку с `FILE_APPEND_DATA | FILE_SHARE_DELETE`, `Path.replace`
     теперь проходит при живом ребёнке-писателе; заодно устранена
     причина находки 1 (затирание событий координатора).
   - ~~`running_count()` считает и ЧУЖИЕ процессы, совпавшие с профилем
     по каталогу кластера, — вопрос подтверждения выхода «Остановить
     N серверов и выйти?» для них неверен (мы их не остановим): считать
     нужно по НАШИМ PID.~~ **Закрыто T-12, 30.08.2026** (задача 4):
     `running_count()`/`log_shutdown()` считают по профилям с непустым
     Job — чужой совпавший `ragent` больше не учитывается.
   - ~~Гигиена ctypes в `platform_1c/job.py`: `argtypes`/`restype` у
     функций WinAPI не проставлены, один `WinDLL` не переиспользуется
     между вызовами.~~ **Закрыто T-12, 30.08.2026** (задача 1):
     `argtypes`/`restype` проставлены у всех функций WinAPI, один
     `WinDLL` на модуль.
   - Эталонный скрипт эксперимента Б2 (`e:\tmp\t09\b2_job.py`) — каталог
     одноразовый и не входит в репозиторий; положить копию скрипта
     в `docs/research/` до того, как каталог будет очищен (вне рамок
     этой волны исправлений — файл там пока не в форме, готовой пройти
     `ruff` вместе с остальным репозиторием, править требует отдельного
     решения: адаптировать код или завести исключение для одноразовых
     research-скриптов).
   - Нет теста на `_timer.isActive()` панели «Журнал профиля» при показе/
     скрытии/сбросе выделения.
   - Переименование ВЫДЕЛЕННОГО профиля не обновляет заголовок панели
     «Журнал профиля» до следующего клика по карточке.
   - `server_spawn.py`, ветка `except JobError`: `process.kill()` без
     последующего `wait()` (zombie-риск) и нечестный текст, если сам
     процесс успел умереть до `job.assign()` (сообщение всё равно
     говорит про отказ Job, а не про то, что сервер умер сам).

### Ручной чек-лист T-10 (для заказчика, перед слиянием — вместе с чек-листом T-08)

Живой запуск собранного экземпляра (не из исходников), профиль
с реальным кластером 1С:

1. Запуск профиля из раздела «Серверы» — ни одного консольного окна
   не появляется (ни у `ragent`, ни у `rmngr`/`rphost`/`dbgs`/`dbda`).
2. Панель «Журнал профиля» наполняется по ходу работы (не только
   в момент запуска) — видна собственная запись `запуск: …` и хотя бы
   одна строка, пойманная от платформы.
3. Панель показывает журнал именно ВЫБРАННОГО профиля — клик по
   другой карточке переключает содержимое панели на её журнал.
4. Повторный запуск того же профиля — прошлый журнал не потерян,
   он лежит рядом как `<id>.1.log` (можно открыть отдельно от панели).
5. Выход из OneCStarter (крестик при выключенном трее либо «Выход»
   в трее) при работающем сервере — запрашивает подтверждение;
   согласие гасит всё дерево процессов профиля целиком.
6. Крах лаунчера — снятие процесса OneCStarter из Диспетчера задач
   (`TaskKill /F` или «Снять задачу») при работающем сервере — гасит
   дерево процессов профиля так же, как штатный выход (Job Object
   kill-on-close, без явного кода остановки).
7. Снять `ragent` из Диспетчера задач, убедиться, что `dbgs` жив
   (пережил `ragent` — держит журнал открытым), нажать «Запустить» —
   запуск обязан пройти без ошибки, а в «Журнале профиля» появится
   строка о пропущенной ротации с текстом ошибки ОС (для занятого
   файла — обычно `WinError 32`).

---

## T-11. Косметика и удобство перед выпуском v2 — `TODO` (бэклог заказчика, 29.08.2026)

Девять пожеланий заказчика по скриншоту раздела «Серверы» и по работе
с разделом «Базы», зафиксированы 29.08.2026, пока T-10 закрывает находки
ручного чек-листа. В ветку T-08/T-10 не входят — отдельная ветка после
слияния в `master`, перед сборкой релиза v2. Каждый пункт ниже сверен
с кодом (что есть сейчас), размечен по размеру и по тому, нужно ли
решение заказчика до реализации. Решения по пунктам 2, 3 и 9 приняты
заказчиком 29.08.2026 (варианты и выбор — в разборе ниже); brainstorming
по ним состоялся в чате, отдельной спеки не нужно — раздел служит
спекой, весь набор идёт одним планом (`writing-plans`) после слияния
T-08/T-10 в `master`.

| # | Пожелание | Размер | Решение заказчика |
| --- | --- | --- | --- |
| 1 | Зазор между именем профиля и статусом в карточке сервера | S | нет |
| 2 | Сортировка списка баз по алфавиту | M | **принято 29.08.2026** — (а) режим показа, файл не трогается; правила порядка — предложение исполнителя, п. 2 |
| 3 | `Alt+Enter` — свойства; все горячие клавиши в настройках одной группой | S | **принято 29.08.2026** — `Alt+Enter` зашить, keymap не делаем |
| 4 | «Добавить базу…» из меню группы — база в группу | S | нет |
| 5 | Выбор группы в диалоге — деревом | M | нет (вариант реализации — исполнителю) |
| 6 | Автоимя из каталога / имени в кластере | S | нет |
| 7 | `Insert` — добавление | S | нет |
| 8 | `Delete` — удаление | S | нет (одно замечание — п. 8) |
| 9 | Настройка «Сворачивать в трей при запуске базы» | S–M | **принято 29.08.2026** — на серверный профиль не действует; без трея молча не сворачивать |

### Разбор пунктов

1. **Имя профиля и статус слипаются** (скриншот: `8.3.25.1633остановлен`).
   Сейчас: `ui/servers/view.py::_build_card` — `title_row` (QHBoxLayout)
   без собственного `setSpacing`, а вложенная раскладка наследует
   spacing родителя (по документации Qt, `QLayout::spacing`: не задан
   явно — берётся у родительской раскладки), родитель —
   `card_layout.setSpacing(2)`. Отсюда 2 px между жирным именем
   и статусом. Сделать: явный `title_row.setSpacing(...)` (8–10 px,
   сверить с мокапом `2026-08-26-v2-servers-mockup.html`), сторож
   в тесте карточки, оффскрин-скриншот до/после — заказчику.

2. **Сортировка по алфавиту.** Сейчас порядок — из файла по
   `OrderInList` (`services/catalog.py`, стабильная сортировка
   `order.sort_key`), и он же — ручной: `Alt+Up/Down` и перетаскивание
   переписывают `OrderInList` соседей (`services/edit.py`), штатный
   стартер показывает тот же порядок из того же файла. «По алфавиту»
   и «ручной порядок» — два взаимоисключающих представления одного
   списка, поэтому пункт не косметика, а решение:
   - (а) **режим показа** — настройка «Порядок списка: как в файле /
     по алфавиту» в группе «СПИСОК БАЗ»; файл не трогается, в алфавитном
     режиме `Alt+Up/Down` и перетаскивание-перестановка отключены
     (перетаскивание в группу остаётся); штатный стартер порядка
     не видит.
   - (б) **разовое действие** «Упорядочить по алфавиту» в меню группы
     и пустого места — переписывает `OrderInList` братьев; порядок
     виден и штатному стартеру, ручная перестановка после него
     работает как раньше; но это правка файла 1С по одной команде,
     отката нет.
   - Рекомендация: (а) с дефолтом «как в файле» — поведение
     существующих установок не меняется, файл 1С без нужды не
     правится (дух инвариантов 3–4). (б) — дополнительно, если
     заказчику нужен порядок и в штатном стартере.
   - **Решение заказчика 29.08.2026: (а)** — режим показа, файл
     не трогается; (б) не делаем.
   - Правила порядка заказчик не решал — предложение исполнителя,
     подтвердить при плане: группы перед базами внутри уровня;
     регистронезависимо; коллация по алфавиту языка системы, не по
     кодам символов (`str.casefold` ставит латиницу перед кириллицей);
     «Недавние» — всегда по времени, «Избранное» и результат поиска —
     по правилу основного списка. Дефолт настройки — «как в файле».

3. **`Alt+Enter` — свойства; горячие клавиши в одной группе.**
   Сейчас: сочетания раздела «Базы» зашиты в `ui/bases/view.py`
   (`__init__`, `QShortcut`): `Ctrl+D` избранное, `Ctrl+N` добавить,
   `Ctrl+1/2/3` тонкий/толстый/конфигуратор, `F3` запуск, `F4`
   конфигуратор, `Alt+Up/Down` перестановка, `Enter`/двойной клик —
   запуск (`activated`), `Enter` в поиске — первая видимая. «Свойства…»
   — только из контекстного меню (`show_properties`, для группы —
   `GroupDialog`). В настройках группа «ГОРЯЧИЕ КЛАВИШИ» содержит
   только глобальный вызов окна (`HotkeyEdit`: требует модификатор,
   `Alt+F4` запрещён решением заказчика).
   - Само сочетание: `Alt+Enter` → свойства текущей строки (запись —
     `show_properties`, группа — диалог группы; записи общего списка —
     недоступно, как пункт меню). Совпадает с соглашением Проводника
     Windows (`Alt+Enter` = «Свойства»). Пункт меню получает подпись
     `Свойства…\tAlt+Enter`. Размер S.
   - «Вынести в настройку»: одно настраиваемое сочетание при десяти
     зашитых — непоследовательно; либо keymap для всех действий
     раздела, либо ни одного. Keymap = ключи `settings.json` по
     действиям, захват через `HotkeyEdit` — которому нужен второй режим
     (без модификатора, но не голая буква/цифра: `F3`/`F4` иначе не
     пройдут), проверка конфликтов между действиями и с глобальным
     хоткеем, подписи меню (`\tF3`) из keymap, сброс к умолчанию.
     Это самостоятельная фича (M–L), не косметика.
   - **Решение заказчика 29.08.2026: `Alt+Enter` зашить**, keymap
     не делаем. Справочная таблица всех сочетаний раздела «Базы»
     в группе «ГОРЯЧИЕ КЛАВИШИ» (только чтение, S) — рекомендация,
     отдельно не подтверждена; в план входит, снимается словом
     заказчика.

4. **«Добавить базу…» из меню группы.** Сейчас пункт есть только
   в `_build_empty_space_menu` (`add_infobase` → диалог с группой =
   корень); `_build_group_menu` — подгруппа / переименовать / удалить.
   Предустановка группы у диалога уже есть — `InfobaseDialog.set_folder`
   (используется `build_dialog_for_dropped_directory`). Сделать: пункт
   первым в `_build_group_menu` с `folder = group_path(item.folder,
   item.name)`; в `_build_disabled_group_menu` (группы общего списка)
   — тот же пункт недоступен с `COMMON_NOTE`. Общая точка
   `add_infobase(folder=ROOT)` — её же используют `Ctrl+N` и `Insert`
   (п. 7).

5. **Выбор группы деревом.** Сейчас `InfobaseDialog` — `QComboBox`
   с плоскими путями `/A/B` (`_group_choices`: `ROOT` + отсортированные
   пути пользовательских групп); тот же приём у `GroupDialog._parent_box`
   («Родитель»). Сделать: один виджет выбора группы деревом для обоих
   диалогов (иначе два разных способа выбрать одно и то же). Варианты —
   `QComboBox` с `QTreeView` в popup (разворачивание и выбор узла
   в popup требуют ручной обработки) или встроенное `QTreeView` в форме
   с текущим путём подписью. Контракты `set_folder`/`folder_choices`/
   `set_parent_path` сохранить — на них тесты. Путь записи, отсутствующий
   среди групп (неявная группа), — узел добавляется в дерево как есть,
   как сегодня добавляется в список.

6. **Автоимя из каталога / имени в кластере.** Сейчас для файловой базы
   уже сделано для «Обзор…» и drag&drop (`accept_directory`: имя =
   `Path(path).name`, только если имя пустое); для пути, набранного
   руками, и для серверной (`_ref`) — нет. Сделать: `editingFinished`
   на `_file_path` и `_ref` → имя, если пустое; правило одно — «только
   пустое»: введённое или уже подставленное имя при смене пути
   не перезаписывается (`textChanged` не годится — подставит имя
   на первом символе и дальше не обновит). Веб-база (`ws`) —
   не запрошено, не делать: последний сегмент URL — плохое имя.

7. **`Insert` — добавление.** Второе сочетание к `Ctrl+N`. Целевая
   группа — по текущей строке: группа → в неё, запись → её группа,
   ничего или виртуальная ветка → корень (то же правило, что
   `folder_for_dropped_directory`). Обязательно: контекст сочетания —
   только дерево (`Qt.ShortcutContext.WidgetShortcut` или
   `keyPressEvent` дерева), не окно: сегодняшние `QShortcut` —
   `WindowShortcut`, для `Ctrl+N` это терпимо, для `Insert`/`Delete`
   в поле поиска — нет, там это правка текста.

8. **`Delete` — удаление.** Сейчас только из контекстного меню:
   «Удалить из списка…» → `remove_key` с подтверждением, «Удалить
   группу…» → `remove_group` с подтверждением (каскад — [Ф] T-05.9).
   Сделать: `Delete` на текущей строке → тот же обработчик, что у пункта
   меню, с тем же подтверждением и той же недоступностью (записи
   общего списка — ничего не происходит, без диалога). Контекст —
   только дерево (см. п. 7). Замечание на решение: `Delete` на записи
   в ветке «Недавние»/«Избранное» — удаление из списка (одно значение
   клавиши везде), не из недавних; подтверждение с именем защищает
   от неожиданности. Мутационная проверка обязательна (операция над
   файлом пользователя): `Delete` без подтверждения файл не трогает.

9. **«Сворачивать в трей при запуске базы».** Сейчас: `close_to_tray`
   (крестик → `hide()`, только при доступном трее —
   `ui/app.py::apply_close_to_tray`: `settings and tray is not None`);
   все пути запуска базы из UI — `Enter`/двойной клик (`_launch_index`),
   поиск (`_launch_first_visible`), `F3`/`F4`/`Ctrl+1-3`
   (`_launch_current`), контекстное меню, меню трея — сходятся
   в `BasesView.launch_key` → `Workspace.launch`. Сделать: поле
   `hide_on_launch: bool = False` в `Settings` (как `servers_root` —
   без смены схемы, отсутствующий ключ = дефолт), тумблер в группе
   «ОКНО И ЗАПУСК» под «Закрытие окна сворачивает в трей», примечание
   «Без трея не действует»; из `launch_key` — сигнал после **успешного**
   запуска (ошибка — окно остаётся, чтобы показать сообщение) →
   `window.hide()` по тому же AND «трей доступен»: без трея скрывать
   нельзя, окно не вернуть иначе как глобальным хоткеем. Независимо
   от `close_to_tray`: скрытие при запуске возможно и при выключенном
   закрытии в трей — иконка трея живёт сама по себе
   (`window.tray_available`). Запуск по ярлыку (`--launch`) окна
   не показывает — не затрагивается. **Решения заказчика 29.08.2026:**
   (а) запуск серверного профиля из раздела «Серверы» — не «запуск
   базы», настройка не действует; (б) без трея — молча
   не сворачивать (в панель задач не уходить).

---

## T-12. Job Object на профиль — `WIP` (код готов, ждёт финального ревью ветки и живого чек-листа, спека T-12 §10)

Спека: [2026-08-29-v2-servers-job-per-profile-design.md](superpowers/specs/2026-08-29-v2-servers-job-per-profile-design.md)
— пересмотр §12.4 спеки v2 «Серверы» по находкам 5–6 ручного чек-листа
T-10 (29.08.2026). План: [2026-08-30-v2-servers-job-per-profile.md](superpowers/plans/2026-08-30-v2-servers-job-per-profile.md).
Ветка `feat/2026-08-30-v2-job-per-profile` от `master` (T-08/T-10 слиты
fast-forward 30.08.2026, 1677 тестов). Субагентный цикл SDD, леджер —
`.superpowers/sdd/2026-08-30-v2-servers-job-per-profile/progress.md`.

Корень задачи (найдено ручным чек-листом T-10): T-08 управлял процессами
сопоставлением по командной строке и `TerminateProcess` по снимку —
рабочим для `ragent`/`rmngr`, но не для всего дерева (находка 6: «Погасить»
не достаёт до `rphost`/`dbgs`) и требующим гонки «снимок → убийство»
(находка 5: сироты живут под новым `ragent`, карточка ошибочно «работает»).
Решение — Job Object на каждый запуск профиля вместо одного на весь
лаунчер: `Job.pids()` даёт истину о дереве от ОС, `close()` гасит всё
разом, без сопоставления по argv.

**Что сделано по задачам:**

- **Задача 1** (`platform_1c/job.py`): `ServerJob.pids()`/`close()`/
  `is_empty()`, протокол `Job` расширен этими методами, ctypes-гигиена
  (`argtypes`/`restype` у функций WinAPI, один `WinDLL` на модуль —
  закрывает долг T-10). Находка исполнителя: TOCTOU в уборке фикстуры
  теста (`_tree_in_job`, kill-on-close асинхронен) — тест и план
  поправлены (`try`/`except NoSuchProcess`). Fix round 1: `close()`
  обнуляет `_handle` только ПОСЛЕ успешного `CloseHandle` — иначе отказ
  `CloseHandle` терял хендл безвозвратно; тесты отказных веток и
  мутационная проверка прогнаны. Commits: `a0f2795`, `a89849a` (fix),
  `a8438a4` (план).
- **Задача 2** (`domain/server_match.py`): `port_holders`/
  `port_holders_text` — чужие процессы, держащие `-port`/`-regport`
  профиля не из наших Job (заменяют `orphan_managers`). Находка
  исполнителя: `collections.abc.AbstractSet` в Python не существует,
  использован `collections.abc.Set` — план поправлен. Commits: `38b966a`,
  `9b55149` (план).
- **Задача 3** (`services/servers.py`, `platform_1c`): `ServersWorkspace`
  — Job на запуск профиля, `port_holders`, `stop()` = `close()` Job;
  `process_control.py` удалён целиком; `job_factory` инъекцией, `view`
  и тесты переведены на Job. Находка исполнителя: порядок в `start()` —
  событие «погашены остатки прошлого запуска» писалось ДО ротации
  журнала и уезжало в `.1.log` при следующем запуске вместо текущего.
  Исправлено: остатки закрываются ДО ротации (отказ `close()` — отказ
  запуска), новый Job → ротация → событие «погашены остатки…» ПОСЛЕ
  ротации → «запуск: …» → spawn. План и спека T-12 §3 поправлены
  исполнителем по этой находке — решение контроллера: принято как
  исправление дефекта плана. Commits: `2d97d7c`, `b4648c1`, `24c6334`,
  `8388556` (план+спека).
- **Задача 4** (`services/servers.py` — реконсиляция): сверка Job
  со снимком — `ragent`, завершившийся извне, оставляет видимые остатки
  в Job; опустевшие Job освобождаются, `running_count()`/`log_shutdown()`
  больше не учитывают их (закрывает долг T-10 «вопрос о выходе считает
  чужие процессы»). Commit: `4c81769`; план поправлен вслед (`0a8e8fa`):
  ревью задачи 3 нашло, что `statuses()` может поднять `ServerError`
  через `_job_pids` (`JobError` из `pids()`), а `rebuild()` зовёт её без
  `try` — страховка перенесена в задачу 5.
- **Задача 5** (`ui/servers/view.py`): таблица состояний карточки (§4
  T-12 — RUNNING/REMNANTS/FOREIGN/STOPPED), совпавший чужой `ragent` —
  только показ (решение 4 §2 T-12), удаление работающего профиля =
  остановка с диалогом подтверждения (решение 3 §2 T-12, отменяет
  решение 8 §2 исходной спеки). Страховка `rebuild()`: `ServerError`
  из `statuses()` — красная строка пути, карточки прошлого `rebuild()`
  остаются; `_last_statuses` переиспользуется в `on_scan_snapshot()`
  вместо второго вызова `statuses()` (второй вызов вне `try` сводил бы
  страховку на нет). Commit: `4de126f`; план поправлен вслед (`18dc9ad`):
  находка ревью — `_last_statuses` вместо второго `statuses()` —
  не была внесена в план сразу, закрыто контроллером правкой плана
  (документ, не код), мутации 15–16 (`test_running_process_wins_over_
  unresolved_version`, `test_scan_snapshot_with_unreadable_job_keeps_
  pending_confirmation`) занесены в таблицу задачи 7.

**Финальный гейт и мутации — задача 7** (30.08.2026, независимый агент —
не автор тестов вехи). Полное дерево: `pytest -q` — 1707 тестов зелёных
(`1707 passed in 80.88s`; первый прогон в тот же день упал
`Windows fatal exception: access violation` внутри `pytestqt.plugin.
_process_events` — глубоко в рантайме pytest-qt/Qt offscreen, не в коде
проекта; немедленный повтор на том же HEAD без единой правки прошёл
чисто — отмечено долгом наблюдения, не блокером), `ruff check .` —
`All checks passed!`, `mypy` — `Success: no issues found in 163 source
files`; все три — код возврата 0. После прогона подставных процессов
(`Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match
'time.sleep\('}`) не осталось.

Сборка (`build/build.ps1 -SkipInstaller`, без редиректа `2>&1`, собранный
экземпляр не запущен): PyInstaller — успех, **smoke: OK**, portable zip —
49,1 МБ, итоговый `dist/OneCStarter` — **118,2 МБ**.

Мутационная проверка — 16/16 отказных тестов убиты названными тестами,
НЕ УБИТЫХ нет. Протокол на каждую: правка → только названный тест
(PowerShell foreground — под git-bash кириллица в выводе pytest бита
кодировкой консоли) → дословный `FAILED`/assertion → `git checkout --`
→ `git status --short` пуст → тот же тест зелёным повторно. Мутации 1–2
(`platform_1c/job.py`) поднимают подставные python-процессы — после
каждой подтверждено отсутствие живого держателя той же командой, что
и в Step 1. Полный протокол с дословными правками —
`.superpowers/sdd/2026-08-30-v2-servers-job-per-profile/task-7-report.md`.

| # | Мутация | Файл | Тест | Результат |
| --- | --- | --- | --- | --- |
| 1 | `close()` обнуляет `_handle`, не зовя `CloseHandle` | `platform_1c/job.py` | `test_close_kills_remnants_after_external_kill` | УПАЛ — `assert _wait_gone(grandchild)`: `AssertionError: остаток пережил закрытие Job`; `1 failed in 6.60s` |
| 2 | `pids()` всегда возвращает `()` | `platform_1c/job.py` | `test_pids_lists_parent_and_grandchild` | УПАЛ — `assert {parent.pid, grandchild} <= pids`: `AssertionError: assert {43896, 63500} <= set()`; `1 failed in 1.70s` |
| 3 | `start()` закрывает старый Job ПОСЛЕ `server_spawn` | `services/servers.py` | `test_start_with_remnants_closes_old_job_before_spawn_and_logs` | УПАЛ — `assert spawn.probed == [True]`: `AssertionError: assert [False] == [True]`; `1 failed in 0.64s` |
| 4 | `start()` не проверяет `port_holders` | `services/servers.py` | `test_start_refuses_when_a_foreign_process_holds_the_profile_port` | УПАЛ — `with pytest.raises(ServerError)`: `Failed: DID NOT RAISE ServerError`; `1 failed in 0.63s` |
| 5 | `start()` не проверяет живой `ragent` в Job (`spawned in job_pids`) | `services/servers.py` | `test_start_refuses_while_own_ragent_is_alive_in_job` | УПАЛ — `with pytest.raises(ServerError)`: `Failed: DID NOT RAISE ServerError`; `1 failed in 0.28s` |
| 6 | `stop()` закрывает ВСЕ Job | `services/servers.py` | `test_stop_closes_only_this_profiles_job` | УПАЛ — `assert factory.created[1].closed is False`: `AssertionError: assert True is False`; `1 failed in 0.30s` |
| 7 | `running_count()` считает по `_match.by_profile` | `services/servers.py` | `TestRunningCount::test_ignores_a_foreign_matched_ragent` | УПАЛ — `assert workspace.running_count() == 0`: `AssertionError: assert 1 == 0`; `1 failed in 0.35s` |
| 8 | `_reconcile_jobs` не забывает `spawned_pid` после события | `services/servers.py` | `test_external_ragent_death_is_logged_once` | УПАЛ — `assert content.count(...) == 1`: `AssertionError: assert 2 == 1` (событие записано дважды); `1 failed in 0.30s` |
| 9 | `remove_profile` не останавливает Job | `services/servers.py` | `test_remove_running_profile_closes_its_job` | УПАЛ — `assert factory.created[0].closed is True`: `AssertionError: assert False is True`; `1 failed in 0.29s` |
| 10 | `_remove` во view удаляет без вопроса | `ui/servers/view.py` | `test_removal_of_running_profile_asks_to_stop_and_refusal_keeps_it_running` | УПАЛ — `assert questions`: `AssertionError: assert []` (диалог не спрошен); `1 failed in 0.31s` |
| 11 | `run_smoke` не передаёт `job_factory` | `ui/app.py` | `test_run_smoke_uses_null_job` | УПАЛ — `assert workspace_kwargs["job_factory"] is NullJob`: `AssertionError: assert <class 'onecstarter.platform_1c.job.ServerJob'> is NullJob`; `1 failed in 0.44s` |
| 12 | `_button_state` считает `FOREIGN` активной «Остановить» | `ui/servers/view.py` | `test_foreign_matched_ragents_are_show_only` | УПАЛ — `assert row.button_enabled is False`: `AssertionError: assert True is False`; `1 failed in 0.30s` |
| 13 | `port_holders` игнорирует `exclude_pids` | `domain/server_match.py` | `test_own_job_pids_are_excluded` | УПАЛ — `assert port_holders(...) == (alien,)`: чужой набор содержит и `ours`, и `alien` вместо только `alien`; `1 failed in 0.13s` |
| 14 | `rebuild()` без `try/except ServicesError` вокруг `statuses()` | `ui/servers/view.py` | `test_rebuild_survives_a_job_that_cannot_be_read` | УПАЛ — необработанный `ServerError: Не удалось прочитать процессы сервера: QueryInformationJobObject отказал` из `view.rebuild()`; `1 failed in 0.34s` |
| 15 | `_status_text`/`_button_state` проверяют `resolved is None` РАНЬШЕ состояния Job | `ui/servers/view.py` | `test_running_process_wins_over_unresolved_version` | УПАЛ — `assert row.status_text == "работает · PID 4242"`: `AssertionError: assert 'версия не установлена' == 'работает · PID 4242'`; `1 failed in 0.31s` |
| 16 | `on_scan_snapshot()` зовёт `_check_pending_confirmation` и при `_status_problem` (убран ранний `return`) | `ui/servers/view.py` | `test_scan_snapshot_with_unreadable_job_keeps_pending_confirmation` | УПАЛ — `assert "работает · PID 4242" not in journal_text`: строка найдена в журнале (§8 сработала на снимке, который не удалось прочитать); `1 failed in 0.31s` |

Каждая мутация внесена, прогнан только названный тест, откат
`git checkout --`, `git status --short` пуст — протокол CLAUDE.md
(«Мутационная проверка тестов») соблюдён по всем шестнадцати пунктам.
Рабочее дерево после всех мутаций подтверждено идентичным HEAD
(`git status --short`/`git diff --stat` пусты).

**Факты [Ф] 29.08.2026** (проба на python-процессах с внуком,
kill-on-close): `QueryInformationJobObject(JobObjectBasicProcessIdList)`
отдаёт всё дерево в Job; после убийства родителя извне остатки остаются
в Job и видны в списке; `CloseHandle` гасит все остатки; пустой Job даёт
`(0, ())`.

### Долг вехи T-12 (сознательно)

1. Чужой `dbgs` на порту 1550 не опознаётся по командной строке
   (`--port=1550`, без `-d`) — с профилем не сопоставляется, «держатель
   порта» его не покажет ([Р], спека §4).
2. Гонка «`close()` остатков → порты освобождаются → новый `ragent`
   успевает связать сокет» не измерена ([Р]) — проверяется живым
   чек-листом §10 п. 3 спеки T-12 (баннер `Debug Server started` как
   признак, что порт 1550 освободился).
3. ~~Строка пути раздела «Серверы» (`ui/servers/view.py::rebuild`, текст
   `статус — по живым процессам` — из мокапа) описывает снимок
   процессов, хотя решение о статусе принимает Job (T-12) —
   актуализировать кодом и мокапом одновременно, отдельной правкой
   (не в документах).~~ **Закрыто волной финального ревью ветки
   (30.08.2026, п. 3):** текст `статус — по Job Object запуска` в коде
   и в мокапе (обе темы), `assert` в `test_path_row_shows_store_path`.

Пункты 4–15 — из финального ревью ветки `feat/2026-08-30-v2-job-per-profile`
(30.08.2026, `.superpowers/sdd/2026-08-30-v2-servers-job-per-profile/final-review.md`,
разделы «Issues» и «Разбор access violation»). Ни один
из них не блокировал слияние: Critical в ревью нет, оба Important и две
дешёвые правки закрыты волной исправлений того же дня.

4. Отказ `close()` остатков в `start()` (`services/servers.py`) не
   восстанавливает `_spawned`: `_spawned.pop` сделан ДО `old_job.close()`,
   и при `JobError` Job возвращается в `_jobs`, а порождённый PID — нет.
   Событие «`ragent` завершился извне» в этом окне уже не запишется
   (реконсиляции нечего забывать); информация не теряется — событие
   `отказ запуска: не удалось погасить остатки …` записано, — но симметрия
   отката нарушена. Там же: текст «остатки прошлого запуска (PID ) не
   погашены» с пустым списком PID, когда старый Job пуст (ветку `if
   job_pids` для текста — одна строка).
5. `on_console` (`ui/app.py`) решает «работает» по снимку процессов, а не
   по Job: наш только что запущенный сервер до первого скана (до 5 с)
   в список «работает» для `ConsoleDialog` не попадает. Честный предикат —
   `RUNNING or FOREIGN`, лучше — через `_card_state`, вынесенный из
   `ui/servers/view.py` в общий модуль (см. п. 10). Для выбора СБОРКИ
   консоли ([Ф] Г3) снимок не абсурден: совпавший чужой `ragent` —
   тоже цель подключения, поэтому это долг, а не дефект.
6. Ветка `JobError` в `_reconcile_jobs` (`services/servers.py`) без теста:
   unit-фейк Job в `tests/unit/test_servers.py` не умеет `pids_error`
   (UI-фейк умеет — волна финального ревью, п. 1). Ветка прочитана,
   но это слот, который крутится каждые 5 с. Тест «один Job не читается —
   соседний реконсилируется, событие не дублируется» — ~15 строк.
7. `_check_pending_confirmation` (`ui/servers/view.py`) дублирует предикат
   RUNNING вместо `_card_state(status) is CardState.RUNNING` — разъедется
   при первой правке предиката.
8. Мёртвые данные после снятия гонки PID: `RagentProcess.create_time`
   (`domain/server_match.py`) и `ProcessInfo.create_time`
   (`platform_1c/process_scan.py`) нигде не читаются (только определения
   и конверсии); `Job.is_empty()` в проде не вызывается ни разу, только
   в тестах — протокол носит метод ради спеки, а не ради кода.
9. Хвосты словаря T-08: `test_no_orphans_means_no_extinguish_button`
   (`tests/ui/test_servers_view.py`) — имя с понятием, которого больше
   нет; фраза в `platform_1c/server_spawn.py` про «долг `platform_1c/job.py`»
   устарела (долг закрыт задачей 1 T-12); там же
   `_open_append_shared` заводит свой `WinDLL` на каждый вызов — не по
   правилу «один `WinDLL` на модуль».
10. Размеры файлов: `services/servers.py` — 953 строки,
    `ui/servers/view.py` — 1096. Блок чистых функций карточки (`CardState`,
    `_card_state`, `_status_text`, `_status_colour`, `_button_state`,
    `_removal_question`) просится в `ui/servers/card_state.py` — тогда им
    сможет пользоваться и `app.py::on_console` (п. 5); консольный блок
    `servers.py` — в отдельный координатор. Летописные докстринги
    («T-08.11 добавляет… T-10 задача 4 перевела… T-12 задача 3 меняет…»)
    стоит однажды сжать до актуального состояния со ссылкой на `tasks.md`.
11. `_read_console_note()` (`ui/servers/view.py`) зовётся после `_clear()`
    и читает HKLM без страховки: `platform_1c/console.py` ловит только
    `FileNotFoundError`, любой другой `OSError` оставит раздел с очищенным
    layout. Дефект T-08 той же формы, что закрытый задачей 5 в `rebuild()`
    — лечится `except OSError → «консоль: недоступна»` при следующем
    касании `rebuild()`.
12. `ServerJob.pids()` (`platform_1c/job.py`) растит буфер без верхней
    границы итераций. Не спин: ёмкость удваивается, патологический
    `ERROR_MORE_DATA` кончится `MemoryError` за ~30 шагов, практически —
    ≤ 2 итерации. Потолок из 8 итераций с `JobError` — гигиена.
13. ~~Спека T-12 §4 не оговаривала подавление держателей портов при
    совпавшем `ragent`.~~ **Закрыто волной финального ревью (п. 5):**
    фраза дописана в §4 спеки.
14. Абзац докстринга `platform_1c/job.py` (строки ~13–17) про закрытие
    хендла операционной системой — без метки достоверности; сам факт есть
    ([Ф] Б2 T-09, чек-лист T-10 п. 6), не хватает одной пометки.
15. **`Windows fatal exception: access violation` в
    `pytestqt.plugin._process_events`** (наблюдался однажды на прогоне
    задачи 7). Правдоподобная причина [Р] — не логика T-12, а паттерн
    UI-тестов с T-08: `tests/ui/test_servers_view.py` строит `ServersView`
    без `qtbot.addWidget(view)`, поэтому Python-обёртка вьюхи с циклами
    «`self` → кнопка → лямбда → `self`» освобождается циклическим GC
    в произвольный момент — в том числе внутри `processEvents()`
    следующего теста, когда у дерева виджетов висят `DeferredDelete`
    от `_clear()`. Ветка добавила ~15 тестов того же паттерна: экспозиция
    выросла, механизм не новый. Действие: `qtbot.addWidget(view)` в каждом
    тесте файла; при повторе — `PYTHONFAULTHANDLER=1 uv run pytest -rA
    --tb=long`, сохранить полный блок с C++-кадром; `setParent(None)`
    перед `deleteLater()` в `_clear()` избыточен.
