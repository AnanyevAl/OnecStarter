# Протокол экспериментов T-05 — допущения запуска и формата перед планом UI

> Цель: закрыть вопросы [tasks.md](../tasks.md) § T-05 фактами с реальной машины
> до brainstorming и плана UI раздела «Базы» (T-04.5). Каждый результат записывается
> в этот же файл в поле «Результат», после чего подтверждённое уходит в скилы
> `v8i-format` / `platform-launch` с меткой **[Ф]**, опровергнутое — исправляется
> в скилах, коде и тестах (карта соответствий — в каждом эксперименте, поле «Куда»).
>
> Запуски 1С выполняет заказчик (или агент по явной команде) — согласовано 05.08.2026
> при выборе пути к UI. Эксперименты трогают реальные `ibases.v8i` и `1cestart.cfg` —
> сначала подготовка.
>
> Блок А блокирует план UI: исходы меняют код запуска. Блок Б правит формат/группы.
> Блок В — опциональный, план UI не блокирует.

## Подготовка (один раз)

1. Резервные копии:

   ```powershell
   Copy-Item "$env:APPDATA\1C\1CEStart\ibases.v8i" "$env:APPDATA\1C\1CEStart\ibases.v8i.bak-t05"
   Copy-Item "$env:APPDATA\1C\1CEStart\1cestart.cfg" "$env:APPDATA\1C\1CEStart\1cestart.cfg.bak-t05"
   ```

2. «**Снять процесс**» (выполнять, пока клиент открыт):

   ```powershell
   Get-CimInstance Win32_Process -Filter "Name='1cv8.exe' OR Name='1cv8c.exe' OR Name='1cv8s.exe'" |
     Select-Object ProcessId, ExecutablePath, CommandLine | Format-List
   ```

3. «**Diff файла**»:

   ```powershell
   Copy-Item "$env:APPDATA\1C\1CEStart\ibases.v8i" e:\tmp\v8i-before.txt
   # ... действие ...
   Copy-Item "$env:APPDATA\1C\1CEStart\ibases.v8i" e:\tmp\v8i-after.txt
   git diff --no-index e:\tmp\v8i-before.txt e:\tmp\v8i-after.txt
   ```

4. «**Прямой запуск**» клиента: PowerShell съедает кавычки внутри аргументов,
   поэтому канонично через `cmd` (вся строка — в одинарных кавычках PS):

   ```powershell
   cmd /c '"C:\Program Files\1cv8\8.3.25.1633\bin\1cv8c.exe" ENTERPRISE /IBName"empty" /AppAutoCheckVersion /AppAutoCheckMode'
   ```

5. Тестовая база — `empty` (**[Ф]** T-02: файловая, `D:\db\empty`, пользователь «Админ»
   с пустым паролем, в секции `WA=1`). Установленные версии (**[Ф]** T-02.1): 8.3.10.2252,
   8.3.18.1334, 8.3.20.2290, 8.3.22.1923, 8.3.25.1560, 8.3.25.1633, 8.3.27.2214.
   Рабочие базы не трогать; для экспериментов отображения (Б) допустима секция-пустышка
   с `Connect=File="D:\db\t05-нет";` — её не запускать, только смотреть в стартере.
6. Все правки `ibases.v8i` и `1cestart.cfg` — при закрытом стартере. `1cestart.cfg` —
   **UTF-16LE с BOM** (скил `platform-launch`): править редактором, сохраняющим кодировку,
   diff после правки обязателен.

---

## Блок А — запуск

### T-05.1. Форма присоединения значения к `/IBConnectionString`

**Вопрос:** как клиент принимает строку соединения из командной строки.
**Сейчас в коде:** `/IBConnectionString"<connect>"` с удвоением внутренних кавычек —
принято **по аналогии** со снятой формой `/IBName` (решение 1 плана 2, допущение
помечено в `src/onecstarter/domain/launch.py`, `build_arguments`). Косвенный намёк
против: **[Ф]** T-02.9 — при запуске «группы» стартер передал строку соединения
**позиционным** аргументом (`"" /AppAutoCheckVersion ...`), без ключа вовсе.

1. Прямой запуск нашей формы (кавычки внутри `Connect` удвоены):

   ```powershell
   cmd /c '"C:\Program Files\1cv8\8.3.25.1633\bin\1cv8c.exe" ENTERPRISE /IBConnectionString"File=""D:\db\empty"";" /AppAutoCheckVersion'
   ```

2. Критерий успеха: открылась именно `empty` (диалог «Доступ к информационной базе»
   либо окно базы) → снять процесс → закрыть.
3. Если ошибка параметра/пустой запуск — варианты по одному, фиксируя исход каждого:
   ключ и значение через пробел (`/IBConnectionString "File=..."`); позиционная форма
   без ключа (`"File=""D:\db\empty"";"` сразу после `ENTERPRISE`); без удвоения кавычек.
4. Отдельно: путь с пробелом (скопировать `D:\db\empty` в `D:\db\t05 пробел`,
   повторить рабочую форму, каталог потом удалить).

**Куда:** `domain/launch.py` (`build_arguments`) + `tests/unit/test_launch.py`;
скил `platform-launch` (`reference.md`, раздел ключей клиента).

**Результат:** **[Ф]** 05.08.2026, агент, платформа 8.3.25.1633. **Наша форма верна.**
Запуск `/IBConnectionString"File=""D:\db\empty"";"` (значение прижато к ключу, внутренние
кавычки удвоены) → клиент загрузил конфигурационную информацию и показал диалог
«Доступ к информационной базе» — база найдена и открыта. Путь с пробелом
(`D:\db\t05 пробел`) — тот же результат. Альтернативные формы проверять не пришлось.
Побочно: запуск по строке соединения `ibases.v8i` не меняет (хеш до/после, снято
до входа). Код правок не требует, допущение в `build_arguments` снять.

### T-05.2. Находит ли `/IBName` базу из общего списка

**Вопрос:** объединяет ли клиент при запуске по `/IBName` пользовательский список
с `CommonInfoBases`. **Сейчас в коде:** [Р, не проверено] дизайн плана 3 §7 — принято,
что объединяет; `services/launch.py` запускает записи из общих списков тем же путём.

1. Создать `e:\tmp\t05-common.v8i` (UTF-8) с секцией, которой **нет** в `ibases.v8i`:

   ```ini
   [T05-Общая]
   Connect=File="D:\db\empty";
   ID=<новый GUID>
   ```

2. В `1cestart.cfg` дописать строку `CommonInfoBases=e:\tmp\t05-common.v8i`
   (кодировку файла сохранить, п. 6 подготовки).
3. Прямой запуск: `... 1cv8c.exe ENTERPRISE /IBName"T05-Общая" /AppAutoCheckVersion`.
   Открылась ли база? Снять процесс.
4. Побочно: открыть окно стартера — видна ли `T05-Общая` в списке; закрыть без запуска.
5. Убрать строку из `1cestart.cfg`, удалить `e:\tmp\t05-common.v8i`.

**Куда:** докстринг `services/launch.py`; обязательства плана UI (ветка общих списков);
скил `platform-launch`.

**Результат:** **[Ф]** 05.08.2026, агент. **Находит.** `1cv8c.exe ENTERPRISE
/IBName"T05-Общая"` открыл базу, существовавшую только в `CommonInfoBases` —
клиент объединяет пользовательский список с общими; допущение [Р] плана 3 §7
подтверждено, `services.launch` работает для общих записей без правок.

Три находки сверх вопроса:

1. **Клиент материализовал общую запись в `ibases.v8i`**: секция скопирована
   с тем же `ID`, дописаны `App=Auto`, `WA=1`, `OrderInList`/`OrderInTree`
   и **`External=1`** (у локальных записей `External=0`) — семантика ключа
   `External` прояснилась. `Version` не скопирован. Значит дубль
   «пользовательский + общий» — штатное состояние после первого запуска,
   и дедупликация T-04.4 по `ID` — единственно верная стратегия.
2. **Материализация прошла полной канонической перезаписью файла прямым
   клиентом `1cv8c.exe`** (не стартером): `DisableLocalSpeechToText=0` всем
   секциям, пустые строки-разделители убраны, `OrderInList=-1` материализован.
   Прежний факт «CLI-запуск файл не трогает» относился к `1cestart.exe`.
   При этом полный цикл по `/IBName` базы пользовательского списка
   (вход → сеанс → штатное закрытие) файл **не** меняет — перезапись именно
   от материализации.
3. **Отказ — не ошибка.** После удаления `CommonInfoBases` и материализованной
   секции тот же запуск дал **окно выбора информационных баз** («Запуск
   1С:Предприятия», список, кнопки), а не сообщение об ошибке. Ненайденное имя
   клиент молча превращает в диалог выбора — наши проверки до запуска обязаны
   ловить «имя не найдено» сами, от платформы диагностики не будет.

### T-05.3. `/IBName` при дублях имён; регистр имени

**Вопрос:** что делает клиент при нескольких базах с одним именем, и учитывается ли
регистр при сопоставлении имени. **Сейчас в коде:** guard `_reject_ambiguous_name`
в `services/workspace.py` отказывает до запуска ([Д] «платформа прекращает запуск
с ошибкой» — экспериментально не снималось); сравнение имён **точное**, регистр
помечен [не проверено].

1. При закрытом стартере вписать в `ibases.v8i` две секции `[T05-Дубль]`
   с разными `Connect` (обе можно на `D:\db\empty` — формат это допускает)
   и разными `ID`.
2. Прямой запуск `/IBName"T05-Дубль"` → записать поведение: ошибка (текст?),
   запуск первой, диалог выбора?
3. Переименовать вторую секцию в `[t05-дубль]` (только регистр) → прямой запуск
   `/IBName"T05-Дубль"` → конфликт или однозначный запуск?
4. Удалить вторую секцию. Прямой запуск `/IBName"t05-дубль"` (регистр не совпадает
   с оставшейся `[T05-Дубль]`) → находит или «база не найдена»?
5. Убрать тестовые секции.

**Куда:** `services/workspace.py` (`_reject_ambiguous_name`) + тесты;
скил `platform-launch`.

**Результат:** **[Ф]** 05.08.2026, агент. Сопоставление имени **регистронезависимое**,
дубли пресекаются самим клиентом:

- две секции `[T05-Дубль]` с разными `Connect` → сообщение **«Не уникальное имя
  информационной базы "T05-Дубль"»**, запуск прекращён — [Д] подтверждён;
- `[T05-Дубль]` + `[t05-дубль]` (различие только регистром) → **та же ошибка**;
- единственная `[T05-Дубль]`, запрос `/IBName"t05-дубль"` → база **найдена**,
  диалог доступа.

**Код требует правки:** `_reject_ambiguous_name` сравнивает имена точно и пропускает
конфликт, который платформа считает дублем, — пользователь получит ошибку платформы
вместо нашей диагностики. Сравнение перевести на casefold; довод докстринга «лишний
отказ хуже пропуска» снят — отказ по casefold ложным не бывает, платформа откажет
всё равно.

### T-05.4. Держит ли `/AppAutoCheckVersion-` нашу версию

**Вопрос:** остаётся ли работать запущенный нами exe, когда `Version` секции просит
другую версию. Это фундамент всей модели запуска `services/launch`: версию разрешаем
сами и передаём `/AppAutoCheckVersion-`; [Д] «иначе платформа переиграет выбор» —
самим ключом эксперимент не снимался, и если платформа переигрывает выбор даже
с ним, показанная в UI версия будет ложью.

1. Базе `empty` выставить `Version=8.3.22.1923` (установлена, точная).
2. Прямой запуск **exe 8.3.25.1633** с `/AppAutoCheckVersion-`:

   ```powershell
   cmd /c '"C:\Program Files\1cv8\8.3.25.1633\bin\1cv8c.exe" ENTERPRISE /IBName"empty" /AppAutoCheckVersion-'
   ```

   Снять процесс: `ExecutablePath` остался `8.3.25.1633`? Появился ли второй процесс
   другой версии?
3. То же с `/AppAutoCheckVersion` (включённым): перезапустила ли платформа клиента
   версии `8.3.22.1923`? Снять процесс.
4. Вернуть `Version` секции как было.

**Куда:** подтверждение модели `services/launch.py`; скил `platform-launch`
(алгоритм подбора версии, [Д] → [Ф] или правка).

**Результат:** **[Ф]** 05.08.2026, агент. Секции `empty` выставлен
`Version=8.3.22.1923`, запускался exe `8.3.25.1633\bin\1cv8c.exe`:

- с `/AppAutoCheckVersion-` — работает **один** процесс `8.3.25.1633`,
  наша версия удержана;
- с `/AppAutoCheckVersion` — наш процесс завершился, платформа перезапустила
  клиента `8.3.22.1923\bin\1cv8c.exe` с командной строкой
  `ENTERPRISE /IBName"empty" /AppAutoCheckMode /AppAutoCheckVersion`
  (ключ `/AppAutoCheckMode` она добавила сама).

Модель запуска `services/launch` подтверждена с обеих сторон: версия, показанная
в UI, совпадает с фактической только при `/AppAutoCheckVersion-`. Код правок
не требует, [Д] в скиле повышается до [Ф].

### T-05.5. `DefaultVersion` вне маски `Version`

**Вопрос:** остаток T-02.2 — уточняет ли `DefaultVersion` маску, которой
не соответствует. **Сейчас в коде:** `domain/selection.py` игнорирует
`DefaultVersion`, не начинающийся с маски (`starts_with`) либо не установленный,
и идёт дальше по цепочке — поведение платформы в этих случаях не снималось.

1. Базе `empty` выставить `Version=8.3.25`, `DefaultVersion=8.3.22.1923` (вне маски).
2. Запуск **из окна стартера** (важно: решать должна платформа, не наша команда) →
   снять процесс: 8.3.22.1923 (уточнил вне маски) или 8.3.25.1633 (маска победила)?
3. Опционально: `DefaultVersion=8.3.25.9999` (в маске, но не установлен) → запуск
   из окна стартера → что запустилось; появилось ли сообщение.
4. Вернуть ключи секции; diff файла после каждого шага (запуск из окна стартера
   перезаписывает файл целиком — **[Ф]** T-02.5).

**Куда:** `domain/selection.py` (`resolve_version`) + табличные тесты;
скилы `platform-launch` и `v8i-format` (факт 3).

**Результат:** **[Ф]** 05.08.2026, агент. Вместо окна стартера использован
эквивалентный CLI-путь `1cestart.exe ENTERPRISE /IBName"empty"` — версию выбирает
тот же стартер (окно оказалось небезопасно автоматизировать, см. журнал ниже).
Секция `empty`, `Version=8.3.25` (маска):

- `DefaultVersion=8.3.22.1923` (установлен, **вне маски**) → запущен
  **`8.3.22.1923`**. `DefaultVersion` побеждает маску, даже когда
  ей не соответствует.
- `DefaultVersion=8.3.25.9999` (в маске, **не установлен**) → запущен
  **`8.3.27.2214`** — молча максимум установленных **вообще**, маска
  не ограничила фолбэк (prefix-max `8.3.25.1633` не выбран).
  `DefaultVersion` в файле остался `8.3.25.9999` — запрошенным (ещё одно
  подтверждение T-02.8).

**Код требует решения:** `resolve_version` в первом случае игнорирует
`DefaultVersion` вне маски (проверка `starts_with`), во втором — падает
на prefix-max; платформа в обоих случаях делает другое. Минимум — поле
`fallback` («что молча запустил бы штатный стартер») обязано считаться
по платформенным правилам, иначе UI-подсказка врёт. Расширить табличные
тесты `test_selection.py` этими двумя строками.

Побочные наблюдения:

1. **Окно списка баз рисует не `1cestart.exe`.** Он мгновенно завершается,
   делегируя окно `1cv8s.exe` **максимальной установленной версии** с пустой
   строкой соединения (`"" /AppAutoCheckVersion /AppAutoInstallLastVersion-`) —
   это объясняет «двухпроцессный» след T-02.9.
2. Кнопка «1С:Предприятие» окна выбора запускает **выделенную в списке** базу;
   попытка выбрать нужную через UI Automation не сработала (SelectionItemPattern
   не поддержан списком 1С), и кнопка запустила ранее выделенную рабочую базу —
   клиент убит на диалоге аутентификации, в базу не входили. Автоматизация окна
   стартера ненадёжна — для GUI-экспериментов блока Б нужен человек за машиной.

---

## Блок Б — формат `.v8i` и группы

### T-05.6. Пустой `Connect=` — группа или база

**Вопрос:** трактовка секции с ключом `Connect` без значения (план 3 §7).
**Сейчас в коде:** признак группы — **отсутствие** `Connect`; `Connect=` с пустым
значением наш разбор считает базой. Сюда же — теоретический ложный отказ backstop-а
групп (отложено финальным ревью ветки плана 4).

1. При закрытом стартере вписать секцию:

   ```ini
   [T05-ПустойConnect]
   Connect=
   ID=<новый GUID>
   Version=8.3.25
   ```

2. Открыть стартер: значок папки или базы? Закрыть без запуска.
3. Запустить любую другую базу из окна стартера (событие полной перезаписи) →
   diff файла: секция уцелела? `Version` вычищен, как у деградировавшей базы
   в T-02.9, или сохранён?
4. Убрать секцию.

**Куда:** `config/v8i.py` (`is_group`) при расхождении; заметка backstop-а
в `services/groups.py`; скил `v8i-format` (факт 2).

**Результат:** **[Ф]** 05.08.2026, заказчик у стартера + агент (diff'ы). **Пустой
`Connect=` — группа.** Стартер показал секцию значком группы, а первая же полная
перезапись (запуск `empty` из окна) **довершила деградацию**: ключ `Connect=`
удалён, `Version` вычищен, дописаны групповые `OrderInList=-1`, `Folder=/`,
`OrderInTree`, `External=0` — секция превращена в группу необратимо, как
«испорченная» база T-02.9/факта 6 скила.

**Код требует правки:** наш `is_group` считает признаком группы **отсутствие**
ключа `Connect`; секцию с `Connect=` (ключ есть, значение пустое) разбор трактует
как базу — расхождение с платформой. Признак группы: `Connect` отсутствует
**или пуст**.

### T-05.7. Регистр при сопоставлении `Folder` с именем группы

**Вопрос:** `Folder=/t05-группа` при группе `[T05-Группа]` — внутри группы или мимо?
**Сейчас в коде:** сравнение путей в `services/paths.py` регистрозависимое,
а `binding_key` приводит путь группы к нижнему регистру — расхождение зафиксировано
финальным ревью ветки плана 4 и ждёт именно этого факта.

1. Мастером стартера создать группу `T05-Группа` в корне.
2. При закрытом стартере вписать пустышку с `Folder` в другом регистре:

   ```ini
   [T05-Пустышка]
   Connect=File="D:\db\t05-нет";
   ID=<новый GUID>
   Folder=/t05-группа
   ```

3. Открыть стартер: пустышка внутри `T05-Группа` или в корне? Закрыть.
4. Запустить любую базу из окна (перезапись) → diff: нормализовала ли платформа
   регистр `Folder`?
5. Группу и пустышку пока не удалять — пригодятся в T-05.9.

**Куда:** `services/paths.py` (докстринг и, при регистронезависимости, сравнение),
`services/model.py` (`binding_key`); скил `v8i-format` (факт 2).

**Результат:** **[Ф]** 05.08.2026, заказчик у стартера + агент (diff'ы).
**Сопоставление регистрозависимое** — противоположность регистронезависимым
именам баз из T-05.3:

- пустышка с `Folder=/t05-группа` при группе `[T05-Группа]` показана **не внутри
  неё**: стартер отрисовал **отдельный узел** `t05-группа`;
- перезапись (запуск `empty` из окна) дописала пустышке канонические ключи,
  но `Folder=/t05-группа` **сохранён байт в байт** — регистр не нормализуется;
- **неявная группа не материализуется**: секции для узла `t05-группа` в файле
  не появилось — узел живёт только в отрисовке дерева. Это заодно закрывает
  «не проверено» скила о базе, чей `Folder` не указывает ни на одну
  секцию-группу: база не сирота и не падает в корень.

**Код:** регистрозависимое сравнение путей в `services/paths.py` — правильное;
приведение пути группы к нижнему регистру в `binding_key` (`services/model.py`) —
расхождение, зафиксированное финальным ревью ветки плана 4, разрешается
в пользу точного регистра: нижний регистр убрать.

### T-05.8. Видит ли стартер группу без `OrderInTree`

**Вопрос:** безвредно ли отсутствие `OrderInTree` у созданной нами группы.
**Сейчас в коде:** [Р] `groups._create` пишет `ID`, `OrderInList=-1`, `Folder`,
`External=0` — без `OrderInTree`, в расчёте на то, что платформа пересчитает.

1. При закрытом стартере вписать секцию ровно в нашем составе ключей:

   ```ini
   [T05-БезДерева]
   ID=<новый GUID>
   OrderInList=-1
   Folder=/
   External=0
   ```

2. Открыть стартер: группа видна в дереве? Где — в конце, в начале? Закрыть.
3. Запустить любую базу из окна (перезапись) → diff: дописан ли `OrderInTree`?
4. Убрать секцию (или удалить в T-05.9 руками стартера — тоже наблюдение).

**Куда:** `services/groups.py` (`_create`, [Р] → [Ф] или запись `OrderInTree`);
скил `v8i-format` (факт 2, состав ключей группы).

**Результат:** **[Ф]** 05.08.2026, заказчик у стартера + агент (diff'ы). **Группа
без `OrderInTree` видна и безвредна.** Секция ровно в нашем составе ключей (`ID`,
`OrderInList=-1`, `Folder=/`, `External=0`) отображена стартером в дереве; полная
перезапись дописала `OrderInTree=82944`, остальные ключи не тронуты. [Р] в
`groups._create` повышается до [Ф], код правок не требует.

### T-05.9. Что стартер делает с содержимым при удалении группы

**Вопрос:** каскад, подъём к родителю или запрет — для текстов подтверждения
в плане UI (`GroupRemoval` требует явной политики; у стартера политика неизвестна).

1. Взять группу `T05-Группа` с пустышкой внутри (из T-05.7); при желании —
   добавить подгруппу мастером.
2. Diff файла вокруг действия: удалить группу в стартере (контекстное меню).
   Записать: было ли предупреждение и его текст; что с вложенными секциями —
   удалены, подняты, осиротели?
3. Прибраться: убрать остатки тестовых секций, сверить файл с бэкапом.

**Куда:** тексты подтверждений и поведение по умолчанию в плане UI;
скил `v8i-format`.

**Результат:** **[Ф]** 05.08.2026, заказчик у стартера + агент (diff'ы). **Каскад
без предупреждения о содержимом.** Группа `T05-Группа` содержала базу-пустышку
и подгруппу `T05-Подгруппа` (создана мастером в той же сессии); удаление через
контекстное меню:

- подтверждение — единственный вопрос **«Удалить группу "T05-Группа"?»** (Да/Нет),
  **о содержимом ни слова**; тот же текст у пустых групп (сняты скриншоты трёх
  диалогов);
- по «Да» удалены **и группа, и вся вложенность**: секции пустышки и подгруппы
  исчезли из файла — каскад, не подъём к родителю и не запрет.

**Для плана UI:** политика платформы — молчаливый каскад; наше подтверждение
`GroupRemoval.RECURSIVE` обязано быть информативнее — перечислять содержимое
или хотя бы называть его количество.

---

## Блок В — опционально (план UI не блокирует)

### T-05.10. Локальный кэш базы

**Активирован 23.08.2026:** очистка кэша вошла в веху «закрыть v1», и решения
заказчика того же дня сузили, что именно надо измерить — чистим **кэш одной
базы** из контекстного меню записи, при занятых файлах **удаляем что получится
и честно докладываем**. Значит нужны три факта: где кэш лежит, как он связан
с конкретной записью, и что бывает при удалении. Без первых двух очистка
удаляла бы по догадке, без третьего — обещала бы результат, которого не знает.

Запуск процессов 1С выполняет заказчик (правило проекта). Тестовая база —
`empty` (**[Ф]** T-02: файловая, `D:\db\empty`); рабочие базы не трогать.

**Шаг 1. Снимок до запуска.** Каталоги-кандидаты — из прошлых наблюдений,
оба под проверкой:

```powershell
$roots = @("$env:LOCALAPPDATA\1C\1cv8", "$env:APPDATA\1C\1cv8")
foreach ($r in $roots) {
  "=== $r ==="
  if (Test-Path $r) { Get-ChildItem $r -Directory | Select-Object Name, LastWriteTime } else { "нет каталога" }
}
```

**Шаг 2. Запустить `empty` тонким клиентом, закрыть, снять снимок снова.**
Записать: какой каталог появился или изменил `LastWriteTime`.

**Шаг 3. Чем именован каталог.** Сверить имя с `ID` секции `empty` в
`ibases.v8i` и со строкой соединения. Три исхода, и они ведут к разному коду:
имя совпадает с `ID` — очистка ищет по `ID` записи; не совпадает ни с чем
узнаваемым (хеш) — понадобится правило вычисления либо опознание по
содержимому; каталогов несколько — надо понять, чем они различаются.

**Шаг 4. Второй базой** (любая другая файловая, годится секция-пустышка)
подтвердить правило именования: одна база — один каталог, или общий на всех.

**Шаг 5. Толстый клиент.** Запустить `empty` толстым, снять снимок: тот же
каталог или отдельный. От этого зависит, чистит ли один пункт меню всё
за базу или только под тот клиент, которым её открывали.

**Шаг 6. Что внутри** (по верхам, без вскрытия форматов): файлы и подкаталоги
первого уровня с размерами. Нужно, чтобы понимать, что предлагается удалять
и сколько это места.

**Шаг 7. Занятость.** Открыть `empty` и, не закрывая клиента, попробовать
удалить каталог кэша (`Remove-Item -Recurse`). Записать: удаляется целиком,
частично или никак; текст отказа. Это прямо определяет сообщение, которое
покажет наша очистка.

**Шаг 8. Последствия удаления.** Закрыть клиента, удалить кэш, запустить базу
снова. Записать: запускается ли, дольше ли первый запуск, не потерялось ли
что-то видимое (настройки формы, история). Без этого мы не вправе обещать
пользователю, что очистка безобидна.

**Шаг 9. Серверный реестр кэшей не трогать** — серверной тестовой базы нет.

**Результат шагов 1, 3, 4, 6 — [Проверено, 23.08.2026, машина заказчика]:
измерено без запуска 1С, по следам прошлых запусков.**

Оба каталога-кандидата существуют, и это **не одно и то же в двух местах,
а два разных хранилища**:

| Что | `%LOCALAPPDATA%\1C\1cv8\<ID>` | `%APPDATA%\1C\1cv8\<ID>` |
| --- | --- | --- |
| Содержимое | `Config`, `ConfigSave`, `SICache` | `1cv8.pfl`, `1cv8c.pfl`, `def.usr`, `logui.txt`, `userDocs_ru.bin`, `userPostings_ru.bin`, `userVocabulary_ru.bin`, подкаталоги-GUID |
| Смысл | кэш конфигурации — то, что и понимают под «очистить кэш» | настройки пользователя: профили клиентов, словарь, история ввода |
| Объём у заказчика | **2 930,8 МБ** | 341,3 МБ |
| Каталогов | 31 | 104 |

**Имя каталога — это `ID` секции из `ibases.v8i`.** Гипотеза шага 3 подтверждена
статистикой, а не одним наблюдением: из 66 записей живого списка **58 имеют
одноимённый каталог** в `%APPDATA%\1C\1cv8` и 14 — в `%LOCALAPPDATA%`.
Совпадение такого масштаба случайным не бывает; расхождение до 66 объясняется
тем, что не каждая база с этой машины запускалась. Шаг 4 закрыт тем же
замером: соответствие «одна база — один каталог», общего каталога на всех нет.

**Обратное неверно: каталог без базы остаётся.** В `%APPDATA%\1C\1cv8`
**46 каталогов не имеют пары** в списке баз — следы удалённых записей.
Для очистки это означает, что искать кэш «перебором каталогов и опознанием»
не нужно: `ID` записи даёт прямой адрес.

**Различие клиентов — только в настройках, не в кэше.** В `%APPDATA%` профили
раздельные (`1cv8.pfl` — толстый, `1cv8c.pfl` — тонкий), в `%LOCALAPPDATA%`
разделения нет: `Config`/`ConfigSave`/`SICache` общие. То есть один пункт меню
чистит кэш базы целиком, независимо от того, каким клиентом её открывали, —
шаг 5 закрыт для кэша и не требует запуска (для профилей вопрос не встаёт:
их мы не трогаем).

**Главное следствие для дизайна, и оно опровергает напрашивавшееся решение.**
«Удалить оба каталога по `ID`» выглядело естественным и было бы ошибкой:
в `%APPDATA%` лежат пользовательские настройки — профили форм, история ввода,
словарь. Их удаление пользователь заметит и не свяжет с «очисткой кэша».
Очистка обязана трогать **только `%LOCALAPPDATA%\1C\1cv8\<ID>`**.


**Терминология (принята заказчиком 23.08.2026).** Кэшей два, и называть их
надо раздельно, иначе разговор о «кэше» всё время двоится:

- **пользовательский кэш** — `%APPDATA%\1C\1Cv8\<ID>`;
- **программный кэш** — `%LOCALAPPDATA%\1C\1Cv8\<ID>`.

**Разница между файловой и серверной базой — [Проверено, 23.08.2026]: в составе
программного кэша её нет.** Замер по всем 60 записям живого списка с `ID`
и `Connect` (49 файловых, 11 серверных); программный кэш существует у 14 из них:

| Вид | Баз с кэшем | `Config` | `ConfigSave`/`SICache` | `cgf-cache` |
| --- | --- | --- | --- | --- |
| файловая | 9 | 9 | 9 | 2 |
| серверная | 5 | 5 | 5 | 0 |

`Config`, `ConfigSave`, `SICache` есть у всех четырнадцати независимо от вида —
значит очистка работает одинаково для файловых и серверных, отдельной ветки
по виду не нужно.

Два отличия, которые могли бы сойти за «разницу видов», ею не объясняются:

- **`cgf-cache`** встречается только у файловых, но у 2 из 9. Раз не у всех
  файловых — дело не в виде, а в чём-то ещё (версия платформы либо режим
  запуска). Для очистки безразлично: удаляется каталог `<ID>` целиком.
- **`userDocs_ru.bin`, `userPostings_ru.bin`, `userVocabulary_ru.bin`**
  в пользовательском кэше — у 5 из 5 серверных против 2 из 9 файловых.
  Похоже на интенсивность работы, а не на вид: это словарь и история ввода,
  они появляются там, где действительно работают.

**Состав кэшей различается по смыслу, и это главное для дизайна.**
В программном — `Config`, `ConfigSave`, `SICache`: кэш конфигурации,
у заказчика 2 930,8 МБ на 31 каталог. В пользовательском — профили клиентов
(`1cv8.pfl` — толстый, `1cv8c.pfl` — тонкий), `def.usr`, `logui.txt`, словарь
и история ввода: 341,3 МБ на 104 каталога. Удаление программного стоит
пользователю времени первого запуска; удаление пользовательского — привычек
интерфейса. Поэтому очистка не сливает их в одну кнопку (решение заказчика:
подменю с двумя пунктами).

**Внутри обоих каталогов лежит подкаталог с третьим GUID**, и для одной базы
он **одинаков** в пользовательском и программном кэше (проверено на паре
файловая/серверная). Это не `ID` записи и не имя базы — предположительно
пользователь информационной базы. Удаление каталога `<ID>` целиком уносит
и его; отдельно этот GUID нам адресовать не нужно.

**Результат шага 7 — [Проверено, 23.08.2026, машина заказчика]: удаление при
открытой базе идёт частично, и это определяет весь сценарий отказа.**

Попытка `Remove-Item -Recurse -Force` по программному кэшу открытой базы дала
**13 ошибок**. Заняты оказались ровно активные хранилища (пути обезличены,
`<ID>` — каталог базы, `<user>` — вложенный GUID пользователя ИБ):

| Занятый объект | Где |
| --- | --- |
| `cache.1CD`, `cache.1CD.cfl`, `cache.1CL`, `cache.1CL.cfl` | `<ID>\<user>\vrs-cache\` |
| `RuntimeCacheStorage` | `<ID>\Config\` |
| `RuntimeCacheStorage` | `<ID>\ConfigSave\` |
| `cacheStorage` | `<ID>\SICache\` |

Тексты Windows дословно — двух видов, и различать их придётся:

- на файлах: `Процесс не может получить доступ к файлу "cache.1CD", так как
  этот файл используется другим процессом` (`IOException`);
- на каталогах, где остался хоть один такой файл: `Папка не пуста`
  (тот же `IOException`) — это **следствие**, а не самостоятельная причина.

**Что из этого следует для реализации.**

1. **Удаление обязано быть по-файловым и продолжающимся.** Останавливаться
   на первой ошибке нельзя: незанятое удаляется нормально, и пользователь
   получает освобождённое место даже при открытой базе. Ровно это заказчик
   и выбрал («пробовать и честно докладывать»).
2. **Считать надо два числа: удалено и не удалось.** Показывать «удалено N
   файлов, освобождено X МБ; M занято запущенной 1С» — и ни в коем случае
   не выдавать пользователю 13 трассировок PowerShell-вида.
3. **Ошибку «Папка не пуста» в отчёт не выносить.** Она вторична: каталог
   не удалился потому, что в нём остался занятый файл, о котором уже сказано.
   Иначе одна занятая база даст пользователю семь сообщений вместо одного.
4. **`vrs-cache` — элемент, которого нет на первом уровне.** Осмотр верхнего
   уровня его не показывал: он лежит внутри `<ID>\<user>\`. Значит очистка
   обходит дерево целиком, а не удаляет три известных каталога по именам —
   список подкаталогов не закрыт, и жёсткий перечень протух бы молча.

**Результат шага 8 — [Проверено, 23.08.2026, машина заказчика]: удаление
программного кэша безобидно.** После очистки база запустилась как обычно,
заметной задержки первого запуска не было, видимых потерь заказчик не отметил.

Значит подтверждение для программного кэша можно писать спокойным тоном:
операция стоит места на диске и, возможно, части времени первого запуска —
пугать пользователя нечем.

**Последствия удаления ПОЛЬЗОВАТЕЛЬСКОГО кэша этим шагом не измерены** —
проверялся программный. Помечено честно, чтобы предупреждение второго пункта
меню не сочинялось: текст для него опирается на **состав** каталога, а он
измерен (шаг 6) — профили клиентов `1cv8.pfl`/`1cv8c.pfl`, `def.usr`,
`logui.txt`, словарь и история ввода `user*_ru.bin`. То есть сказать
«пропадут настройки форм и история ввода» мы вправе по составу, а утверждать
«и больше ничего» — нет.

### Итог T-05.10

Закрыт 23.08.2026. Из девяти шагов семь измерены, причём четыре — без запуска
1С, по следам прошлых сеансов. Что получено для реализации очистки:

1. Адрес кэша — по `ID` записи, перебор и опознание не нужны.
2. Кэшей два, различаются по смыслу; чистятся отдельными пунктами подменю.
3. Вид базы (файловая/серверная) на состав программного кэша не влияет.
4. Обход — рекурсивный по дереву, а не по трём известным именам: `vrs-cache`
   лежит уровнем глубже и в перечень бы не попал.
5. Удаление при открытой базе частичное; отчёт — два числа, вторичную
   «Папка не пуста» не показывать.
6. Программный кэш удалять безопасно; про пользовательский говорим по составу.

### T-05.11. Форма URL веб-базы

`services/launch._launch_web` берёт значение `ws` как есть [не проверено].
Выполнять при наличии тестовой ws-базы; на рабочих не проверять.

**Результат:**

### T-05.12. `/WA+` при настроенной ОС-аутентификации

Остаток T-02.7: требует базы, где пользователю ИБ сопоставлен пользователь ОС
(настройка в Конфигураторе на копии `empty`). К плану UI не относится — трогает
только справочник `platform-launch`.

**Результат:**

---

## После экспериментов

1. Результаты → метки **[Ф]** в скилах `v8i-format` и `platform-launch`;
   опровергнутое исправить в скилах, коде и тестах по полю «Куда» каждого эксперимента.
2. Обновить статусы в [tasks.md](../tasks.md) § T-05.
3. Убрать тестовые секции и строки из `ibases.v8i` и `1cestart.cfg`; при сомнениях —
   восстановить из `*.bak-t05`.
4. Разблокируется T-04.5: brainstorming объёма (резка 4a/4b) → план UI.
