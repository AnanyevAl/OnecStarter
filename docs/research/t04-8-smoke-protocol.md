# Протокол ручного smoke вехи T-04.8 «Завершение v1»

> Цель: прогнать обе функции вехи — настройку «Клиент по умолчанию» и очистку
> кэша — на живой машине, глазами и руками, поверх зелёных offscreen-тестов.
> Каждый результат записывается в этот же файл, в поле «Результат» своего шага.
> Расхождение с ожиданием — дефект: разбирается systematic-debugging, не
> подгоняется текст под исход.
>
> Выполняет заказчик: запуск GUI программы и процессов 1С — граница проекта.
> Агент выполняет только read-only замеры (снятие процесса, размеры кэшей,
> diff файлов) — по ходу прогона, по команде «замерь».
>
> Испытуемый — из исходников (`uv run onecstarter`), база — `empty`
> (**[Ф]** T-02: файловая, `D:\db\empty`; клиентов запускать и кэши чистить
> безопасно). Рабочие базы не трогать; их пользовательский кэш — тем более:
> последствия удаления не проверены (спека вехи, §7 п. 1).

## Состояние прогона на 23.08.2026

| Блок | Шаги | Состояние |
| --- | --- | --- |
| А (клиент по умолчанию) | А1–А7 | не начат |
| Б (очистка кэша) | Б1–Б9 | не начат |

## Подготовка (один раз)

1. Резервные копии (кэши не бэкапим — их и чистим; настройки и список — да):

   ```powershell
   Copy-Item "$env:APPDATA\1C\1CEStart\ibases.v8i" "$env:APPDATA\1C\1CEStart\ibases.v8i.bak-t048"
   Copy-Item "$env:APPDATA\OneCStarter\settings.json" "$env:APPDATA\OneCStarter\settings.json.bak-t048"
   ```

2. «**Адрес кэшей empty**» (ID в протокол не пишем — извлекается на месте):

   ```powershell
   $id = $null; $in = $false
   foreach ($line in Get-Content "$env:APPDATA\1C\1CEStart\ibases.v8i") {
     if ($line -match '^\[(.*)\]$') { $in = ($Matches[1] -eq 'empty') }
     elseif ($in -and $line -match '^ID=(.+)$') { $id = $Matches[1]; break }
   }
   $prog = "$env:LOCALAPPDATA\1C\1Cv8\$id"; $user = "$env:APPDATA\1C\1Cv8\$id"
   ```

3. «**Снимок кэша**» (размер и число файлов — сверять с диалогами программы):

   ```powershell
   Get-ChildItem -Recurse -File $prog | Measure-Object -Sum Length |
     Select-Object Count, @{n='MB';e={[math]::Round($_.Sum/1MB,1)}}
   ```

4. «**Снять процесс**» (выполнять, пока клиент 1С открыт):

   ```powershell
   Get-CimInstance Win32_Process -Filter "Name='1cv8.exe' OR Name='1cv8c.exe' OR Name='1cv8s.exe'" |
     Select-Object ProcessId, ExecutablePath, CommandLine | Format-List
   ```

5. Запуск испытуемого — из корня репозитория: `uv run onecstarter`.

Критерий «какой клиент запустился» — всегда «Снять процесс», не заголовок
окна: тонкий = `1cv8c.exe`, толстый = `1cv8.exe` (ENTERPRISE), конфигуратор =
`1cv8.exe` (DESIGNER в командной строке). Урок T-04.7: устный ответ трижды
расходился с фактами — сверяем артефактами, не словами.

## Блок А — клиент по умолчанию

**А1. Вид настройки.** Раздел «Настройки», группа «ОКНО И ЗАПУСК»: строка
«Клиент по умолчанию» с сегментом «Тонкий | Толстый», выбран «Тонкий»;
подпись строки объясняет приоритет (App записи и Ctrl+1/2 главнее).
**Ожидание:** шестая настройка на месте, дефолт тонкий.
**Результат:** ✅ 23.08.2026, заказчик: всё как описано — строка на месте,
выбран «Тонкий», подпись и место совпадают. `settings.json` до прогона ключа
`default_client` не содержал — дефолт пришёл из отсутствующего ключа, как
и задумано.

**А2. Дефолт — тонкий.** При «Тонкий»: запустить `empty` (F3 или двойной
клик), «Снять процесс», закрыть клиента.
**Ожидание:** `1cv8c.exe`, ENTERPRISE, `/AppAutoCheckMode` в командной строке.
**Результат:** ✅ 23.08.2026. Снято агентом:
`1cv8c.exe` (8.3.27.2214), `ENTERPRISE /IBName"empty" /AppAutoCheckVersion-
/AppAutoCheckMode` — дословно ожидаемая строка.

**А3. Смена на лету.** Переключить сегмент на «Толстый» — программу НЕ
перезапускать. Запустить `empty`, «Снять процесс», закрыть клиента.
**Ожидание:** `1cv8.exe`, ENTERPRISE — настройка доехала без пересборки окна.
**Результат:** ⚠️ 23.08.2026 — **расхождение, вскрывшее пробел спеки**.
Проводка настройки работает: наша история запусков записала
`"last_client": "thick"` (16:19:18), то есть программа запустила `1cv8.exe`.
Но через 3 секунды (16:19:21) процессом `empty` оказался `1cv8c.exe`
с **платформенной** командной строкой (`ENTERPRISE /IBName"empty" /N"Админ"` —
без наших флагов, `/N` из профиля) и мёртвым родителем: `/AppAutoCheckMode`,
который мы передаём при клиенте из настройки (`auto_check_mode=True` в ветке
`default_app` `choose_client`), позволил платформе определить режим базы
(управляемое приложение, основной клиент тонкий) и **перезапустить нашего
толстого тонким**. Контрольная пара — шаг А5 ниже: толстый БЕЗ
`/AppAutoCheckMode` (принудительный выбор) платформой не перезапускается.
Вывод: на базах с основным клиентом «тонкий» настройка «Толстый» видимого
эффекта не имеет. Спека §2.2/§2.4 случай не проговаривала. Решение
заказчика — см. «Решение по А3» после блока А.

**А4. `App` записи главнее настройки.** В `ibases.v8i` дописать в секцию
`[empty]` строку `App=ThinClient` (блокнотом; программа перечитает файл
watcher'ом сама). Настройка остаётся «Толстый». Запустить `empty`,
«Снять процесс», закрыть.
**Ожидание:** `1cv8c.exe` — тонкий: App записи победил настройку; в командной
строке НЕТ `/AppAutoCheckMode` (**[Ф]** T-02.6: при явном App не передаётся).
**Результат:**

**А5. Принудительный выбор главнее всего.** Не убирая `App=ThinClient`,
настройка «Толстый»: запустить `empty` через Ctrl+2 («Толстый клиент»
контекстного меню тоже годится), «Снять процесс», закрыть.
**Ожидание:** `1cv8.exe`, ENTERPRISE — принудительный выбор побил и App,
и настройку.
**Результат:** ✅ 23.08.2026 (выполнен раньше очереди, до А4, как контроль
гипотезы А3; `App=ThinClient` на этот момент ещё не дописан — проверена
половина «принудительный бьёт настройку»). Ctrl+2 → `1cv8.exe` (8.3.27.2214),
`ENTERPRISE /IBName"empty" /AppAutoCheckVersion-` — наша строка, без
`/AppAutoCheckMode` (forced → `auto_check_mode=False`); процесс прожил
больше 10 секунд, платформа не перезапустила. Пара к А3: тот же толстый
клиент с `/AppAutoCheckMode` был перезапущен, без него — удержан.

**А6. Персистентность.** Настройка «Толстый»: полностью выйти из программы
(через меню трея, не крестиком). Проверить файл:
`Select-String default_client "$env:APPDATA\OneCStarter\settings.json"` →
`"default_client": "thick"`. Запустить программу заново — сегмент на «Толстый».
**Ожидание:** значение пережило перезапуск, сегмент синхронен файлу.
**Результат:**

**А7. Ярлык.** При настройке «Толстый»: контекстное меню `empty` → «Создать
ярлык…» на рабочий стол; двойной клик по ярлыку; «Снять процесс», закрыть
клиента, ярлык удалить.
**Ожидание:** `1cv8.exe` — настройка действует и на запуск по `--ib-name`
(`build_runtime` читает `settings.json`). Из исходников ярлык зовёт
`python -m onecstarter` — это штатно (`shortcut_command`, frozen=False).
**Результат:**

### Решение по А3 (заказчик, 23.08.2026)

Принят вариант «настройка — явный выбор»: клиент из настройки передаётся
**без** `/AppAutoCheckMode` (симметрия с явным `App` записи, **[Ф]** T-02.6);
«Тонкий» остаётся дефолтом с прежним поведением — проводка передаёт `None`,
запуск как до вехи, с `/AppAutoCheckMode` (§2.1: у существующих установок
ничего не меняется). Спека §2.2 дополнена тем же коммитом; правка кода —
ветка `fix/2026-08-23-default-client-autocheckmode`.

**А3-повтор (после слияния правки).** Перезапустить программу
(`uv run onecstarter`), настройка «Толстый»: запустить `empty`, «Снять
процесс» спустя ≥10 секунд, закрыть клиента.
**Ожидание:** `1cv8.exe`, наша строка `ENTERPRISE /IBName"empty"
/AppAutoCheckVersion-` — без `/AppAutoCheckMode`; процесс жив, платформа
не перезапустила.
**Результат:**

**Уборка блока А:** убрать `App=ThinClient` из секции `[empty]`; настройку
вернуть на «Тонкий» (или оставить «Толстый» — решение заказчика, записать).
**Сделано:**

## Блок Б — очистка кэша

**Б1. Состав меню.** Правый клик по `empty`: подменю «Очистить кэш» — между
«Свойства…» и разделителем (ниже — «В избранное», за вторым разделителем —
«Удалить из списка…»). В подменю два пункта: «Пользовательский…»,
«Программный…», оба активны (кэши на диске есть), сочетаний клавиш нет.
**Ожидание:** ровно так (спека §3.2).
**Результат:**

**Б2. Группа.** Правый клик по любой строке-группе.
**Ожидание:** подменю «Очистить кэш» в меню группы отсутствует вовсе (§3.4).
**Результат:**

**Б3. Чужой записи без кэша — «кэш пуст».** Найти запись, у которой каталога
программного кэша нет (агент подскажет по снимку), открыть её меню.
**Ожидание:** пункт «Программный…» неактивен, тултип при наведении — «кэш пуст».
Ничего не кликать.
**Результат:**

**Б4 (опционально). Запись без ID.** Дописать в конец `ibases.v8i` секцию:
`[t048-безid]` + `Connect=File="D:\db\t048";` (без строки ID). Открыть её меню.
**Ожидание:** оба пункта подменю неактивны, тултип называет отсутствие ID.
После проверки удалить запись через «Удалить из списка…» (подтверждение — «Да»).
**Результат:**

**Б5. Отказ подтверждения ничего не удаляет.** «Снимок кэша» до. Пункт
«Программный…» на `empty` → диалог.
**Ожидание и действия:** в тексте — имя «empty» и размер, совпадающий со
снимком (±, размер считается до удаления); кнопки «Да»/«Нет» по-русски;
фокус на «Нет» — нажать **Enter** → диалог закрылся, сводки нет. Открыть
ещё раз — нажать **Esc** → то же. «Снимок кэша» после — не изменился.
**Результат:**

**Б6. Частичное удаление при открытой базе.** Открыть `empty` тонким клиентом
и НЕ закрывать. «Программный…» → «Да».
**Ожидание:** сводка вида «Удалено N файлов, освобождено X. Не удалось удалить
K — файлы заняты запущенной 1С; закройте программу и повторите», K > 0;
никаких трассировок и вторичных «папка не пуста» (§3.7). Закрыть клиента.
**Результат:**

**Б7. Полная очистка.** «Программный…» на `empty` → в диалоге размер меньше,
чем в Б5 (часть уже удалена) → «Да».
**Ожидание:** сводка из двух чисел БЕЗ фразы про «не удалось»; после —
`Test-Path $prog` → `False` (каталог снят целиком, вместе с корнем).
**Результат:**

**Б8. Пункт погас.** Снова открыть меню `empty`.
**Ожидание:** «Программный…» неактивен, тултип «кэш пуст» — доступность
пересчитана по факту диска (§3.4).
**Результат:**

**Б9. Удаление безвредно + пользовательский кэш.** Запустить `empty` — база
открывается как обычно (**[Ф]** T-05.10 шаг 8 — здесь повторяется через нашу
программу); `Test-Path $prog` → `True` (кэш пересоздан). Закрыть клиента.
Затем «Пользовательский…» на `empty`:
**Ожидание:** текст перечисляет, что уйдёт (настройки форм, история ввода,
словарь), и НЕ обещает «ничего страшного» (§3.5, §7). «Да» → сводка, каталог
`$user` снят. Запустить `empty` ещё раз — открывается (для пустой базы;
про потерю настроек на богатой базе это НЕ говорит — метку §7 п. 1 в спеке
не поднимать выше «частично: пустая база»).
**Результат:**

**Уборка блока Б:** решение заказчика — оставить кэши `empty` пересозданными
(норма) и удалить резервные `.bak-t048`, когда прогон закрыт.
**Сделано:**
