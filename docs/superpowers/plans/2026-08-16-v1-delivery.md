# T-04.6 «Поставка» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Собрать OneCStarter 0.1.0 (PyInstaller one-dir + Inno Setup + zip), исключить
молчаливый отказ старта как класс и опубликовать проект на GitHub со свежей историей.

**Architecture:** Старт делится на быстрый синхронный путь (локальные малые файлы → окно
сразу) и две фоновые задачи (обнаружение платформ, общие списки), доставляющие данные
в главный поток сигналами. Ошибки до окна ловятся перехватом в `__main__` (лог +
нативный `MessageBoxW`). Сборка — spec-файл в `build/` + оркестратор `build.ps1`,
заканчивающийся smoke собранного экземпляра.

**Tech Stack:** Python 3.13, PySide6, PyInstaller (one-dir), Inno Setup 6, uv, pytest/pytest-qt.

**Спека:** [2026-08-16-v1-delivery-design.md](../specs/2026-08-16-v1-delivery-design.md).

## Global Constraints

- Инвариант 1: Qt только в `src/onecstarter/ui/`; `domain`, `config`, `platform_1c`,
  `security`, `services` и новый `diagnostics` не импортируют PySide6.
- Инвариант 4: запись пользовательских файлов — только `atomic_write`.
- Инвариант 5: секреты, строки соединения, имена и пути баз, пути установок платформы —
  **не попадают в лог** (спека §4.1). Разрешены счётчики, длительности, номера версий,
  пути к нашим файлам в `%APPDATA%\OneCStarter\`.
- Версия — только из `pyproject.toml`. Релиз этапа: `0.1.0`.
- `mypy` strict для всего, кроме `onecstarter.ui.*`; `ruff` без замечаний.
- `Workspace` мутируется только из главного потока; фоновые функции — чистые, без Qt.
- Потоки фона — демоны: процесс обязан завершаться при потоке, заблокированном в `open()`.
- Защитные тесты проходят мутационную проверку; **мутацию ставит не автор теста**;
  порядок: правка → тесты зелёные → коммит → мутация → откат (`git checkout` только
  после коммита правки). Задачи с обязательной мутацией помечены «МУТАЦИЯ».
- Тесты не трогают живой `%APPDATA%` и не порождают процессы 1С.
- Коммиты — после каждой задачи, сообщение в стиле репозитория (русский, `feat:`/`fix:`/`docs:`/`build:`).
- Прогон `uv run pytest -q && uv run ruff check . && uv run mypy` — проверять по коду
  выхода каждой команды, не по хвосту вывода.

## Карта файлов

| Файл | Судьба | Ответственность |
| --- | --- | --- |
| `src/onecstarter/services/catalog.py` | правка | + `CommonListData`, `read_common_lists` (чтение), `common_items_from_data` (разбор); − `load_common_items` |
| `src/onecstarter/services/workspace.py` | правка | снимок общих списков, `apply_common_lists`, `set_installations`, pending-состояния, отказ `launch` до готовности |
| `src/onecstarter/services/display.py` | правка | `version_cell(..., discovery_pending=)` |
| `src/onecstarter/ui/background.py` | новый | `StartupTasks`: потоки-демоны + сигналы + лог фаз |
| `src/onecstarter/ui/app.py` | правка | быстрый `build_runtime`, `_build_main_window`, `run_smoke`, ожидание в `run_launch` |
| `src/onecstarter/ui/bases/view.py` | правка | `apply_installations`, pending в `rebuild()` |
| `src/onecstarter/diagnostics.py` | новый | лог + ротация, faulthandler, `MessageBoxW`; **без Qt** |
| `src/onecstarter/__main__.py` | правка | настройка лога, глобальный перехват, `--smoke` |
| `build/entry.py` | новый | точка входа PyInstaller |
| `build/onecstarter.spec` | новый | сборка one-dir, два exe, версия из pyproject |
| `build/make_icon.py` | новый | генерация `build/onecstarter.ico` |
| `build/smoke.py` | новый | smoke собранного экземпляра |
| `build/build.ps1` | новый | оркестратор: PyInstaller → smoke → zip → ISCC → отчёт |
| `build/installer.iss` | новый | Inno Setup, per-user |
| `.gitattributes`, `.gitignore`, `LICENSE`, `README.md`, `pyproject.toml` | правка/новые | публикация |

---

### Task 1: catalog — чтение общих списков отдельно от разбора

**Files:**
- Modify: `src/onecstarter/services/catalog.py` (функция `load_common_items`, строки 82–99)
- Test: `tests/unit/test_catalog.py`

**Interfaces:**
- Produces: `CommonListData(payloads: tuple[tuple[Path, bytes], ...], errors: tuple[CommonListError, ...])`;
  `EMPTY_COMMON_DATA: CommonListData`;
  `read_common_lists(cfg_paths: Iterable[Path]) -> CommonListData` — эффект (читает ФС), без Qt;
  `common_items_from_data(data: CommonListData, entries: Mapping[str, BaseUserData]) -> tuple[list[InfobaseItem], list[CommonListError]]` — чистая.
- Consumes: существующие `common_list_paths`, `items_from_document`, `parse_v8i`, `CommonListError`.

- [ ] **Step 1: Найти все использования `load_common_items`**

Run: `grep -rn "load_common_items" src tests`
Ожидается: `workspace.py` (импорт и `_rebuild`), `catalog.py` (определение), тесты `test_catalog.py`.
Если есть другие места — учесть их в шаге 5.

- [ ] **Step 2: Написать падающие тесты**

В `tests/unit/test_catalog.py` (импорты дополнить по месту):

```python
from onecstarter.services.catalog import (
    EMPTY_COMMON_DATA,
    CommonListData,
    common_items_from_data,
    read_common_lists,
)


def test_read_common_lists_reads_payloads_and_collects_errors(tmp_path):
    cfg = tmp_path / "1cestart.cfg"
    good = tmp_path / "good.v8i"
    good.write_bytes("[Общая]\r\nConnect=File=\"C:\\demo\";\r\n".encode("utf-8-sig"))
    missing = tmp_path / "нет-такого.v8i"
    # BOM обязателен: decode() узнаёт UTF-16LE только по нему (находка Task 1 —
    # без BOM байты молча читаются как UTF-8 и CommonInfoBases не находится).
    cfg.write_bytes(
        codecs.BOM_UTF16_LE
        + f"CommonInfoBases={good}\r\nCommonInfoBases={missing}\r\n".encode("utf-16-le")
    )
    data = read_common_lists([cfg])
    assert [path for path, _ in data.payloads] == [good]
    assert data.payloads[0][1] == good.read_bytes()
    assert [error.path for error in data.errors] == [missing]


def test_common_items_from_data_parses_each_payload_and_keeps_errors(tmp_path):
    payload = "[Общая]\r\nConnect=File=\"C:\\demo\";\r\n".encode("utf-8-sig")
    error = CommonListError(tmp_path / "битый.v8i", "нет доступа")
    data = CommonListData(((tmp_path / "a.v8i", payload),), (error,))
    items, errors = common_items_from_data(data, {})
    assert [item.name for item in items] == ["Общая"]
    assert errors == [error]


def test_empty_common_data_gives_no_items_and_no_errors():
    items, errors = common_items_from_data(EMPTY_COMMON_DATA, {})
    assert items == []
    assert errors == []
```

- [ ] **Step 3: Прогнать — убедиться, что падают**

Run: `uv run pytest tests/unit/test_catalog.py -q`
Expected: FAIL — `ImportError: cannot import name 'CommonListData'`.

- [ ] **Step 4: Реализация в `catalog.py`**

Вместо `load_common_items` (докстринги существующих соседей — образец тона):

```python
@dataclass(frozen=True)
class CommonListData:
    """Прочитанные, но не разобранные общие списки: снимок для Workspace.

    Байты, а не разобранные записи: наши данные (избранное, история)
    подмешиваются при разборе, и после каждого их изменения разбор
    повторяется по этому же снимку — без нового похода по сети.
    """

    payloads: tuple[tuple[Path, bytes], ...]
    errors: tuple[CommonListError, ...]


EMPTY_COMMON_DATA = CommonListData((), ())


def read_common_lists(cfg_paths: Iterable[Path]) -> CommonListData:
    """Прочитать общие списки с диска. Единственный эффект — чтение.

    Вызывается из фонового потока (спека T-04.6, §3.2): сетевые шары
    из CommonInfoBases не должны блокировать показ окна. Недоступный
    список попадает в ошибки, остальные читаются дальше.
    """
    payloads: list[tuple[Path, bytes]] = []
    errors: list[CommonListError] = []
    for path in common_list_paths(cfg_paths):
        try:
            payloads.append((path, path.read_bytes()))
        except OSError as error:
            errors.append(CommonListError(path, str(error)))
    return CommonListData(tuple(payloads), tuple(errors))


def common_items_from_data(
    data: CommonListData, entries: Mapping[str, BaseUserData]
) -> tuple[list[InfobaseItem], list[CommonListError]]:
    """Разобрать снимок общих списков. Чистая функция — ничего не читает."""
    items: list[InfobaseItem] = []
    for _path, payload in data.payloads:
        items.extend(
            items_from_document(parse_v8i(payload), InfobaseSource.COMMON, entries)
        )
    return items, list(data.errors)
```

`load_common_items` удалить. Старые тесты на неё в `test_catalog.py` переписать
на пару `read_common_lists` + `common_items_from_data`, сохранив проверяемые
случаи (недоступный файл → ошибка, чтение нескольких списков).

- [ ] **Step 5: Обновить `workspace.py._rebuild` минимально (компиляция)**

В `workspace.py` заменить импорт `load_common_items` на `EMPTY_COMMON_DATA`,
`CommonListData`, `common_items_from_data`; в `_rebuild`:

```python
common, errors = common_items_from_data(EMPTY_COMMON_DATA, self._user)
```

Это ВРЕМЕННО отключает общие списки — Task 2 вводит снимок по-настоящему.
Тесты Workspace/UI на общие списки на этом шаге упадут — это ожидаемо,
их чинит Task 2. Прогнать только каталог:

Run: `uv run pytest tests/unit/test_catalog.py -q`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/onecstarter/services/catalog.py src/onecstarter/services/workspace.py tests/unit/test_catalog.py
git commit -m "feat: чтение общих списков отделено от разбора (CommonListData)"
```

---

### Task 2: Workspace — снимок общих списков и pending-установки

**Files:**
- Modify: `src/onecstarter/services/workspace.py` (конструктор ~стр. 88–118, `launch` ~стр. 265, `_rebuild` ~стр. 452)
- Modify: `tests/ui/conftest.py` (`workspace_factory`, ~стр. 36–68)
- Test: `tests/unit/test_workspace.py`

**Interfaces:**
- Consumes: `CommonListData`, `EMPTY_COMMON_DATA`, `common_items_from_data`, `read_common_lists` (Task 1).
- Produces (использует UI в Tasks 4–6):
  - конструктор: `installations: Sequence[Installation] | None` — `None` значит «обнаружение не завершено»;
  - `Workspace.installations_pending -> bool` (property);
  - `Workspace.set_installations(installations: Sequence[Installation]) -> None`;
  - `Workspace.common_lists_pending -> bool` (property);
  - `Workspace.apply_common_lists(data: CommonListData) -> None`;
  - `Workspace.launch(...)` поднимает `LaunchError("Обнаружение установленных версий платформы ещё не завершено — повторите попытку через несколько секунд")` при `installations_pending`.

- [ ] **Step 1: Написать падающие тесты**

В `tests/unit/test_workspace.py` (фабрику/хелперы файла использовать по месту;
`_with_common_list` — существующий хелпер этого файла):

```python
def test_constructor_does_not_read_common_lists(tmp_path, ...):
    # cfg с CommonInfoBases на существующий список — как в _with_common_list
    workspace = ...  # конструктор с cfg_paths=(cfg,)
    names = {item.name for item in workspace.items()}
    assert "Общая база" not in names  # снимок ещё не применён
    assert workspace.common_lists_pending

    workspace.apply_common_lists(read_common_lists([cfg]))
    names = {item.name for item in workspace.items()}
    assert "Общая база" in names
    assert not workspace.common_lists_pending


def test_rebuild_does_not_reread_common_lists_from_disk(tmp_path, ...):
    workspace = ...  # с применённым снимком, см. выше
    common_file.write_bytes(b"")  # источник изменился на диске
    workspace.set_favorite(user_key, True)  # любая операция с _rebuild
    names = {item.name for item in workspace.items()}
    assert "Общая база" in names  # снимок жив, диск не перечитан


def test_installations_none_means_pending_and_launch_refuses(...):
    workspace = ...  # installations=None
    assert workspace.installations_pending
    with pytest.raises(LaunchError) as excinfo:
        workspace.launch(some_key)
    assert "не завершено" in str(excinfo.value)
    workspace.set_installations(INSTALLED)
    assert not workspace.installations_pending
    # запуск проходит (spawn-заглушка фиксирует вызов)


def test_empty_installations_are_not_pending(...):
    workspace = ...  # installations=[]
    assert not workspace.installations_pending
```

- [ ] **Step 2: Прогнать — падают**

Run: `uv run pytest tests/unit/test_workspace.py -q`
Expected: FAIL — нет `common_lists_pending` / `apply_common_lists`.

- [ ] **Step 3: Реализация в `workspace.py`**

Конструктор:

```python
installations: Sequence[Installation] | None,
...
self._installations = None if installations is None else list(installations)
self._common_data: CommonListData = EMPTY_COMMON_DATA
self._common_loaded = False
```

Новые члены (докстринги — почему `None` и снимок, со ссылкой на спеку §3.3–3.4):

```python
@property
def installations_pending(self) -> bool:
    return self._installations is None

def set_installations(self, installations: Sequence[Installation]) -> None:
    self._installations = list(installations)

@property
def common_lists_pending(self) -> bool:
    return not self._common_loaded

def apply_common_lists(self, data: CommonListData) -> None:
    self._common_data = data
    self._common_loaded = True
    self._rebuild()
```

`_rebuild`: `common, errors = common_items_from_data(self._common_data, self._user)`.

`launch` — первой строкой:

```python
if self._installations is None:
    raise LaunchError(
        "Обнаружение установленных версий платформы ещё не завершено — "
        "повторите попытку через несколько секунд"
    )
```

и далее `installations=self._installations` как раньше.
`set_installations` без `_rebuild`: записи от установок не зависят.

- [ ] **Step 4: Обновить фабрики тестов**

`tests/ui/conftest.py`, `workspace_factory`: после создания `Workspace` добавить
безусловно (возвращает фабрику в «загруженный» мир, каким он был до Task 1):

```python
workspace.apply_common_lists(read_common_lists(list(cfg_paths)))
```

То же — в хелперах `tests/unit/test_workspace.py`, создающих Workspace
с `cfg_paths` (`_with_common_list` и соседние). Прямые конструкторы с готовым
списком установок не трогать — семантика сохранена.

- [ ] **Step 5: Полный прогон**

Run: `uv run pytest -q && uv run ruff check . && uv run mypy`
Expected: все зелёные, коды 0 у каждой команды (упавшие UI-тесты общих списков
после Task 1 здесь обязаны ожить).

- [ ] **Step 6: Commit**

```bash
git add -A src tests
git commit -m "feat: Workspace — снимок общих списков и явное состояние «установки не известны»"
```

- [ ] **Step 7: МУТАЦИЯ (ставит не автор тестов)**

По одной, с откатом после каждой; фиксация результатов — в реестре исполнения:
1. Вернуть чтение общих списков в `_rebuild` (`common_items_from_data(read_common_lists(...))`) —
   обязан упасть `test_constructor_does_not_read_common_lists` **и/или**
   `test_rebuild_does_not_reread_common_lists_from_disk`.
2. Убрать guard из `launch` — обязан упасть `test_installations_none_means_pending_and_launch_refuses`.
3. В `installations_pending` вернуть `not self._installations` (пустой список = pending) —
   обязан упасть `test_empty_installations_are_not_pending`.

---

### Task 3: display.version_cell — состояние «обнаружение не завершено»

**Files:**
- Modify: `src/onecstarter/services/display.py` (`version_cell`, ~стр. 149)
- Test: `tests/unit/test_display.py`

**Interfaces:**
- Produces: `version_cell(item, installations, cfg_rules, *, discovery_pending: bool = False) -> VersionCell`.
  При `discovery_pending=True` для не-групп и не-веб: `VersionCell("…", False, None)`.
  Группа и веб-база ведут себя как раньше (им установки не нужны).

- [ ] **Step 1: Табличные тесты**

В `tests/unit/test_display.py`, рядом с существующими тестами `version_cell`
(строить `InfobaseItem` теми же хелперами файла):

```python
@pytest.mark.parametrize(
    ("connect", "expected_text"),
    [
        ('File="C:\\demo";', "…"),
        ('Srvr="s";Ref="d";', "…"),
        ("мусор", "…"),
    ],
)
def test_version_cell_pending_shows_ellipsis_without_problem(connect, expected_text):
    cell = version_cell(_base(connect=connect), [], [], discovery_pending=True)
    assert cell == VersionCell(expected_text, False, None)


def test_version_cell_pending_keeps_web_and_group_behaviour():
    web = version_cell(_base(connect='ws="https://host/demo";'), [], [], discovery_pending=True)
    assert web.text == "веб"
    group = version_cell(_group(), [], [], discovery_pending=True)
    assert group == VersionCell("", False, None)
```

- [ ] **Step 2: Прогон — падают**

Run: `uv run pytest tests/unit/test_display.py -q`
Expected: FAIL — неожиданный именованный аргумент `discovery_pending`.

- [ ] **Step 3: Реализация**

В `version_cell` после веток `is_group` и `WEB`, до `resolve_version`:

```python
if discovery_pending:
    # Пустой список установок и «обнаружение не завершено» — разные
    # состояния (спека T-04.6, §3.4): вывести второе из первого нельзя,
    # колонка врала бы «нет установленных версий», пока фон работает.
    return VersionCell("…", False, None)
```

- [ ] **Step 4: Прогон**

Run: `uv run pytest tests/unit/test_display.py -q && uv run mypy`
Expected: PASS, коды 0.

- [ ] **Step 5: Commit**

```bash
git add src/onecstarter/services/display.py tests/unit/test_display.py
git commit -m "feat: колонка версии показывает «…», пока обнаружение платформ не завершено"
```

---

### Task 4: ui/background.py — StartupTasks

**Files:**
- Create: `src/onecstarter/ui/background.py`
- Test: `tests/ui/test_background.py`

**Interfaces:**
- Consumes: чистые функции без аргументов (замыкания создаёт `app.py`):
  `discover: Callable[[], list[Installation]]`, `read_common: Callable[[], CommonListData]`.
- Produces:
  - `StartupTasks(discover, read_common, *, spawn=..., parent=None)`;
  - `StartupTasks.start() -> None` — запускает обе задачи;
  - сигнал `installations_ready(object)` — `list[Installation]`;
  - сигнал `common_lists_ready(object)` — `CommonListData`;
  - `spawn: Callable[[Callable[[], None]], None]` — инъекция для тестов
    (по умолчанию поток-демон).

- [ ] **Step 1: Написать падающие тесты**

`tests/ui/test_background.py`:

```python
"""StartupTasks: доставка результатов фона сигналами и лог фаз."""

import logging
import threading
from pathlib import Path

from onecstarter.services.catalog import EMPTY_COMMON_DATA
from onecstarter.ui.background import StartupTasks

_SYNC = lambda task: task()  # noqa: E731 — синхронный «поток» для детерминизма


def test_start_emits_both_results(qtbot):
    got: dict[str, object] = {}
    tasks = StartupTasks(lambda: ["inst"], lambda: EMPTY_COMMON_DATA, spawn=_SYNC)
    tasks.installations_ready.connect(lambda found: got.__setitem__("inst", found))
    tasks.common_lists_ready.connect(lambda data: got.__setitem__("common", data))
    tasks.start()
    assert got == {"inst": ["inst"], "common": EMPTY_COMMON_DATA}


def test_real_threads_deliver_into_the_event_loop(qtbot):
    tasks = StartupTasks(lambda: [], lambda: EMPTY_COMMON_DATA)
    with qtbot.waitSignals(
        [tasks.installations_ready, tasks.common_lists_ready], timeout=5000
    ):
        tasks.start()


def test_default_spawn_uses_daemon_threads():
    seen: list[bool] = []
    tasks = StartupTasks(
        lambda: seen.append(threading.current_thread().daemon) or [],
        lambda: EMPTY_COMMON_DATA,
    )
    done = threading.Event()
    tasks.installations_ready.connect(lambda _found: done.set())
    tasks.start()
    # сигнал queued — ждём сам поток, а не доставку
    assert done.wait(5) or seen  # поток отработал
    assert seen == [True]


def test_failed_task_logs_and_emits_empty(qtbot, caplog):
    def explode() -> list:
        raise RuntimeError("нет доступа")

    got: dict[str, object] = {}
    tasks = StartupTasks(explode, lambda: EMPTY_COMMON_DATA, spawn=_SYNC)
    tasks.installations_ready.connect(lambda found: got.__setitem__("inst", found))
    with caplog.at_level(logging.ERROR):
        tasks.start()
    assert got["inst"] == []
    assert "обнаружение" in caplog.text


def test_log_carries_counts_not_paths(caplog):
    """Сторож инварианта 5: в лог — счётчики, не содержимое (МУТАЦИЯ)."""
    from onecstarter.services.catalog import CommonListData

    payload = 'Srvr="секретный-сервер";Ref="скрытая";'.encode()
    data = CommonListData(((Path(r"\\share\список.v8i"), payload),), ())
    tasks = StartupTasks(lambda: [], lambda: data, spawn=_SYNC)
    with caplog.at_level(logging.INFO):
        tasks.start()
    assert "секретный-сервер" not in caplog.text
    assert "share" not in caplog.text
```

(`test_default_spawn_uses_daemon_threads` при доставке queued-сигнала без цикла
может не получить колбэк — потому проверка через `threading.Event` на сам факт
работы потока; при ложных срабатываниях упростить до проверки `seen` после `join`
потоков — решение исполнителя, поведение «поток — демон» обязано быть проверено.)

- [ ] **Step 2: Прогон — падают**

Run: `uv run pytest tests/ui/test_background.py -q`
Expected: FAIL — модуля нет.

- [ ] **Step 3: Реализация `src/onecstarter/ui/background.py`**

```python
"""Фоновые задачи старта: обнаружение платформ и общие списки.

Потоки и сигналы живут здесь, в ui (инвариант 1); сами задачи — чистые
функции, они возвращают данные, применяет их главный поток по сигналу.
Потоки — демоны: поток, заблокированный в `open()` на exe под антивирусом
или на мёртвой шаре, убить нельзя, и он не должен удерживать процесс
при выходе (спека T-04.6, §3.5, инцидент 15.08.2026).

В лог — счётчики и длительности, не содержимое: лог прикладывают
к issue (спека §4.1, инвариант 5).
"""

import logging
import threading
import time
from collections.abc import Callable

from PySide6.QtCore import QObject, Signal

from onecstarter.domain.version import Installation
from onecstarter.services.catalog import EMPTY_COMMON_DATA, CommonListData

_log = logging.getLogger("onecstarter.startup")


def _spawn_daemon(task: Callable[[], None]) -> None:
    threading.Thread(target=task, daemon=True).start()


class StartupTasks(QObject):
    """Две независимые задачи: зависание одной не топит вторую (спека §3.2)."""

    installations_ready = Signal(object)  # list[Installation]
    common_lists_ready = Signal(object)  # CommonListData

    def __init__(
        self,
        discover: Callable[[], list[Installation]],
        read_common: Callable[[], CommonListData],
        *,
        spawn: Callable[[Callable[[], None]], None] = _spawn_daemon,
        parent: QObject | None = None,
    ) -> None:
        super().__init__(parent)
        self._discover = discover
        self._read_common = read_common
        self._spawn = spawn

    def start(self) -> None:
        self._spawn(self._run_discovery)
        self._spawn(self._run_common)

    def _run_discovery(self) -> None:
        started = time.monotonic()
        _log.info("обнаружение платформ: начато")
        try:
            found = self._discover()
        except Exception:
            # Падение фона не должно оставлять окно в вечном «…»: логируем
            # причину и отдаём пустой результат — состояние видно и в окне,
            # и в логе.
            _log.exception("обнаружение платформ: отказ")
            found = []
        _log.info(
            "обнаружение платформ: закончено за %d мс, найдено %d",
            int((time.monotonic() - started) * 1000),
            len(found),
        )
        self.installations_ready.emit(found)

    def _run_common(self) -> None:
        started = time.monotonic()
        _log.info("общие списки: чтение начато")
        try:
            data = self._read_common()
        except Exception:
            _log.exception("общие списки: отказ")
            data = EMPTY_COMMON_DATA
        _log.info(
            "общие списки: закончено за %d мс, файлов %d, ошибок %d",
            int((time.monotonic() - started) * 1000),
            len(data.payloads),
            len(data.errors),
        )
        self.common_lists_ready.emit(data)
```

- [ ] **Step 4: Прогон**

Run: `uv run pytest tests/ui/test_background.py -q && uv run ruff check .`
Expected: PASS, коды 0.

- [ ] **Step 5: Commit**

```bash
git add src/onecstarter/ui/background.py tests/ui/test_background.py
git commit -m "feat: фоновые задачи старта — потоки-демоны, доставка сигналами, лог фаз"
```

- [ ] **Step 6: МУТАЦИЯ (ставит не автор тестов)**

1. В `_run_common` добавить в лог путь списка (`data.payloads[0][0]`) — обязан
   упасть `test_log_carries_counts_not_paths`.
2. Убрать `daemon=True` в `_spawn_daemon` — обязан упасть
   `test_default_spawn_uses_daemon_threads`.
3. В `_run_discovery` убрать `except` — обязан упасть `test_failed_task_logs_and_emits_empty`.

---

### Task 5: app.py и BasesView — окно сразу, данные по готовности

**Files:**
- Modify: `src/onecstarter/ui/app.py` (`Runtime`, `build_runtime`, `main`)
- Modify: `src/onecstarter/ui/bases/view.py` (конструктор ~стр. 284–304, `rebuild` ~стр. 450–454)
- Test: `tests/ui/test_app.py`, `tests/ui/test_bases_view.py`

**Interfaces:**
- Consumes: Tasks 1–4 (`read_common_lists`, `set_installations`, `apply_common_lists`,
  `version_cell(..., discovery_pending=)`, `StartupTasks`).
- Produces:
  - `Runtime(workspace: Workspace, cfg_rules: list[DefaultVersionRule], conventions: list[ClientConvention], settings: Path)` — поле `installations` удалено;
  - `build_runtime(env) -> Runtime` — без обнаружения и без чтения общих списков;
  - `_build_main_window(application: QApplication, runtime: Runtime, env: Mapping[str, str]) -> tuple[MainWindow, StartupTasks]` — вся сборка окна из `main()` + создание `StartupTasks` с подключёнными хэндлерами; `tasks.start()` НЕ вызывает;
  - `BasesView(workspace, *, installations: Sequence[Installation] | None, ...)` — `None` = «…» в колонке версии;
  - `BasesView.apply_installations(installations: Sequence[Installation]) -> None`.

- [ ] **Step 1: Написать падающие тесты**

В `tests/ui/test_bases_view.py` (хелпер `_view` файла — образец):

```python
def test_pending_installations_show_ellipsis_then_versions(qtbot, workspace_factory):
    view, *_ = _view(qtbot, workspace_factory, installations=None)
    texts = _column_texts(view, column=1)  # обходчик колонок файла
    assert set(texts) == {"…"}  # у всех не-групп
    view.apply_installations(INSTALLED)
    texts = _column_texts(view, column=1)
    assert "…" not in texts
```

В `tests/ui/test_app.py`:

```python
def test_runtime_has_no_installations_and_pending_workspace(tmp_path):
    runtime = build_runtime({"APPDATA": str(tmp_path)})
    assert runtime.workspace.installations_pending
    assert runtime.workspace.common_lists_pending


def test_build_main_window_wires_background_results(qtbot, tmp_path, ...):
    # _build_main_window с runtime на tmp_path; вместо start() — эмит руками:
    window, tasks = _build_main_window(application, runtime, {"APPDATA": str(tmp_path)})
    tasks.installations_ready.emit(INSTALLED)
    tasks.common_lists_ready.emit(EMPTY_COMMON_DATA)
    assert not runtime.workspace.installations_pending
    assert not runtime.workspace.common_lists_pending


def test_startup_log_has_no_connect_strings(qtbot, caplog, ...):
    """Сторож инварианта 5 на фазовые сообщения сборки окна (МУТАЦИЯ)."""
    # runtime поверх фикстуры со строками Srvr=/File= в ibases.v8i
    with caplog.at_level(logging.INFO):
        window, tasks = _build_main_window(...)
    assert "Srvr" not in caplog.text
    assert "File=" not in caplog.text
```

Существующие тесты `test_app.py` поправить под новую сигнатуру `Runtime`
(поле `installations` удалено, добавлено `conventions`) — фикстура `runtime_with`
и `_Assembly`.

- [ ] **Step 2: Прогон — падают**

Run: `uv run pytest tests/ui/test_app.py tests/ui/test_bases_view.py -q`
Expected: FAIL.

- [ ] **Step 3: Реализация `BasesView`**

Конструктор: `installations: Sequence[Installation] | None`;
`self._installations = None if installations is None else list(installations)`.
В `rebuild()` (стр. ~450):

```python
pending = self._installations is None
cells = {
    item.key: version_cell(
        item,
        self._installations or [],
        self._cfg_rules,
        discovery_pending=pending,
    )
    for item in items
    if not item.is_group
}
```

Новый метод рядом с `apply_palette`:

```python
def apply_installations(self, installations: Sequence[Installation]) -> None:
    """Обнаружение закончилось: показать версии вместо «…»."""
    self._installations = list(installations)
    self.rebuild()
```

Места, передающие `self._installations` в диалоги (~стр. 917, 1000): передавать
`self._installations or []` — диалог в первые полсекунды честно не знает версий.

- [ ] **Step 4: Реализация `app.py`**

`Runtime` и `build_runtime`:

```python
@dataclass(frozen=True)
class Runtime:
    workspace: Workspace
    cfg_rules: list[DefaultVersionRule]
    conventions: list[ClientConvention]
    settings: Path


def build_runtime(env: Mapping[str, str]) -> Runtime:
    """Быстрая часть старта: только локальные малые файлы (спека §3.2).

    Ни обнаружения платформ, ни общих списков: обе работы могут висеть
    минутами (антивирус, сетевые шары) и уходят в StartupTasks.
    """
    conventions = load_conventions()
    cfgs = cfg_paths(env)
    entries: list[tuple[str, str]] = []
    for cfg in cfgs:
        try:
            entries.extend(parse_cestart_cfg(cfg.read_bytes()))
        except OSError:
            continue
    rules = default_version_rules(entries)
    appdata = Path(env.get("APPDATA", "."))
    paths = WorkspacePaths(
        ibases=appdata / "1C" / "1CEStart" / "ibases.v8i",
        user_data=appdata / "OneCStarter" / "bases.json",
        cfg_paths=tuple(cfgs),
    )
    workspace = Workspace(
        paths,
        installations=None,
        conventions=conventions,
        cfg_rules=rules,
        default_app=None,
    )
    settings_path = appdata / "OneCStarter" / "settings.json"
    return Runtime(workspace, rules, list(conventions), settings_path)
```

`_build_main_window(application, runtime, env)` — перенести из `main()` всё
между обработкой ошибок `build_runtime` и `window.show()` (контроллер темы,
`BasesView(installations=None, ...)`, разделы, значки, watcher, трей, хоткей),
плюс:

```python
tasks = StartupTasks(
    lambda: find_installations(env, runtime.conventions),
    lambda: read_common_lists(list(runtime.workspace.paths.cfg_paths)),
    parent=window,
)

def on_installations(found: list[Installation]) -> None:
    runtime.workspace.set_installations(found)
    view.apply_installations(found)

def on_common(data: CommonListData) -> None:
    runtime.workspace.apply_common_lists(data)
    view.rebuild()

tasks.installations_ready.connect(on_installations)
tasks.common_lists_ready.connect(on_common)
return window, tasks
```

`main()` после рефакторинга:

```python
def main(argv: list[str] | None = None) -> int:
    application = QApplication(argv if argv is not None else sys.argv)
    application.setApplicationName("OneCStarter")
    application.setStyleSheet(theme.stylesheet(theme.DARK))
    try:
        runtime = build_runtime(os.environ)
    except UserDataUnavailableError as error:
        QMessageBox.critical(None, "OneCStarter", str(error))
        return 1
    except OSError as error:
        QMessageBox.critical(
            None, "OneCStarter", f"Не удалось прочитать список баз: {error}"
        )
        return 1
    window, tasks = _build_main_window(application, runtime, os.environ)
    window.show()
    _log.info("окно показано")
    tasks.start()
    return application.exec()
```

`_log = logging.getLogger("onecstarter.startup")` на уровне модуля.
Существующие комментарии-обоснования из `main()` переносятся вместе с кодом.

- [ ] **Step 5: Полный прогон**

Run: `uv run pytest -q && uv run ruff check . && uv run mypy`
Expected: PASS, коды 0.

- [ ] **Step 6: Commit**

```bash
git add -A src tests
git commit -m "feat: окно показывается до обнаружения платформ и общих списков"
```

- [ ] **Step 7: МУТАЦИЯ (ставит не автор тестов)**

1. В `build_runtime` вернуть `find_installations(env, conventions)` и передать
   в `Workspace` — обязан упасть `test_runtime_has_no_installations_and_pending_workspace`.
2. В `on_installations` убрать `view.apply_installations` — обязан упасть
   `test_pending_installations_show_ellipsis_then_versions` (если он ходит через
   `_build_main_window`) либо UI-тест `test_build_main_window_wires_background_results`
   расширить проверкой колонки. Хотя бы один тест обязан падать на этой мутации —
   если не падает ни один, добавить недостающую проверку.

---

### Task 6: run_launch — ожидание с отменяемым прогрессом

**Files:**
- Modify: `src/onecstarter/ui/app.py` (`run_launch`, `_complain`)
- Test: `tests/ui/test_app.py`

**Interfaces:**
- Consumes: `StartupTasks`, pending-свойства Workspace (Tasks 2, 4).
- Produces: `run_launch(name, env, *, make_tasks: Callable[[], StartupTasks] | None = None) -> int` —
  инъекция фабрики задач только для тестов (проектный приём: см. `choose_shortcut_path`
  в `BasesView`); `None` — собрать настоящие. Внутренняя `_wait_startup(workspace, tasks) -> bool`
  (`False` — отмена).

- [ ] **Step 1: Написать падающие тесты**

В `tests/ui/test_app.py`:

```python
def test_run_launch_waits_for_pending_workspace(monkeypatch, tmp_path, ...):
    # runtime из фабрики, но с installations=None и не применёнными общими
    # списками; make_tasks с _SYNC-спавном возвращает INSTALLED/EMPTY_COMMON_DATA
    assert run_launch("Демо Бухгалтерия", {}, make_tasks=...) == 0
    assert calls  # запуск дошёл до spawn-заглушки


def test_run_launch_cancel_returns_one_without_launch(monkeypatch, ...):
    # make_tasks: spawn, который никого не запускает (задачи молчат);
    # отмена: QTimer.singleShot(0, dialog.cancel) — диалог достаётся
    # через инъекцию или обход topLevelWidgets
    assert run_launch("Демо Бухгалтерия", {}, make_tasks=...) == 1
    assert not calls


def test_run_launch_ready_workspace_skips_waiting(runtime_with, shown_errors):
    # фикстурный runtime не pending — существующее поведение живо
    assert run_launch("Демо Бухгалтерия", {}) == 0
```

(точная механика доступа к диалогу для `cancel` — решение исполнителя;
допустимо вынести создание диалога в инъектируемую фабрику
`make_progress: Callable[[], QProgressDialog]`.)

- [ ] **Step 2: Прогон — падают**

Run: `uv run pytest tests/ui/test_app.py -q`
Expected: FAIL.

- [ ] **Step 3: Реализация**

```python
def _wait_startup(workspace: Workspace, tasks: StartupTasks) -> bool:
    """Дождаться фоновых задач; False — пользователь отменил.

    Диалог показывается только если не уложились в полсекунды: в норме
    (тёплый кэш антивируса) ожидание невидимо. Отмена возвращает управление
    сразу — фоновые потоки демоны, их не ждём (спека §3.5).
    """
    loop = QEventLoop()
    dialog = QProgressDialog(
        "Обнаружение установленных версий платформы…", "Отмена", 0, 0
    )
    dialog.setWindowTitle("OneCStarter")
    dialog.setMinimumDuration(0)  # показом управляем сами, см. таймер ниже
    cancelled: list[bool] = []

    def on_cancel() -> None:
        # Не lambda: (cancelled.append(True), loop.quit()) — mypy strict
        # ругается на func-returns-value (append/quit возвращают None,
        # использованные как элементы кортежа). Находка T-06.
        cancelled.append(True)
        loop.quit()

    dialog.canceled.connect(on_cancel)
    pending = {
        "installations": workspace.installations_pending,
        "common": workspace.common_lists_pending,
    }

    def done(kind: str) -> None:
        pending[kind] = False
        if not any(pending.values()):
            loop.quit()

    def on_installations(found: list[Installation]) -> None:
        workspace.set_installations(found)
        done("installations")

    def on_common(data: CommonListData) -> None:
        workspace.apply_common_lists(data)
        done("common")

    tasks.installations_ready.connect(on_installations)
    tasks.common_lists_ready.connect(on_common)
    timer = QTimer()
    timer.setSingleShot(True)
    timer.timeout.connect(dialog.show)
    timer.start(500)
    tasks.start()
    if any(pending.values()):
        loop.exec()
    timer.stop()
    # Результат — до close(): QProgressDialog.close() сама эмитирует canceled
    # (тот же путь, что закрытие крестиком окна) независимо от того, был
    # диалог показан — иначе успешное завершение задач тоже попало бы
    # в cancelled, и функция всегда отдавала бы False (правка по находке
    # T-06: RED/GREEN на офскрине показал это немедленно).
    result = not cancelled
    dialog.close()
    return result
```

`run_launch`:

```python
def run_launch(
    name: str,
    env: Mapping[str, str],
    *,
    make_tasks: Callable[[], StartupTasks] | None = None,
) -> int:
    if not name.strip():
        return _complain(...)  # как сейчас
    try:
        runtime = build_runtime(env)
    except UserDataUnavailableError as error:
        return _complain(str(error))
    except OSError as error:
        return _complain(f"Не удалось прочитать список баз: {error}")
    workspace = runtime.workspace
    if workspace.installations_pending or workspace.common_lists_pending:
        if QApplication.instance() is None:
            QApplication([])
        tasks = make_tasks() if make_tasks is not None else StartupTasks(
            lambda: find_installations(env, runtime.conventions),
            lambda: read_common_lists(list(workspace.paths.cfg_paths)),
        )
        if not _wait_startup(workspace, tasks):
            return 1  # отмена — потоки-демоны умрут вместе с процессом
    try:
        workspace.launch(workspace.find_by_name(name))
    except ServicesError as error:
        return _complain(str(error))
    return 0
```

- [ ] **Step 4: Полный прогон**

Run: `uv run pytest -q && uv run ruff check . && uv run mypy`
Expected: PASS, коды 0.

- [ ] **Step 5: Commit**

```bash
git add src/onecstarter/ui/app.py tests/ui/test_app.py
git commit -m "feat: запуск по ярлыку ждёт обнаружение с отменяемым прогрессом"
```

- [ ] **Step 6: МУТАЦИЯ (ставит не автор тестов)**

1. `_wait_startup` всегда возвращает `True` (игнор отмены) — обязан упасть
   `test_run_launch_cancel_returns_one_without_launch`.
2. Убрать условие `if ...pending` перед ожиданием (ждать всегда) — обязан упасть
   `test_run_launch_ready_workspace_skips_waiting` (если он проходит и с ожиданием —
   значит тест не отличает пути, усилить его проверкой, что `make_tasks` не звался:
   передать фабрику-бомбу `lambda: (_ for _ in ()).throw(AssertionError)`).

---

### Task 7: diagnostics + перехват в `__main__`

**Files:**
- Create: `src/onecstarter/diagnostics.py`
- Modify: `src/onecstarter/__main__.py`
- Test: `tests/unit/test_diagnostics.py`, `tests/unit/test_entry_point.py`

**Interfaces:**
- Produces (используют `__main__` и Task 8):
  - `diagnostics.setup_logging(env: Mapping[str, str]) -> Path | None` — путь к логу
    или `None`, если каталог не создать (приложение важнее лога);
  - `diagnostics.enable_faulthandler(env: Mapping[str, str]) -> None`;
  - `diagnostics.show_fatal_error(message: str) -> None` — `MessageBoxW` без Qt;
  - `diagnostics.log_directory(env) -> Path` = `%APPDATA%\OneCStarter\logs`;
  - `__main__._dispatch(arguments: list[str]) -> int` — прежняя логика выбора режима.
- Constraints: `diagnostics` не импортирует PySide6 (сторож `test_no_qt_in_core.py`
  должен покрывать модуль — проверить его список пакетов и включить корень пакета).

- [ ] **Step 1: Написать падающие тесты**

`tests/unit/test_diagnostics.py`:

```python
"""diagnostics: лог и перехват — без Qt, до окна."""

import logging

from onecstarter import diagnostics


def _cleanup_root() -> None:
    root = logging.getLogger()
    for handler in list(root.handlers):
        root.removeHandler(handler)
        handler.close()


def test_setup_logging_creates_rotating_file(tmp_path):
    try:
        path = diagnostics.setup_logging({"APPDATA": str(tmp_path)})
        assert path == tmp_path / "OneCStarter" / "logs" / "onecstarter.log"
        logging.getLogger("onecstarter.test").info("строка")
        for handler in logging.getLogger().handlers:
            handler.flush()
        assert "строка" in path.read_text(encoding="utf-8")
    finally:
        _cleanup_root()


def test_setup_logging_survives_unwritable_directory(tmp_path):
    blocker = tmp_path / "APPDATA"
    blocker.write_text("файл на месте каталога")
    try:
        assert diagnostics.setup_logging({"APPDATA": str(blocker)}) is None
    finally:
        _cleanup_root()
```

В `tests/unit/test_entry_point.py` — перехват (`app_stub` файла — образец
подмены режимов):

```python
def test_main_catches_dispatch_failure_and_reports(monkeypatch, tmp_path):
    import onecstarter.__main__ as entry

    shown: list[str] = []
    monkeypatch.setattr(entry.diagnostics, "show_fatal_error", shown.append)
    monkeypatch.setattr(
        entry, "_dispatch", lambda arguments: (_ for _ in ()).throw(RuntimeError("бах"))
    )
    monkeypatch.setenv("APPDATA", str(tmp_path))
    assert entry.main([]) == 1
    assert shown and "onecstarter.log" in shown[0]
    log = tmp_path / "OneCStarter" / "logs" / "onecstarter.log"
    assert "бах" in log.read_text(encoding="utf-8")
```

(тестам, включающим `setup_logging`, чистить root-логгер в `finally` —
хэндлеры между тестами не текут.)

- [ ] **Step 2: Прогон — падают**

Run: `uv run pytest tests/unit/test_diagnostics.py tests/unit/test_entry_point.py -q`
Expected: FAIL — модуля нет.

- [ ] **Step 3: Реализация `src/onecstarter/diagnostics.py`**

```python
"""Лог, faulthandler и последнее окно ошибки — без Qt.

Работает до любого импорта ui: перехват в __main__ обязан поймать и отказ
самого импорта (спека T-04.6, §4.2 — «ModuleNotFoundError ушёл в никуда»,
§9 п. 4 спеки 4a). Содержимое лога — счётчики и длительности, без строк
соединения и путей баз (инвариант 5, спека §4.1).
"""

import ctypes
import faulthandler
import logging
import logging.handlers
import sys
from collections.abc import Mapping
from pathlib import Path

LOG_NAME = "onecstarter.log"
CRASH_NAME = "crash.log"
_MB_ICONERROR = 0x10
_FORMAT = "%(asctime)s %(levelname)s %(name)s: %(message)s"


def log_directory(env: Mapping[str, str]) -> Path:
    return Path(env.get("APPDATA", ".")) / "OneCStarter" / "logs"


def setup_logging(env: Mapping[str, str]) -> Path | None:
    """Настроить лог; None — не вышло. Приложение важнее лога (спека §4.2)."""
    directory = log_directory(env)
    try:
        directory.mkdir(parents=True, exist_ok=True)
        handler = logging.handlers.RotatingFileHandler(
            directory / LOG_NAME, maxBytes=512 * 1024, backupCount=3, encoding="utf-8"
        )
    except OSError:
        return None
    handler.setFormatter(logging.Formatter(_FORMAT))
    root = logging.getLogger()
    root.setLevel(logging.INFO)
    root.addHandler(handler)
    if sys.stderr is not None:
        # Консольный exe (OneCStarterc.exe): лог виден живьём — сценарий
        # «запустите консольный exe и пришлите вывод» (спека §4.3).
        mirror = logging.StreamHandler()
        mirror.setFormatter(logging.Formatter(_FORMAT))
        root.addHandler(mirror)
    return directory / LOG_NAME


def enable_faulthandler(env: Mapping[str, str]) -> None:
    """Падения нативного кода Qt — в crash.log. Файл остаётся открытым намеренно."""
    try:
        stream = (log_directory(env) / CRASH_NAME).open("a", encoding="utf-8")
        faulthandler.enable(stream)
    except OSError:
        pass


def show_fatal_error(message: str) -> None:
    """Системное окно без Qt: у оконной сборки нет ни консоли, ни stderr."""
    try:
        ctypes.windll.user32.MessageBoxW(None, message, "OneCStarter", _MB_ICONERROR)
    except Exception:  # noqa: BLE001 — не Windows: лог уже записан, окна не будет
        pass
```

- [ ] **Step 4: Реализация `__main__`**

Существующее тело `main` становится `_dispatch(arguments)`; новый `main`:

```python
import logging
import os
import sys
from collections.abc import Sequence

from onecstarter import diagnostics


def main(argv: Sequence[str] | None = None) -> int:
    arguments = list(sys.argv[1:] if argv is None else argv)
    log_path = diagnostics.setup_logging(os.environ)
    diagnostics.enable_faulthandler(os.environ)
    try:
        return _dispatch(arguments)
    except Exception:
        logging.getLogger("onecstarter").exception("необработанная ошибка старта")
        details = f"\n\nПодробности: {log_path}" if log_path else ""
        diagnostics.show_fatal_error(
            "OneCStarter не смог запуститься из-за внутренней ошибки." + details
        )
        return 1


def _dispatch(arguments: list[str]) -> int:
    name = parse_ib_name(arguments)
    if name is None:
        from onecstarter.ui.app import main as show_window

        return show_window()
    from onecstarter.ui.app import run_launch

    return run_launch(name, os.environ)
```

Модульный докстринг `__main__` дополнить: почему лог настраивается до импорта ui.

- [ ] **Step 5: Проверить сторож «ядро без Qt»**

Открыть `tests/unit/test_no_qt_in_core.py`: если `diagnostics` (корень пакета)
не покрыт его обходом — добавить. Прогнать его.

- [ ] **Step 6: Полный прогон**

Run: `uv run pytest -q && uv run ruff check . && uv run mypy`
Expected: PASS, коды 0.

- [ ] **Step 7: Commit**

```bash
git add src/onecstarter/diagnostics.py src/onecstarter/__main__.py tests/unit/test_diagnostics.py tests/unit/test_entry_point.py tests/unit/test_no_qt_in_core.py
git commit -m "feat: лог, faulthandler и перехват старта — молчаливый отказ исключён"
```

- [ ] **Step 8: МУТАЦИЯ (ставит не автор тестов)**

1. Убрать `try/except` из `main` (вернуть прямой вызов `_dispatch`) — обязан
   упасть `test_main_catches_dispatch_failure_and_reports`.
2. В `setup_logging` заменить `return None` на проброс исключения — обязан
   упасть `test_setup_logging_survives_unwritable_directory`.

---

### Task 8: режим `--smoke`

**Files:**
- Modify: `src/onecstarter/__main__.py` (`parse_smoke_dir`, `_dispatch`)
- Modify: `src/onecstarter/ui/app.py` (`run_smoke`)
- Test: `tests/unit/test_entry_point.py`, `tests/ui/test_app.py`

**Interfaces:**
- Produces:
  - `__main__.SMOKE_OPTION = "--smoke"`; `parse_smoke_dir(argv: Sequence[str]) -> str | None`
    (формы `--smoke DIR` и `--smoke=DIR`, разбор как у `parse_ib_name`);
  - `app.run_smoke(target_dir: str, env: Mapping[str, str]) -> int` — 0: окно собрано
    и показано, обе фоновые задачи завершились (таймаут 30 с), ярлык `smoke.lnk`
    записан в `target_dir`; иначе 1.
- Consumes: `_build_main_window` (Task 5), `shortcut_command`/`build_shell_link`
  (`config/shell_link.py`), `atomic_write` (`config/atomic.py`).

- [ ] **Step 1: Написать падающие тесты**

`tests/unit/test_entry_point.py`:

```python
@pytest.mark.parametrize(
    ("argv", "expected"),
    [
        (["--smoke", r"C:\tmp"], r"C:\tmp"),
        ([r"--smoke=C:\tmp"], r"C:\tmp"),
        (["--smoke"], ""),
        ([], None),
        (["--ib-name", "Демо"], None),
    ],
)
def test_parse_smoke_dir(argv, expected):
    assert parse_smoke_dir(argv) == expected


def test_main_with_smoke_runs_smoke_mode(app_stub):
    # по образцу test_main_with_ib_name_launches_that_base: заглушка
    # ui.app.run_smoke фиксирует вызов (каталог, env)
    ...
```

`tests/ui/test_app.py`:

```python
def test_run_smoke_writes_shortcut_and_reports_zero(tmp_path, monkeypatch, ...):
    appdata = tmp_path / "appdata"
    target = tmp_path / "out"
    target.mkdir()
    # env только с APPDATA: обнаружение на пустом окружении мгновенно
    assert run_smoke(str(target), {"APPDATA": str(appdata)}) == 0
    assert (target / "smoke.lnk").exists()


def test_run_smoke_times_out_without_background(monkeypatch, tmp_path):
    # подменить StartupTasks на молчащие (spawn-пустышка) и таймаут в 100 мс —
    # через инъекцию run_smoke(..., timeout_ms=100, make_tasks=...) либо
    # monkeypatch модульного имени; код 1, ярлыка нет
    ...
```

(для второго теста добавить `run_smoke(..., *, timeout_ms: int = 30000)` —
честный параметр вместо магии.)

- [ ] **Step 2: Прогон — падают**

Run: `uv run pytest tests/unit/test_entry_point.py -q`
Expected: FAIL — нет `parse_smoke_dir`.

- [ ] **Step 3: Реализация `__main__`**

`parse_smoke_dir` — копия механики `parse_ib_name` с `SMOKE_OPTION` (докстринг:
почему свой разбор — тот же довод про pythonw). В `_dispatch` первой проверкой:

```python
smoke_dir = parse_smoke_dir(arguments)
if smoke_dir is not None:
    if not smoke_dir.strip():
        return 1  # каталог обязателен; лог уже настроен, причина в логе
    from onecstarter.ui.app import run_smoke

    return run_smoke(smoke_dir, os.environ)
```

(пустое значение логировать `logging.getLogger("onecstarter").error(...)`.)

- [ ] **Step 4: Реализация `run_smoke` в `app.py`**

```python
def run_smoke(
    target_dir: str, env: Mapping[str, str], *, timeout_ms: int = 30000
) -> int:
    """Самопроверка собранного экземпляра — вызывает build/smoke.py (спека §5).

    Окружение готовит вызывающий: QT_QPA_PLATFORM=offscreen и подменённый
    APPDATA — живые данные не трогаются, процессы 1С не порождаются.
    Ярлык пишется нашим кодом с фактическим frozen: в сборке исполняется
    ветка frozen=True (шаг 8 задачи 17, долг №7).
    """
    application = QApplication([])
    try:
        runtime = build_runtime(env)
    except (UserDataUnavailableError, OSError):
        logging.getLogger("onecstarter.startup").exception("smoke: рантайм не собрался")
        return 1
    window, tasks = _build_main_window(application, runtime, env)
    pending = {"installations": True, "common": True}
    loop = QEventLoop()

    def done(kind: str) -> None:
        pending[kind] = False
        if not any(pending.values()):
            loop.quit()

    tasks.installations_ready.connect(lambda _found: done("installations"))
    tasks.common_lists_ready.connect(lambda _data: done("common"))
    timer = QTimer()
    timer.setSingleShot(True)
    timer.timeout.connect(loop.quit)
    timer.start(timeout_ms)
    window.show()
    _log.info("smoke: окно показано")
    tasks.start()
    loop.exec()
    if any(pending.values()):
        _log.error("smoke: фоновые задачи не завершились за %d мс", timeout_ms)
        return 1
    target, arguments = shortcut_command(
        sys.executable, "OneCStarter smoke", frozen=bool(getattr(sys, "frozen", False))
    )
    payload = build_shell_link(target, arguments, target.parent, "OneCStarter smoke")
    atomic_write(Path(target_dir) / "smoke.lnk", payload)
    _log.info("smoke: ярлык записан")
    return 0
```

(инъекцию `make_tasks` для теста таймаута добавить по образцу `run_launch`.)

- [ ] **Step 5: Полный прогон**

Run: `uv run pytest -q && uv run ruff check . && uv run mypy`
Expected: PASS, коды 0.

- [ ] **Step 6: Commit**

```bash
git add src/onecstarter/__main__.py src/onecstarter/ui/app.py tests/unit/test_entry_point.py tests/ui/test_app.py
git commit -m "feat: режим --smoke — самопроверка собранного экземпляра"
```

---

### Task 9: PyInstaller — entry, иконка, spec

**Files:**
- Modify: `pyproject.toml` (dev-группа)
- Create: `build/entry.py`, `build/make_icon.py`, `build/onecstarter.ico`, `build/onecstarter.spec`
- Modify: `.gitignore`

**Interfaces:**
- Produces: `dist/OneCStarter/OneCStarter.exe` (windowed) и `OneCStarterc.exe` (console),
  оба с иконкой и версионным ресурсом из `pyproject.toml`.
- Consumes: `onecstarter.__main__.main` (Task 7–8).

- [ ] **Step 1: Зависимость и .gitignore**

```bash
uv add --dev "pyinstaller>=6.10"
```

В `.gitignore` блок build заменить на:

```
dist/
build/*
!build/*.spec
!build/entry.py
!build/make_icon.py
!build/onecstarter.ico
!build/smoke.py
!build/build.ps1
!build/installer.iss
```

- [ ] **Step 2: `build/entry.py`**

```python
"""Точка входа PyInstaller: тонкая обёртка над onecstarter.__main__.

Отдельный файл, а не __main__.py пакета: PyInstaller требует скрипт,
а скрипт с именем __main__.py конфликтует с одноимённым модулем сборки.
"""

from onecstarter.__main__ import main

if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: `build/make_icon.py` и генерация иконки**

```python
"""Генерация build/onecstarter.ico из кодовой графики приложения.

Без логотипа 1С (граница бренда, требования §4). ICO собирается вручную
из PNG-кадров: формат с PNG внутри поддерживается с Windows Vista,
Pillow ради одного файла не нужен.

Запуск: uv run python build/make_icon.py  (перерисовать и закоммитить).
"""

import struct
import sys
from pathlib import Path

from PySide6.QtCore import QBuffer, QRectF, Qt
from PySide6.QtGui import QColor, QGuiApplication, QImage, QPainter

from onecstarter.ui import theme

SIZES = (16, 24, 32, 48, 64, 128, 256)
OUTPUT = Path(__file__).parent / "onecstarter.ico"


def draw_frame(size: int) -> bytes:
    """Скруглённый квадрат цвета поверхности + «полки» акцентом — как значок рельсы."""
    image = QImage(size, size, QImage.Format.Format_ARGB32)
    image.fill(Qt.GlobalColor.transparent)
    painter = QPainter(image)
    painter.setRenderHint(QPainter.RenderHint.Antialiasing)
    painter.setPen(Qt.PenStyle.NoPen)
    painter.setBrush(QColor(theme.DARK.surface))
    radius = size * 0.2
    painter.drawRoundedRect(QRectF(0, 0, size, size), radius, radius)
    painter.setBrush(QColor(theme.DARK.accent_fill))
    margin = size * 0.22
    bar_height = size * 0.12
    step = size * 0.2
    for row in range(3):
        top = margin + row * step
        painter.drawRoundedRect(
            QRectF(margin, top, size - 2 * margin, bar_height),
            bar_height / 2,
            bar_height / 2,
        )
    painter.end()
    buffer = QBuffer()
    buffer.open(QBuffer.OpenModeFlag.WriteOnly)
    image.save(buffer, "PNG")
    return bytes(buffer.data())


def pack_ico(frames: list[tuple[int, bytes]]) -> bytes:
    header = struct.pack("<HHH", 0, 1, len(frames))
    entries = b""
    body = b""
    offset = 6 + 16 * len(frames)
    for size, png in frames:
        entries += struct.pack(
            "<BBBBHHII", size % 256, size % 256, 0, 0, 1, 32, len(png), offset
        )
        offset += len(png)
        body += png
    return header + entries + body


def main() -> int:
    QGuiApplication(sys.argv)  # offscreen выставляет вызывающий
    OUTPUT.write_bytes(pack_ico([(size, draw_frame(size)) for size in SIZES]))
    print(f"записан {OUTPUT}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

Сгенерировать и осмотреть (свойства файла в Проводнике покажут кадры):

```powershell
$env:QT_QPA_PLATFORM = "offscreen"; uv run python build/make_icon.py
```

(если у `Palette` нет поля `surface` — взять фактическое имя роли фона
из `ui/theme.py` и поправить обе ссылки.)

- [ ] **Step 4: `build/onecstarter.spec`**

```python
"""Сборка OneCStarter — единственный источник правды (CLAUDE.md, «Сборка»).

one-dir, два exe из одного Analysis: OneCStarter.exe (windowed)
и OneCStarterc.exe (console, для диагностики — спека T-04.6, §4.3).
Версия — из pyproject.toml, единственного места (CLAUDE.md).
"""

import tomllib
from pathlib import Path

from PyInstaller.utils.win32.versioninfo import (
    FixedFileInfo,
    StringFileInfo,
    StringStruct,
    StringTable,
    VarFileInfo,
    VarStruct,
    VSVersionInfo,
)

ROOT = Path(SPECPATH).parent  # noqa: F821 — SPECPATH определяет PyInstaller
VERSION = tomllib.loads((ROOT / "pyproject.toml").read_text("utf-8"))["project"]["version"]
NUMBERS = tuple(int(part) for part in VERSION.split("."))[:4]
NUMBERS = NUMBERS + (0,) * (4 - len(NUMBERS))
ICON = str(Path(SPECPATH) / "onecstarter.ico")  # noqa: F821

version_info = VSVersionInfo(
    ffi=FixedFileInfo(filevers=NUMBERS, prodvers=NUMBERS),
    kids=[
        StringFileInfo(
            [
                StringTable(
                    "041904B0",
                    [
                        StringStruct("ProductName", "OneCStarter"),
                        StringStruct("FileDescription", "Запуск информационных баз 1С:Предприятие"),
                        StringStruct("FileVersion", VERSION),
                        StringStruct("ProductVersion", VERSION),
                        StringStruct("LegalCopyright", "MIT License"),
                    ],
                )
            ]
        ),
        VarFileInfo([VarStruct("Translation", [1049, 1200])]),
    ],
)

a = Analysis(
    [str(Path(SPECPATH) / "entry.py")],  # noqa: F821
    pathex=[str(ROOT / "src")],
    # registry.toml читается через importlib.resources — PyInstaller сам
    # не-Python ресурс не находит (находка Task 9: без datas первый smoke
    # собранного exe падал FileNotFoundError).
    datas=[(str(ROOT / "src" / "onecstarter" / "platform_1c" / "registry.toml"), "onecstarter/platform_1c")],
    excludes=["tkinter"],
)
pyz = PYZ(a.pure)

exe_gui = EXE(
    pyz,
    a.scripts,
    exclude_binaries=True,
    name="OneCStarter",
    console=False,
    icon=ICON,
    version=version_info,
)
exe_cli = EXE(
    pyz,
    a.scripts,
    exclude_binaries=True,
    name="OneCStarterc",
    console=True,
    icon=ICON,
    version=version_info,
)
coll = COLLECT(exe_gui, exe_cli, a.binaries, a.datas, name="OneCStarter")
```

- [ ] **Step 5: Первая сборка руками**

```powershell
uv run pyinstaller build/onecstarter.spec --noconfirm --distpath dist --workpath build/pyi
```

Expected: `dist/OneCStarter/` с обоими exe. Проверить свойства exe
(версия 0.0.0 пока — до Task 12), запуск `dist/OneCStarter/OneCStarterc.exe --smoke <временный каталог>`
руками с подменённым `APPDATA` — код 0. Если PyInstaller не находит Qt-плагины
или падает импорт — чинить здесь, до оркестратора.

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock .gitignore build/entry.py build/make_icon.py build/onecstarter.ico build/onecstarter.spec
git commit -m "build: PyInstaller one-dir — два exe, версия из pyproject, иконка из кода"
```

---

### Task 10: build/smoke.py и build/build.ps1

**Files:**
- Create: `build/smoke.py`, `build/build.ps1`

**Interfaces:**
- Produces: `build/build.ps1` — единая команда сборки: PyInstaller → smoke → zip
  (→ ISCC добавит Task 11); `build/smoke.py <dist-dir>` — код 0/1.
- Consumes: `--smoke` (Task 8), spec (Task 9).

- [ ] **Step 1: `build/smoke.py`**

```python
"""Smoke собранного экземпляра. Обязателен для каждой сборки (CLAUDE.md).

Проверяет собранный dist, а не исходники: (1) консольный exe поднимает окно
offscreen и обе фоновые задачи; (2) ярлык, созданный frozen-веткой, целится
в запущенный exe (шаг 8 задачи 17); (3) лог создан и несёт фазу «окно
показано». APPDATA подменяется — живые данные машины не трогаются.
"""

import os
import subprocess
import sys
import tempfile
from pathlib import Path


def shortcut_target(lnk: Path) -> str:
    """Цель ярлыка штатным читателем Windows — не нашим кодом."""
    command = (
        "(New-Object -ComObject WScript.Shell)"
        f".CreateShortcut('{lnk}').TargetPath"
    )
    return subprocess.run(
        ["powershell", "-NoProfile", "-Command", command],
        capture_output=True,
        text=True,
        check=True,
    ).stdout.strip()


def main() -> int:
    dist = Path(sys.argv[1]).resolve()
    console_exe = dist / "OneCStarterc.exe"
    if not console_exe.is_file():
        print(f"smoke: нет {console_exe}")
        return 1
    with tempfile.TemporaryDirectory() as scratch:
        appdata = Path(scratch) / "appdata"
        out = Path(scratch) / "out"
        out.mkdir()
        env = dict(os.environ)
        env["APPDATA"] = str(appdata)
        env["QT_QPA_PLATFORM"] = "offscreen"
        run = subprocess.run(
            [str(console_exe), "--smoke", str(out)],
            env=env,
            capture_output=True,
            text=True,
            timeout=120,
        )
        if run.returncode != 0:
            print(f"smoke: exe вернул {run.returncode}\n{run.stdout}\n{run.stderr}")
            return 1
        log = appdata / "OneCStarter" / "logs" / "onecstarter.log"
        if not log.is_file() or "окно показано" not in log.read_text("utf-8"):
            print("smoke: лог не создан или фаза «окно показано» не записана")
            return 1
        lnk = out / "smoke.lnk"
        target = shortcut_target(lnk)
        if Path(target) != console_exe:
            print(f"smoke: цель ярлыка {target!r}, ожидался {console_exe}")
            return 1
    print("smoke: OK")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: `build/build.ps1`**

Файл сохранять в UTF-8 **с BOM**: PowerShell 5.1 без BOM читает кириллицу
как ANSI и падает `ParserError` (находка Task 10, воспроизведено).

```powershell
# Сборка поставки. Порядок обязателен: smoke идёт ДО артефактов —
# сборка, не прошедшая smoke, не существует (CLAUDE.md, «Сборка»).
param([switch]$SkipInstaller)

$ErrorActionPreference = "Stop"
$root = Split-Path $PSScriptRoot -Parent
Set-Location $root

$version = uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"
if ($LASTEXITCODE -ne 0) { exit 1 }
Write-Host "== OneCStarter $version =="

uv run pyinstaller build/onecstarter.spec --noconfirm --distpath dist --workpath build/pyi
if ($LASTEXITCODE -ne 0) { Write-Host "PyInstaller: отказ"; exit 1 }

uv run python build/smoke.py dist/OneCStarter
if ($LASTEXITCODE -ne 0) { Write-Host "smoke: отказ"; exit 1 }

$zip = "dist/OneCStarter-$version-portable.zip"
if (Test-Path $zip) { Remove-Item $zip }
Compress-Archive -Path "dist/OneCStarter" -DestinationPath $zip
Write-Host ("zip: {0:N1} МБ" -f ((Get-Item $zip).Length / 1MB))

if (-not $SkipInstaller) {
    $iscc = @(
        "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe",
        "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
    ) | Where-Object { Test-Path $_ } | Select-Object -First 1
    if (-not $iscc) { Write-Host "Inno Setup 6 не найден (ISCC.exe)"; exit 1 }
    & $iscc "/DAppVersion=$version" /Odist build/installer.iss
    if ($LASTEXITCODE -ne 0) { Write-Host "ISCC: отказ"; exit 1 }
    $setup = "dist/OneCStarter-$version-setup.exe"
    Write-Host ("setup: {0:N1} МБ" -f ((Get-Item $setup).Length / 1MB))
}

$size = (Get-ChildItem dist/OneCStarter -Recurse | Measure-Object Length -Sum).Sum
Write-Host ("dist: {0:N1} МБ. Готово." -f ($size / 1MB))
```

- [ ] **Step 3: Прогон без установщика**

```powershell
powershell -NoProfile -File build/build.ps1 -SkipInstaller
```

Expected: `smoke: OK`, zip в `dist/`, размеры в отчёте. Ненулевой код любого
шага — остановка. Зафиксировать размер dist в реестре исполнения.

- [ ] **Step 4: Commit**

```bash
git add build/smoke.py build/build.ps1
git commit -m "build: оркестратор сборки со smoke собранного экземпляра и zip"
```

---


> **Правка 21.08.2026 (ручной прогон T-04.7, шаг А6).** Предписание
> «`[UninstallDelete]` намеренно НЕТ» в эталонном блоке этой задачи выполнено
> буквально — и оказалось неполным: без секции деинсталляция оставляла скелет
> из шести пустых каталогов PyInstaller. Секция добавлена, но затрагивает
> только служебный `{app}\_internal` и сам `{app}`; довод плана — «данные
> пользователя переживают удаление» — в силе и достигается тем, что они лежат
> вне `{app}`. Актуальная редакция и обоснование — в `build/installer.iss`.

### Task 11: установщик Inno Setup

**Files:**
- Create: `build/installer.iss`

**Interfaces:**
- Produces: `dist/OneCStarter-<версия>-setup.exe` — per-user, без прав администратора.
- Consumes: `dist/OneCStarter/` (Task 10); версию передаёт `build.ps1` через `/DAppVersion`.

- [ ] **Step 1: Установить Inno Setup 6 (если нет)**

```powershell
winget install --id JRSoftware.InnoSetup -e --source winget
```

Наличие `Russian.isl` проверить в `<Inno>\Languages\` — он в стандартной
поставке; если его нет, строку языка с ним убрать, оставив английский,
и зафиксировать это в реестре исполнения.

- [ ] **Step 2: `build/installer.iss`**

```ini
; Per-user установщик OneCStarter (спека T-04.6, §6): без прав
; администратора, данные пользователя при удалении не трогаются.
; AppId зафиксирован навсегда — по нему Windows узнаёт обновление.

#ifndef AppVersion
  #define AppVersion "0.0.0"
#endif

[Setup]
AppId={{8C1F4A2E-9D37-4B6A-A1C5-0E2D7F5B9C41}
AppName=OneCStarter
AppVersion={#AppVersion}
AppPublisher=OneCStarter project
DefaultDirName={localappdata}\Programs\OneCStarter
PrivilegesRequired=lowest
DisableProgramGroupPage=yes
UninstallDisplayIcon={app}\OneCStarter.exe
OutputBaseFilename=OneCStarter-{#AppVersion}-setup
Compression=lzma2
SolidCompression=yes
WizardStyle=modern

[Languages]
Name: "russian"; MessagesFile: "compiler:Languages\Russian.isl"
Name: "english"; MessagesFile: "compiler:Default.isl"

[Files]
Source: "..\dist\OneCStarter\*"; DestDir: "{app}"; Flags: recursesubdirs ignoreversion

[Icons]
Name: "{userprograms}\OneCStarter"; Filename: "{app}\OneCStarter.exe"

[Run]
Filename: "{app}\OneCStarter.exe"; Description: "{cm:LaunchProgram,OneCStarter}"; Flags: nowait postinstall skipifsilent

; [UninstallDelete] намеренно НЕТ: %APPDATA%\OneCStarter (история, избранное,
; настройки) и ibases.v8i переживают удаление — «удаление ничего не теряет».
```

- [ ] **Step 3: Полная сборка**

```powershell
powershell -NoProfile -File build/build.ps1
```

Expected: оба артефакта в `dist/`, отчёт с размерами. Поставить и удалить
установщик на машине сборки: после удаления `%APPDATA%\OneCStarter\` цел.

- [ ] **Step 4: Commit**

```bash
git add build/installer.iss
git commit -m "build: per-user установщик Inno Setup"
```

---

### Task 12: артефакты публикации — .gitattributes, LICENSE, README, версия

**Files:**
- Create: `.gitattributes`, `LICENSE`, `README.md`
- Modify: `pyproject.toml` (версия `0.1.0`)

- [ ] **Step 1: `.gitattributes` и перенормализация (долг №5)**

```
* text=auto
tests/fixtures/** -text
*.lnk binary
*.ico binary
*.png binary
```

```bash
git add .gitattributes
git add --renormalize .
git status
```

Осмотреть список перенормализованных файлов; прогнать полный набор:

Run: `uv run pytest -q && uv run ruff check . && uv run mypy`
Expected: PASS — фикстуры не тронуты (`tests/fixtures/** -text`), сторожа
кодировок зелёные. Если тест фикстур упал — перенормализация зацепила
байтовые файлы, разобраться ДО коммита.

```bash
git commit -m "chore: .gitattributes и перенормализация переводов строк (долг №5 ревью 4b)"
```

- [ ] **Step 2: LICENSE (MIT)**

Полный текст MIT с копирайтом `Copyright (c) 2026 Alexey Ananyev` —
стандартный текст лицензии без изменений.

- [ ] **Step 3: README.md**

Структура (русский основной, EN-шапка в два предложения):

```markdown
# OneCStarter

> Fast launcher for 1C:Enterprise infobases on Windows: tree, search,
> explicit platform version control. Russian UI; docs below are in Russian.

Программа запуска информационных баз 1С:Предприятие для специалистов:
дерево групп, мгновенный поиск, избранное, история, явный контроль версии
платформы, ярлыки на базы, drag&drop каталога файловой базы.

## Чем лучше штатного стартера
<таблица А/Б/Г/Д из docs/requirements.md, §2 — сжатая версия>

## Установка
- **Установщик:** OneCStarter-<версия>-setup.exe — per-user, права
  администратора не нужны.
- **Portable:** распаковать OneCStarter-<версия>-portable.zip, запустить
  OneCStarter.exe.

SmartScreen при первом запуске предупредит о неизвестном издателе:
сборки не подписаны (сертификата нет). Исходники открыты — сборку можно
воспроизвести самостоятельно (см. ниже).

## Совместимость со штатным стартером
ibases.v8i — источник правды; после любой операции файл остаётся рабочим
для 1CEStart. Удаление OneCStarter ничего не теряет.

## Диагностика
Лог: %APPDATA%\OneCStarter\logs\onecstarter.log. Если приложение
не стартует — запустите OneCStarterc.exe (консольная сборка) и приложите
вывод к issue.

## Сборка из исходников
uv sync; uv run pytest; powershell -File build/build.ps1
Установщик требует Inno Setup 6.

## Границы
1С:Предприятие 7.7 не поддерживается; телеметрии нет; пароли
не сохраняются. Подробнее: docs/requirements.md.

## Лицензия
MIT.
```

Скриншот НЕ вставлять на этом шаге: он снимается на живом запуске
в контрольной точке Task 13 (offscreen маскирует шрифты — урок T-06)
и добавляется коммитом `docs: скриншот для README` в `docs/assets/`.

- [ ] **Step 4: Версия 0.1.0**

В `pyproject.toml`: `version = "0.1.0"`. Пересобрать и убедиться, что версия
дошла до артефактов:

```powershell
powershell -NoProfile -File build/build.ps1 -SkipInstaller
```

Expected: `smoke: OK`, `dist/OneCStarter-0.1.0-portable.zip`, свойства
`OneCStarter.exe` показывают 0.1.0.

- [ ] **Step 5: Commit**

```bash
git add LICENSE README.md pyproject.toml
git commit -m "docs: LICENSE MIT, README, версия 0.1.0"
```

---

### Task 13: контрольная точка заказчика (ручная, СТОП)

**До неё:** полный прогон `uv run pytest -q && uv run ruff check . && uv run mypy`
и свежая сборка `build/build.ps1` — всё зелёное.

**Чек-лист заказчику** (результаты — в реестр исполнения):

- [ ] Установить `dist/OneCStarter-0.1.0-setup.exe` (per-user, без UAC).
- [ ] **Холодный старт** — прямой тест инцидента 15.08: перезагрузка (кэш
  антивируса остывает) → запуск из Пуска → окно с деревом баз появляется
  сразу; колонка версии заполняется по мере обнаружения; лог
  `%APPDATA%\OneCStarter\logs\onecstarter.log` несёт фазы с длительностями.
- [ ] Тёплый старт: окно и версии без видимой задержки.
- [ ] Создать ярлык на базу из установленного приложения; запуск по ярлыку —
  **только с явного согласия на запуск 1С**.
- [ ] `OneCStarterc.exe` из каталога установки: лог дублируется в консоль.
- [ ] Снять скриншот окна для README (живой экран, не offscreen).
- [ ] Удалить приложение установщиком → `%APPDATA%\OneCStarter\` и `ibases.v8i`
  на месте → поставить обратно.

Замечания заказчика разбираются как задачи этого плана до слияния.

---

### Task 14: слияние и публикация (частично ручная, СТОП перед необратимым)

**Порядок жёсткий: свежий корень — только после слияния; push — только после аудита.**

- [ ] **Step 1: Финальное ревью ветки** — по процессу проекта, на самой сильной
  модели, с мутационной дисциплиной. Находки закрываются до слияния.

- [ ] **Step 1а: Актуализация статусных документов — на ветке, ДО слияния.**
  Находка финального ревью: свежий корень и тег v0.1.0 понесут дерево как есть,
  и устаревшие статусы соврут публичному читателю. Обновить:
  - `docs/tasks.md`: строка T-04.6 → DONE с датой и фактами (размер сборки,
    число тестов);
  - `CLAUDE.md`, раздел «Текущий статус»: убрать «требования не написаны /
    не писать код» — описать фактическое состояние одним абзацем.

```bash
git checkout master
git merge --ff-only 2026-08-16-delivery
uv run pytest -q && uv run ruff check . && uv run mypy
```

Expected: fast-forward, все команды с кодом 0.

- [ ] **Step 3: Свежий корень (спека §7.2). СТОП: подтвердить у заказчика.**

```bash
git branch archive/pre-publish-history master
root=$(git commit-tree "master^{tree}" -m "OneCStarter 0.1.0")
git reset --hard "$root"
git log --oneline          # ровно один коммит
git branch -a              # archive/pre-publish-history и archive/2026-08-09... на месте
```

`archive/pre-publish-history` **не пушится никогда** — в ней коммит `4121853`
с настоящим SID.

- [ ] **Step 4: Аудит дерева перед push (спека §7.3)**

```bash
git grep -I "S-1-5-21" || echo "SID: чисто"
git grep -I "\\\\\\\\" -- ":!*.md" || echo "UNC: чисто"   # найденное осмотреть руками
# Реальные токены заказчика в публикуемом дереве хранить нельзя — поэтому
# их список живёт ВНЕ дерева: в непубликуемом леджере исполнения
# (.superpowers/sdd/2026-08-16-v1-delivery/progress.md, запись «Task 13
# (частично)»). Исполнитель шага берёт токены оттуда и подставляет
# в TOKENS; пустой TOKENS делает проверку пустышкой — это провал шага,
# а не «чисто» (ре-ревью финальной волны, 2026-08-16):
TOKENS="взять из леджера"; test "$TOKENS" != "взять из леджера" || { echo "TOKENS не подставлены"; false; }
git grep -iI -e "$TOKENS" || echo "токены: чисто"
uv run pytest tests/unit/test_fixture_v8i.py tests/unit/test_shell_link.py -q
```

Плюс руками: `git log --format="%an %ae"` — авторство корневого коммита
публикуется, заказчик подтверждает имя и email; беглый осмотр `docs/`
на реальные имена машин/организаций. Результаты аудита — в реестр исполнения.

- [ ] **Step 5: GitHub. СТОП: имя репозитория и аккаунт — решение заказчика.**

Заказчик создаёт публичный репозиторий (или даёт добро на `gh repo create`).
Затем:

```bash
git remote add origin <url>
git push -u origin master
git tag v0.1.0
git push origin v0.1.0
```

- [ ] **Step 6: Проверить CI** — вкладка Actions: прогон `ci.yml` зелёный.
  Если Qt-тесты падают на runner — добавить `QT_QPA_PLATFORM: offscreen`
  в env шага pytest, закоммитить, дождаться зелёного.

- [ ] **Step 7: Release**

```bash
gh release create v0.1.0 \
  dist/OneCStarter-0.1.0-portable.zip \
  dist/OneCStarter-0.1.0-setup.exe \
  --title "OneCStarter 0.1.0" \
  --notes "Первый публичный выпуск: дерево баз и запуск с контролем версии платформы, правки списка, группы, светлая тема, ярлыки, диагностика старта. Артефакты не подписаны — SmartScreen предупредит при первом запуске."
```

- [ ] **Step 8: Хвосты**

- бейдж CI в README (`.github/workflows/ci.yml` badge URL появившегося репозитория),
  коммит и push (скриншот уже в `docs/assets/` — сделан в Task 13);
- `.superpowers/sdd/next-session-prompt.md` переписать под новое состояние
  (свежий корень! прежние хеши невалидны).
- Обновление `docs/tasks.md` и `CLAUDE.md` перенесено в Step 1а (до слияния) —
  находка финального ревью: публикуемый корень не должен нести устаревшие статусы.

---

## Самопроверка плана по спеке

- §3 перестройка старта → Tasks 1–6 (три источника зависания: exe — Task 5
  фон, InstalledLocation — Task 5 фон, общие списки — Tasks 1–2, 5 фон).
- §3.4 «pending ≠ пусто» → Tasks 2, 3, 5.
- §3.5 `--ib-name` → Task 6; демоны → Task 4.
- §4 видимость ошибок → Task 7 (лог, перехват, faulthandler), §4.3 консольный
  exe → Task 9 (spec) + Task 7 (зеркало в stderr).
- §5 сборка → Tasks 9–10; smoke с frozen-веткой ярлыка (долг №7) → Tasks 8, 10.
- §6 установщик → Task 11.
- §7 публикация → Tasks 12, 14; долг №5 CRLF → Task 12; SID/свежая история →
  Task 14.
- §8 тестирование → мутационные шаги в Tasks 2, 4, 5, 6, 7; ручные точки →
  Task 13.
