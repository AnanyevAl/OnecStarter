# T-11 «Косметика и удобство перед выпуском v2» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Закрыть девять пожеланий заказчика из `docs/tasks.md`, раздел T-11 (зазор имени и статуса в карточке сервера; `Alt+Enter` — свойства и справочник сочетаний в настройках; «Добавить базу…» из меню группы и по `Insert`; удаление по `Delete`; автоимя из каталога/имени в кластере; выбор группы деревом; настройка «запуск базы сворачивает окно в трей»; режим показа «как в файле / по алфавиту») плюс задача 0 — детерминированный teardown виджетов в `tests/ui/test_servers_view.py` (долг T-12, п. 15).

**Architecture:** Всё поведение — в существующих точках: `ui/bases/view.py` (сочетания, меню, `Insert`/`Delete` в `_BasesTree.keyPressEvent`, сигнал `launched`), `ui/dialogs/infobase.py` и `group.py` (общий `GroupPicker`, автоимя), `services/settings.py` (два новых поля, схема не меняется), `services/display.py` (чистая сортировка, инвариант 1), `ui/settings_view.py` (тумблер, сегмент порядка, справочник), `ui/app.py` (проводка). Новых модулей два: `ui/shortcuts.py` (таблица сочетаний — единый источник для регистрации и справочника) и `ui/dialogs/group_picker.py`.

**Tech Stack:** Python ≥ 3.13, PySide6 (только `ui`), pytest + pytest-qt (offscreen), ruff, mypy strict вне `onecstarter.ui.*`.

Спека: раздел **T-11** в [docs/tasks.md](../../tasks.md) (бэклог 29.08.2026 с разбором каждого пункта; решения заказчика 29.08.2026 по пп. 2, 3, 9 — в нём же). Отдельного документа спеки нет — раздел служит спекой.
Ветка: **новая**, от `master` (HEAD ≥ `a174e1c`, T-08/T-10/T-12 слиты): `feat/2026-08-30-v2-polish`. Леджер SDD — `.superpowers/sdd/2026-08-30-v2-polish/progress.md`.

## Global Constraints

- Qt только в `src/onecstarter/ui/`; `services/display.py` и `services/settings.py` остаются без `PySide6` (сторож `tests/unit/test_no_qt_in_core.py`, список `CORE` не меняется — новых модулей ядра нет).
- Схема `settings.json` остаётся `1`: новые ключи `hide_on_launch` и `list_order` необязательны, отсутствующий ключ = дефолт, чужое значение = дефолт (докстринг модуля `settings.py`, правило «bump схемы строго хуже»).
- Дефолты не меняют поведение существующих установок: `hide_on_launch=False`, `list_order=ListOrder.FILE`.
- Решения заказчика 29.08.2026 (tasks.md, T-11): п. 2 — вариант (а) режим показа, файл `.v8i` не трогается, разовое действие (б) НЕ делается; п. 3 — `Alt+Enter` зашит, keymap НЕ делается, справочник — только чтение; п. 9 — на запуск серверного профиля настройка не действует, без трея окно молча не сворачивается (и не уходит в панель задач).
- Все диалоги в тестах — без `exec()`: сборка отделена от показа (`_build_*`), `exec` подменяется `monkeypatch`'ем на `Rejected`, подтверждения приходят инъекцией (`confirm_removal`, `ask_group_removal`). Тест, повесивший suite на модальном окне, — дефект теста.
- `Insert`/`Delete` действуют ТОЛЬКО при фокусе в дереве (`_BasesTree.keyPressEvent`), не `QShortcut` уровня окна: в поле поиска это правка текста.
- Подписи UI по-русски; цвета — только роли `Palette`; в докстрингах/комментариях с кириллицей ruff требует `# noqa: RUF001/RUF002/RUF003` там, где ругается на «неоднозначные» символы (`«»`, `—`, `…` рядом с латиницей) — ставить как в соседнем коде, не отключать правило.
- Проверки после каждой задачи: `uv run pytest -q` (ОБЫЧНЫЙ режим, не фоновый; suite, не завершившийся за 5 мин, — зависание на модальном диалоге), `uv run ruff check .`, `uv run mypy` — коды 0. Под git-bash кириллица в выводе pytest бита кодировкой консоли — точечные прогоны с текстом ошибок запускать из PowerShell.
- Защитные тесты — докстринг начинается с «ЗАЩИТНЫЙ ТЕСТ» и называет мутацию; их мутации ставит независимый агент (задача 10). Обязательны для: отказа подтверждения `Delete` (файл не тронут), бездействия на записях общего списка, «без трея не скрывать», «в алфавитном режиме перестановка не пишет файл».
- Реальные пути машины заказчика, имена организаций и пользователей в код, тесты и docs не попадают; фикстура — `tests/fixtures/anonymized.v8i`.
- Коммиты — на каждую задачу, сообщение в стиле репозитория: `feat|fix|test|docs: T-11 — …`.

## Карта файлов

| Файл | Что с ним | Задача |
| --- | --- | --- |
| `tests/ui/test_servers_view.py` | autouse-фикстура: каждый `ServersView` — под `qtbot.addWidget` | 0 |
| `src/onecstarter/ui/servers/view.py` | `TITLE_ROW_SPACING`, `title_row.setSpacing`, аксессор `profile_title_row` | 1 |
| `src/onecstarter/ui/shortcuts.py` (новый) | `ShortcutSpec`, `BASES_SHORTCUTS` | 2 |
| `src/onecstarter/ui/bases/view.py` | `Alt+Enter` → `_show_current_properties`; подпись меню; `add_infobase(folder)`; «Добавить базу…» в меню группы; `Insert`/`Delete` в `_BasesTree.keyPressEvent`; `_add_infobase_at_current`, `_remove_current`; сигнал `launched`; провайдер `list_order`, запрет перестановки в алфавитном режиме; аксессор `tree()` | 2, 4, 5, 8, 9 |
| `src/onecstarter/ui/settings_view.py` | `_add_block` + справочник сочетаний; тумблер «Запуск базы сворачивает окно в трей»; сегмент «Порядок списка» (`_build_segment`); `_sync` | 3, 8, 9 |
| `src/onecstarter/ui/dialogs/infobase.py` | автоимя по `editingFinished`; `GroupPicker` вместо `QComboBox` | 6, 7 |
| `src/onecstarter/ui/dialogs/group_picker.py` (новый) | `build_items`, `GroupPicker` | 7 |
| `src/onecstarter/ui/dialogs/group.py` | `GroupPicker` вместо `QComboBox` | 7 |
| `src/onecstarter/services/settings.py` | `hide_on_launch`, `ListOrder`, `list_order`, `_bool_of(default=)`, `_order_of` | 8, 9 |
| `src/onecstarter/services/display.py` | `collation_key`, `sort_rows`, параметр `order` у `display_forest` | 9 |
| `src/onecstarter/ui/app.py` | `view.launched` → `window.hide()`; `list_order=` провайдер; перестройка по `list_order` | 8, 9 |
| `docs/tasks.md` | T-11 → DONE, зафиксированные правила п. 2 и форма п. 5 | 10 |
| тесты: `tests/ui/test_servers_view.py`, `test_bases_view.py`, `test_settings_view.py`, `test_infobase_dialog.py`, `test_group_dialog.py`, `test_group_picker.py` (новый), `test_app.py`; `tests/unit/test_settings.py`, `test_display.py` | по задачам | 0–9 |

---

### Task 0: `tests/ui/test_servers_view.py` — детерминированный teardown виджетов

**Files:**
- Modify: `tests/ui/test_servers_view.py` (после класса `FakeSpawn`, перед `def _profile`)

**Interfaces:**
- Consumes: `ServersView` (`onecstarter.ui.servers.view`), фикстура `qtbot` pytest-qt.
- Produces: ничего для кода; для соседних тестов файла — каждый `ServersView(...)`, построенный в тесте, закрывается и удаляется в teardown этого же теста.

Контекст: долг T-12, п. 15 (tasks.md) — `Windows fatal exception: access violation` в `pytestqt.plugin._process_events` наблюдался дважды 30.08.2026 (~2 из ~8 полных прогонов). Правдоподобная причина [Р]: 63 теста файла строят `ServersView(...)` напрямую и никогда не отдают его `qtbot.addWidget` — Python-обёртка с циклами «self → кнопка → лямбда → self» освобождается циклическим GC в произвольный момент, в том числе внутри `processEvents()` следующего теста. Файл на 30.08 не содержит ни одного `qtbot.addWidget` (`grep -c` = 0) при 63 конструкциях `ServersView(`.

- [ ] **Step 1: Добавить autouse-фикстуру**

Вставить после класса `FakeSpawn` (перед `def _profile(...)`):

```python
@pytest.fixture(autouse=True)
def _register_views_with_qtbot(qtbot: Any, monkeypatch: pytest.MonkeyPatch) -> None:
    """Каждый `ServersView` файла — под управлением qtbot (долг T-12, п. 15).

    Тесты файла строят `ServersView(...)` напрямую (63 места на 30.08.2026)
    и не отдают его `qtbot.addWidget`: Python-обёртка вьюхи с циклами
    «self → кнопка → лямбда → self» освобождалась циклическим GC
    в произвольный момент — в том числе внутри `processEvents()` следующего
    теста, когда у дерева виджетов ещё висят `DeferredDelete` от `_clear()`.
    Наблюдалось дважды 30.08.2026 как `Windows fatal exception: access
    violation` в `pytestqt.plugin._process_events` ([Р] разбор финального
    ревью T-12). `qtbot.addWidget` закрывает и удаляет виджет в teardown
    того же теста, детерминированно. Обёртка над `__init__`, а не правка
    63 вызовов: одно место, и каждый новый тест файла защищён сам собой.
    """  # noqa: RUF002
    original_init = ServersView.__init__

    def init_and_register(self: ServersView, *args: Any, **kwargs: Any) -> None:
        original_init(self, *args, **kwargs)
        qtbot.addWidget(self)

    monkeypatch.setattr(ServersView, "__init__", init_and_register)


def test_views_are_registered_with_qtbot() -> None:
    """Фикстура действительно подменила конструктор — иначе она пустая формальность."""
    assert ServersView.__init__.__name__ == "init_and_register"
```

- [ ] **Step 2: Прогнать файл пять раз подряд**

Run (PowerShell): `1..5 | ForEach-Object { uv run pytest tests/ui/test_servers_view.py -q -p no:cacheprovider }`
Expected: пять раз `passed`, без `access violation`. Если падение воспроизвелось — не чинить вслепую: `$env:PYTHONFAULTHANDLER=1; uv run pytest tests/ui/test_servers_view.py -rA --tb=long`, сохранить полный блок с C++-кадром в `.superpowers/sdd/2026-08-30-v2-polish/av-trace.txt`, записать в леджер и продолжать план — задача 10 решает, что с этим делать.

- [ ] **Step 3: Полный прогон и коммит**

Run: `uv run pytest -q`, `uv run ruff check .`, `uv run mypy`
Expected: коды 0.

```bash
git add tests/ui/test_servers_view.py
git commit -m "test: T-11 задача 0 — ServersView под qtbot.addWidget автофикстурой (долг T-12, п. 15)"
```

---

### Task 1: Зазор между именем профиля и статусом в карточке сервера (п. 1)

**Files:**
- Modify: `src/onecstarter/ui/servers/view.py` (константа рядом с `_MONO`; `_build_card`: `title_row`; список и аксессор для тестов)
- Test: `tests/ui/test_servers_view.py`

**Interfaces:**
- Produces: `TITLE_ROW_SPACING: int = 9` (модульная константа `ui/servers/view.py`); `ServersView.profile_title_row(index: int) -> QHBoxLayout`.

Причина (найдена при фиксации бэклога): `title_row` — вложенный `QHBoxLayout` без собственного `setSpacing`; вложенная раскладка наследует spacing родителя (документация Qt, `QLayout::spacing`), а `card_layout.setSpacing(2)` — итого 2 px между жирным именем и статусом: `8.3.25.1633остановлен`. Мокап `docs/superpowers/specs/assets/2026-08-26-v2-servers-mockup.html`, `.srv .t { gap: 9px }` — 9 px.

- [ ] **Step 1: Падающий тест**

В `tests/ui/test_servers_view.py` добавить импорт `TITLE_ROW_SPACING` в строку `from onecstarter.ui.servers.view import CardState, ServersView, _card_state` и тест (рядом с `test_stopped_status_uses_dim_colour`):

```python
def test_card_title_row_separates_name_from_status(
    application: QApplication, tmp_path: Path
) -> None:
    """ЗАЩИТНЫЙ ТЕСТ (T-11, п. 1): между именем профиля и статусом — зазор мокапа.

    Мутация: убрать `title_row.setSpacing(TITLE_ROW_SPACING)` — вложенная
    раскладка унаследует 2 px у `card_layout`, имя и статус снова слипнутся
    (`8.3.25.1633остановлен`, скриншот заказчика 29.08.2026).
    """  # noqa: RUF002
    profile = _profile()
    workspace = _workspace(tmp_path, (profile,))
    workspace.apply_scan(ScanSnapshot(agents=(), managers=()))

    view = ServersView(workspace, installed=lambda: [_installation()], palette=theme.DARK)

    assert TITLE_ROW_SPACING >= 8, "мокап .srv .t: gap 9px"
    assert view.profile_title_row(0).spacing() == TITLE_ROW_SPACING
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `uv run pytest tests/ui/test_servers_view.py::test_card_title_row_separates_name_from_status -q`
Expected: FAIL — `ImportError: cannot import name 'TITLE_ROW_SPACING'`.

- [ ] **Step 3: Реализация**

В `src/onecstarter/ui/servers/view.py` после `_MONO = ...`:

```python
# Зазор между именем профиля и статусом в строке заголовка карточки —
# мокап v2 `.srv .t { gap: 9px }` (T-11, п. 1). Вложенная раскладка без
# собственного spacing наследует его у родителя (документация Qt,
# `QLayout::spacing`), а `card_layout` держит 2 px — имя и статус слипались.
TITLE_ROW_SPACING = 9
```

В `__init__` рядом с `self._profile_status_labels: list[QLabel] = []` добавить `self._profile_title_rows: list[QHBoxLayout] = []`; в `rebuild()` рядом с `self._profile_status_labels = []` — `self._profile_title_rows = []`.

В `_build_card` сразу после `title_row = QHBoxLayout()`:

```python
        title_row.setSpacing(TITLE_ROW_SPACING)
```

и рядом с `self._profile_status_labels.append(status_label)`:

```python
        self._profile_title_rows.append(title_row)
```

В блоке `# -- доступ для тестов` после `profile_status_label`:

```python
    def profile_title_row(self, index: int) -> QHBoxLayout:
        """Строка заголовка карточки (имя + статус) — тесту зазора (T-11, п. 1)."""
        return self._profile_title_rows[index]
```

- [ ] **Step 4: Тест зелёный, скриншот заказчику**

Run: `uv run pytest tests/ui/test_servers_view.py -q`
Expected: PASS.

Оффскрин-скриншот «после» — файл `.superpowers/sdd/2026-08-30-v2-polish/shot_servers.py`:

```python
"""Оффскрин-скриншот раздела «Серверы» с одним остановленным профилем (T-11, п. 1)."""
import os
import sys
from pathlib import Path

os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")
from PySide6.QtWidgets import QApplication  # noqa: E402

from onecstarter.services.servers import ScanSnapshot  # noqa: E402
from onecstarter.ui import theme  # noqa: E402
from onecstarter.ui.servers.view import ServersView  # noqa: E402
from tests.ui.test_servers_view import _installation, _profile, _workspace  # noqa: E402

app = QApplication(sys.argv)
app.setStyleSheet(theme.stylesheet(theme.DARK))
out = Path(sys.argv[1])
workspace = _workspace(out.parent, (_profile(),))
workspace.apply_scan(ScanSnapshot(agents=(), managers=()))
view = ServersView(workspace, installed=lambda: [_installation()], palette=theme.DARK)
view.resize(900, 300)
view.show()
app.processEvents()
view.grab().save(str(out))
print(out)
```

Run: `uv run python .superpowers/sdd/2026-08-30-v2-polish/shot_servers.py .superpowers/sdd/2026-08-30-v2-polish/servers-after.png`
Expected: PNG, в котором между именем и статусом виден зазор. Скриншот «до» — тот же скрипт на `git stash` не нужен: «до» есть у заказчика (скриншот 29.08.2026 в tasks.md).

- [ ] **Step 5: Коммит**

```bash
git add src/onecstarter/ui/servers/view.py tests/ui/test_servers_view.py
git commit -m "fix: T-11 п. 1 — зазор 9 px между именем профиля и статусом (мокап .srv .t)"
```

---

### Task 2: `Alt+Enter` — свойства текущей строки; таблица сочетаний (п. 3, часть 1)

**Files:**
- Create: `src/onecstarter/ui/shortcuts.py`
- Modify: `src/onecstarter/ui/bases/view.py` (`__init__`: регистрация; `_build_menu`: подпись «Свойства…»; новый метод `_show_current_properties`)
- Test: `tests/ui/test_bases_view.py`

**Interfaces:**
- Produces:
  - `ui/shortcuts.py`: `@dataclass(frozen=True) class ShortcutSpec(label: str, title: str, sequences: tuple[str, ...])`; `BASES_SHORTCUTS: tuple[ShortcutSpec, ...]`. `sequences` — строки `QKeySequence` (`"F3"`, `"Alt+Return"`, …), которыми вьюха регистрирует `QShortcut`; пустой кортеж — клавиша обрабатывается не `QShortcut` (Enter в дереве/поиске, `Insert`/`Delete` — задачи 4–5).
  - `BasesView._show_current_properties() -> None`.
  - Подпись пункта меню: `"Свойства…\tAlt+Enter"`.

- [ ] **Step 1: Модуль таблицы сочетаний**

Создать `src/onecstarter/ui/shortcuts.py`:

```python
"""Сочетания клавиш раздела «Базы» — одна таблица для регистрации и справочника.

Решение заказчика 29.08.2026 (tasks.md, T-11, п. 3): сочетания зашиты,
настраиваемого keymap нет; раздел «Настройки» показывает их справочником
(только чтение, задача 3). Таблица живёт здесь, а не в `bases/view.py`,
чтобы справочник не импортировал вьюху; расхождение таблицы с реально
созданными `QShortcut` ловит `test_shortcut_reference_matches_registered_
shortcuts` в `tests/ui/test_bases_view.py`.

`sequences` — строки `QKeySequence`, которыми вьюха регистрирует сочетание.
Пустой кортеж — клавиша обрабатывается не `QShortcut`: Enter — сигналами
`activated`/`returnPressed`, `Insert`/`Delete` — `keyPressEvent` дерева
(только при фокусе в нём, T-11 пп. 7–8).
"""  # noqa: RUF002

from dataclasses import dataclass


@dataclass(frozen=True)
class ShortcutSpec:
    label: str
    title: str
    sequences: tuple[str, ...]


BASES_SHORTCUTS: tuple[ShortcutSpec, ...] = (
    ShortcutSpec("Enter", "Запустить выбранную базу; в поиске — первую найденную", ()),
    ShortcutSpec("F3", "Запустить (1С:Предприятие)", ("F3",)),
    ShortcutSpec("F4", "Конфигуратор", ("F4",)),
    ShortcutSpec("Ctrl+1", "Тонкий клиент", ("Ctrl+1",)),
    ShortcutSpec("Ctrl+2", "Толстый клиент", ("Ctrl+2",)),
    ShortcutSpec("Ctrl+3", "Конфигуратор (то же, что F4)", ("Ctrl+3",)),
    ShortcutSpec("Alt+Enter", "Свойства записи или группы", ("Alt+Return", "Alt+Enter")),
    ShortcutSpec("Ctrl+D", "В избранное / убрать из избранного", ("Ctrl+D",)),
    ShortcutSpec("Ctrl+N", "Добавить базу", ("Ctrl+N",)),
    ShortcutSpec("Insert", "Добавить базу в группу текущей строки", ()),
    ShortcutSpec("Delete", "Удалить запись или группу из списка (с подтверждением)", ()),
    ShortcutSpec("Alt+↑ / Alt+↓", "Переставить запись внутри группы", ("Alt+Up", "Alt+Down")),
)
```

- [ ] **Step 2: Падающие тесты**

В `tests/ui/test_bases_view.py`: к импортам добавить `from PySide6.QtGui import QDragMoveEvent, QDropEvent, QKeyEvent, QKeySequence, QShortcut` (расширить существующую строку) и `from onecstarter.ui.shortcuts import BASES_SHORTCUTS`. Изменить существующий `test_context_menu_has_properties_action`: последнюю строку заменить на

```python
    assert "Свойства…\tAlt+Enter" in texts
```

Добавить тесты (после `test_f4_launches_designer` — там уже есть `_show_exposed`):

```python
def test_shortcut_reference_matches_registered_shortcuts(qtbot, workspace_factory):
    """Таблица `BASES_SHORTCUTS` — то, что вьюха реально регистрирует (T-11, п. 3).

    Справочник в настройках (задача 3) рисуется по этой таблице; сочетание,
    записанное в таблице, но не созданное вьюхой, было бы враньём в UI.
    """
    view, _, _, _ = _view(qtbot, workspace_factory)
    registered = {shortcut.key().toString() for shortcut in view.findChildren(QShortcut)}
    expected = {
        QKeySequence(sequence).toString()
        for spec in BASES_SHORTCUTS
        for sequence in spec.sequences
    }
    assert expected <= registered, expected - registered


def test_alt_enter_opens_properties_of_the_current_base(qtbot, workspace_factory, monkeypatch):
    """`Alt+Enter` — «Свойства…» текущей записи (решение заказчика 29.08.2026: зашить)."""
    view, _, _, _ = _view(qtbot, workspace_factory)
    _show_exposed(qtbot, view)
    _select_key(view, _DEMO_ACCOUNTING_KEY)
    shown: list[str] = []
    monkeypatch.setattr(view, "show_properties", shown.append)

    qtbot.keyClick(view, Qt.Key.Key_Return, Qt.KeyboardModifier.AltModifier)

    assert shown == [_DEMO_ACCOUNTING_KEY]


def test_alt_enter_on_a_group_opens_the_group_dialog(qtbot, workspace_factory, monkeypatch):
    view, _, _, _ = _view(qtbot, workspace_factory)
    _show_exposed(qtbot, view)
    _select_key(view, _CLIENTS_KEY)
    shown: list[str] = []
    monkeypatch.setattr(view, "rename_group", shown.append)

    qtbot.keyClick(view, Qt.Key.Key_Return, Qt.KeyboardModifier.AltModifier)

    assert shown == [_CLIENTS_KEY]


def test_alt_enter_on_common_record_does_nothing(
    qtbot, workspace_factory, common_base_cfg_paths, monkeypatch
):
    """ЗАЩИТНЫЙ ТЕСТ: запись общего списка — тот же порог, что у пункта меню.

    Мутация: убрать проверку `item.source is InfobaseSource.COMMON`
    в `_show_current_properties` — откроется диалог, чей «ОК» отвергнет
    `ReadOnlySourceError` (`_build_menu` для таких записей гасит «Свойства…»).
    """  # noqa: RUF002
    view, _, _, _ = _view(qtbot, workspace_factory, cfg_paths=common_base_cfg_paths)
    _show_exposed(qtbot, view)
    _select_key(view, COMMON_BASE_KEY)
    shown: list[str] = []
    monkeypatch.setattr(view, "show_properties", shown.append)

    qtbot.keyClick(view, Qt.Key.Key_Return, Qt.KeyboardModifier.AltModifier)

    assert shown == []
```

`_DEMO_ACCOUNTING_KEY`, `_CLIENTS_KEY`, `COMMON_BASE_KEY` уже определены в файле/`conftest.py`.

- [ ] **Step 3: Убедиться, что тесты падают**

Run: `uv run pytest tests/ui/test_bases_view.py -q -k "shortcut_reference or alt_enter or properties_action"`
Expected: FAIL — `ModuleNotFoundError: onecstarter.ui.shortcuts` до шага 1; после него — `expected - registered` содержит `Alt+Return`/`Alt+Enter`, `shown == []`, подпись меню без `\tAlt+Enter`.

- [ ] **Step 4: Реализация во вьюхе**

В `src/onecstarter/ui/bases/view.py`, `__init__`, после строк `QShortcut(QKeySequence("Alt+Down"), ...)`:

```python
        # T-11, п. 3 (решение заказчика 29.08.2026 — зашить, не keymap):
        # Alt+Enter — «Свойства», как в Проводнике Windows. Return и Enter  # noqa: RUF003
        # (цифровой блок) — разные клавиши Qt, регистрируются обе; справочник
        # в настройках рисуется по ui/shortcuts.py, тест сверяет его с этими
        # регистрациями.
        QShortcut(QKeySequence("Alt+Return"), self, self._show_current_properties)
        QShortcut(QKeySequence("Alt+Enter"), self, self._show_current_properties)
```

В `_build_menu` заменить `properties = menu.addAction("Свойства…", ...)` на:

```python
        properties = menu.addAction("Свойства…\tAlt+Enter", lambda: self.show_properties(key))
```

После `_toggle_current_favorite` добавить:

```python
    def _show_current_properties(self) -> None:
        """`Alt+Enter` — свойства текущей строки (T-11, п. 3).

        Запись → `show_properties`, группа → диалог группы (`rename_group`).
        Тот же порог, что у пунктов меню: запись или группа общего списка
        (`_build_menu` гасит «Свойства…», `_group_menu_for` — всё меню) —
        бездействие, а не диалог, который отвергнут при «ОК». Неявный узел,
        заголовок ветки, пустое дерево — бездействие.
        """  # noqa: RUF002
        index = self._tree.currentIndex().siblingAtColumn(0)
        if not index.isValid():
            return
        kind = index.data(KIND_ROLE)
        key = index.data(KEY_ROLE)
        if not isinstance(key, str):
            return
        item = next((i for i in self._workspace.items() if i.key == key), None)
        if item is None or item.source is InfobaseSource.COMMON:
            return
        if kind == RowKind.BASE.value:
            self.show_properties(key)
        elif kind == RowKind.GROUP.value:
            self.rename_group(key)
```

- [ ] **Step 5: Тесты зелёные, коммит**

Run: `uv run pytest tests/ui/test_bases_view.py -q`, затем `uv run ruff check .`, `uv run mypy`
Expected: PASS, коды 0.

```bash
git add src/onecstarter/ui/shortcuts.py src/onecstarter/ui/bases/view.py tests/ui/test_bases_view.py
git commit -m "feat: T-11 п. 3 — Alt+Enter открывает свойства, таблица сочетаний ui/shortcuts.py"
```

---

### Task 3: Справочник сочетаний в группе «ГОРЯЧИЕ КЛАВИШИ» (п. 3, часть 2)

**Files:**
- Modify: `src/onecstarter/ui/settings_view.py` (`__init__` после строки глобального хоткея; новые `_add_block`, `_build_shortcut_reference`, аксессор `shortcut_reference_rows`)
- Test: `tests/ui/test_settings_view.py`

**Interfaces:**
- Consumes: `BASES_SHORTCUTS` из `onecstarter.ui.shortcuts` (задача 2).
- Produces: `SettingsView.shortcut_reference_rows() -> list[tuple[str, str]]`; строка настроек с заголовком `"Сочетания раздела «Базы»"` (зарегистрирована в `row_note`/`row_control`).

- [ ] **Step 1: Падающий тест**

В `tests/ui/test_settings_view.py` добавить импорт `from onecstarter.ui.shortcuts import BASES_SHORTCUTS` и тест после `test_hotkey_can_be_cleared`:

```python
def test_shortcut_reference_lists_every_bases_shortcut(
    application: QApplication, tmp_path: Path
) -> None:
    """Справочник сочетаний — по `BASES_SHORTCUTS`, целиком и в её порядке (T-11, п. 3)."""
    view, _ = _view(application, tmp_path)
    assert view.shortcut_reference_rows() == [
        (spec.label, spec.title) for spec in BASES_SHORTCUTS
    ]
    assert "не меняются" in view.row_note("Сочетания раздела «Базы»").text()
    assert view.row_control("Сочетания раздела «Базы»").isHidden() is False
```

- [ ] **Step 2: Убедиться, что тест падает**

Run: `uv run pytest tests/ui/test_settings_view.py::test_shortcut_reference_lists_every_bases_shortcut -q`
Expected: FAIL — `AttributeError: 'SettingsView' object has no attribute 'shortcut_reference_rows'`.

- [ ] **Step 3: Реализация**

В `src/onecstarter/ui/settings_view.py`: импорт `QGridLayout` в список `PySide6.QtWidgets`; `from onecstarter.ui.shortcuts import BASES_SHORTCUTS`. В `__init__` сразу после `self._add_row("Глобальный вызов окна", ...)`:

```python
        self._shortcut_rows: list[tuple[str, str]] = []
        self._add_block(
            "Сочетания раздела «Базы»",
            "Зашиты в программу и не меняются (решение заказчика 29.08.2026)",
            self._build_shortcut_reference(),
        )
```

После `_add_row` добавить:

```python
    def _add_block(self, title: str, note: str, body: QWidget) -> None:
        """Строка настроек во всю ширину: заголовок, подпись, тело под ними.

        Для справочной таблицы `_add_row` не годится: та ставит орган
        управления справа узкой колонкой, а таблице нужна ширина раздела.
        Регистрируется в `_row_notes`/`_row_controls` так же, как строки
        `_add_row`, — тесты находят её теми же аксессорами.
        """
        row_title = QLabel(title)
        row_note = QLabel(note)
        row_note.setObjectName("SettingsNote")
        row_note.setWordWrap(True)
        self._row_notes[title] = row_note
        self._row_controls[title] = body
        self._layout.addWidget(row_title)
        self._layout.addWidget(row_note)
        self._layout.addWidget(body)

    def _build_shortcut_reference(self) -> QWidget:
        """Таблица «сочетание — действие» по `BASES_SHORTCUTS` (T-11, п. 3, только чтение)."""
        table = QWidget()
        grid = QGridLayout(table)
        grid.setContentsMargins(0, 0, 0, 0)
        grid.setHorizontalSpacing(18)
        grid.setVerticalSpacing(2)
        for row, spec in enumerate(BASES_SHORTCUTS):
            keys = QLabel(spec.label)
            keys_font = keys.font()
            keys_font.setBold(True)
            keys.setFont(keys_font)
            grid.addWidget(keys, row, 0)
            grid.addWidget(QLabel(spec.title), row, 1)
            self._shortcut_rows.append((spec.label, spec.title))
        grid.setColumnStretch(1, 1)
        return table
```

В блок `# --- доступ для тестов`:

```python
    def shortcut_reference_rows(self) -> list[tuple[str, str]]:
        """Строки справочника сочетаний в порядке показа — что реально попало в таблицу."""
        return list(self._shortcut_rows)
```

- [ ] **Step 4: Тесты зелёные, коммит**

Run: `uv run pytest tests/ui/test_settings_view.py -q` (в т. ч. `test_groups_are_in_section_order` — порядок групп не меняется), `uv run ruff check .`, `uv run mypy`
Expected: PASS, коды 0.

```bash
git add src/onecstarter/ui/settings_view.py tests/ui/test_settings_view.py
git commit -m "feat: T-11 п. 3 — справочник сочетаний раздела «Базы» в группе ГОРЯЧИЕ КЛАВИШИ"
```

---

### Task 4: «Добавить базу…» из меню группы и по `Insert` (пп. 4, 7)

**Files:**
- Modify: `src/onecstarter/ui/bases/view.py` (`_BasesTree.keyPressEvent`; `_build_add_dialog(folder)`; `add_infobase(folder)`; `_build_group_menu`, `_build_disabled_group_menu`, `_build_empty_space_menu`; регистрация `Ctrl+N`; новый `_add_infobase_at_current`; аксессор `tree()`)
- Test: `tests/ui/test_bases_view.py`

**Interfaces:**
- Produces:
  - `BasesView._build_add_dialog(folder: str = ROOT) -> InfobaseDialog` — диалог с предустановленной группой (`dialog.set_folder(folder)`; `folder` — путь в форме `_group_paths()`, т. е. `normalize_folder`, корень `"/"`).
  - `BasesView.add_infobase(folder: str = ROOT) -> None`.
  - `BasesView._add_infobase_at_current() -> None` — `Insert`.
  - `BasesView.tree() -> QTreeView` — аксессор для тестов.
  - `_BasesTree.keyPressEvent`: `Insert` без модификаторов → `view._add_infobase_at_current()`; `Delete` без модификаторов → `view._remove_current()` (метод появляется в задаче 5 — здесь ветка `Delete` ещё НЕ добавляется).
  - Меню группы: первый пункт `"Добавить базу…"`; недоступное меню (`_build_disabled_group_menu`) — четыре пункта, первый `"Добавить базу…"`.

Важно про `addAction`/`QShortcut` и параметр `folder`: `QAction.triggered` передаёт `checked: bool`, и PySide подставит его первым позиционным аргументом — `add_infobase(False)` вместо `add_infobase()`. Поэтому все подключения `add_infobase` идут через `lambda: self.add_infobase(...)`, не через голую ссылку на метод.

- [ ] **Step 1: Падающие тесты**

В `tests/ui/test_bases_view.py` изменить существующий `test_common_group_context_menu_disables_all_three_actions`: ожидаемый список — четыре пункта:

```python
    assert texts == [
        "Добавить базу…", "Создать группу…", "Переименовать группу…", "Удалить группу…"
    ]
```

и переименовать его в `test_common_group_context_menu_disables_all_four_actions` (докстринг оставить, дописав строку «T-11, п. 4: четвёртый пункт «Добавить базу…» — тем же правилом»). Тест неявного узла (`test_implicit_group_context_menu_disables_all_three_actions`, строка ~1660) тоже сверяет точный список подписей — переименовать в `..._four_actions` и обновить ожидание тем же способом (находка исполнителя задачи 4: план ошибочно считал, что тест проверяет только тултипы).

Добавить тесты после `test_group_menu_for_user_group_offers_full_operations`:

```python
def test_group_menu_offers_to_add_a_base_into_that_group(qtbot, workspace_factory, monkeypatch):
    """T-11, п. 4: «Добавить базу…» в меню группы — база попадает в эту группу."""
    view, _, _, _ = _view(qtbot, workspace_factory)
    item = next(i for i in view.workspace().items() if i.key == _CLIENTS_KEY)
    folders: list[str] = []
    monkeypatch.setattr(view, "add_infobase", folders.append)

    menu = view._build_group_menu(item, _CLIENTS_KEY)
    actions = {action.text(): action for action in menu.actions()}
    assert list(actions)[0] == "Добавить базу…"
    actions["Добавить базу…"].trigger()

    assert folders == ["Клиенты"]


def test_add_dialog_is_prefilled_with_the_requested_group(qtbot, workspace_factory):
    view, _, _, _ = _view(qtbot, workspace_factory)
    dialog = view._build_add_dialog("Клиенты")
    qtbot.addWidget(dialog)
    dialog.set_kind(ConnectKind.FILE)
    dialog.set_file_path(r"D:\bases\new")
    dialog.set_name("Новая")
    assert dialog.new_record()[2] == "Клиенты"


def test_empty_space_menu_add_goes_to_root(qtbot, workspace_factory, monkeypatch):
    """`QAction.triggered` несёт `checked`; голая ссылка на метод получила бы его как `folder`."""
    view, _, _, _ = _view(qtbot, workspace_factory)
    folders: list[str] = []
    monkeypatch.setattr(view, "add_infobase", folders.append)

    menu = view._build_empty_space_menu()
    next(a for a in menu.actions() if a.text() == "Добавить базу…").trigger()

    assert folders == [ROOT]


def test_insert_adds_into_the_group_of_the_current_row(qtbot, workspace_factory, monkeypatch):
    """ЗАЩИТНЫЙ ТЕСТ (T-11, п. 7): `Insert` в дереве — группа текущей строки, не корень.

    Мутация: `_add_infobase_at_current` передаёт `ROOT` всегда — обе проверки
    ниже дадут `"/"` вместо `"Клиенты"`.
    """
    view, _, _, _ = _view(qtbot, workspace_factory)
    folders: list[str] = []
    monkeypatch.setattr(view, "add_infobase", folders.append)

    _select_key(view, _CLIENTS_KEY)
    qtbot.keyClick(view.tree(), Qt.Key.Key_Insert)
    _select_key(view, _DEMO_ACCOUNTING_KEY)
    qtbot.keyClick(view.tree(), Qt.Key.Key_Insert)

    assert folders == ["Клиенты", "Клиенты"]


def test_insert_without_current_row_adds_into_root(qtbot, workspace_factory, monkeypatch):
    view, _, _, _ = _view(qtbot, workspace_factory)
    folders: list[str] = []
    monkeypatch.setattr(view, "add_infobase", folders.append)
    view.tree().setCurrentIndex(QModelIndex())

    qtbot.keyClick(view.tree(), Qt.Key.Key_Insert)

    assert folders == [ROOT]


def test_insert_in_search_field_does_not_add(qtbot, workspace_factory, monkeypatch):
    """`Insert` — операция дерева, а не окна: в поле поиска это режим ввода, не добавление."""
    view, _, _, _ = _view(qtbot, workspace_factory)
    folders: list[str] = []
    monkeypatch.setattr(view, "add_infobase", folders.append)

    qtbot.keyClick(view.search(), Qt.Key.Key_Insert)

    assert folders == []
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `uv run pytest tests/ui/test_bases_view.py -q -k "insert or add_a_base_into or prefilled_with_the_requested or add_goes_to_root or disables_all_four"`
Expected: FAIL — `AttributeError: tree`, `_build_add_dialog() takes 1 positional argument`, список из трёх пунктов.

- [ ] **Step 3: Реализация**

`src/onecstarter/ui/bases/view.py`:

1. Импорт `QKeyEvent` — добавить в список `from PySide6.QtGui import (...)`.

2. В `_BasesTree` (после `__init__`):

```python
    def keyPressEvent(self, event: QKeyEvent) -> None:  # noqa: N802
        """`Insert`/`Delete` — операции над списком, но только при фокусе в дереве.

        T-11, пп. 7–8: `QShortcut` уровня окна (как у F3/Ctrl+N) перехватил бы
        `Delete` и в поле поиска, где это правка текста. Обработчик дерева
        ограничивает клавиши структурно, без проверки «кто в фокусе».
        Модификаторы не допускаются: `Ctrl+Insert`/`Shift+Delete` — чужие
        соглашения (копировать/вырезать), их не перехватываем.
        """
        if event.modifiers() == Qt.KeyboardModifier.NoModifier:
            if event.key() == Qt.Key.Key_Insert:
                self._view._add_infobase_at_current()
                event.accept()
                return
        super().keyPressEvent(event)
```

3. В блоке `# -- доступ для тестов, трея и оболочки` после `def search`:

```python
    def tree(self) -> QTreeView:
        return self._tree
```

4. `Ctrl+N`: заменить `QShortcut(QKeySequence("Ctrl+N"), self, self.add_infobase)` на

```python
        QShortcut(QKeySequence("Ctrl+N"), self, lambda: self.add_infobase())
```

5. `_build_add_dialog` — сигнатура и тело:

```python
    def _build_add_dialog(self, folder: str = ROOT) -> InfobaseDialog:
        """Собрать диалог добавления записи без показа (для тестов и add_infobase).

        Тот же приём, что у `_build_menu`/`_build_properties_dialog`: `exec()`
        блокирует офскрин-тесты, поэтому сборка отделена от показа. Без этого
        разделения `add_infobase` повторил бы дефект, который ревью задачи 8
        нашло у `show_properties`, — единственный пользовательский путь без
        покрытия (задача 10, урок 2).

        `folder` — группа, предложенная диалогом (T-11, пп. 4 и 7), в форме
        `_group_paths()`; вызывающие берут её оттуда же (`group_path` у меню
        группы, `folder_for_dropped_directory` у `Insert`), поэтому
        `set_folder` здесь не отказывает.
        """  # noqa: RUF002
        dialog = InfobaseDialog.for_new(
            groups=self._group_paths(),
            installations=self._installations or [],
            cfg_rules=self._cfg_rules,
            parent=self,
        )
        dialog.set_folder(folder)
        return dialog
```

(комментарий про обнаружение платформ из прежней версии оставить над `installations=`).

6. `add_infobase`:

```python
    def add_infobase(self, folder: str = ROOT) -> None:
        """«Добавить базу…» — пустое место дерева (корень), меню группы (в неё), `Ctrl+N`, `Insert`."""
        dialog = self._build_add_dialog(folder)
        if dialog.exec() != QDialog.DialogCode.Accepted:
            return
        self._apply_new_infobase(dialog)
```

7. Новый метод после `add_infobase`:

```python
    def _add_infobase_at_current(self) -> None:
        """`Insert` — добавить базу в группу текущей строки (T-11, п. 7).

        Цель — по тому же правилу, что у брошенного каталога
        (`folder_for_dropped_directory`): группа → в неё, запись → её группа,
        всё остальное (пусто, виртуальная ветка, неявный узел, запись общего
        списка) — корень.
        """
        index = self._tree.currentIndex().siblingAtColumn(0)
        key = index.data(KEY_ROLE) if index.isValid() else None
        kind = index.data(KIND_ROLE) if index.isValid() else None
        self.add_infobase(
            self.folder_for_dropped_directory(
                key if isinstance(key, str) else None,
                kind if isinstance(kind, str) else None,
            )
        )
```

8. Меню: в `_build_empty_space_menu` — `menu.addAction("Добавить базу…", lambda: self.add_infobase(ROOT))` (явный `ROOT`, не пустые скобки: тест `test_empty_space_menu_add_goes_to_root` подменяет `add_infobase` на `folders.append`, у которого дефолта нет — находка исполнителя задачи 4); в `_build_group_menu` первым пунктом (до «Создать группу…»):

```python
        menu.addAction("Добавить базу…", lambda: self.add_infobase(own_path))
```

и дописать в докстринг: «Первый пункт — «Добавить базу…» с группой `own_path` (T-11, п. 4)». В `_build_disabled_group_menu` кортеж подписей — `("Добавить базу…", "Создать группу…", "Переименовать группу…", "Удалить группу…")`, в докстринге «три пункта» → «четыре пункта».

- [ ] **Step 4: Тесты зелёные, коммит**

Run: `uv run pytest tests/ui/test_bases_view.py -q`, `uv run ruff check .`, `uv run mypy`
Expected: PASS, коды 0.

```bash
git add src/onecstarter/ui/bases/view.py tests/ui/test_bases_view.py
git commit -m "feat: T-11 пп. 4, 7 — «Добавить базу…» из меню группы и по Insert в группу текущей строки"
```

---

### Task 5: Удаление по `Delete` (п. 8)

**Files:**
- Modify: `src/onecstarter/ui/bases/view.py` (`_BasesTree.keyPressEvent`: ветка `Delete`; новый `_remove_current`)
- Test: `tests/ui/test_bases_view.py`

**Interfaces:**
- Consumes: `remove_key(key)` (подтверждение `self._confirm_removal`), `remove_group(key)` (подтверждение `self._ask_group_removal`), `tree()` из задачи 4.
- Produces: `BasesView._remove_current() -> None`.

- [ ] **Step 1: Падающие тесты**

Добавить после `test_remove_key_reports_unknown_item_when_target_is_gone`:

```python
def test_delete_key_asks_and_removes_the_current_base(qtbot, workspace_factory):
    """T-11, п. 8: `Delete` в дереве — тот же путь, что «Удалить из списка…»."""
    asked: list[str] = []

    def _confirm(_parent: object, item: InfobaseItem) -> bool:
        asked.append(item.key)
        return True

    view, _, errors, _ = _view(qtbot, workspace_factory, confirm_removal=_confirm)
    _select_key(view, _DEMO_ACCOUNTING_KEY)

    qtbot.keyClick(view.tree(), Qt.Key.Key_Delete)

    assert asked == [_DEMO_ACCOUNTING_KEY]
    assert errors == []
    assert all(i.key != _DEMO_ACCOUNTING_KEY for i in view.workspace().items())


def test_delete_key_with_declined_confirmation_keeps_the_file(qtbot, workspace_factory):
    """ЗАЩИТНЫЙ ТЕСТ: отказ подтверждения — файл списка не тронут.

    Мутация: `_remove_current` зовёт `self._workspace.remove_infobase(key)`
    напрямую, минуя `remove_key` с его подтверждением, — байты файла изменятся.
    """
    asked: list[int] = []

    def _confirm(*_args: object) -> bool:
        asked.append(1)
        return False

    view, _, errors, _ = _view(qtbot, workspace_factory, confirm_removal=_confirm)
    before = view.workspace().paths.ibases.read_bytes()
    _select_key(view, _DEMO_ACCOUNTING_KEY)

    qtbot.keyClick(view.tree(), Qt.Key.Key_Delete)

    assert asked == [1]
    assert view.workspace().paths.ibases.read_bytes() == before
    assert errors == []


def test_delete_key_on_a_group_goes_through_group_removal(qtbot, workspace_factory):
    asked: list[str] = []

    def _ask(_parent: object, name: str, *_rest: object) -> GroupRemoval | None:
        asked.append(name)
        return None

    view, _, errors, _ = _view(qtbot, workspace_factory, ask_group_removal=_ask)
    before = view.workspace().paths.ibases.read_bytes()
    _select_key(view, _CLIENTS_KEY)

    qtbot.keyClick(view.tree(), Qt.Key.Key_Delete)

    assert asked == ["Клиенты"]
    assert view.workspace().paths.ibases.read_bytes() == before
    assert errors == []


def test_delete_key_on_common_record_does_nothing(
    qtbot, workspace_factory, common_base_cfg_paths
):
    """ЗАЩИТНЫЙ ТЕСТ: запись общего списка — ни вопроса, ни ошибки (порог пункта меню).

    Мутация: убрать проверку `item.source is InfobaseSource.COMMON`
    в `_remove_current` — появится подтверждение, а за ним `ReadOnlySourceError`.
    """
    asked: list[int] = []

    def _confirm(*_args: object) -> bool:
        asked.append(1)
        return True

    view, _, errors, _ = _view(
        qtbot, workspace_factory, confirm_removal=_confirm, cfg_paths=common_base_cfg_paths
    )
    _select_key(view, COMMON_BASE_KEY)

    qtbot.keyClick(view.tree(), Qt.Key.Key_Delete)

    assert asked == []
    assert errors == []


def test_delete_in_search_field_edits_text_not_the_list(qtbot, workspace_factory):
    asked: list[int] = []

    def _confirm(*_args: object) -> bool:
        asked.append(1)
        return True

    view, _, _, _ = _view(qtbot, workspace_factory, confirm_removal=_confirm)
    _select_key(view, _DEMO_ACCOUNTING_KEY)
    view.search().setText("abc")
    view.search().setCursorPosition(0)

    qtbot.keyClick(view.search(), Qt.Key.Key_Delete)

    assert view.search().text() == "bc"
    assert asked == []
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `uv run pytest tests/ui/test_bases_view.py -q -k delete_key`
Expected: FAIL — `asked == []` там, где ждём вопрос; файл нетронут «по ошибке» (ветки `Delete` ещё нет).

- [ ] **Step 3: Реализация**

В `_BasesTree.keyPressEvent` внутри `if event.modifiers() == NoModifier:` после ветки `Insert`:

```python
            if event.key() == Qt.Key.Key_Delete:
                self._view._remove_current()
                event.accept()
                return
```

В `BasesView` после `_add_infobase_at_current`:

```python
    def _remove_current(self) -> None:
        """`Delete` — удалить текущую запись или группу тем же путём, что пункт меню (T-11, п. 8).

        Подтверждение — внутри `remove_key`/`remove_group`, здесь не
        дублируется. Тот же порог, что у пунктов меню: запись или группа
        общего списка — бездействие без диалога (`_build_menu` гасит «Удалить
        из списка…», `_group_menu_for` — всё меню). Запись в «Недавних»/
        «Избранном» — удаление из списка (значение клавиши одно), не из ветки;
        от неожиданности защищает подтверждение с именем записи.
        """  # noqa: RUF002
        index = self._tree.currentIndex().siblingAtColumn(0)
        if not index.isValid():
            return
        kind = index.data(KIND_ROLE)
        key = index.data(KEY_ROLE)
        if not isinstance(key, str):
            return
        item = next((i for i in self._workspace.items() if i.key == key), None)
        if item is None or item.source is InfobaseSource.COMMON:
            return
        if kind == RowKind.BASE.value:
            self.remove_key(key)
        elif kind == RowKind.GROUP.value:
            self.remove_group(key)
```

- [ ] **Step 4: Тесты зелёные, коммит**

Run: `uv run pytest tests/ui/test_bases_view.py -q`, `uv run ruff check .`, `uv run mypy`
Expected: PASS, коды 0.

```bash
git add src/onecstarter/ui/bases/view.py tests/ui/test_bases_view.py
git commit -m "feat: T-11 п. 8 — Delete в дереве удаляет запись/группу тем же путём, что меню"
```

---

### Task 6: Автоимя из каталога и имени в кластере (п. 6)

**Files:**
- Modify: `src/onecstarter/ui/dialogs/infobase.py` (`__init__`: подключение `editingFinished`; `accept_directory`; новые `_suggest_name*`)
- Test: `tests/ui/test_infobase_dialog.py`

**Interfaces:**
- Produces: `InfobaseDialog._suggest_name(candidate: str) -> None` (правило одно: имя подставляется, только если пустое); `_suggest_name_from_path()`, `_suggest_name_from_ref()` — слоты `editingFinished` полей `_file_path` и `_ref`.

Сейчас имя подставляется только «Обзором…» и drag&drop (`accept_directory`). Для набранного руками пути и для серверной базы (`Ref`) — нет. `editingFinished`, не `textChanged`: по `textChanged` имя подставилось бы на первом символе и дальше не обновлялось. Веб-база (`ws`) — не запрошена, не делается.

- [ ] **Step 1: Падающие тесты**

В `tests/ui/test_infobase_dialog.py` после `test_browse_button_does_nothing_when_cancelled`:

```python
# -- T-11, п. 6: автоимя из набранного пути и имени в кластере --------------


def test_typed_file_path_suggests_the_directory_name(qtbot: Any) -> None:
    dialog = InfobaseDialog.for_new(groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    dialog.set_kind(ConnectKind.FILE)
    dialog.set_file_path(r"D:\bases\Зарплата")

    dialog._file_path.editingFinished.emit()

    assert dialog.name_text() == "Зарплата"


def test_typed_cluster_ref_suggests_the_name(qtbot: Any) -> None:
    dialog = InfobaseDialog.for_new(groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    dialog.set_kind(ConnectKind.SERVER)
    dialog.set_server("srv")
    dialog.set_ref("ACC_2026")

    dialog._ref.editingFinished.emit()

    assert dialog.name_text() == "ACC_2026"


def test_typed_name_survives_path_edit(qtbot: Any) -> None:
    """ЗАЩИТНЫЙ ТЕСТ: подстановка только в пустое имя — введённое не перезаписывается.

    Мутация: `_suggest_name` без проверки `not self._name.text().strip()`.
    """
    dialog = InfobaseDialog.for_new(groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    dialog.set_name("Своё имя")
    dialog.set_kind(ConnectKind.FILE)
    dialog.set_file_path(r"D:\bases\Зарплата")

    dialog._file_path.editingFinished.emit()

    assert dialog.name_text() == "Своё имя"


def test_drive_root_suggests_nothing(qtbot: Any) -> None:
    dialog = InfobaseDialog.for_new(groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    dialog.set_kind(ConnectKind.FILE)
    dialog.set_file_path("D:\\")

    dialog._file_path.editingFinished.emit()

    assert dialog.name_text() == ""


def test_editing_dialog_keeps_its_name_on_path_edit(qtbot: Any) -> None:
    item = _item('File="D:\\b";', ())
    dialog = InfobaseDialog(item, groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    dialog.set_file_path(r"E:\c\Другая")

    dialog._file_path.editingFinished.emit()

    assert dialog.name_text() == "Бухгалтерия"
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `uv run pytest tests/ui/test_infobase_dialog.py -q -k "suggests or survives_path_edit or keeps_its_name_on_path"`
Expected: FAIL — `name_text() == ""` в первых двух.

- [ ] **Step 3: Реализация**

В `InfobaseDialog.__init__` после `self._kind_box.currentIndexChanged.connect(self._refresh_ok_state)`:

```python
        # T-11, п. 6: имя из каталога / имени в кластере, если поле имени
        # пустое. editingFinished, не textChanged: по textChanged имя  # noqa: RUF003
        # подставилось бы на первом же символе и дальше не обновлялось.
        self._file_path.editingFinished.connect(self._suggest_name_from_path)
        self._ref.editingFinished.connect(self._suggest_name_from_ref)
```

`accept_directory`: заменить две последние строки (`if not self._name.text().strip(): self._name.setText(Path(path).name)`) на `self._suggest_name(Path(path).name)`.

Новые методы после `accept_directory`:

```python
    def _suggest_name_from_path(self) -> None:
        self._suggest_name(Path(self._file_path.text().strip()).name)

    def _suggest_name_from_ref(self) -> None:
        self._suggest_name(self._ref.text().strip())

    def _suggest_name(self, candidate: str) -> None:
        """Подставить имя, только если поле пустое — одно правило на «Обзор…», drag&drop и ввод.

        Введённое или уже подставленное имя при смене пути не перезаписывается:
        второе правило («обновлять авто-подставленное») потребовало бы помнить,
        откуда имя взялось, и удивляло бы при правке пути у уже названной базы.
        Пустой кандидат (`D:\\`, пустое поле) — ничего не подставляется.
        """  # noqa: RUF002
        if candidate and not self._name.text().strip():
            self._name.setText(candidate)
```

- [ ] **Step 4: Тесты зелёные, коммит**

Run: `uv run pytest tests/ui/test_infobase_dialog.py -q`, `uv run ruff check .`, `uv run mypy`
Expected: PASS (в т. ч. три прежних теста «Обзор…»), коды 0.

```bash
git add src/onecstarter/ui/dialogs/infobase.py tests/ui/test_infobase_dialog.py
git commit -m "feat: T-11 п. 6 — автоимя из набранного каталога и имени в кластере (только в пустое имя)"
```

---

### Task 7: Выбор группы деревом — `GroupPicker` в обоих диалогах (п. 5)

**Files:**
- Create: `src/onecstarter/ui/dialogs/group_picker.py`
- Modify: `src/onecstarter/ui/dialogs/infobase.py` (все `self._folder.*`: конструктор, `groups_shown`, `new_record`, `set_folder`, `changes`), `src/onecstarter/ui/dialogs/group.py` (все `self._parent_box.*`)
- Test: `tests/ui/test_group_picker.py` (новый), `tests/ui/test_infobase_dialog.py`, `tests/ui/test_group_dialog.py`

**Interfaces:**
- Produces:
  - `build_items(paths: Sequence[str]) -> list[tuple[str, str]]` — пары `(подпись, путь)` в порядке дерева: корень первым (`ROOT_LABEL = "/ (корень)"`), далее по сегментам пути (родитель раньше потомков, братья по алфавиту `str`-сортировки сегментов); подпись — последний сегмент с отступом `INDENT * глубина`, а путь, чей родитель в наборе отсутствует (неявная группа), — целиком, без отступа.
  - `class GroupPicker(QComboBox)`: `__init__(paths, parent=None)`; `paths() -> list[str]`; `current_path() -> str`; `set_current_path(path) -> None` (`ValueError`, если пути нет); `ensure_path(path) -> None` (добавить путь с сохранением порядка дерева и текущего выбора).
  - Контракты диалогов НЕ меняются: `InfobaseDialog.groups_shown()/set_folder()/new_record()/changes()`, `GroupDialog.parent_path()/set_parent_path()` — те же значения (пути), что раньше.

Форма реализации (решение исполнителя, tasks.md T-11 п. 5 — «вариант реализации — исполнителю»): `QComboBox` с отступами вместо `QTreeView` в popup. Настоящее дерево в popup требует ручной обработки разворачивания узлов и выбора родителя, а список групп заказчика — десяток строк: отступ показывает ту же иерархию, клавиатурная навигация остаётся штатной.

- [ ] **Step 1: Падающие тесты**

Создать `tests/ui/test_group_picker.py`:

```python
"""Выбор группы деревом — `GroupPicker` (T-11, п. 5)."""

from typing import Any

import pytest

from onecstarter.ui.dialogs.group_picker import INDENT, ROOT_LABEL, GroupPicker, build_items


def test_items_follow_tree_order_with_indent() -> None:
    assert build_items(["/", "Клиенты/Розница", "Клиенты", "Архив"]) == [
        (ROOT_LABEL, "/"),
        ("Архив", "Архив"),
        ("Клиенты", "Клиенты"),
        (f"{INDENT}Розница", "Клиенты/Розница"),
    ]


def test_root_is_added_even_when_absent() -> None:
    assert build_items(["Клиенты"])[0] == (ROOT_LABEL, "/")


def test_path_without_parent_is_shown_whole() -> None:
    """Неявная группа записи ([Ф] T-05.7): родителя в списке нет — путь целиком, без отступа."""
    assert build_items(["/", "Нет такой группы/Вложенная"]) == [
        (ROOT_LABEL, "/"),
        ("Нет такой группы/Вложенная", "Нет такой группы/Вложенная"),
    ]


def test_picker_reports_paths_not_labels(qtbot: Any) -> None:
    picker = GroupPicker(["/", "Клиенты", "Клиенты/Розница"])
    qtbot.addWidget(picker)
    assert picker.paths() == ["/", "Клиенты", "Клиенты/Розница"]
    assert picker.current_path() == "/"


def test_set_current_path_selects_by_path(qtbot: Any) -> None:
    picker = GroupPicker(["/", "Клиенты", "Клиенты/Розница"])
    qtbot.addWidget(picker)
    picker.set_current_path("Клиенты/Розница")
    assert picker.current_path() == "Клиенты/Розница"
    assert picker.currentText() == f"{INDENT}Розница"


def test_set_current_path_rejects_unknown_path(qtbot: Any) -> None:
    picker = GroupPicker(["/"])
    qtbot.addWidget(picker)
    with pytest.raises(ValueError, match="Незнакомая"):
        picker.set_current_path("Незнакомая")


def test_ensure_path_keeps_order_and_selection(qtbot: Any) -> None:
    picker = GroupPicker(["/", "Клиенты"])
    qtbot.addWidget(picker)
    picker.set_current_path("Клиенты")
    picker.ensure_path("Архив")
    assert picker.paths() == ["/", "Архив", "Клиенты"]
    assert picker.current_path() == "Клиенты"
    picker.ensure_path("Клиенты")
    assert picker.paths() == ["/", "Архив", "Клиенты"]
```

В `tests/ui/test_infobase_dialog.py` добавить:

```python
def test_folder_picker_shows_groups_as_a_tree(qtbot: Any) -> None:
    """T-11, п. 5: выпадающий список групп — с отступом по вложенности, значения — пути."""
    item = _item('File="D:\\b";', (), folder="/Клиенты/Розница")
    dialog = InfobaseDialog(
        item, groups=["/", "Клиенты", "Клиенты/Розница"], installations=INSTALLED, cfg_rules=[]
    )
    qtbot.addWidget(dialog)
    assert dialog.groups_shown() == ["/", "Клиенты", "Клиенты/Розница"]
    assert dialog._folder.currentText() == f"{INDENT}Розница"
    assert dialog.changes() == ({}, None)
```

(импорт `from onecstarter.ui.dialogs.group_picker import INDENT`). В `tests/ui/test_group_dialog.py`:

```python
def test_parent_picker_shows_groups_as_a_tree(qtbot: Any) -> None:
    """Группы первого уровня — без отступа, второго — с одним `INDENT`; значения — пути."""
    item = _group_item("Магазин", folder="/Клиенты/Розница")
    dialog = GroupDialog(item, ["/", "Клиенты", "Клиенты/Розница"])
    qtbot.addWidget(dialog)
    assert dialog._parent_box.currentText() == f"{INDENT}Розница"
    assert dialog.parent_path() == "Клиенты/Розница"
    assert dialog._parent_box.paths() == ["/", "Клиенты", "Клиенты/Розница"]
```

(импорт `INDENT` там же).

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `uv run pytest tests/ui/test_group_picker.py tests/ui/test_infobase_dialog.py tests/ui/test_group_dialog.py -q`
Expected: FAIL — `ModuleNotFoundError: onecstarter.ui.dialogs.group_picker`.

- [ ] **Step 3: Реализация `GroupPicker`**

Создать `src/onecstarter/ui/dialogs/group_picker.py`:

```python
"""Выбор группы деревом — общий орган диалогов записи и группы (T-11, п. 5).

`QComboBox`, пункты которого отступом повторяют вложенность групп: корень
первым, родитель всегда раньше потомков, глубина — по числу сегментов пути.
Значение пункта (`userData`) — нормализованный путь (`services/paths.
normalize_folder`), подпись — последний сегмент с отступом. Путь, чей
родитель в наборе отсутствует (неявная группа, [Ф] T-05.7), показывается
целиком: отступ без родителя над ним вводил бы в заблуждение.

Настоящий `QTreeView` в popup не взят намеренно: разворачивание узлов
и выбор родителя в popup требуют ручной обработки, а список групп
заказчика измеряется десятком строк — отступ показывает ту же иерархию,
клавиатурная навигация остаётся штатной.

Контракт для диалогов и тестов: `paths()` — пути в порядке показа,
`current_path()`/`set_current_path()` — выбранное; `set_current_path`
падает `ValueError` на пути, которого орган не предлагает (тот же приём,
что `InfobaseDialog.set_folder`, M9 круг правок 1).
"""  # noqa: RUF002

from collections.abc import Sequence

from PySide6.QtWidgets import QComboBox, QWidget

from onecstarter.services.paths import ROOT

INDENT = "    "
ROOT_LABEL = "/ (корень)"


def _segments(path: str) -> list[str]:
    return [] if path == ROOT else path.split("/")


def build_items(paths: Sequence[str]) -> list[tuple[str, str]]:
    """Пары (подпись, путь) в порядке дерева; путь без родителя в наборе — целиком."""
    ordered = sorted({ROOT, *paths}, key=_segments)
    known = set(ordered)
    items: list[tuple[str, str]] = []
    for path in ordered:
        segments = _segments(path)
        if not segments:
            items.append((ROOT_LABEL, path))
            continue
        parent = "/".join(segments[:-1]) or ROOT
        if parent in known:
            items.append((f"{INDENT * (len(segments) - 1)}{segments[-1]}", path))
        else:
            items.append((path, path))
    return items


class GroupPicker(QComboBox):
    def __init__(self, paths: Sequence[str], parent: QWidget | None = None) -> None:
        super().__init__(parent)
        self._fill(paths)

    def _fill(self, paths: Sequence[str]) -> None:
        for label, path in build_items(paths):
            self.addItem(label, path)

    def paths(self) -> list[str]:
        return [self.itemData(index) for index in range(self.count())]

    def current_path(self) -> str:
        data = self.currentData()
        return data if isinstance(data, str) else ROOT

    def set_current_path(self, path: str) -> None:
        index = self.findData(path)
        if index < 0:
            raise ValueError(f"орган не предлагает группу «{path}»")  # noqa: RUF001
        self.setCurrentIndex(index)

    def ensure_path(self, path: str) -> None:
        """Добавить путь, которого нет среди групп, сохранив порядок дерева и выбор.

        Список путей снимается ДО `clear()` — после очистки `paths()` пуст.
        """
        if self.findData(path) >= 0:
            return
        current = self.current_path()
        known = self.paths()
        self.clear()
        self._fill([*known, path])
        self.set_current_path(current)
```

- [ ] **Step 4: Диалоги на `GroupPicker`**

`src/onecstarter/ui/dialogs/infobase.py`: импорт `from onecstarter.ui.dialogs.group_picker import GroupPicker`; `QComboBox` остаётся (для `_kind_box`, `_version`, `_app`). Заменить блок конструктора

```python
        folder_options = list(groups)
        current_folder = normalize_folder(item.folder) if item is not None else ROOT
        if current_folder not in folder_options:
            folder_options.append(current_folder)
        self._folder = QComboBox()
        self._folder.addItems(folder_options)
        self._folder.setCurrentText(current_folder)
```

на

```python
        current_folder = normalize_folder(item.folder) if item is not None else ROOT
        self._folder = GroupPicker(groups)
        # Путь записи, отсутствующий среди групп (неявная группа, [Ф] T-05.7),
        # добавляется как есть — иначе «текущая» группа не совпала бы с фактом.
        self._folder.ensure_path(current_folder)
        self._folder.set_current_path(current_folder)
```

(комментарий про формы `groups`/`item.folder` над блоком сохранить). Далее: `groups_shown()` → `return self._folder.paths()`; `new_record()` — `self._folder.currentText()` → `self._folder.current_path()`; `changes()` — `folder = self._folder.currentText()` → `folder = self._folder.current_path()`; `set_folder`:

```python
    def set_folder(self, value: str) -> None:
        try:
            self._folder.set_current_path(value)
        except ValueError:
            raise ValueError(f"диалог не предлагает группу «{value}»") from None  # noqa: RUF001
```

`src/onecstarter/ui/dialogs/group.py`: импорт `GroupPicker`, убрать `QComboBox` из импортов; конструктор:

```python
        current_folder = normalize_folder(item.folder) if item is not None else default_folder
        self._parent_box = GroupPicker(groups)
        self._parent_box.ensure_path(current_folder)
        self._parent_box.set_current_path(current_folder)
```

`parent_path()` → `self._parent_box.current_path()`; `set_parent_path`:

```python
    def set_parent_path(self, value: str) -> None:
        try:
            self._parent_box.set_current_path(value)
        except ValueError:
            raise ValueError(f"диалог не предлагает путь «{value}»") from None  # noqa: RUF001
```

- [ ] **Step 5: Тесты зелёные, коммит**

Run: `uv run pytest tests/ui -q` (диалоги, `test_bases_view` — `set_folder`/`groups_shown`/`build_dialog_for_dropped_directory` идут через новый орган), `uv run ruff check .`, `uv run mypy`
Expected: PASS, коды 0.

```bash
git add src/onecstarter/ui/dialogs/group_picker.py src/onecstarter/ui/dialogs/infobase.py src/onecstarter/ui/dialogs/group.py tests/ui/test_group_picker.py tests/ui/test_infobase_dialog.py tests/ui/test_group_dialog.py
git commit -m "feat: T-11 п. 5 — выбор группы деревом (GroupPicker) в диалогах записи и группы"
```

---

### Task 8: Настройка «Запуск базы сворачивает окно в трей» (п. 9)

**Files:**
- Modify: `src/onecstarter/services/settings.py` (`Settings.hide_on_launch`, `_bool_of(default=)`, load/save)
- Modify: `src/onecstarter/ui/bases/view.py` (`launched = Signal(str)`, `launch_key`)
- Modify: `src/onecstarter/ui/settings_view.py` (тумблер в «ОКНО И ЗАПУСК», `_sync`, аксессор)
- Modify: `src/onecstarter/ui/app.py` (`hide_after_launch`)
- Test: `tests/unit/test_settings.py`, `tests/ui/test_bases_view.py`, `tests/ui/test_settings_view.py`, `tests/ui/test_app.py`

**Interfaces:**
- Produces:
  - `Settings.hide_on_launch: bool = False`; ключ файла `"hide_on_launch"`; `_bool_of(value: Any, *, default: bool = True) -> bool`.
  - `BasesView.launched: Signal(str)` — эмитируется ключом записи после УСПЕШНОГО `Workspace.launch` (при `ServicesError` — нет).
  - `SettingsView.hide_on_launch_checkbox() -> QCheckBox`; строка `"Запуск базы сворачивает окно в трей"`.
  - `ui/app.py`: `view.launched → hide_after_launch(key)`: `window.hide()` только при `store.settings.hide_on_launch and tray is not None`.

Решения заказчика 29.08.2026: (а) на запуск серверного профиля не действует — сигнал есть только у `BasesView`; (б) без трея молча не сворачивать (в панель задач не уходить).

- [ ] **Step 1: Падающие тесты — `services/settings.py`**

В `tests/unit/test_settings.py`: в `test_schema_is_written` добавить в ожидаемый словарь `"hide_on_launch": False`; в `test_defaults_of_new_fields` — `assert settings.hide_on_launch is False`; в `test_round_trip_keeps_all_fields` — `hide_on_launch=True` в конструкторе. Новый класс после `TestServersRoot`:

```python
class TestHideOnLaunch:
    """T-11, п. 9: запуск базы сворачивает окно в трей; дефолт — выключено."""

    def test_default_is_off(self) -> None:
        assert Settings().hide_on_launch is False

    def test_round_trip(self, tmp_path: Path) -> None:
        path = tmp_path / "settings.json"
        save_settings(path, Settings(hide_on_launch=True))
        assert load_settings(path).hide_on_launch is True

    def test_old_file_without_the_key_reads_off(self, tmp_path: Path) -> None:
        """ЗАЩИТНЫЙ ТЕСТ: файл прошлой версии без ключа — «выключено», не «включено».

        Мутация: `_bool_of(payload.get("hide_on_launch"))` без `default=False` —
        общий `_bool_of` отдаёт `True`, и у всех существующих установок окно
        начало бы прятаться при каждом запуске.
        """
        path = tmp_path / "settings.json"
        path.write_text(json.dumps({"schema": 1, "theme": "dark"}), encoding="utf-8")
        assert load_settings(path).hide_on_launch is False

    @pytest.mark.parametrize("value", ["да", 1, None, [], {}])
    def test_broken_value_falls_back_to_off(self, tmp_path: Path, value: object) -> None:
        path = tmp_path / "settings.json"
        path.write_text(json.dumps({"schema": 1, "hide_on_launch": value}), encoding="utf-8")
        assert load_settings(path).hide_on_launch is False
```

Run: `uv run pytest tests/unit/test_settings.py -q`
Expected: FAIL — `TypeError: unexpected keyword argument 'hide_on_launch'`.

- [ ] **Step 2: Реализация `services/settings.py`**

В `Settings` после `servers_root`:

```python
    # T-11, п. 9 (решение заказчика 29.08.2026): успешный запуск базы прячет
    # окно в трей. Дефолт False — поведение существующих установок не меняется.
    # На запуск серверного профиля не действует (решение (а)); без трея
    # не действует (решение (б)) — это решает проводка ui/app.py, не модель.
    hide_on_launch: bool = False
```

`load_settings`: `hide_on_launch=_bool_of(payload.get("hide_on_launch"), default=False),`; `save_settings`: `"hide_on_launch": settings.hide_on_launch,`. `_bool_of`:

```python
def _bool_of(value: Any, *, default: bool = True) -> bool:
    """Не-булево — не порча файла: дефолт поля, как у режима темы."""  # noqa: RUF002
    return value if isinstance(value, bool) else default
```

Run: `uv run pytest tests/unit/test_settings.py -q` → PASS.

- [ ] **Step 3: Падающие тесты — сигнал `launched`**

В `tests/ui/test_bases_view.py` после `test_launch_error_goes_to_handler_not_up`:

```python
def test_launched_is_emitted_after_a_successful_launch(qtbot, workspace_factory):
    view, calls, _, _ = _view(qtbot, workspace_factory)
    seen: list[str] = []
    view.launched.connect(seen.append)

    view.launch_key(_DEMO_ACCOUNTING_KEY)

    assert len(calls) == 1
    assert seen == [_DEMO_ACCOUNTING_KEY]


def test_launched_is_not_emitted_when_launch_fails(qtbot, workspace_factory, monkeypatch):
    """ЗАЩИТНЫЙ ТЕСТ: отказ запуска — окно обязано остаться, чтобы показать ошибку.

    Мутация: `self.launched.emit(key)` до `self._workspace.launch(...)` или вне
    `try` — сигнал уйдёт и при `LaunchError`. Отказ подстроен подменой
    `Workspace.launch`: `workspace_factory` при `installations=None` сама
    подставляет `INSTALLED` (см. её докстринг), pending-состояния через
    `_view` не получить.
    """
    view, calls, errors, _ = _view(qtbot, workspace_factory)
    seen: list[str] = []
    view.launched.connect(seen.append)

    def refuse(key: str, forced: object = None) -> None:
        raise LaunchError("обнаружение платформ ещё не завершено")

    monkeypatch.setattr(view.workspace(), "launch", refuse)

    view.launch_key(_DEMO_ACCOUNTING_KEY)

    assert calls == []
    assert len(errors) == 1
    assert isinstance(errors[0], LaunchError)
    assert seen == []
```

`LaunchError` уже импортирован в файле (`onecstarter.services.errors`).

Run: `uv run pytest tests/ui/test_bases_view.py -q -k launched_is`
Expected: FAIL — `AttributeError: 'BasesView' object has no attribute 'launched'`.

- [ ] **Step 4: Реализация сигнала**

`src/onecstarter/ui/bases/view.py`: импорт `Signal` в строку `from PySide6.QtCore import QModelIndex, QPoint, QStandardPaths, Qt, Signal`. В `class BasesView(QWidget):` первой строкой тела:

```python
    # T-11, п. 9: ключ записи после УСПЕШНОГО запуска — проводка ui/app.py
    # прячет окно в трей по настройке. Все пути запуска из UI (Enter/двойной
    # клик, поиск, F3/F4/Ctrl+1-3, меню, трей) сходятся в launch_key.  # noqa: RUF003
    launched = Signal(str)
```

`launch_key`:

```python
    def launch_key(self, key: str, forced: ClientKind | None = None) -> None:
        try:
            self._workspace.launch(key, forced)
        except ServicesError as error:
            self._on_error(error)
        else:
            self.launched.emit(key)
        self.rebuild()
```

Run: `uv run pytest tests/ui/test_bases_view.py -q` → PASS.

- [ ] **Step 5: Падающие тесты — раздел «Настройки»**

В `tests/ui/test_settings_view.py` после `test_tray_toggle_starts_from_file`:

```python
def test_hide_on_launch_toggle_persists(application: QApplication, tmp_path: Path) -> None:
    view, store = _view(application, tmp_path)
    assert view.hide_on_launch_checkbox().isChecked() is False
    assert view.row_control("Запуск базы сворачивает окно в трей") is view.hide_on_launch_checkbox()
    assert "Без трея" in view.row_note("Запуск базы сворачивает окно в трей").text()

    view.hide_on_launch_checkbox().setChecked(True)

    assert store.settings.hide_on_launch is True


def test_hide_on_launch_toggle_starts_from_file(application: QApplication, tmp_path: Path) -> None:
    save_settings(tmp_path / "settings.json", Settings(hide_on_launch=True))
    view, _ = _view(application, tmp_path)
    assert view.hide_on_launch_checkbox().isChecked() is True


def test_hide_on_launch_syncs_from_store_without_echo(
    application: QApplication, tmp_path: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    view, store = _view(application, tmp_path)
    calls: list[dict[str, object]] = []
    original_update = store.update

    def spy_update(**changes: object) -> None:
        calls.append(changes)
        original_update(**changes)

    monkeypatch.setattr(store, "update", spy_update)

    store.update(hide_on_launch=True)

    assert view.hide_on_launch_checkbox().isChecked() is True
    assert calls == [{"hide_on_launch": True}]
```

Run: `uv run pytest tests/ui/test_settings_view.py -q -k hide_on_launch` → FAIL (`AttributeError`).

- [ ] **Step 6: Реализация тумблера**

В `SettingsView.__init__` сразу после `self._add_row("Закрытие окна сворачивает в трей", ...)`:

```python
        # T-11, п. 9 (решение заказчика 29.08.2026): любой способ запуска базы
        # прячет окно; на серверные профили не действует; без трея — не действует.
        self._hide_on_launch = QCheckBox()
        self._hide_on_launch.setChecked(store.settings.hide_on_launch)
        self._hide_on_launch.toggled.connect(self._choose_hide_on_launch)
        self._add_row(
            "Запуск базы сворачивает окно в трей",
            "Любой способ запуска: Enter, F3/F4, меню, поиск, трей. Без трея не действует",
            self._hide_on_launch,
        )
```

Реакция (рядом с `_choose_tray`): `def _choose_hide_on_launch(self, checked: bool) -> None: self._store.update(hide_on_launch=checked)`. Аксессор (рядом с `tray_checkbox`): `def hide_on_launch_checkbox(self) -> QCheckBox: return self._hide_on_launch`. В `_sync` после блока `_tray`:

```python
        blocked = self._hide_on_launch.blockSignals(True)
        self._hide_on_launch.setChecked(settings.hide_on_launch)
        self._hide_on_launch.blockSignals(blocked)
```

Run: `uv run pytest tests/ui/test_settings_view.py -q` → PASS.

- [ ] **Step 7: Падающие тесты — проводка `app.py`**

В `tests/ui/test_app.py` после `test_main_does_not_hide_the_window_into_a_missing_tray`:

```python
_DEMO_ACCOUNTING_KEY = "id:44444444-4444-4444-4444-444444444444"


def test_launch_hides_the_window_when_the_setting_is_on(assembled_with_tray: _Assembly) -> None:
    """T-11, п. 9: успешный запуск базы при включённой настройке прячет окно в трей."""
    assembled_with_tray.store.update(hide_on_launch=True)
    assert assembled_with_tray.window.isHidden() is False

    assembled_with_tray.view.launch_key(_DEMO_ACCOUNTING_KEY)

    assert len(assembled_with_tray.launch_calls) == 1
    assert assembled_with_tray.window.isHidden() is True


def test_launch_keeps_the_window_when_the_setting_is_off(assembled_with_tray: _Assembly) -> None:
    assembled_with_tray.view.launch_key(_DEMO_ACCOUNTING_KEY)
    assert assembled_with_tray.window.isHidden() is False


def test_launch_keeps_the_window_without_tray(assembled: _Assembly) -> None:
    """ЗАЩИТНЫЙ ТЕСТ: без трея окно не прячется (решение заказчика 29.08.2026, б).

    Мутация: `hide_after_launch` без `tray is not None` — окно исчезнет,
    вернуть его сможет только глобальный хоткей.
    """
    assembled.store.update(hide_on_launch=True)

    assembled.view.launch_key(_DEMO_ACCOUNTING_KEY)

    assert len(assembled.launch_calls) == 1
    assert assembled.window.isHidden() is False
```

Run: `uv run pytest tests/ui/test_app.py -q -k "launch_hides or launch_keeps"` → FAIL (первый: `isHidden() is False`).

- [ ] **Step 8: Проводка**

В `src/onecstarter/ui/app.py` сразу после определения `apply_close_to_tray`:

```python
    def hide_after_launch(_key: str) -> None:
        # T-11, п. 9. Без трея не скрывать (решение заказчика 29.08.2026, б):
        # окно, спрятанное без значка в трее, нечем вернуть, кроме глобального
        # хоткея — тот же довод, что у apply_close_to_tray. Серверный профиль  # noqa: RUF003
        # (решение а) сюда не попадает: сигнал есть только у BasesView.
        if store.settings.hide_on_launch and tray is not None:
            window.hide()

    view.launched.connect(hide_after_launch)
```

Run: `uv run pytest tests/ui/test_app.py -q` → PASS.

- [ ] **Step 9: Полный прогон, коммит**

Run: `uv run pytest -q`, `uv run ruff check .`, `uv run mypy` → коды 0.

```bash
git add src/onecstarter/services/settings.py src/onecstarter/ui/bases/view.py src/onecstarter/ui/settings_view.py src/onecstarter/ui/app.py tests/unit/test_settings.py tests/ui/test_bases_view.py tests/ui/test_settings_view.py tests/ui/test_app.py
git commit -m "feat: T-11 п. 9 — настройка «Запуск базы сворачивает окно в трей» (без трея не действует)"
```

---

### Task 9: Режим показа «как в файле / по алфавиту» (п. 2)

**Files:**
- Modify: `src/onecstarter/services/settings.py` (`ListOrder`, `Settings.list_order`, `_order_of`, load/save, `__all__`)
- Modify: `src/onecstarter/services/display.py` (`collation_key`, `sort_rows`, параметр `order`)
- Modify: `src/onecstarter/ui/bases/view.py` (провайдер `list_order`; `rebuild`; `_move_current`; `handle_drop`)
- Modify: `src/onecstarter/ui/settings_view.py` (`_build_segment`, сегмент «Порядок списка», `_sync`, аксессор)
- Modify: `src/onecstarter/ui/app.py` (`list_order=`; перестройка по смене порядка)
- Test: `tests/unit/test_settings.py`, `tests/unit/test_display.py`, `tests/ui/test_bases_view.py`, `tests/ui/test_settings_view.py`, `tests/ui/test_app.py`

**Interfaces:**
- Produces:
  - `class ListOrder(Enum)`: `FILE = "file"`, `ALPHABETICAL = "alphabetical"` (в `services/settings.py`, экспорт в `__all__`); `Settings.list_order: ListOrder = ListOrder.FILE`; ключ файла `"list_order"`; `_order_of(value) -> ListOrder` (незнакомое → `FILE`).
  - `display.collation_key(text: str) -> tuple[tuple[str | int, ...], str]` — `(casefold с «ё»→«е», разбитый на текст и числа, casefold)`; до решения заказчика 30.08.2026 — `tuple[str, str]`, цифры как строки.
  - `display.sort_rows(rows: Sequence[Row]) -> list[Row]` — рекурсивно: группы (`GROUP`, `IMPLICIT_GROUP`) перед базами, внутри каждого класса — по `collation_key(label)`, `NOTE` — в конце в прежнем порядке.
  - `display_forest(items, tree, common_errors, *, recent_limit: int, order: ListOrder) -> list[Row]` — `order` обязательный (как `recent_limit`: настройка пользователя, дефолт был бы вторым источником истины). В `ALPHABETICAL`: дерево файла, «Избранное» и «Общие списки» — через `sort_rows`; «Недавние» — всегда по времени.
  - `BasesView.__init__(..., recent_limit, list_order: Callable[[], ListOrder], ...)` — обязательный keyword-провайдер; в `ALPHABETICAL` `_move_current` и `handle_drop` внутри одной группы (`BEFORE`/`AFTER` при равном `folder`) — отказ с сообщением `ALPHABETICAL_REORDER_NOTE` через `_on_error` (находка финального ревью ветки: молчаливый отказ повторял дефект smoke №2), файл не трогается; перенос между группами работает.
  - `SettingsView.order_buttons() -> list[QPushButton]` («Как в файле», «По алфавиту») в группе «СПИСОК БАЗ» после «Записей в «Недавних»».

Правила порядка (tasks.md T-11 п. 2 — «предложение исполнителя, подтвердить при плане»; фиксируются здесь и переносятся в tasks.md задачей 10): группы перед базами внутри уровня; регистронезависимо; латиница перед кириллицей (как в Проводнике Windows); «ё» приравнена к «е» (как в словарях; по кодам она стояла бы после «я»); цепочки ASCII-цифр — как числа, как в Проводнике (`StrCmpLogicalW`): «База 2» перед «База 10», «8.3.9» перед «8.3.24» (вопрос финального ревью ветки; решение заказчика 30.08.2026 — числовой порядок). Правило детерминированное, без `locale.strxfrm`: результат не зависит от локали машины и проверяется таблично [Р].

- [ ] **Step 1: Падающие тесты — настройки**

`tests/unit/test_settings.py`: импорт `ListOrder`; в `test_schema_is_written` — `"list_order": "file"`; в `test_defaults_of_new_fields` — `assert settings.list_order is ListOrder.FILE`; в `test_round_trip_keeps_all_fields` — `list_order=ListOrder.ALPHABETICAL`. Новый класс:

```python
class TestListOrder:
    """T-11, п. 2 (решение заказчика 29.08.2026, вариант а): режим показа, файл не трогается."""

    def test_default_is_file_order(self) -> None:
        assert Settings().list_order is ListOrder.FILE

    def test_round_trip(self, tmp_path: Path) -> None:
        path = tmp_path / "settings.json"
        save_settings(path, Settings(list_order=ListOrder.ALPHABETICAL))
        assert load_settings(path).list_order is ListOrder.ALPHABETICAL

    @pytest.mark.parametrize("value", ["по алфавиту", 1, None, [], {}])
    def test_unknown_list_order_falls_back_to_file(self, tmp_path: Path, value: object) -> None:
        """ЗАЩИТНЫЙ ТЕСТ. Мутация: `_order_of` отдаёт `ALPHABETICAL` на незнакомое значение."""
        path = tmp_path / "settings.json"
        path.write_text(json.dumps({"schema": 1, "list_order": value}), encoding="utf-8")
        assert load_settings(path).list_order is ListOrder.FILE
```

Run: `uv run pytest tests/unit/test_settings.py -q` → FAIL (`ImportError: ListOrder`).

- [ ] **Step 2: Реализация настроек**

`services/settings.py`: после `DefaultClient`:

```python
class ListOrder(Enum):
    """Порядок записей в дереве «Баз» (T-11, п. 2; решение заказчика 29.08.2026 — вариант а).

    Режим ПОКАЗА: файл `.v8i` не трогается, `OrderInList` не переписывается,
    штатный стартер порядка не видит. `FILE` — как в файле (дефолт, поведение
    прежних установок); `ALPHABETICAL` — группы перед базами, `display.
    collation_key`. Разовое действие «переписать OrderInList по алфавиту»
    (вариант б) заказчиком отклонено.
    """  # noqa: RUF002

    FILE = "file"
    ALPHABETICAL = "alphabetical"
```

`Settings`: `list_order: ListOrder = ListOrder.FILE` (после `hide_on_launch`); `load_settings`: `list_order=_order_of(payload.get("list_order")),`; `save_settings`: `"list_order": settings.list_order.value,`; `__all__` — добавить `"ListOrder"`; парсер:

```python
def _order_of(value: Any) -> ListOrder:
    """Незнакомое значение — не порча: более новая версия могла записать свой режим."""
    try:
        return ListOrder(value)
    except ValueError:
        return ListOrder.FILE
```

Run: `uv run pytest tests/unit/test_settings.py -q` → PASS.

- [ ] **Step 3: Падающие тесты — витрина**

`tests/unit/test_display.py`: импорт `from onecstarter.services.settings import DEFAULT_RECENT_LIMIT, ListOrder`; импорт `collation_key, sort_rows` из `display`. Во ВСЕ восемь вызовов `display_forest(...)` файла (строки ~46, 97, 106, 125, 135, 155, 177, 403) добавить `order=ListOrder.FILE`; хелпер `_forest` получает параметр:

```python
def _forest(
    entries: dict[str, BaseUserData] | None = None, *, order: ListOrder = ListOrder.FILE
) -> list[Row]:
    items = _items(entries)
    return display_forest(
        items, build_tree(items), [], recent_limit=DEFAULT_RECENT_LIMIT, order=order
    )
```

Новые тесты в конце файла:

```python
# -- T-11, п. 2: алфавитный режим показа ---------------------------------------


@pytest.mark.parametrize(
    ("names", "expected"),
    [
        (["Портал", "база", "Архив"], ["Архив", "база", "Портал"]),
        (["Ёлка", "Дом", "Еда", "Жук"], ["Дом", "Еда", "Ёлка", "Жук"]),
        (["Яблоко", "Zed", "апрель", "Beta"], ["Beta", "Zed", "апрель", "Яблоко"]),
        (["б2", "Б1", "б10"], ["Б1", "б2", "б10"]),
        (["База 10", "База 2", "База 1"], ["База 1", "База 2", "База 10"]),
        (["8.3.24", "8.3.9"], ["8.3.9", "8.3.24"]),
        (["База 1", "База"], ["База", "База 1"]),
        (["a1b", "a01b"], ["a01b", "a1b"]),
    ],
)
def test_collation_key_table(names: list[str], expected: list[str]) -> None:
    """Регистр не важен, «ё» как «е», латиница перед кириллицей, цифры — как числа ([Р]).

    Числа — как в Проводнике Windows (`StrCmpLogicalW`): «База 2» перед
    «База 10», «8.3.9» перед «8.3.24» (решение заказчика 30.08.2026).
    Равные числа с разной записью («a01b»/«a1b») упорядочены устойчиво
    вторым элементом ключа, а не порядком ввода.
    """
    assert sorted(names, key=collation_key) == expected


def test_file_order_is_untouched_by_default() -> None:
    labels = [row.label for row in _forest()]
    assert labels == [row.label for row in _forest(order=ListOrder.FILE)]
    assert labels.index("Учёт серверный") < labels.index("Портал") < labels.index("Без идентификатора")


def test_alphabetical_order_puts_groups_first_then_bases_by_name() -> None:
    forest = _forest(order=ListOrder.ALPHABETICAL)
    assert [row.label for row in forest] == [
        "Клиенты", "Нет такой группы", "Пустая группа",
        "Без идентификатора", "Портал", "Учёт серверный",
    ]
    clients = forest[0]
    assert [row.label for row in clients.children] == ["Розница", "Демо Бухгалтерия"]
    assert [row.kind for row in forest[:3]] == [
        RowKind.GROUP, RowKind.IMPLICIT_GROUP, RowKind.GROUP
    ]


def test_alphabetical_order_sorts_favorites_but_not_recent() -> None:
    stamp = datetime(2026, 8, 1, tzinfo=UTC)
    entries = {
        "id:44444444-4444-4444-4444-444444444444": BaseUserData(
            favorite=True, last_launched_at=stamp.replace(day=2), launch_count=1
        ),
        "id:55555555-5555-5555-5555-555555555555": BaseUserData(
            favorite=True, last_launched_at=stamp.replace(day=3), launch_count=2
        ),
    }
    forest = _forest(entries, order=ListOrder.ALPHABETICAL)
    favorites = next(row for row in forest if row.label == "Избранное")
    recent = next(row for row in forest if row.label == "Недавние")
    assert [row.label for row in favorites.children] == ["Демо Бухгалтерия", "Демо Розница"]
    assert [row.label for row in recent.children] == ["Демо Розница", "Демо Бухгалтерия"]


def test_sort_rows_keeps_note_rows_last() -> None:
    rows = [
        Row(RowKind.NOTE, "ошибка", None),
        Row(RowKind.BASE, "Яблоко", None),
        Row(RowKind.GROUP, "Архив", None),
    ]
    assert [row.label for row in sort_rows(rows)] == ["Архив", "Яблоко", "ошибка"]
```

Ожидаемые метки — по `tests/fixtures/anonymized.v8i`: корень содержит группы «Клиенты», «Пустая группа», неявный узел «Нет такой группы» (от `Folder=/Нет такой группы` записи «Потерянная») и базы «Учёт серверный», «Портал», «Без идентификатора»; внутри «Клиенты» — подгруппа «Розница» и база «Демо Бухгалтерия». Если фикстура на момент исполнения отличается — сверить `grep "^\[" tests/fixtures/anonymized.v8i` и поправить ожидания, не правило.

Run: `uv run pytest tests/unit/test_display.py -q` → FAIL (`ImportError`).

- [ ] **Step 4: Реализация витрины**

`services/display.py`: импорты `import re` (числовой шаг ключа, решение заказчика 30.08.2026) и `from onecstarter.services.settings import ListOrder`. После `filter_rows` добавить:

```python
_DIGIT_RUN = re.compile(r"([0-9]+)")


def collation_key(text: str) -> tuple[tuple[str | int, ...], str]:
    """Ключ алфавитного порядка (T-11, п. 2): регистр не важен, «ё» как «е», числа — числа.

    [Р] Детерминированное правило вместо `locale.strxfrm`: результат
    не зависит от локали машины и проверяется таблично. Латиница идёт
    перед кириллицей — как в Проводнике Windows; «ё» приравнена к «е»,
    как в словарях (по кодам она стояла бы после «я»). Цепочки цифр
    сравниваются как числа — тоже как в Проводнике (`StrCmpLogicalW`):
    «База 2» перед «База 10», «8.3.9» перед «8.3.24» (решение заказчика
    30.08.2026). Только ASCII-цифры: `re.split` по захватывающей группе
    строго чередует текст (чётные места) и число (нечётные), поэтому
    кортежи разных строк сравнимы поэлементно без смешения типов.
    Второй элемент — полный casefold — делает порядок «Ель»/«Ёль»
    и «01»/«1» устойчивым, не зависящим от порядка ввода.
    """  # noqa: RUF002
    folded = text.casefold()
    parts = _DIGIT_RUN.split(folded.replace("ё", "е"))
    natural = tuple(int(part) if index % 2 else part for index, part in enumerate(parts))
    return (natural, folded)


def _sorted_siblings(rows: Sequence[Row]) -> list[Row]:
    """Группы (и неявные узлы) перед базами, внутри класса — по `collation_key`; NOTE — в конце."""
    groups = sorted(
        (row for row in rows if row.kind in (RowKind.GROUP, RowKind.IMPLICIT_GROUP)),
        key=lambda row: collation_key(row.label),
    )
    bases = sorted(
        (row for row in rows if row.kind is RowKind.BASE),
        key=lambda row: collation_key(row.label),
    )
    notes = [row for row in rows if row.kind is RowKind.NOTE]
    return [*groups, *bases, *notes]


def sort_rows(rows: Sequence[Row]) -> list[Row]:
    """Алфавитный порядок всего поддерева, рекурсивно (режим `ListOrder.ALPHABETICAL`)."""
    return [
        replace(row, children=tuple(sort_rows(row.children)))
        for row in _sorted_siblings(rows)
    ]
```

`display_forest`: сигнатура `..., *, recent_limit: int, order: ListOrder) -> list[Row]`; в докстринг: «`order` — так же обязателен и по той же причине; `ALPHABETICAL` сортирует дерево файла, «Избранное» и «Общие списки» (`sort_rows`), «Недавние» всегда по времени.» Тело:

```python
    alphabetical = order is ListOrder.ALPHABETICAL
    forest: list[Row] = []
    bases = [item for item in items if not item.is_group]
    favorites = [_base_row(item) for item in bases if item.favorite]
    if alphabetical:
        favorites = _sorted_siblings(favorites)
    if favorites:
        forest.append(Row(RowKind.SECTION, "Избранное", None, tuple(favorites)))
    launched = sorted(
        (item for item in bases if item.last_launched_at is not None),
        key=lambda item: item.last_launched_at or _EPOCH,
        reverse=True,
    )
    recent = tuple(_base_row(item) for item in launched[:recent_limit])
    if recent:
        forest.append(Row(RowKind.SECTION, "Недавние", None, recent))
    file_rows = [_row_of(node) for node in tree]
    forest.extend(sort_rows(file_rows) if alphabetical else file_rows)
    common = [item for item in items if item.source is InfobaseSource.COMMON]
    common_rows = [_row_of(node) for node in build_tree(common)]
    if alphabetical:
        common_rows = sort_rows(common_rows)
    common_rows.extend(
        Row(RowKind.NOTE, f"{error.path}: {error.message}", None)
        for error in common_errors
    )
    if common_rows:
        forest.append(Row(RowKind.SECTION, "Общие списки", None, tuple(common_rows)))
    return forest
```

Run: `uv run pytest tests/unit/test_display.py -q` → PASS; `uv run mypy` → 0 (`services` strict).

- [ ] **Step 5: Падающие тесты — вьюха**

`tests/ui/test_bases_view.py`: импорт `from onecstarter.services.settings import DEFAULT_RECENT_LIMIT, ListOrder`. В хелпер `_view` добавить параметр `list_order: Callable[[], ListOrder] | None = None` и в конструктор `list_order=list_order or (lambda: ListOrder.FILE),`. В каждую из девяти прямых конструкций `BasesView(` файла (`grep -n "BasesView(" tests/ui/test_bases_view.py`, кроме `_view`) добавить `list_order=lambda: ListOrder.FILE,` после `recent_limit=...`. Новые тесты после `test_alt_down_moves_the_current_row_down` (или соседнего теста Alt+↓):

```python
def test_alphabetical_mode_disables_keyboard_reorder(qtbot, workspace_factory, monkeypatch):
    """ЗАЩИТНЫЙ ТЕСТ (T-11, п. 2, вариант а): в алфавитном режиме Alt+↑ не пишет файл.

    Мутация 1: убрать проверку `self._list_order() is ListOrder.ALPHABETICAL`
    в `_move_current` — `move_within_group` перепишет `OrderInList`, который
    в этом режиме не показывается. Падает на `moves == []`.

    Мутация 2 (находка финального ревью ветки): заменить
    `self._on_error(InvalidRequestError(ALPHABETICAL_REORDER_NOTE))` на голый
    `return` в `_move_current` — файл по-прежнему не пишется, но пользователь
    не получает объяснения, почему перестановка не сработала (дефект того
    же класса, что customer сообщил в smoke №2). Падает на `len(errors) == 1`.

    `Alt+Up`, не `Alt+Down` (находка исполнителя задачи 9): «Демо Бухгалтерия»
    — последний ребёнок «Клиенты» и в файловом, и в алфавитном порядке,
    поэтому `Alt+Down` упирался в границу соседей ДО проверки режима и тест
    был зелёным на мутации. Вверх сосед есть (подгруппа «Розница»).
    """
    view, _, errors, _ = _view(qtbot, workspace_factory, list_order=lambda: ListOrder.ALPHABETICAL)
    _show_exposed(qtbot, view)
    moves = _spy_on(monkeypatch, view.workspace(), "move_within_group")
    before = view.workspace().paths.ibases.read_bytes()
    _select_key(view, _DEMO_ACCOUNTING_KEY)

    qtbot.keyClick(view, Qt.Key.Key_Up, Qt.KeyboardModifier.AltModifier)

    assert moves == []
    assert view.workspace().paths.ibases.read_bytes() == before
    assert len(errors) == 1 and isinstance(errors[0], InvalidRequestError)
    assert ALPHABETICAL_REORDER_NOTE in str(errors[0])


def test_alphabetical_mode_drop_within_group_does_not_reorder(
    qtbot, workspace_factory, monkeypatch
):
    """В алфавитном режиме drop внутри группы не переставляет и не молчит.

    Мутация (находка финального ревью ветки): заменить `self._on_error(...)`
    в `handle_drop` на голый `return` — файл по-прежнему не пишется, но
    подсказки не будет. Падает на `len(errors) == 1`.
    """
    view, _, errors, _ = _view(qtbot, workspace_factory, list_order=lambda: ListOrder.ALPHABETICAL)
    moves = _spy_on(monkeypatch, view.workspace(), "move_within_group")
    updates = _spy_on(monkeypatch, view.workspace(), "update_infobase")
    siblings = [
        i for i in view.workspace().items()
        if not i.is_group and normalize_folder(i.folder) == ROOT
    ]
    assert len(siblings) >= 2

    view.handle_drop(siblings[0].key, siblings[1].key, DropTarget.AFTER)

    assert moves == []
    assert updates == []
    assert len(errors) == 1 and isinstance(errors[0], InvalidRequestError)
    assert ALPHABETICAL_REORDER_NOTE in str(errors[0])


def test_alphabetical_mode_drop_into_another_group_still_moves(
    qtbot, workspace_factory, monkeypatch
):
    view, _, errors, _ = _view(qtbot, workspace_factory, list_order=lambda: ListOrder.ALPHABETICAL)
    updates = _spy_on(monkeypatch, view.workspace(), "update_infobase")
    root_base = next(
        i for i in view.workspace().items()
        if not i.is_group and normalize_folder(i.folder) == ROOT
    )

    view.handle_drop(root_base.key, _CLIENTS_KEY, DropTarget.INTO)

    assert len(updates) == 1
    assert errors == []


def test_alphabetical_mode_shows_groups_first(qtbot, workspace_factory):
    view, _, _, _ = _view(qtbot, workspace_factory, list_order=lambda: ListOrder.ALPHABETICAL)
    top = [
        view.model().index(row, 0).data()
        for row in range(view.model().rowCount())
    ]
    assert top[:3] == ["Клиенты", "Нет такой группы", "Пустая группа"]
```

Run: `uv run pytest tests/ui/test_bases_view.py -q -k alphabetical` → FAIL (`TypeError: unexpected keyword 'list_order'`).

- [ ] **Step 6: Реализация во вьюхе**

`src/onecstarter/ui/bases/view.py`: импорт `from onecstarter.services.settings import ListOrder`. Конструктор — после `recent_limit: Callable[[], int],` добавить `list_order: Callable[[], ListOrder],`; после `self._recent_limit = recent_limit`:

```python
        # T-11, п. 2: режим показа — тот же провайдер, что recent_limit.
        self._list_order = list_order
```

`rebuild()`: `display_forest(..., recent_limit=self._recent_limit(), order=self._list_order())`.

`_move_current` — ПОСЛЕ существующих ранних `return` (`not index.isValid() or not
_is_in_file_tree(index)`, затем `kind not in (BASE, GROUP) or key not str`), а не первой
строкой тела: находка ре-ревью волны (30.08.2026) — проверка первой строкой ловила
и пустой выбор, и курсор в виртуальной ветке (Избранное/Недавние/Общие списки), и
заголовок раздела, показывая подсказку там, где FILE-режим тоже молчит и переключение
на «Как в файле» ничего не изменило бы. Правильное место — сразу после того, как
установлено, что курсор стоит на настоящей записи/группе дерева файла (ровно то
условие, при котором FILE-режим выполнил бы перестановку), и до вычисления соседа:

```python
        index = self._tree.currentIndex().siblingAtColumn(0)
        if not index.isValid() or not self._is_in_file_tree(index):
            return
        kind = index.data(KIND_ROLE)
        key = index.data(KEY_ROLE)
        if kind not in (RowKind.BASE.value, RowKind.GROUP.value) or not isinstance(key, str):
            return
        if self._list_order() is ListOrder.ALPHABETICAL:
            # Порядок задаёт алфавит, а не OrderInList: писать в файл нечего
            # и незачем (T-11, п. 2, вариант а) — но отказ обязан быть виден,
            # и только для строки, которую FILE-режим и правда переставил бы.
            self._on_error(InvalidRequestError(ALPHABETICAL_REORDER_NOTE))
            return
```

и дописать в докстринг: «В алфавитном режиме, когда курсор стоит на настоящей записи/
группе дерева файла, перестановки нет вовсе (T-11, п. 2) — отказ идёт через
`_on_error`/`ALPHABETICAL_REORDER_NOTE`, не молча (находка финального ревью ветки:
голый `return` повторял дефект smoke №2); виртуальные ветки и пустой выбор молчат
в обоих режимах показа, проверка режима стоит после соответствующих ранних `return`
(находка ре-ревью волны)». `handle_drop` — в ветке
`if where is not DropTarget.INTO:` после проверки равенства папок:

```python
            if target is not None and normalize_folder(source.folder) == normalize_folder(
                target.folder
            ):
                if self._list_order() is ListOrder.ALPHABETICAL:
                    self._on_error(InvalidRequestError(ALPHABETICAL_REORDER_NOTE))
                    return
                self._reorder(source_key, target.key, where)
                return
```

Модуль-level константа рядом с `NO_CACHE_ROOT_NOTE`:

```python
ALPHABETICAL_REORDER_NOTE = (
    "Порядок задан алфавитом — перестановка отключена. Переключите «Порядок списка» "
    "в настройках на «Как в файле», чтобы менять порядок вручную"
)
```

`src/onecstarter/ui/app.py`: в `BasesView(...)` после `recent_limit=lambda: store.settings.recent_limit,` добавить `list_order=lambda: store.settings.list_order,`.

Run: `uv run pytest tests/ui/test_bases_view.py tests/ui/test_app.py -q` → PASS.

- [ ] **Step 7: Падающие тесты — раздел «Настройки» и проводка**

`tests/ui/test_settings_view.py` (импорт `ListOrder`):

```python
def test_order_segment_offers_two_modes_with_file_selected(
    application: QApplication, tmp_path: Path
) -> None:
    view, _ = _view(application, tmp_path)
    assert [button.text() for button in view.order_buttons()] == ["Как в файле", "По алфавиту"]
    assert view.order_buttons()[0].isChecked()
    assert view.row_note("Порядок списка").text() != ""


def test_order_choice_updates_store(application: QApplication, tmp_path: Path) -> None:
    view, store = _view(application, tmp_path)
    view.order_buttons()[1].click()
    assert store.settings.list_order is ListOrder.ALPHABETICAL


def test_order_segment_syncs_from_store(application: QApplication, tmp_path: Path) -> None:
    view, store = _view(application, tmp_path)
    store.update(list_order=ListOrder.ALPHABETICAL)
    assert view.order_buttons()[1].isChecked()
```

`tests/ui/test_app.py` после `test_recent_limit_provider_is_live_from_settings`:

```python
def test_store_changed_rebuilds_the_tree_on_list_order_change(assembled: _Assembly) -> None:
    """T-11, п. 2: смена порядка — перестройка дерева ровно раз, как у recent_limit."""
    view = assembled.view
    store = assembled.store

    view.rebuild_calls = 0
    store.update(list_order=ListOrder.ALPHABETICAL)
    assert view.rebuild_calls == 1

    view.rebuild_calls = 0
    store.update(hide_on_launch=True)
    assert view.rebuild_calls == 0, "hide_on_launch к дереву отношения не имеет"


def test_list_order_provider_is_live_from_settings(assembled: _Assembly) -> None:
    assert assembled.view._list_order() is ListOrder.FILE
    assembled.store.update(list_order=ListOrder.ALPHABETICAL)
    assert assembled.view._list_order() is ListOrder.ALPHABETICAL
```

(импорт `ListOrder` в `test_app.py`).

Run: `uv run pytest tests/ui/test_settings_view.py tests/ui/test_app.py -q -k "order"` → FAIL.

- [ ] **Step 8: Реализация — сегмент и проводка**

`src/onecstarter/ui/settings_view.py`: импорты `from collections.abc import Callable, Sequence`, `from typing import Any`, `ListOrder` из `settings`. Константа после `CLIENT_CHOICES`:

```python
ORDER_CHOICES = (
    (ListOrder.FILE, "Как в файле"),
    (ListOrder.ALPHABETICAL, "По алфавиту"),
)
```

В `__init__`: `self._order_buttons: list[QPushButton] = []` рядом с `_client_buttons`; в группе «СПИСОК БАЗ» после строки «Записей в «Недавних»»:

```python
        self._add_row(
            "Порядок списка",
            "«По алфавиту» — только показ: файл списка и штатный стартер порядка "
            "не видят, перестановка Alt+↑/↓ и мышью отвечает подсказкой, "
            "а не меняет порядок",
            self._build_order_segment(),
        )
```

(текст поправлен по находке финального ревью ветки: «отключена» само по себе вводило
в заблуждение — индикатор drop по-прежнему рисуется, отказ идёт через подсказку.)

Общий билдер сегмента (после `_build_client_segment`; `_build_client_segment` переписать через него):

```python
    def _build_segment(
        self,
        choices: Sequence[tuple[Any, str]],
        is_current: Callable[[Any], bool],
        choose: Callable[[Any], None],
        registry: list[QPushButton],
    ) -> QWidget:
        """Сегментный переключатель — один билдер на клиента и порядок списка (T-11)."""
        seg = QWidget()
        seg.setObjectName("ThemeSeg")
        seg_layout = QHBoxLayout(seg)
        seg_layout.setContentsMargins(0, 0, 0, 0)
        seg_layout.setSpacing(0)
        buttons = QButtonGroup(self)
        buttons.setExclusive(True)
        for value, label in choices:
            button = QPushButton(label)
            button.setCheckable(True)
            button.setChecked(is_current(value))
            button.clicked.connect(lambda _checked=False, v=value: choose(v))
            buttons.addButton(button)
            seg_layout.addWidget(button)
            registry.append(button)
        return seg

    def _build_client_segment(self) -> QWidget:
        return self._build_segment(
            CLIENT_CHOICES,
            lambda client: client is self._store.settings.default_client,
            self._choose_client,
            self._client_buttons,
        )

    def _build_order_segment(self) -> QWidget:
        return self._build_segment(
            ORDER_CHOICES,
            lambda order: order is self._store.settings.list_order,
            self._choose_order,
            self._order_buttons,
        )
```

Реакция: `def _choose_order(self, order: ListOrder) -> None: self._store.update(list_order=order)`. Аксессор: `def order_buttons(self) -> list[QPushButton]: return list(self._order_buttons)`. `_sync` — после блока `_client_buttons`:

```python
        for button, (order, _label) in zip(self._order_buttons, ORDER_CHOICES, strict=True):
            button.setChecked(order is settings.list_order)
```

`src/onecstarter/ui/app.py`: заменить `recent_limit_seen`/`rebuild_if_recent_limit_changed` на

```python
    list_settings_seen = (store.settings.recent_limit, store.settings.list_order)

    def rebuild_if_list_settings_changed() -> None:
        # Круг исправлений 1, находка 2 (T-04.7): `store.changed` эмитируется
        # на любую настройку, а дереву есть дело только до тех, по которым  # noqa: RUF003
        # оно строится: `recent_limit` («Недавние») и, с T-11 п. 2,
        # `list_order`. Смена темы перестраивает дерево своим путём
        # (controller.changed → apply_palette → rebuild()), остальные
        # настройки к дереву отношения не имеют.
        nonlocal list_settings_seen
        current = (store.settings.recent_limit, store.settings.list_order)
        if current == list_settings_seen:
            return
        list_settings_seen = current
        view.rebuild()
```

и `store.changed.connect(rebuild_if_list_settings_changed)`.

Run: `uv run pytest -q`, `uv run ruff check .`, `uv run mypy` → коды 0 (в т. ч. прежний `test_store_changed_rebuilds_the_tree_only_on_list_settings_change` — переименован находкой финального ревью ветки, правило: `recent_limit` или `list_order`).

- [ ] **Step 9: Коммит**

```bash
git add src/onecstarter/services/settings.py src/onecstarter/services/display.py src/onecstarter/ui/bases/view.py src/onecstarter/ui/settings_view.py src/onecstarter/ui/app.py tests/unit/test_settings.py tests/unit/test_display.py tests/ui/test_bases_view.py tests/ui/test_settings_view.py tests/ui/test_app.py
git commit -m "feat: T-11 п. 2 — режим показа «как в файле / по алфавиту» без правки файла"
```

---

### Task 10: Финальный гейт, мутационная стадия, документы

**Files:**
- Modify: `docs/tasks.md` (раздел T-11)
- Create: `.superpowers/sdd/2026-08-30-v2-polish/mutation-report.md`

**Interfaces:** нет.

- [ ] **Step 1: Полный гейт**

Run: `uv run pytest -q` (дважды подряд — второй прогон ради наблюдения за `access violation` задачи 0), `uv run ruff check .`, `uv run mypy`, затем `powershell -File build/build.ps1 -SkipInstaller` (smoke собранного экземпляра обязан пройти; собранный exe не запускать сверх smoke).
Expected: все коды 0, `smoke: OK`; число тестов и размер `dist` — в леджер.

- [ ] **Step 2: Мутационная стадия — независимый агент (не автор тестов вехи)**

Протокол CLAUDE.md: правка → только названный тест (PowerShell foreground) → дословный `FAILED`/assertion → `git checkout --` → `git status --short` пуст → тот же тест зелёным повторно. Отчёт — `.superpowers/sdd/2026-08-30-v2-polish/mutation-report.md`, таблица как у T-12.

| # | Мутация | Файл | Тест |
| --- | --- | --- | --- |
| 1 | убрать `title_row.setSpacing(TITLE_ROW_SPACING)` | `ui/servers/view.py` | `test_card_title_row_separates_name_from_status` |
| 2 | `_show_current_properties` без проверки `InfobaseSource.COMMON` | `ui/bases/view.py` | `test_alt_enter_on_common_record_does_nothing` |
| 3 | `_add_infobase_at_current` передаёт `ROOT` всегда | `ui/bases/view.py` | `test_insert_adds_into_the_group_of_the_current_row` |
| 4 | `_remove_current` зовёт `self._workspace.remove_infobase(key)` напрямую | `ui/bases/view.py` | `test_delete_key_with_declined_confirmation_keeps_the_file` |
| 5 | `_remove_current` без проверки `InfobaseSource.COMMON` | `ui/bases/view.py` | `test_delete_key_on_common_record_does_nothing` |
| 6 | `keyPressEvent` дерева не перехватывает `Delete` (ветка удалена) | `ui/bases/view.py` | `test_delete_key_asks_and_removes_the_current_base` |
| 7 | `_suggest_name` без проверки пустого имени | `ui/dialogs/infobase.py` | `test_typed_name_survives_path_edit` |
| 8 | `_bool_of(payload.get("hide_on_launch"))` без `default=False` | `services/settings.py` | `TestHideOnLaunch::test_old_file_without_the_key_reads_off` |
| 9 | `launched.emit(key)` до `self._workspace.launch(...)` | `ui/bases/view.py` | `test_launched_is_not_emitted_when_launch_fails` |
| 10 | `hide_after_launch` без `tray is not None` | `ui/app.py` | `test_launch_keeps_the_window_without_tray` |
| 11 | `_move_current` без проверки `ALPHABETICAL` | `ui/bases/view.py` | `test_alphabetical_mode_disables_keyboard_reorder` |
| 12 | `_order_of` отдаёт `ALPHABETICAL` на незнакомое | `services/settings.py` | `TestListOrder::test_unknown_list_order_falls_back_to_file` |
| 13 | `sort_rows` не сортирует детей (без рекурсии) | `services/display.py` | `test_alphabetical_order_puts_groups_first_then_bases_by_name` |

Expected: 13/13 убиты; НЕ убитая мутация — дефект теста, чинится до слияния.

- [ ] **Step 3: Документы**

`docs/tasks.md`, раздел T-11:
- заголовок → `## T-11. Косметика и удобство перед выпуском v2 — \`DONE\` (<дата>, ветка \`feat/2026-08-30-v2-polish\`)`;
- абзац итога после таблицы: число тестов (было 1711 на 30.08), `ruff`/`mypy` 0, сборка со smoke, 13/13 мутаций, ссылка на план;
- п. 2 «Разбора пунктов»: подпункт «Правила порядка заказчик не решал — предложение исполнителя…» переписать как принятое правило: «группы перед базами; регистронезависимо; латиница перед кириллицей (как в Проводнике); «ё» как «е»; детерминированно, без `locale` [Р]; «Недавние» по времени; в алфавитном режиме Alt+↑/↓ и перестановка мышью отключены, перенос между группами работает»;
- п. 5: зафиксировать форму — `GroupPicker` (`QComboBox` с отступами, значения — пути) и почему не `QTreeView` в popup;
- п. 3: справочник в «ГОРЯЧИЕ КЛАВИШИ» реализован (если заказчик его не снял);
- долг T-12 п. 15: дописать «autouse-фикстура `qtbot.addWidget` в `test_servers_view.py` (T-11, задача 0); наблюдение продолжается — падений за N прогонов не было / было (с трассой)».

Run: `git diff --stat docs/tasks.md` — правки только в разделе T-11 и в строке долга T-12 п. 15.

- [ ] **Step 4: Коммит и передача**

```bash
git add docs/tasks.md .superpowers/sdd/2026-08-30-v2-polish/mutation-report.md
git commit -m "docs: T-11 — гейт, мутации 13/13, статус DONE"
```

Дальше — `superpowers:finishing-a-development-branch`: финальное ревью ветки, ручной чек-лист заказчику (ниже), слияние — решение заказчика.

### Ручной чек-лист T-11 (для заказчика, собранный экземпляр)

1. «Серверы»: между именем профиля и статусом виден зазор.
2. «Базы»: `Alt+Enter` на записи открывает «Свойства…», на группе — диалог группы; в меню записи пункт подписан `Alt+Enter`.
3. «Настройки» → «ГОРЯЧИЕ КЛАВИШИ»: таблица сочетаний раздела «Базы».
4. ПКМ на группе → «Добавить базу…» — в диалоге предвыбрана эта группа; `Insert` на строке группы/записи — то же; в поле поиска `Insert` ничего не добавляет.
5. `Delete` на записи — вопрос с именем, «Нет» ничего не меняет; на группе — вопрос с содержимым; в поле поиска `Delete` стирает символ.
6. Диалог добавления: пустое имя заполняется именем каталога после ввода пути (Tab/Enter) и именем базы в кластере для серверной; введённое имя не перезаписывается.
7. Выбор группы — с отступами по вложенности, в обоих диалогах.
8. «Настройки» → «Запуск базы сворачивает окно в трей»: включено — запуск Enter/F3/меню/поиск прячет окно, трей возвращает; выключено — окно остаётся. При отключённом трее Windows (если есть возможность проверить) — окно остаётся.
9. «Настройки» → «Порядок списка» → «По алфавиту»: группы первыми, по алфавиту; Alt+↓ ничего не делает; файл `ibases.v8i` не меняется (mtime); «Как в файле» — прежний порядок.
