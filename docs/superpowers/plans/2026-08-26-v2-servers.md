# v2 «Серверы» (T-08) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Раздел «Серверы»: именованные профили `ragent` с запуском/остановкой и статусом по живым процессам, справочный показ чужих серверов, смена версии MMC-консоли одним действием с UAC.

**Architecture:** Логика — чистые функции в `domain/` (сборка аргументов, разбор argv, сопоставление процессов, выбор версии); эффекты — за протоколами в `platform_1c/` (скан psutil, завершение дерева, повышение, реестр консоли); координация — `services/servers.py` по образцу `Workspace`; Qt — только `ui/servers/`. Хранение профилей — `%APPDATA%\OneCStarter\servers.json` c политикой `.bad` как у `bases.json`.

**Tech Stack:** Python ≥ 3.13, PySide6 (только ui), psutil (новая зависимость — [Ф] T-07.13: ~90 мс тёплый снимок, видит путь exe SYSTEM-процессов), pytest, uv.

Спека: [2026-08-26-v2-servers-design.md](../specs/2026-08-26-v2-servers-design.md).
Факты: [t07-protocol.md](../../research/t07-protocol.md) — все «[Ф] А…/Б…/Г…» ниже оттуда.

## Global Constraints

- Qt только в `src/onecstarter/ui/` (инвариант 1); каждый новый модуль ядра добавляется в кортеж `CORE` в `tests/unit/test_no_qt_in_core.py:8` **в той же задаче**, где создаётся.
- Чистые функции без ФС и процессов внутри (инвариант 2); всё окружение — аргументами.
- Запись пользовательских файлов — только `config/atomic.py::atomic_write` (инвариант 4).
- Секретов в вехе нет и не появляется (инвариант 5): пароли кластера не хранятся и не передаются.
- Проверки: `uv run pytest`, `uv run ruff check .`, `uv run mypy` — все коды 0 после каждой задачи. mypy strict для всего, кроме `onecstarter.ui.*`.
- Тесты НИКОГДА не запускают живой `ragent`/`regsvr32`/MMC (правило «Границы» CLAUDE.md); системные эффекты — за протоколами, в тестах фейки.
- Тесты отказов и сохранности пишутся до реализации (TDD) и помечаются в докстринге «ЗАЩИТНЫЙ ТЕСТ» — их мутационная проверка независимым агентом идёт отдельной стадией (задача 17), мутацию ставит не автор теста.
- Коммит после каждой задачи; сообщения — `feat:`/`test:`/`chore:` по образцу истории.
- Подписи UI — по-русски; цвета — только роли `Palette` (урок T-06: «работает» — акцент, беда — problem, зелёного в палитре нет).

---

### Task 1: Домен — профиль сервера и командная строка ragent

**Files:**
- Create: `src/onecstarter/domain/server.py`
- Test: `tests/unit/test_server.py`
- Modify: `tests/unit/test_no_qt_in_core.py:8` (строка `"onecstarter.domain.server",`)

**Interfaces:**
- Consumes: `onecstarter.domain.version.VersionNumber`, `parse_version` (существуют).
- Produces: `ServerProfile` (frozen dataclass: `id: str`, `name: str`, `version: str`, `port: int`, `regport: int`, `range_start: int`, `range_end: int`, `cluster_dir: str`, `debug: bool = True`, `http: bool = True`, `extra_args: str = ""`); `build_ragent_arguments(profile: ServerProfile) -> str`; `ServerConvention` (frozen dataclass: `min_version: VersionNumber`, `bin_dir: str`, `ragent: str`, `radmin: str`, `console: str`); `server_convention_for(version, conventions) -> ServerConvention | None`.

- [x] **Step 1: Написать падающие табличные тесты**

```python
"""Табличные тесты чистых функций — мутационная проверка не требуется (CLAUDE.md)."""
import pytest

from onecstarter.domain.server import (
    ServerConvention,
    ServerProfile,
    build_ragent_arguments,
    server_convention_for,
)
from onecstarter.domain.version import parse_version


def _profile(**overrides: object) -> ServerProfile:
    values: dict = {
        "id": "a" * 32,
        "name": "8.3.25 отладка",
        "version": "8.3.25",
        "port": 1540,
        "regport": 1541,
        "range_start": 1560,
        "range_end": 1591,
        "cluster_dir": r"E:\srv\srv_8.3.25.1633",
    }
    values.update(overrides)
    return ServerProfile(**values)


class TestBuildRagentArguments:
    def test_srv_sh_form_byte_exact(self) -> None:
        # [Ф] А1: форма снята со срабатывавших запусков сессии T-07.
        assert build_ragent_arguments(_profile()) == (
            r"-debug -http -port 1540 -regport 1541 -range 1560:1591 -d E:\srv\srv_8.3.25.1633"
        )

    def test_path_with_space_is_quoted(self) -> None:
        # [Ф] А1: путь с пробелом работает в стандартных Windows-кавычках.
        got = build_ragent_arguments(_profile(cluster_dir=r"E:\srv\with space"))
        assert got.endswith(r'-d "E:\srv\with space"')

    def test_flags_off_are_omitted(self) -> None:
        got = build_ragent_arguments(_profile(debug=False, http=False))
        assert got.startswith("-port 1540")

    def test_extra_args_go_last(self) -> None:
        got = build_ragent_arguments(_profile(extra_args="-seclev 1"))
        assert got.endswith("-seclev 1")

    def test_empty_extra_args_add_nothing(self) -> None:
        assert not build_ragent_arguments(_profile(extra_args="  ")).endswith(" ")


class TestServerConventionFor:
    def test_picks_best_min_version(self) -> None:
        old = ServerConvention(parse_version("8.2"), "bin", "ragent.exe", "radmin.dll", "common/1CV8 Servers (x86-64).msc")
        assert server_convention_for(parse_version("8.3.25.1633"), [old]) is old

    def test_none_below_floor(self) -> None:
        conv = ServerConvention(parse_version("8.2"), "bin", "ragent.exe", "radmin.dll", "common/1CV8 Servers (x86-64).msc")
        assert server_convention_for(parse_version("8.1"), [conv]) is None
```

- [x] **Step 2: Прогнать — убедиться, что падают**

Run: `uv run pytest tests/unit/test_server.py -q`
Expected: FAIL — `ModuleNotFoundError: onecstarter.domain.server`

- [x] **Step 3: Реализация**

```python
"""Профиль локального сервера 1С и сборка аргументов ragent.

Форма аргументов снята с работавших запусков сессии T-07 ([Ф] А1
t07-protocol.md): ключи регистронезависимы, но пишем в нижнем регистре
как srv.sh; путь с пробелом — в стандартных Windows-кавычках; без
пробела — без кавычек (обе формы измерены, прочие не проверялись).
"""

from collections.abc import Sequence
from dataclasses import dataclass

from onecstarter.domain.version import VersionNumber


@dataclass(frozen=True)
class ServerProfile:
    id: str
    name: str
    version: str  # запрошенная: полный номер или маска, как ввёл пользователь
    port: int
    regport: int
    range_start: int
    range_end: int
    cluster_dir: str
    debug: bool = True
    http: bool = True
    extra_args: str = ""


@dataclass(frozen=True)
class ServerConvention:
    min_version: VersionNumber
    bin_dir: str
    ragent: str
    radmin: str
    console: str  # путь .msc от КОРНЯ 1cv8 (родителя каталогов версий), [Ф] Г1


def server_convention_for(
    version: VersionNumber, conventions: Sequence[ServerConvention]
) -> ServerConvention | None:
    best: ServerConvention | None = None
    for convention in conventions:
        if version < convention.min_version:
            continue
        if best is None or convention.min_version > best.min_version:
            best = convention
    return best


def build_ragent_arguments(profile: ServerProfile) -> str:
    parts: list[str] = []
    if profile.debug:
        parts.append("-debug")
    if profile.http:
        parts.append("-http")
    parts.append(f"-port {profile.port}")
    parts.append(f"-regport {profile.regport}")
    parts.append(f"-range {profile.range_start}:{profile.range_end}")
    directory = profile.cluster_dir
    parts.append(f'-d "{directory}"' if " " in directory else f"-d {directory}")
    extra = profile.extra_args.strip()
    if extra:
        parts.append(extra)
    return " ".join(parts)
```

- [x] **Step 4: Прогнать тесты + добавить модуль в `CORE`**

В `tests/unit/test_no_qt_in_core.py` в кортеж `CORE` добавить строку
`"onecstarter.domain.server",` (после `"onecstarter.domain.selection",`).

Run: `uv run pytest tests/unit/test_server.py tests/unit/test_no_qt_in_core.py -q`
Expected: PASS

- [x] **Step 5: Линт, типы, коммит**

```bash
uv run ruff check . && uv run mypy
git add src/onecstarter/domain/server.py tests/unit/test_server.py tests/unit/test_no_qt_in_core.py
git commit -m "feat: профиль сервера и сборка аргументов ragent (T-08, задача 1)"
```

---

### Task 2: Домен — выбор версии сервера и валидация профиля

**Files:**
- Modify: `src/onecstarter/domain/server.py`
- Test: `tests/unit/test_server.py`

**Interfaces:**
- Consumes: `ServerProfile` (задача 1), `VersionNumber.starts_with`, `parse_version`.
- Produces: `resolve_server_version(requested: str, installed: Sequence[VersionNumber]) -> VersionNumber | None`; `validate_profile(profile: ServerProfile, others: Sequence[ServerProfile]) -> list[str]` (ошибки, пустой список — валиден); `warn_range_overlap(profile: ServerProfile, others: Sequence[ServerProfile]) -> list[str]` (предупреждения). Обе принимают `others` БЕЗ самого профиля (вызывающий исключает по `id`).

- [x] **Step 1: Табличные тесты**

```python
from onecstarter.domain.server import (
    resolve_server_version,
    validate_profile,
    warn_range_overlap,
)


class TestResolveServerVersion:
    INSTALLED = [parse_version(v) for v in ("8.3.25.1560", "8.3.25.1633", "8.3.27.2214")]

    @pytest.mark.parametrize(
        ("requested", "expected"),
        [
            ("8.3.25.1560", "8.3.25.1560"),  # точная — ровно она
            ("8.3.25", "8.3.25.1633"),  # маска — максимум с префиксом ([Ф] T-02.1)
            ("8.3", "8.3.27.2214"),
            ("8.3.25.1700", None),  # точной нет — None, без тихого фолбэка
            ("8.5", None),
            ("мусор", None),  # неразборчивое — None, валидация ловит отдельно
        ],
    )
    def test_table(self, requested: str, expected: str | None) -> None:
        got = resolve_server_version(requested, self.INSTALLED)
        assert (str(got) if got else None) == expected


class TestValidateProfile:
    def test_valid_profile_has_no_errors(self) -> None:
        assert validate_profile(_profile(), []) == []

    @pytest.mark.parametrize(
        ("overrides", "fragment"),
        [
            ({"name": "  "}, "имя"),
            ({"version": "не версия"}, "версия"),
            ({"port": 0}, "порт"),
            ({"regport": 70000}, "порт"),
            ({"range_start": 1591, "range_end": 1560}, "диапазон"),
            ({"port": 1541}, "порт"),  # port == regport
            ({"cluster_dir": " "}, "каталог"),
        ],
    )
    def test_bad_fields(self, overrides: dict, fragment: str) -> None:
        errors = validate_profile(_profile(**overrides), [])
        assert errors and fragment in errors[0].casefold()

    def test_port_clash_with_other_profile(self) -> None:
        other = _profile(id="b" * 32, name="сосед", regport=2541, range_start=2560, range_end=2591, cluster_dir=r"E:\srv\other")
        errors = validate_profile(_profile(), [other])  # port 1540 у обоих
        assert any("1540" in error for error in errors)

    def test_cluster_dir_clash_is_case_insensitive(self) -> None:
        other = _profile(id="b" * 32, port=2540, regport=2541, cluster_dir=r"e:/SRV/srv_8.3.25.1633/")
        errors = validate_profile(_profile(), [other])
        assert any("каталог" in error.casefold() for error in errors)


class TestWarnRangeOverlap:
    def test_overlap_is_warning_not_error(self) -> None:
        # [Ф] А5: пересечение диапазонов безвредно — предупреждение, не отказ.
        other = _profile(id="b" * 32, port=2540, regport=2541, range_start=1580, range_end=1611, cluster_dir=r"E:\srv\other")
        assert validate_profile(_profile(), [other]) == []
        assert warn_range_overlap(_profile(), [other]) != []

    def test_no_overlap_no_warning(self) -> None:
        other = _profile(id="b" * 32, port=2540, regport=2541, range_start=2560, range_end=2591, cluster_dir=r"E:\srv\other")
        assert warn_range_overlap(_profile(), [other]) == []
```

- [x] **Step 2: Прогнать — падают** (`ImportError: resolve_server_version`)

- [x] **Step 3: Реализация** (в `domain/server.py`; `normalize_cluster_dir` появится в задаче 3 — здесь временная локальная нормализация недопустима, поэтому валидация каталога сравнивает через отложенный импорт функции из `server_match` НЕЛЬЗЯ — циклы. Решение: `validate_profile` принимает нормализатор параметром со значением по умолчанию — простое `casefold` + замена `/`→`\` + срез хвостовых `\`; задача 3 подключит полный.)

```python
_PORT_MIN, _PORT_MAX = 1, 65535


def _basic_normalize(text: str) -> str:
    return text.replace("/", "\\").rstrip("\\").casefold()


def resolve_server_version(
    requested: str, installed: Sequence[VersionNumber]
) -> VersionNumber | None:
    """Точное совпадение либо максимум с префиксом ([Ф] T-02.1 — правило клиентов).

    Тихого фолбэка «максимум вообще» нет намеренно: платформа так делает
    при запуске клиентов, но сервер мы запускаем сами, и запуск не той
    версии на чужом каталоге кластера дороже честного отказа.
    """
    from onecstarter.domain.version import parse_version

    try:
        wanted = parse_version(requested.strip())
    except ValueError:
        return None
    if wanted in installed:
        return wanted
    matching = [version for version in installed if version.starts_with(wanted)]
    return max(matching) if matching else None


def validate_profile(
    profile: ServerProfile,
    others: Sequence[ServerProfile],
    normalize: Callable[[str], str] = _basic_normalize,
) -> list[str]:
    errors: list[str] = []
    if not profile.name.strip():
        errors.append("Имя профиля не заполнено")
    try:
        from onecstarter.domain.version import parse_version

        parse_version(profile.version.strip())
    except ValueError:
        errors.append(f"Версия {profile.version!r} не разбирается как номер")
    for label, value in (("Порт", profile.port), ("Порт регистрации", profile.regport)):
        if not _PORT_MIN <= value <= _PORT_MAX:
            errors.append(f"{label} {value} вне диапазона 1–65535")
    if profile.range_start > profile.range_end:
        errors.append("Диапазон портов задом наперёд")
    if profile.port == profile.regport:
        errors.append(f"Порт и порт регистрации совпадают ({profile.port})")
    if not profile.cluster_dir.strip():
        errors.append("Каталог кластера не заполнен")
    taken = {
        port: other.name
        for other in others
        for port in (other.port, other.regport)
    }
    for port in (profile.port, profile.regport):
        if port in taken:
            errors.append(f"Порт {port} уже занят профилем «{taken[port]}»")
    mine = normalize(profile.cluster_dir)
    for other in others:
        if normalize(other.cluster_dir) == mine:
            errors.append(f"Каталог кластера уже занят профилем «{other.name}»")
    return errors


def warn_range_overlap(
    profile: ServerProfile, others: Sequence[ServerProfile]
) -> list[str]:
    """[Ф] А5: пересечение безвредно — процессы разводятся по свободным портам."""
    warnings: list[str] = []
    for other in others:
        if profile.range_start <= other.range_end and other.range_start <= profile.range_end:
            warnings.append(
                f"Диапазон пересекается с профилем «{other.name}» — не ошибка, "
                "но при исчерпании портов серверы начнут мешать друг другу"
            )
    return warnings
```

(Импорты `Callable` из `collections.abc`, `parse_version` — на уровень модуля; локальные импорты в эскизе только для компактности примера — в коде поднять наверх.)

- [x] **Step 4: Прогнать — зелёные**; **Step 5:** `ruff`/`mypy`/коммит `feat: выбор версии сервера и валидация профиля (T-08, задача 2)`

---

### Task 3: Домен — разбор командной строки ragent и нормализация каталога

**Files:**
- Create: `src/onecstarter/domain/server_match.py`
- Test: `tests/unit/test_server_match.py`
- Modify: `tests/unit/test_no_qt_in_core.py` (строка `"onecstarter.domain.server_match",`)

**Interfaces:**
- Produces: `RagentProcess` (frozen: `pid: int`, `executable: Path | None`, `argv: tuple[str, ...] | None`, `create_time: float`) — `None` в полях = нет доступа ([Ф] В1); `RagentParams` (frozen: `port: int | None`, `regport: int | None`, `range_text: str | None`, `cluster_dir: str | None`); `extract_ragent_params(argv: Sequence[str]) -> RagentParams`; `normalize_cluster_dir(text: str) -> str`; `version_from_exe_path(path: Path) -> VersionNumber | None`.

- [x] **Step 1: Табличные тесты**

```python
from pathlib import Path

import pytest

from onecstarter.domain.server_match import (
    extract_ragent_params,
    normalize_cluster_dir,
    version_from_exe_path,
)


class TestNormalizeClusterDir:
    @pytest.mark.parametrize(
        ("raw", "expected"),
        [
            (r"E:\tmp\t07\srv1", r"e:\tmp\t07\srv1"),
            ("E:/tmp/t07/srv1", r"e:\tmp\t07\srv1"),  # [Ф] Б1: прямые слэши живут в argv
            (r"e:\tmp\t07\srv1" + "\\", r"e:\tmp\t07\srv1"),  # [Ф] rmngr дописывает хвост
            ('"e:\\tmp\\srv with space\\\\"', r"e:\tmp\srv with space"),  # [Ф] А1: кавычки + слэш внутри
            (r"E:\\tmp\\\\t07", r"e:\tmp\t07"),  # повторные разделители
            (r"E:\Tmp\T07\SRV1", r"e:\tmp\t07\srv1"),  # NTFS регистронезависима
        ],
    )
    def test_table(self, raw: str, expected: str) -> None:
        assert normalize_cluster_dir(raw) == expected


class TestExtractRagentParams:
    ARGV = ("ragent.exe", "-debug", "-http", "-port", "2540", "-regport", "2541", "-range", "2560:2591", "-d", r"e:\tmp\t07\srv1")

    def test_full_argv(self) -> None:
        params = extract_ragent_params(self.ARGV)
        assert (params.port, params.regport, params.range_text) == (2540, 2541, "2560:2591")
        assert params.cluster_dir == r"e:\tmp\t07\srv1"

    def test_key_names_are_case_insensitive(self) -> None:
        # [Ф] А1: -PORT/-REGPORT/-RANGE/-D подняли полноценное дерево.
        argv = ("ragent.exe", "-PORT", "2540", "-D", r"e:\srv")
        params = extract_ragent_params(argv)
        assert params.port == 2540 and params.cluster_dir == r"e:\srv"

    def test_missing_keys_give_none(self) -> None:
        params = extract_ragent_params(("ragent.exe", "-debug"))
        assert params.port is None and params.cluster_dir is None

    def test_non_numeric_port_is_none(self) -> None:
        assert extract_ragent_params(("ragent.exe", "-port", "хлам")).port is None

    def test_value_eaten_by_next_key_is_none(self) -> None:
        # -d без значения перед следующим ключом: значение не выдумывается.
        assert extract_ragent_params(("ragent.exe", "-d", "-port", "2540")).cluster_dir is None


class TestVersionFromExePath:
    def test_standard_layout(self) -> None:
        path = Path(r"C:\Program Files\1cv8\8.3.25.1633\bin\ragent.exe")
        assert str(version_from_exe_path(path)) == "8.3.25.1633"

    def test_alien_layout_is_none(self) -> None:
        assert version_from_exe_path(Path(r"C:\tools\ragent.exe")) is None
```

- [x] **Step 2: Прогнать — падают**; **Step 3: Реализация**

```python
"""Разбор командных строк семейства ragent и сопоставление с профилями.

Краевые случаи нормализации сняты живьём ([Ф] Б1/А1 t07-protocol.md):
прямые слэши сохраняются, rmngr дописывает хвостовой разделитель —
в том числе внутрь кавычек; NTFS регистронезависима. Имена ключей
регистронезависимы ([Ф] А1).
"""

import re
from collections.abc import Sequence
from dataclasses import dataclass
from pathlib import Path

from onecstarter.domain.version import VersionNumber, parse_version


@dataclass(frozen=True)
class RagentProcess:
    pid: int
    executable: Path | None  # None — нет доступа ([Ф] В1: чужой пользователь/служба)
    argv: tuple[str, ...] | None
    create_time: float


@dataclass(frozen=True)
class RagentParams:
    port: int | None
    regport: int | None
    range_text: str | None
    cluster_dir: str | None


_SEPARATORS = re.compile(r"[\\/]+")


def normalize_cluster_dir(text: str) -> str:
    cleaned = text.strip().strip('"')
    cleaned = _SEPARATORS.sub("\\\\", cleaned)
    return cleaned.rstrip("\\").casefold()


_VALUE_KEYS = {"-port": "port", "-regport": "regport", "-range": "range_text", "-d": "cluster_dir"}


def extract_ragent_params(argv: Sequence[str]) -> RagentParams:
    values: dict[str, str] = {}
    index = 1
    while index < len(argv):
        token = argv[index]
        field = _VALUE_KEYS.get(token.casefold())
        if field is not None and index + 1 < len(argv):
            candidate = argv[index + 1]
            if not candidate.startswith("-"):
                values[field] = candidate
                index += 2
                continue
        index += 1
    def _int_of(name: str) -> int | None:
        raw = values.get(name)
        if raw is None or not raw.isdigit():
            return None
        return int(raw)
    return RagentParams(
        port=_int_of("port"),
        regport=_int_of("regport"),
        range_text=values.get("range_text"),
        cluster_dir=values.get("cluster_dir"),
    )


def version_from_exe_path(path: Path) -> VersionNumber | None:
    try:
        return parse_version(path.parent.parent.name)
    except ValueError:
        return None
```

- [x] **Step 4: Прогнать + `CORE` + сверить: `validate_profile` из задачи 2 получает `normalize=normalize_cluster_dir` на стороне вызывающего (координатор, задача 10) — здесь ничего не менять.** **Step 5:** линт/типы/коммит `feat: разбор argv ragent и нормализация каталога кластера (T-08, задача 3)`

---

### Task 4: Домен — сопоставление процессов и профилей

**Files:**
- Modify: `src/onecstarter/domain/server_match.py`
- Test: `tests/unit/test_server_match.py`

**Interfaces:**
- Consumes: задачи 1–3.
- Produces: `ForeignServer` (frozen: `process: RagentProcess`, `version: VersionNumber | None`, `params: RagentParams | None`) — `params is None` = командная строка недоступна; `MatchResult` (frozen: `by_profile: Mapping[str, tuple[RagentProcess, ...]]` — ключ `profile.id`, `foreign: tuple[ForeignServer, ...]`); `match_profiles(profiles: Sequence[ServerProfile], processes: Sequence[RagentProcess]) -> MatchResult`.

- [x] **Step 1: Тесты**

```python
from onecstarter.domain.server import ServerProfile
from onecstarter.domain.server_match import RagentProcess, match_profiles


def _proc(pid: int, cluster: str | None, *, exe: str | None = r"C:\Program Files\1cv8\8.3.25.1633\bin\ragent.exe") -> RagentProcess:
    argv = None if cluster is None else ("ragent.exe", "-port", "2540", "-d", cluster)
    return RagentProcess(pid=pid, executable=Path(exe) if exe else None, argv=argv, create_time=100.0 + pid)


class TestMatchProfiles:
    def test_match_by_normalized_dir(self) -> None:
        profile = _profile()  # cluster_dir=E:\srv\srv_8.3.25.1633
        result = match_profiles([profile], [_proc(1, "e:/SRV/srv_8.3.25.1633/")])
        assert [p.pid for p in result.by_profile[profile.id]] == [1]
        assert result.foreign == ()

    def test_unmatched_goes_foreign_with_version(self) -> None:
        result = match_profiles([_profile()], [_proc(2, r"D:\clusters\prod")])
        assert result.by_profile[_profile().id] == ()
        foreign = result.foreign[0]
        assert str(foreign.version) == "8.3.25.1633" and foreign.params is not None

    def test_opaque_process_is_always_foreign(self) -> None:
        # [Ф] В1: argv чужих процессов не виден — сопоставление невозможно в принципе.
        result = match_profiles([_profile()], [_proc(3, None, exe=None)])
        foreign = result.foreign[0]
        assert foreign.params is None and foreign.version is None

    def test_two_processes_on_one_dir_both_match(self) -> None:
        procs = [_proc(1, r"E:\srv\srv_8.3.25.1633"), _proc(2, r"e:\srv\srv_8.3.25.1633\\")]
        result = match_profiles([_profile()], procs)
        assert len(result.by_profile[_profile().id]) == 2
```

- [x] **Step 2: падают**; **Step 3: Реализация**

```python
@dataclass(frozen=True)
class ForeignServer:
    process: RagentProcess
    version: VersionNumber | None
    params: RagentParams | None  # None — командная строка недоступна ([Ф] В1)


@dataclass(frozen=True)
class MatchResult:
    by_profile: Mapping[str, tuple[RagentProcess, ...]]
    foreign: tuple[ForeignServer, ...]


def match_profiles(
    profiles: Sequence[ServerProfile], processes: Sequence[RagentProcess]
) -> MatchResult:
    wanted = {normalize_cluster_dir(p.cluster_dir): p.id for p in profiles}
    matched: dict[str, list[RagentProcess]] = {p.id: [] for p in profiles}
    foreign: list[ForeignServer] = []
    for process in processes:
        params = extract_ragent_params(process.argv) if process.argv else None
        cluster = params.cluster_dir if params else None
        profile_id = wanted.get(normalize_cluster_dir(cluster)) if cluster else None
        if profile_id is not None:
            matched[profile_id].append(process)
            continue
        version = version_from_exe_path(process.executable) if process.executable else None
        foreign.append(ForeignServer(process=process, version=version, params=params))
    return MatchResult(
        by_profile={key: tuple(value) for key, value in matched.items()},
        foreign=tuple(foreign),
    )
```

(Импорт `ServerProfile` из `onecstarter.domain.server`, `Mapping` из `collections.abc`.)

- [x] **Step 4: зелёные**; **Step 5:** коммит `feat: сопоставление ragent-процессов с профилями (T-08, задача 4)`

---

### Task 5: Реестр раскладки — серверные соглашения и фильтр установок

**Files:**
- Modify: `src/onecstarter/platform_1c/registry.toml`, `src/onecstarter/platform_1c/registry.py`
- Create: `src/onecstarter/platform_1c/server_discovery.py`
- Test: `tests/unit/test_server_discovery.py`
- Modify: `tests/unit/test_no_qt_in_core.py` (`"onecstarter.platform_1c.server_discovery",`)

**Interfaces:**
- Consumes: `ServerConvention`, `server_convention_for` (задача 1); `Installation` (`domain/version.py`).
- Produces: `load_server_conventions(data: bytes | None = None) -> list[ServerConvention]` (в `registry.py`, по образцу `load_conventions`); `ServerInstallation` (frozen: `installation: Installation`, `ragent: Path`, `radmin: Path`); `server_installations(installations: Sequence[Installation], conventions: Sequence[ServerConvention]) -> list[ServerInstallation]`; `console_path(root: Path, convention: ServerConvention) -> Path` — `root` = родитель каталога версии (`installation.path.parent`).

- [x] **Step 1: В `registry.toml` добавить** (данные, не код — боль Д):

```toml
# Серверные компоненты ([Ф] Г1: во всех версиях 8.3.10…8.5.4 машины заказчика).
[[server_conventions]]
min_version = "8.2"
bin_dir = "bin"
ragent = "ragent.exe"
radmin = "radmin.dll"
# Путь от КОРНЯ 1cv8 (родителя каталогов версий), не от каталога версии ([Ф] Г1).
console = "common/1CV8 Servers (x86-64).msc"
```

- [x] **Step 2: Тесты**

```python
from pathlib import Path

from onecstarter.domain.server import ServerConvention
from onecstarter.domain.version import Arch, Installation, parse_version
from onecstarter.platform_1c.registry import load_server_conventions
from onecstarter.platform_1c.server_discovery import console_path, server_installations

CONV = ServerConvention(parse_version("8.2"), "bin", "ragent.exe", "radmin.dll", "common/1CV8 Servers (x86-64).msc")


def test_load_server_conventions_from_shipped_toml() -> None:
    conventions = load_server_conventions()
    assert conventions and conventions[0].ragent == "ragent.exe"


def test_filters_by_presence_of_server_components(tmp_path: Path) -> None:
    full = tmp_path / "8.3.25.1633"
    (full / "bin").mkdir(parents=True)
    (full / "bin" / "ragent.exe").write_bytes(b"")
    (full / "bin" / "radmin.dll").write_bytes(b"")
    bare = tmp_path / "8.3.27.2214"
    (bare / "bin").mkdir(parents=True)  # клиентская установка без сервера
    installations = [
        Installation(parse_version(p.name), p, Arch.X64) for p in (full, bare)
    ]
    found = server_installations(installations, [CONV])
    assert [str(item.installation.version) for item in found] == ["8.3.25.1633"]
    assert found[0].ragent == full / "bin" / "ragent.exe"


def test_console_path_is_from_root_not_version_dir(tmp_path: Path) -> None:
    assert console_path(tmp_path, CONV) == tmp_path / "common" / "1CV8 Servers (x86-64).msc"
```

- [x] **Step 3: Реализация** (`load_server_conventions` в `registry.py` читает `payload["server_conventions"]` и строит `ServerConvention(min_version=parse_version(...), bin_dir=..., ragent=..., radmin=..., console=...)`; `server_discovery.py` — чистый фильтр: для каждой установки `server_convention_for(version)`, проверить `is_file()` у `ragent`/`radmin`, собрать `ServerInstallation`.)

- [x] **Step 4: зелёные + `CORE`**; **Step 5:** коммит `feat: серверные соглашения раскладки и фильтр установок (T-08, задача 5)`

---

### Task 6: Хранилище профилей servers.json

**Files:**
- Modify: `src/onecstarter/services/errors.py`
- Create: `src/onecstarter/services/server_store.py`
- Test: `tests/unit/test_server_store.py`
- Modify: `tests/unit/test_no_qt_in_core.py` (`"onecstarter.services.server_store",`)

**Interfaces:**
- Consumes: `ServerProfile`, `atomic_write`.
- Produces: в `errors.py` — `ServerError(ServicesError)`, `ServersUnavailableError(ServerError)`, `ServerStopError(ServerError)`, `ConsoleRegistrationError(ServerError)`, `ConsoleRegistrationDeclinedError(ServerError)`; в `server_store.py` — `SCHEMA_VERSION = 1`, `load_profiles(path: Path) -> list[ServerProfile]`, `save_profiles(path: Path, profiles: Sequence[ServerProfile]) -> None`. JSON-схема — спека §5: `{"schema": 1, "profiles": [{id, name, version, port, regport, "range": "1560:1591", cluster_dir, debug, http, extra_args}]}`; порядок списка сохраняется.

- [x] **Step 1: Тесты** (политика — калька `user_data.py`, `.bad`-тесты — ЗАЩИТНЫЕ)

```python
class TestRoundTrip:
    def test_profiles_survive_save_load(self, tmp_path: Path) -> None:
        path = tmp_path / "servers.json"
        save_profiles(path, [_profile()])
        assert load_profiles(path) == [_profile()]

    def test_range_is_serialized_as_colon_string(self, tmp_path: Path) -> None:
        path = tmp_path / "servers.json"
        save_profiles(path, [_profile()])
        payload = json.loads(path.read_text(encoding="utf-8"))
        assert payload["profiles"][0]["range"] == "1560:1591"

    def test_missing_file_is_empty_list(self, tmp_path: Path) -> None:
        assert load_profiles(tmp_path / "нет.json") == []


class TestBadPolicy:
    def test_corrupt_file_moves_to_bad(self, tmp_path: Path) -> None:
        """ЗАЩИТНЫЙ ТЕСТ: испорченный файл уезжает в .bad, не затирается молча."""
        path = tmp_path / "servers.json"
        path.write_text("{мусор", encoding="utf-8")
        assert load_profiles(path) == []
        assert (tmp_path / "servers.json.bad").exists() and not path.exists()

    def test_unreadable_file_raises_not_empty(self, tmp_path: Path) -> None:
        """ЗАЩИТНЫЙ ТЕСТ: недоступный файл — ошибка, не пустой список.

        Молчаливая подмена пустотой означала бы, что первое же сохранение
        затрёт профили пользователя (докстринг user_data.py — тот же довод).
        """
        directory = tmp_path / "servers.json"
        directory.mkdir()  # каталог на месте файла: IsADirectoryError=OSError
        with pytest.raises(ServersUnavailableError):
            load_profiles(directory)
```

- [x] **Step 2: падают**; **Step 3: Реализация** — калька `load_user_data`/`save_user_data`/`_move_aside` из `services/user_data.py` (та же лестница `FileNotFoundError → []`, `UnicodeDecodeError → _move_aside`, `OSError → ServersUnavailableError`, разбор с `_move_aside` при `ValueError/KeyError/TypeError`); кодек: `"range": f"{p.range_start}:{p.range_end}"` при записи, разбор по `partition(":")` при чтении, неизвестные ключи записей игнорируются (новые поля — необязательные ключи без bump схемы, правило `settings.py`).

- [x] **Step 4: зелёные + `CORE`**; **Step 5:** коммит `feat: хранилище профилей servers.json с политикой .bad (T-08, задача 6)`

---

### Task 7: Настройка «Корень каталогов серверов»

**Files:**
- Modify: `src/onecstarter/services/settings.py`, `src/onecstarter/ui/settings_view.py`
- Test: `tests/unit/test_settings.py`, `tests/ui/test_settings_view.py`

**Interfaces:**
- Produces: поле `Settings.servers_root: str = ""` (пустая строка — не задан, спека §3.5); ключ `"servers_root"` в `save_settings`; чтение — `_servers_root_of(value)` (не-строка → `""`). В `SettingsView` — группа «СЕРВЕРЫ», строка «Корень каталогов серверов»: поле пути + кнопка «Обзор…», запись через `store.update(servers_root=...)`; заголовок/подпись доступны аксессорами `row_note`/`row_control` (существуют).

- [x] **Step 1: Тесты** — в `tests/unit/test_settings.py`: `test_servers_root_default_is_empty` (старый файл без ключа → `""` — сохранность, ЗАЩИТНЫЙ: файл прошлой версии не уезжает в `.bad`), `test_servers_root_round_trip`, `test_servers_root_non_string_falls_back_to_empty`; в `tests/ui/test_settings_view.py`: строка существует, правка поля зовёт `store.update` ровно один раз (образец соседних тестов файла).

- [x] **Step 2–4: TDD-цикл** по образцу существующих полей (`default_client` — та же механика: поле dataclass + кодек + вьюха).

- [x] **Step 5:** коммит `feat: настройка «Корень каталогов серверов» (T-08, задача 7)`

---

### Task 8: Сканер процессов (psutil) и его фейк

**Files:**
- Modify: `pyproject.toml` (зависимость `psutil>=7`)
- Create: `src/onecstarter/platform_1c/process_scan.py`
- Test: `tests/unit/test_process_scan.py`
- Modify: `tests/unit/test_no_qt_in_core.py` (`"onecstarter.platform_1c.process_scan",`)

**Interfaces:**
- Produces: `ProcessInfo` (frozen: `pid: int`, `name: str`, `executable: Path | None`, `argv: tuple[str, ...] | None`, `create_time: float`); `class ProcessScanner(Protocol): def snapshot(self, names: frozenset[str]) -> list[ProcessInfo]` — `names` в casefold; `PsutilScanner` (реализация: `psutil.process_iter(attrs=["pid","name","cmdline","exe","create_time"])`, недоступные поля → `None`, [Ф] В1/В3); `NullScanner` (пустой снимок — для smoke, образец `NullRegistry`).

- [x] **Step 1:** `uv add psutil` → `uv run python -c "import psutil; print(psutil.__version__)"`.
  Правка вслед за находкой (задача 8): psutil 7.x не несёт `py.typed` —
  для mypy strict добавляется `types-psutil` в dev-группу (в runtime
  не попадает).

- [x] **Step 2: Тесты** — `NullScanner` пуст; интеграционный: запустить подставной питон-процесс с ragent-подобным хвостом argv (`subprocess.Popen([sys.executable, "-c", "import time; time.sleep(30)", "-port", "9999", "-d", str(tmp_path)])`), `PsutilScanner().snapshot(frozenset({"python.exe"}))` обязан найти его PID с argv и `create_time > 0`; в `finally` — `proc.kill()`. Живой 1С не участвует.

- [x] **Step 3: Реализация** (~40 строк: обход `process_iter`, фильтр по `info["name"].casefold() in names`, сборка `ProcessInfo`, `AccessDenied`-поля остаются `None`).

- [x] **Step 4: зелёные + `CORE`**; **Step 5:** коммит `feat: сканер процессов на psutil + NullScanner (T-08, задача 8)`

---

### Task 9: Завершение дерева, повышение, реестр консоли

**Files:**
- Create: `src/onecstarter/platform_1c/process_control.py`, `src/onecstarter/platform_1c/elevation.py`, `src/onecstarter/platform_1c/console.py`
- Test: `tests/unit/test_process_control.py`, `tests/unit/test_console.py`
- Modify: `tests/unit/test_no_qt_in_core.py` (три строки)

**Interfaces:**
- `process_control.py`: `ProcessMismatchError(Exception)`; `class ProcessControl(Protocol): def children(self, pid: int) -> list[ProcessInfo]; def terminate(self, pid: int, expected_create_time: float) -> None`; `PsutilControl` — `terminate` сверяет `psutil.Process(pid).create_time()` с ожидаемым (допуск 0.5 с — источник тот же psutil), несовпадение → `ProcessMismatchError`, `NoSuchProcess` → молча (уже нет — цель достигнута); `NullControl` (для smoke: `children` пуст, `terminate` — no-op).
- `elevation.py`: `ElevationDeclinedError(Exception)`; `run_elevated(executable: str, arguments: str, *, shell_execute: Callable[..., int] | None = None) -> int` — `ShellExecuteExW` c `runas` через ctypes, ожидание, `GetExitCodeProcess`; `ERROR_CANCELLED` (1223) → `ElevationDeclinedError`. Инъекция `shell_execute` — тестам.
- `console.py`: `RADMIN_CLSIDS: tuple[str, str] = ("{803144C8-17E6-4926-86C5-C195B6D226D4}", "{A42674D4-2D97-4988-A81D-2C113CC42A95}")` ([Ф] Г2); `registered_radmin_path(read_value: Callable[[str], str | None] = _winreg_read) -> Path | None` — читает `HKLM\SOFTWARE\Classes\CLSID\{...}\InprocServer32` (default value) первого CLSID; `register_arguments(dll: Path) -> str` → `f'/s "{dll}"'` — команда для `run_elevated("regsvr32", ...)`, одна, без `/u` ([Ф] Г2).

- [x] **Step 1: Тесты.** `PsutilControl.terminate` на подставном питон-процессе: завершает при верном `create_time`; ЗАЩИТНЫЙ — `pytest.raises(ProcessMismatchError)` при `expected_create_time` со сдвигом в час, процесс остаётся жив (проверить и убить в `finally`). `console.registered_radmin_path` — на фейковом `read_value` (словарь путь→значение): найден → `Path`, нет ключей → `None`. `register_arguments` — точная строка с кавычками. `run_elevated` — на фейковом `shell_execute`: код возврата проходит насквозь; 1223 → `ElevationDeclinedError`. Живой реестр и настоящий UAC в тестах не трогаются.

- [x] **Step 2–4: TDD-цикл + `CORE` (три модуля).**

- [x] **Step 5:** коммит `feat: завершение дерева процессов, UAC-повышение, чтение регистрации консоли (T-08, задача 9)`

---

### Task 10: Координатор ServersWorkspace — профили и хранение

**Files:**
- Create: `src/onecstarter/services/servers.py`
- Test: `tests/unit/test_servers.py`
- Modify: `tests/unit/test_no_qt_in_core.py` (`"onecstarter.services.servers",`)

**Interfaces:**
- Produces:

```python
class ServersWorkspace:
    def __init__(
        self,
        store_path: Path,
        *,
        control: ProcessControl,
        spawn: Callable[[LaunchCommand], int] = process.spawn,
        run_elevated: Callable[[str, str], int] = elevation.run_elevated,
        open_file: Callable[[str], None] = os.startfile,
        registered_radmin: Callable[[], Path | None] = console.registered_radmin_path,
        new_id: Callable[[], str] = lambda: uuid.uuid4().hex,
    ) -> None: ...
    def profiles(self) -> list[ServerProfile]
    def add_profile(self, profile: ServerProfile) -> None      # id подставляет new_id, валидация, запись
    def update_profile(self, profile: ServerProfile) -> None   # по id; валидация против остальных
    def remove_profile(self, profile_id: str) -> None
```

  Валидация зовёт `validate_profile(..., normalize=normalize_cluster_dir)`; ошибки — `ServerError` с текстом первой. Запись — `save_profiles`; отказ `OSError` → откат списка в памяти + `ServerError` (образец `Workspace._store_user`).

- [x] **Step 1: Тесты** (фейки: `FakeControl` — журнал вызовов; хранение — `tmp_path`):
  добавление/правка/удаление с перечитыванием файла; отказ валидации (дубль порта) — `pytest.raises(ServerError)`, файл не тронут; ЗАЩИТНЫЙ `test_failed_save_rolls_back_memory`: подставить `store_path` каталогом после первого сохранения → `add_profile` падает `ServerError`, `profiles()` показывает прежний список (не новый) — экран не врёт о содержимом файла.

- [x] **Step 2–4: TDD-цикл + `CORE`.** **Step 5:** коммит `feat: ServersWorkspace — профили с валидацией и откатом (T-08, задача 10)`

---

### Task 11: Координатор — снимок, статусы, чужие, сироты

**Files:**
- Modify: `src/onecstarter/services/servers.py`
- Test: `tests/unit/test_servers.py`

**Interfaces:**
- Produces: `SCAN_NAMES: frozenset[str] = frozenset({"ragent.exe", "rmngr.exe"})` — rmngr нужен для сирот ([Ф] А3), `rphost`/`dbda` не сканируются (без `-d`, для статуса не нужны — [Ф] Б1); `scan_servers(scanner: ProcessScanner) -> ScanSnapshot` (модульная функция — зовётся из фонового потока ui); `ScanSnapshot` (frozen: `agents: tuple[ProcessInfo, ...]`, `managers: tuple[ProcessInfo, ...]`); методы:

```python
def apply_scan(self, snapshot: ScanSnapshot) -> None          # главный поток
def statuses(self, installed: Sequence[VersionNumber]) -> list[ServerStatus]
def foreign_servers(self) -> list[ForeignServer]
def orphan_managers(self, profile_id: str) -> list[ProcessInfo]
```

  `ServerStatus` (frozen): `profile: ServerProfile`, `resolved: VersionNumber | None`, `processes: tuple[RagentProcess, ...]`, `orphans: tuple[ProcessInfo, ...]`, `dir_mismatch: bool` — каталог строился не от разрешённой версии (спека §3.2: предупреждение, не блокер; эвристика: имя листового каталога содержит номер версии и он ≠ `resolved`). Сирота ([Ф] А3): `rmngr`, чей `-port` == `profile.regport` или `-d` совпал с профилем, при пустом `by_profile[profile.id]`.

- [x] **Step 1: Тесты:** статус running (1 процесс), stopped (0), несколько (2 — по мокапу: остановка станет неактивна в UI); resolved по маске против `installed`; чужой с argv и без ([Ф] В1 — ограниченная строка); ЗАЩИТНЫЙ `test_orphan_manager_is_reported`: rmngr на regport профиля без живого ragent попадает в `orphans` — иначе следующий запуск умрёт молча об его порт ([Ф] А3); `dir_mismatch`: профиль-маска `8.3.25`, каталог `srv_8.3.25.1560`, resolved `8.3.25.1633` → `True`; точная версия и совпадающий каталог → `False`.

- [x] **Step 2–4: TDD-цикл.** **Step 5:** коммит `feat: снимок процессов, статусы, чужие серверы и сироты (T-08, задача 11)`

---

### Task 12: Координатор — запуск и остановка деревом

**Files:**
- Modify: `src/onecstarter/services/servers.py`
- Test: `tests/unit/test_servers.py`

**Interfaces:**
- Produces:

```python
def start(
    self,
    profile_id: str,
    server_installations: Sequence[ServerInstallation],
) -> int  # pid; ServerError при отказе
def stop(self, profile_id: str) -> None      # ServerStopError при несовпадении create_time
def stop_orphans(self, profile_id: str) -> None
```

  `start`: разрешить версию (`resolve_server_version`) → найти `ServerInstallation` → `LaunchCommand(executable=<ragent>, arguments=build_ragent_arguments(profile))` → отказ, если по последнему снимку у профиля уже есть совпавший процесс (§6.4: второй ragent на том же каталоге не запускается никогда) → `spawn`. Ошибки до порождения — `ServerError` без частичных эффектов.
  `stop`: по последнему снимку взять процессы профиля; на каждый: `control.children(pid)` (снимок детей ДО убийства), `control.terminate(pid, create_time)`, затем `terminate` каждого ребёнка ([Ф] Б2: TerminateProcess не убивает детей; жёсткое завершение безопасно — кластер поднимается с тем же clstid). `ProcessMismatchError` → `ServerStopError` («PID переиспользован — обновите список»), чужие процессы не трогаются.

- [x] **Step 1: Тесты** (`FakeSpawn` — журнал `LaunchCommand`; `FakeControl` — дети по словарю, журнал `terminate`, режим несовпадения):
  успешный `start` — команда побайтово (`"<ragent>" -debug -http -port ...`); отказ «версия не установлена»; ЗАЩИТНЫЙ `test_start_refuses_when_already_running` (§6.4); ЗАЩИТНЫЙ `test_stop_kills_exactly_matched_pid_and_children` — terminate ровно PID профиля + его детей из `children()`, чужой PID из снимка не тронут; ЗАЩИТНЫЙ `test_stop_mismatched_create_time_raises_and_kills_nobody` (гонка PID §6.2); `stop_orphans` гасит только сирот профиля.

- [x] **Step 2–4: TDD-цикл.** **Step 5:** коммит `feat: запуск и остановка сервера деревом со сверкой create_time (T-08, задача 12)`

---

### Task 13: Координатор — консоль администрирования

**Files:**
- Modify: `src/onecstarter/services/servers.py`
- Test: `tests/unit/test_servers.py`

**Interfaces:**
- Produces:

```python
def current_console_version(self) -> VersionNumber | None   # из registered_radmin() + version_from_exe_path
def register_console(self, target: ServerInstallation) -> None
def open_console(self, root: Path, convention: ServerConvention) -> None  # open_file(console_path(...))
```

  `register_console`: `run_elevated("regsvr32", register_arguments(target.radmin))`; код ≠ 0 → `ConsoleRegistrationError` (код в тексте); `ElevationDeclinedError` → `ConsoleRegistrationDeclinedError` — **штатный исход**, UI показывает «Версия консоли не изменена», не ошибку программы (§7). Регистрация зовётся ТОЛЬКО из явного действия UI — координатор не зовёт её ни при создании, ни при скане.

- [x] **Step 1: Тесты:** `current_console_version` по фейковому `registered_radmin`; успешная регистрация — `run_elevated` вызван ровно с `('regsvr32', '/s "<dll>"')`; код 3 → `ConsoleRegistrationError`; отказ UAC → `ConsoleRegistrationDeclinedError`; ЗАЩИТНЫЙ `test_nothing_registers_without_explicit_call`: конструктор + `apply_scan` + `statuses` не трогают `run_elevated` (мутация «регистрация при открытии раздела» обязана убиваться).

- [x] **Step 2–4: TDD-цикл.** **Step 5:** коммит `feat: смена версии консоли администрирования с UAC (T-08, задача 13)`

---

### Task 14: UI — значок раздела и ServersView

**Files:**
- Modify: `src/onecstarter/ui/rail_icons.py`
- Create: `src/onecstarter/ui/servers/__init__.py`, `src/onecstarter/ui/servers/view.py`
- Test: `tests/ui/test_servers_view.py`, `tests/ui/test_rail_icons.py`

**Interfaces:**
- Consumes: `ServersWorkspace` (задачи 10–13), `Palette`.
- Produces: `rail_icons.servers_icon(palette: Palette) -> QIcon` (рисованные «полки» сервера через существующие `_icon`/`_pixmap` — образец `bases_icon`); `ServersView(QWidget)`:

```python
class ServersView(QWidget):
    def __init__(
        self,
        workspace: ServersWorkspace,
        *,
        installed: Callable[[], list[ServerInstallation]],
        palette: Palette,
        confirm_removal: Callable[[str], bool] | None = None,   # тестам — фейк диалога
        show_error: Callable[[str], None] | None = None,
    ) -> None: ...
    def rebuild(self) -> None                       # перечитать statuses/foreign в виджеты
    def apply_palette(self, palette: Palette) -> None
    def profile_rows(self) -> list[...]             # аксессор тестам: имя, статус-текст, активность кнопки
```

  Содержимое — мокап [2026-08-26-v2-servers-mockup.html](../specs/assets/2026-08-26-v2-servers-mockup.html): шапка (заголовок, «Консоль администрирования…», «+ Профиль»), строка пути `servers.json` + «консоль: <версия>»; карточки профилей (имя, чип статуса, `запрошенная → разрешённая`, порты, каталог, кнопка); предупреждение `dir_mismatch` и сироты — строкой под карточкой с кнопкой «Погасить»; блок «Другие серверы на машине» (полная и ограниченная форма строки — [Ф] В1); цвета — только роли палитры («работает» — акцент).

- [x] **Step 1: Тесты** (образец файла — `tests/ui/test_settings_view.py`; воркспейс с фейками из задач 10–12): карточка running показывает «Остановить», stopped — «Запустить»; версия не установлена — кнопка неактивна, текст problem-роли; несколько процессов — остановка неактивна с объяснением; ЗАЩИТНЫЙ `test_foreign_server_has_no_stop_button` (решение заказчика 5); ограниченная строка чужого — «нет доступа…» без портов; удаление работающего зовёт `confirm_removal` с текстом, содержащим «продолжит работать» (решение 8), отказ — профиль на месте.

- [x] **Step 2–4: TDD-цикл.** **Step 5:** коммит `feat: раздел «Серверы» — список профилей и чужие серверы (T-08, задача 14)`

---

### Task 15: UI — диалог профиля и диалог консоли

**Files:**
- Create: `src/onecstarter/ui/servers/dialog.py`
- Modify: `src/onecstarter/ui/servers/view.py` (вызовы диалогов)
- Test: `tests/ui/test_server_dialog.py`

**Interfaces:**
- Produces: `ServerProfileDialog(QDialog)` — поля мокапа (имя, версия со списком серверных установок + ручная маска и живой подписью `→ <разрешённая>`, порт/регпорт/диапазон, каталог с «Обзор…» и подстановкой `<servers_root>\srv_<разрешённая>` (спека §3.2, от разрешённой, не маски), тумблеры `-debug`/`-http`, доп. аргументы); `classmethod for_new(...)` / `for_edit(profile, ...)` (образец `InfobaseDialog.for_new`); валидация в живом времени: ошибки `validate_profile` под полями, «ОК» неактивен; предупреждения `warn_range_overlap` и смены `regport` («новый пустой реестр рядом со старым», [Ф] А2) — жёлтой строкой, «ОК» активен; `result_profile() -> ServerProfile`. `ConsoleDialog(QDialog)` — список версий с `radmin.dll`, чип «текущая», пометка версий работающих профилей ([Ф] Г3: совпадение до сборки обязательно), подпись про UAC, кнопки «Сделать текущей и открыть» / «Открыть» / «Отмена».

- [x] **Step 1: Тесты:** дубль порта с чужим профилем — «ОК» неактивен, причина названа; исправление — активен; смена regport в `for_edit` — предупреждение видно, «ОК» активен; подстановка каталога от разрешённой версии при выборе маски; пустой `servers_root` — каталог обязателен руками (пустое поле → ошибка); `ConsoleDialog`: текущая помечена, работающие версии помечены, выбор отдаёт правильную `ServerInstallation`.

- [x] **Step 2–4: TDD-цикл.** **Step 5:** коммит `feat: диалоги профиля сервера и консоли администрирования (T-08, задача 15)`

---

### Task 16: UI — фоновый монитор и сборка приложения

**Files:**
- Create: `src/onecstarter/ui/servers/monitor.py`
- Modify: `src/onecstarter/ui/app.py`
- Test: `tests/ui/test_server_monitor.py`, `tests/ui/test_app.py`

**Interfaces:**
- Produces: `ServerMonitor(QObject)` — калька `StartupTasks`: `snapshot_ready = Signal(object)`; `__init__(scanner: ProcessScanner, *, interval_ms: int = 5000, spawn=_spawn_daemon, parent=None)`; `start()` — `QTimer` в главном потоке, тик → демон-поток → `scan_servers(scanner)` → `emit`; `scan_now()` — внеочередной тик (после `start`, перед `stop` — спека §4.4); отказ скана — `_log_failure` + пустой снимок, лог без содержимого.
- В `app.py`: `Runtime` получает поле `servers: Path` (`appdata / "OneCStarter" / "servers.json"` в `build_runtime`); `_build_main_window` собирает `ServersWorkspace(runtime.servers, control=PsutilControl())` (для `run_smoke` — инъекция `NullScanner`/`NullControl` параметром `process_scanner`/`process_control` по образцу `autostart_registry` — smoke не сканирует машину сборщика, урок долга №8 T-04.7), `ServersView`, `ServerMonitor(PsutilScanner())`; `sections = [("Базы", view), ("Серверы", servers_view), ("Настройки", settings_view)]` + `set_section_icon(..., rail_icons.servers_icon)`; `monitor.snapshot_ready` → `servers_workspace.apply_scan` + `servers_view.rebuild`; список серверных установок держится в изменяемом холдере: `server_installed: list[ServerInstallation] = []`, `on_installations` дополняется `server_installed[:] = server_installations(found, load_server_conventions())`, а `ServersView` получает конструктором `installed=lambda: list(server_installed)` (сеттера у вьюхи нет — тот же приём, что `recent_limit=lambda:` у `BasesView`); `monitor.start()` — рядом с `tasks.start()` в `main()`, НЕ в `_build_main_window` (правило «собрать, не запуская»).
- **Подтверждающий скан (§8):** `ServersView` получает конструктором `request_scan: Callable[[], None]` (в проводке — `monitor.scan_now`); обработчик кнопки «Запустить» после успешного `workspace.start(...)` зовёт `request_scan()` и запоминает `profile_id` как «ожидает подтверждения»; если следующий `rebuild()` показывает профиль без процессов — сообщение по мокапу: `Сервер «<имя>» завершился сразу после запуска. Частая причина — занятый порт (<port>).` ([Ф] А3/А4: смерть молчалива, лога нет — никуда не отсылаем). Обработчик «Остановить» зовёт `request_scan()` ДО `workspace.stop(...)` не требуется — свежесть PID обеспечивает сверка `create_time` (§6.2); вместо этого после `stop` — `request_scan()` для обновления списка.

- [x] **Step 1: Тесты:** монитор с фейковым сканером и синхронным `spawn=lambda task: task()` эмитит снимок по `scan_now()`; отказ сканера — пустой снимок, тест не падает; в `test_app.py` — сборка окна несёт три раздела в порядке «Базы, Серверы, Настройки» ([Ф] мокап), у раздела «Серверы» есть значок; ЗАЩИТНЫЙ `test_run_smoke_uses_null_scanner`: smoke-сборка не создаёт `PsutilScanner` (журнал фейка пуст — образец `test_run_smoke` с реестром); ЗАЩИТНЫЙ `test_start_that_dies_silently_is_reported` (в `test_servers_view.py`): `start` прошёл, подтверждающий снимок пуст → `show_error` вызван с текстом, содержащим номер порта — иначе молчаливую смерть ragent ([Ф] А3) никто не увидит.

- [x] **Step 2–4: TDD-цикл.** **Step 5:** коммит `feat: фоновый монитор серверов и раздел в сборке приложения (T-08, задача 16)`

---

### Task 17: Verification-only — полный прогон и мутационная стадия

**Files:**
- Modify: `docs/tasks.md` (статус T-08)

**Шаги:**

- [x] **Step 1:** `uv run pytest -q` (все), `uv run ruff check .`, `uv run mypy` — коды 0.
- [x] **Step 2:** Smoke собранного экземпляра: `build/build.ps1` — обязан пройти с `NullScanner`; зафиксировать дельту веса dist с psutil (критерий T-07.13) и отсутствие срабатываний Defender/Касперского на сборке; результат — в `docs/tasks.md` (T-07.13 закрывается или открывается находка).
- [x] **Step 3: Мутационная стадия** — независимый агент (не автор тестов), по каждому ЗАЩИТНОМУ тесту ровно предписанная мутация, результат дословно, откат, чистое дерево:
  1. `server_store`: `_move_aside` заменить на `return []` (испорченный файл молча съеден) → обязан упасть `test_corrupt_file_moves_to_bad`.
  2. `server_store`: `OSError` при чтении → `return []` → `test_unreadable_file_raises_not_empty`.
  3. `ServersWorkspace`: убрать откат списка при отказе записи → `test_failed_save_rolls_back_memory`.
  4. `start`: снять проверку «уже работает» → `test_start_refuses_when_already_running`.
  5. `stop`: гасить первый попавшийся PID снимка вместо совпавшего → `test_stop_kills_exactly_matched_pid_and_children`.
  6. `stop`: снять сверку `create_time` (`terminate` без исключения) → `test_stop_mismatched_create_time_raises_and_kills_nobody`.
  7. `statuses`: сироты не вычисляются (`orphans=()`) → `test_orphan_manager_is_reported`.
  8. `register_console`: вызвать регистрацию из `apply_scan` → `test_nothing_registers_without_explicit_call`.
  9. `ServersView`: кнопка остановки у чужого сервера всегда активна → `test_foreign_server_has_no_stop_button`.
  10. `run_smoke`: подставить настоящий `PsutilScanner` вместо инъекции → `test_run_smoke_uses_null_scanner`.
  Табличные тесты чистых функций (задачи 1–5) мутационной проверки не требуют (правило CLAUDE.md).
- [x] **Step 4:** Обновить `docs/tasks.md`: T-08 — итог задачи, счётчик тестов, результаты smoke и мутационной стадии; спека сверяется с кодом — расхождение правит документ (правило «переписывается правило»).
- [x] **Step 5:** коммит `docs: T-08 — итог реализации, smoke и мутационная стадия`.

---

## Чего в плане нет — сознательно

- Статус «запущен как служба» — признак службы не снят (T-07.7, нужна машина со службой): типичная служба несопоставима и попадает в «Другие серверы» ограниченной строкой ([Ф] В1, спека §3.1). Детектор не пишется вовсе — YAGNI до факта.
- Действие «Консоль для этого сервера» в строке профиля — предложение после Г3, решение заказчика не принято; в объёме только общий диалог консоли.
- Дефолты ключей ragent — не сняты (порт 1540 занят рабочим сервером заказчика): показ чужого ragent без ключей отображает «порты: не указаны», дефолты не выдумываются.
- Обновление мокапа — отдельная короткая задача вне плана кода.
