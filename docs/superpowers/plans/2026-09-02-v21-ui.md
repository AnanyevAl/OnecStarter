# Интерфейсные правки v2.1 — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** реализовать четыре интерфейсных замечания заказчика по спеке
[2026-09-02-v21-ui-design.md](../specs/2026-09-02-v21-ui-design.md): сворачиваемые
группы настроек, широкое поле пути, «Версия» и «Клиент» в диалоге добавления базы,
подменю «Версия платформы» в списке баз.

**Architecture:** новый виджет `ui/settings_group.py::CollapsibleGroup` берёт
на себя свёртку и рисованный шеврон; `SettingsView` перестаёт класть строки
в корневой layout и кладёт их в тело текущей группы. Список версий переезжает
из `ui/dialogs/infobase.py` в `services/display.py`, чтобы стать общим для диалога
и нового подменю. Запись идёт прежними путями `Workspace.add_infobase` /
`update_infobase` — второго способа менять `.v8i` не появляется.

**Tech Stack:** Python 3.14, PySide6 6.11.1, pytest + pytest-qt, uv, ruff, mypy.

## Global Constraints

- **Qt только в `src/onecstarter/ui/`.** `domain`, `config`, `platform_1c`,
  `security`, `services` не импортируют `PySide6` ни прямо, ни транзитивно.
- **`mypy` в режиме `strict`** для всего, кроме `onecstarter.ui.*`.
- **Кириллица и `ruff`:** построчные `# noqa: RUF001/RUF002/RUF003`;
  `RUF100` ловит лишние — не ставить про запас.
- **Ветка:** `feat/2026-09-02-v21-ui`, базируется на `master@6ac8729`.
  Не пушить без явного согласия заказчика.
- **Полный прогон pytest — только в файл**, никогда через `| tail`.
  Команда: `PYTHONFAULTHANDLER=1 uv run pytest -rA --tb=long -p no:cacheprovider > <файл> 2>&1`.
- **Код выхода 139** (`access violation` в `pytest-qt`) — известный долг T-12 п. 15,
  НЕ регресс: повторить прогон, повтор обязан пройти. Регрессом считать только
  воспроизводимое падение.
- **Защитный тест обязан упасть на мутации** до того, как считается написанным.
  Что именно ломали — записать в `docs/tasks.md`. На табличных тестах чистых
  функций мутация не требуется.
- **Оболочка — PowerShell 5.1:** нет `&&` и `||`; многострочное сообщение
  коммита — только `git commit -F <файл>`.
- **Версия** правится в одном месте — `pyproject.toml`, в последней задаче.
- **Приватное в репозиторий не попадает:** реальные пути с именем пользователя,
  имена баз, GUID.

---

## File Structure

| Файл | Ответственность | Задача |
| --- | --- | --- |
| `src/onecstarter/ui/settings_group.py` | **создаётся.** `CollapsibleGroup` и `chevron_pixmap` | 1 |
| `tests/ui/test_settings_group.py` | **создаётся.** Поведение виджета в изоляции | 1 |
| `src/onecstarter/ui/theme.py` | Правила `#SettingsGroupLabel`, `#SettingsBlockLabel` | 2 |
| `src/onecstarter/ui/settings_view.py` | Сборка раздела на группах; флаг `wide_control` | 2, 3 |
| `tests/ui/test_settings_view.py` | Долг §1.6, новые проверки свёртки и ширины поля | 2, 3 |
| `src/onecstarter/services/display.py` | **`version_options`** — общий список версий | 4 |
| `tests/unit/test_display.py` | Табличные тесты `version_options` | 4 |
| `src/onecstarter/ui/dialogs/infobase.py` | Зовёт `version_options`; «Версия»/«Клиент» для новой записи; `new_record` | 4, 7 |
| `src/onecstarter/ui/bases/view.py` | `_add_version_menu`; `_apply_new_infobase` | 5, 7 |
| `tests/ui/test_bases_view.py` | Состав подменю, отказ на общем списке, запись версии | 5 |
| `src/onecstarter/services/model.py` | **`NewInfobase`** — данные новой записи | 6 |
| `src/onecstarter/services/workspace.py` | `add_infobase(version, app)` | 6 |
| `tests/unit/test_workspace.py` | Версия и клиент при добавлении | 6 |
| `pyproject.toml`, `docs/tasks.md` | Версия и закрытие вехи | 8 |

---

## Task 1: `CollapsibleGroup` и рисованный шеврон

**Files:**
- Create: `src/onecstarter/ui/settings_group.py`
- Test: `tests/ui/test_settings_group.py`

**Interfaces:**
- Consumes: `onecstarter.ui.theme.Palette` (уже существует).
- Produces:
  - `chevron_pixmap(expanded: bool, colour: str, box: int = 13) -> QPixmap`
  - `CollapsibleGroup(title: str, *, accent: bool = True, rule: bool = True,
    note: QWidget | None = None, object_name: str = "SettingsGroupLabel",
    parent: QWidget | None = None)`
  - методы: `set_expanded(bool) -> None`, `is_expanded() -> bool`,
    `body_layout() -> QVBoxLayout`, `title() -> str`,
    `set_palette(palette: Palette) -> None`
  - сигнал `toggled(bool)`

**Ловушка Qt, из-за которой шрифт ставится кодом, а не в `stylesheet()`:**
Qt Style Sheets **не поддерживают `letter-spacing`**. Разрядка заголовка
из утверждённого мокапа задаётся через `QFont.setLetterSpacing`. Чтобы
свойства шрифта не оказались в двух местах сразу (QSS перебивает `setFont`
непредсказуемо), **весь** шрифт заголовка ставится кодом здесь, а в QSS
для `#SettingsGroupLabel` остаются только цвет и «хром» (см. задачу 2).

- [ ] **Step 1: Написать падающий тест**

Создать `tests/ui/test_settings_group.py`:

```python
"""Сворачиваемая группа настроек: свёртка, шеврон, тело."""

from PySide6.QtWidgets import QApplication, QLabel

from onecstarter.ui.settings_group import CollapsibleGroup, chevron_pixmap
from onecstarter.ui.theme import DARK, LIGHT


def test_group_is_collapsed_on_creation(application: QApplication) -> None:
    """По умолчанию свёрнуто — решение заказчика 02.09.2026 (спека §1.4)."""
    group = CollapsibleGroup("СЕРВЕРЫ")
    body = QLabel("строка")
    group.body_layout().addWidget(body)

    assert group.is_expanded() is False
    assert body.isHidden() is True


def test_expanding_shows_the_body_and_collapsing_hides_it(
    application: QApplication,
) -> None:
    group = CollapsibleGroup("СЕРВЕРЫ")
    body = QLabel("строка")
    group.body_layout().addWidget(body)

    group.set_expanded(True)
    assert body.isHidden() is False

    group.set_expanded(False)
    assert body.isHidden() is True


def test_toggled_signal_reports_the_new_state(application: QApplication) -> None:
    group = CollapsibleGroup("СЕРВЕРЫ")
    seen: list[bool] = []
    group.toggled.connect(seen.append)

    group.set_expanded(True)
    group.set_expanded(False)

    assert seen == [True, False]


def test_chevron_differs_between_states(application: QApplication) -> None:
    """Свёрнуто — «>», раскрыто — он же вниз. Одна картинка на оба состояния
    означала бы, что шеврон не сообщает состояние вовсе."""
    collapsed = chevron_pixmap(False, DARK.accent).toImage()
    expanded = chevron_pixmap(True, DARK.accent).toImage()

    assert not collapsed.isNull()
    assert collapsed != expanded


def test_chevron_follows_the_palette(application: QApplication) -> None:
    """Смена темы перекрашивает шеврон: он рисуется, а не берётся из шрифта."""
    group = CollapsibleGroup("СЕРВЕРЫ")
    group.set_palette(DARK)
    dark = group.chevron().toImage()
    group.set_palette(LIGHT)

    assert group.chevron().toImage() != dark


def test_note_stays_visible_while_the_body_is_collapsed(
    application: QApplication,
) -> None:
    """Подпись блока — не часть сворачиваемого тела (нужна `_add_block`, задача 2)."""
    note = QLabel("подпись")
    group = CollapsibleGroup("Сочетания раздела «Базы»", note=note)

    assert group.is_expanded() is False
    assert note.isHidden() is False
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/ui/test_settings_group.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'onecstarter.ui.settings_group'`

- [ ] **Step 3: Написать минимальную реализацию**

Создать `src/onecstarter/ui/settings_group.py`:

```python
"""Сворачиваемая группа настроек: заголовок-кнопка, разделитель, тело.

Шеврон рисуется `QPainter`-ом (спека §1.3). Два отвергнутых способа:
глиф `▸` зависит от наличия символа в шрифте — в офскрин-прогоне мокапа
он выходил плашкой, — а `QToolButton.setArrowType` рисует залитый
треугольник, а не «>», и вид зависит ещё и от темы оформления Windows.
Рисованный шеврон не зависит ни от того, ни от другого.

Заголовок — `QToolButton`, а не `QLabel` с `mousePressEvent`: кнопка даёт
фокус по `Tab`, срабатывание пробелом и `Enter` и роль в дереве
доступности бесплатно, а ручная обработка мыши на метке — ничего из этого.

Шрифт заголовка ставится кодом, а не в `stylesheet()`: Qt Style Sheets
не поддерживают `letter-spacing`, а разрядка есть в утверждённом мокапе.
Держать часть свойств шрифта в QSS, а часть в `setFont` нельзя — они
перебивают друг друга непредсказуемо, поэтому в QSS остаются только цвет
и «хром» (см. `theme.py`).
"""  # noqa: RUF002

from PySide6.QtCore import QPointF, Qt, Signal
from PySide6.QtGui import QColor, QIcon, QPainter, QPainterPath, QPen, QPixmap
from PySide6.QtWidgets import QFrame, QToolButton, QVBoxLayout, QWidget

from onecstarter.ui.theme import DARK, Palette

CHEVRON_BOX = 13
CHEVRON_THICKNESS = 1.7
TITLE_POINT_SIZE = 10
TITLE_LETTER_SPACING = 0.8


def chevron_pixmap(expanded: bool, colour: str, box: int = CHEVRON_BOX) -> QPixmap:
    """«>» в свёрнутом виде, он же повёрнутый вниз — в раскрытом."""
    pixmap = QPixmap(box, box)
    pixmap.fill(Qt.GlobalColor.transparent)
    painter = QPainter(pixmap)
    painter.setRenderHint(QPainter.RenderHint.Antialiasing)
    pen = QPen(QColor(colour), CHEVRON_THICKNESS)
    pen.setCapStyle(Qt.PenCapStyle.RoundCap)
    pen.setJoinStyle(Qt.PenJoinStyle.RoundJoin)
    painter.setPen(pen)

    near, far, mid = 4.0, box - 4.0, box / 2.0
    path = QPainterPath()
    if expanded:
        path.moveTo(QPointF(near, mid - 1.5))
        path.lineTo(QPointF(mid, far - 1.5))
        path.lineTo(QPointF(far, mid - 1.5))
    else:
        path.moveTo(QPointF(mid - 1.5, near))
        path.lineTo(QPointF(far - 1.5, mid))
        path.lineTo(QPointF(mid - 1.5, far))
    painter.drawPath(path)
    painter.end()
    return pixmap


class CollapsibleGroup(QWidget):
    """Заголовок-кнопка, необязательные разделитель и подпись, скрываемое тело."""

    toggled = Signal(bool)

    def __init__(
        self,
        title: str,
        *,
        accent: bool = True,
        rule: bool = True,
        note: QWidget | None = None,
        object_name: str = "SettingsGroupLabel",
        parent: QWidget | None = None,
    ) -> None:
        super().__init__(parent)
        self._title = title
        self._accent = accent
        self._palette = DARK
        self._chevron = chevron_pixmap(False, DARK.accent)

        self._button = QToolButton()
        self._button.setObjectName(object_name)
        self._button.setText(title)
        self._button.setCheckable(True)
        self._button.setChecked(False)
        self._button.setAutoRaise(True)
        self._button.setToolButtonStyle(Qt.ToolButtonStyle.ToolButtonTextBesideIcon)
        self._button.setCursor(Qt.CursorShape.PointingHandCursor)
        font = self._button.font()
        font.setBold(True)
        if accent:
            font.setPointSize(TITLE_POINT_SIZE)
            font.setLetterSpacing(
                font.SpacingType.AbsoluteSpacing, TITLE_LETTER_SPACING
            )
        self._button.setFont(font)
        self._button.toggled.connect(self._on_toggled)

        self._rule: QFrame | None = None
        if rule:
            self._rule = QFrame()
            self._rule.setFrameShape(QFrame.Shape.HLine)
            self._rule.setFixedHeight(1)

        self._body = QWidget()
        self._body_layout = QVBoxLayout(self._body)
        self._body_layout.setContentsMargins(0, 0, 0, 0)
        self._body_layout.setSpacing(6)
        self._body.setVisible(False)

        layout = QVBoxLayout(self)
        layout.setContentsMargins(0, 0, 0, 0)
        layout.setSpacing(4)
        layout.addWidget(self._button)
        if self._rule is not None:
            layout.addWidget(self._rule)
        if note is not None:
            layout.addWidget(note)
        layout.addWidget(self._body)

        self.set_palette(DARK)

    # -- состояние ---------------------------------------------------------

    def set_expanded(self, expanded: bool) -> None:
        self._button.setChecked(expanded)

    def is_expanded(self) -> bool:
        return self._button.isChecked()

    def body_layout(self) -> QVBoxLayout:
        return self._body_layout

    def title(self) -> str:
        return self._title

    def button(self) -> QToolButton:
        """Заголовок — тестам, проверяющим срабатывание по клику и фокус."""
        return self._button

    def chevron(self) -> QPixmap:
        """Текущая картинка шеврона — тестам смены темы."""
        return self._chevron

    def set_palette(self, palette: Palette) -> None:
        """Перекрасить заголовок, шеврон и разделитель под действующую тему."""
        self._palette = palette
        self._refresh_chevron()
        if self._rule is not None:
            self._rule.setStyleSheet(f"background: {palette.border}; border: none;")

    # -- внутреннее --------------------------------------------------------

    def _on_toggled(self, checked: bool) -> None:
        self._body.setVisible(checked)
        self._refresh_chevron()
        self.toggled.emit(checked)

    def _refresh_chevron(self) -> None:
        colour = self._palette.accent if self._accent else self._palette.text
        self._chevron = chevron_pixmap(self.is_expanded(), colour)
        self._button.setIcon(QIcon(self._chevron))
```

- [ ] **Step 4: Прогнать и убедиться, что проходит**

Run: `uv run pytest tests/ui/test_settings_group.py -v`
Expected: PASS, 6 тестов, вывод чистый.

- [ ] **Step 5: Мутационная проверка**

Временно заменить в `_refresh_chevron` строку расчёта на
`self._chevron = chevron_pixmap(False, colour)` (шеврон всегда «свёрнут»).
Прогнать `uv run pytest tests/ui/test_settings_group.py -v`.
Expected: FAIL в `test_chevron_follows_the_palette`? **Нет** — упасть обязан
`test_chevron_differs_between_states` только если сломать сам
`chevron_pixmap`. Правильная мутация для виджета: убрать
`self._body.setVisible(checked)` из `_on_toggled`.
Expected: FAIL в `test_expanding_shows_the_body_and_collapsing_hides_it`.
Откатить правкой файла (не `git checkout`), записать результат
в `docs/tasks.md` при закрытии вехи.

- [ ] **Step 6: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/ui/settings_group.py tests/ui/test_settings_group.py
git commit -m "feat: CollapsibleGroup — сворачиваемая группа настроек с рисованным шевроном"
```

---

## Task 2: `SettingsView` на сворачиваемых группах

**Files:**
- Modify: `src/onecstarter/ui/theme.py:136`
- Modify: `src/onecstarter/ui/settings_view.py` (`_add_group`, `_add_row`,
  `_add_block`, `_sync`, аксессоры)
- Modify: `tests/ui/test_settings_view.py:421`, `:597`, `:629`

**Interfaces:**
- Consumes: `CollapsibleGroup`, `chevron_pixmap` из задачи 1;
  `ThemeController.palette` (уже существует, свойство).
- Produces: `SettingsView.group(title) -> CollapsibleGroup`,
  `SettingsView.expand_group(title) -> None`,
  `SettingsView.is_group_expanded(title) -> bool`.

**Долг §1.6.** Три существующие проверки перестанут ловить то, ради чего
написаны: `isHidden()` отличал «виджет потерян при сборке» от нормального,
а со свёрткой по умолчанию скрыто всё. Каждая обязана сначала раскрыть
свою группу. Это часть задачи, а не последующая уборка.

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/ui/test_settings_view.py`:

```python
def test_all_groups_are_collapsed_on_creation(
    application: QApplication, tmp_path: Path
) -> None:
    """Решение заказчика 02.09.2026: раздел открывается свёрнутым целиком."""
    view, _ = _view(application, tmp_path)

    assert [view.is_group_expanded(title) for title in view.group_labels()] == [
        False for _ in view.group_labels()
    ]


def test_expanding_a_group_shows_its_rows(
    application: QApplication, tmp_path: Path
) -> None:
    view, _ = _view(application, tmp_path)
    note = view.row_note("Корень каталогов серверов")
    assert note.isHidden() is True

    view.expand_group("СЕРВЕРЫ")
    assert note.isHidden() is False

    view.group("СЕРВЕРЫ").set_expanded(False)
    assert note.isHidden() is True


def test_shortcut_reference_is_collapsed_inside_hotkeys_group(
    application: QApplication, tmp_path: Path
) -> None:
    """Справочник — второй уровень свёртки: 12 строк читаются один раз."""
    view, _ = _view(application, tmp_path)
    view.expand_group("ГОРЯЧИЕ КЛАВИШИ")

    assert view.is_group_expanded("Сочетания раздела «Базы»") is False
    assert view.row_note("Сочетания раздела «Базы»").isHidden() is False
    assert view.row_control("Сочетания раздела «Базы»").isHidden() is True
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/ui/test_settings_view.py -k "collapsed or expanding or shortcut_reference" -v`
Expected: FAIL — `AttributeError: 'SettingsView' object has no attribute 'is_group_expanded'`

- [ ] **Step 3: Правило стиля — только цвет и «хром»**

В `src/onecstarter/ui/theme.py` заменить строку 136:

```python
#SettingsGroupLabel {{
    color: {palette.accent}; border: none; background: transparent;
    text-align: left; padding: 0;
}}
#SettingsBlockLabel {{
    color: {palette.text}; border: none; background: transparent;
    text-align: left; padding: 0;
}}
```

`font-size` и `font-weight` отсюда уходят намеренно: их ставит
`CollapsibleGroup` вместе с `letter-spacing`, который QSS не умеет
(см. докстринг `settings_group.py`). Ни один тест на эти правила
не завязан — проверено `grep` по `*.py`.

- [ ] **Step 4: Перевести сборку раздела на группы**

В `src/onecstarter/ui/settings_view.py`:

```python
# в __init__, до первого _add_group:
self._groups: dict[str, CollapsibleGroup] = {}
self._current_body: QVBoxLayout | None = None

def _add_group(self, title: str) -> None:
    if self._groups:
        self._layout.addSpacing(16)
    group = CollapsibleGroup(title)
    group.set_palette(self._controller.palette)
    self._layout.addWidget(group)
    self._groups[title] = group
    self._group_labels.append(title)
    self._current_body = group.body_layout()

def _target_layout(self) -> QVBoxLayout:
    """Куда класть строку: тело текущей группы.

    Строк вне группы в разделе нет и не должно быть — иначе они окажутся
    вне свёртки и переживут её, а раздел обещает обратное.
    """  # noqa: RUF002
    if self._current_body is None:
        raise RuntimeError("строка настроек добавлена до первой группы")
    return self._current_body
```

В `_add_row` заменить `self._layout.addLayout(row)` на
`self._target_layout().addLayout(row)`.

`_add_block` переписать на вложенную группу:

```python
def _add_block(self, title: str, note: str, body: QWidget) -> None:
    """Блок во всю ширину, свёрнутый по умолчанию (спека §1.5).

    Подпись живёт вне сворачиваемого тела: она объясняет, что внутри,
    и в свёрнутом виде нужна больше, чем в раскрытом.
    """  # noqa: RUF002
    row_note = QLabel(note)
    row_note.setObjectName("SettingsNote")
    row_note.setWordWrap(True)
    self._row_notes[title] = row_note
    self._row_controls[title] = body

    block = CollapsibleGroup(
        title, accent=False, rule=False, note=row_note,
        object_name="SettingsBlockLabel",
    )
    block.set_palette(self._controller.palette)
    block.body_layout().addWidget(body)
    self._groups[title] = block
    self._target_layout().addWidget(block)
```

Аксессоры:

```python
def group(self, title: str) -> CollapsibleGroup:
    """Группа по заголовку — тестам, проверяющим свёртку."""
    return self._groups[title]

def expand_group(self, title: str) -> None:
    self._groups[title].set_expanded(True)

def is_group_expanded(self, title: str) -> bool:
    return self._groups[title].is_expanded()
```

В конце `_sync` добавить перекраску:

```python
for group in self._groups.values():
    group.set_palette(self._controller.palette)
```

Импорт: `from onecstarter.ui.settings_group import CollapsibleGroup`.

- [ ] **Step 5: Закрыть долг §1.6 в существующих тестах**

Три места в `tests/ui/test_settings_view.py`, каждое — раскрыть свою группу
перед проверкой `isHidden()`:

```python
# строка ~421, test_shortcut_reference_lists_every_bases_shortcut
view.expand_group("ГОРЯЧИЕ КЛАВИШИ")
view.expand_group("Сочетания раздела «Базы»")
assert view.row_control("Сочетания раздела «Базы»").isHidden() is False

# строка ~597, test_autostart_row_note_...
view.expand_group("ОКНО И ЗАПУСК")
assert not note.isHidden()

# строка ~629, test_servers_root_row_note_explains_new_profile_default
view.expand_group("СЕРВЕРЫ")
assert not note.isHidden()
```

К каждому добавить комментарий, почему раскрытие обязательно:

```python
# Раскрыть обязательно: со свёрткой по умолчанию (спека §1.4) скрыто всё,
# и `isHidden` без раскрытия перестал бы отличать потерянный виджет
# от нормального — сторож, поставленный мутационной проверкой 22.08.2026,
# выродился бы во всегда истинный.
```

- [ ] **Step 6: Прогнать раздел целиком**

Run: `uv run pytest tests/ui/test_settings_view.py tests/ui/test_settings_group.py -v`
Expected: PASS, вывод чистый.

- [ ] **Step 7: Мутационная проверка долга**

Убрать из трёх тестов строки `view.expand_group(...)` и прогнать снова.
Expected: FAIL во всех трёх — сторож действительно смотрит на видимость,
а не проходит по инерции. Вернуть строки правкой файла.

Вторая мутация: в `_add_group` заменить `CollapsibleGroup(title)` на
группу, раскрытую сразу (`group.set_expanded(True)` следом).
Expected: FAIL в `test_all_groups_are_collapsed_on_creation`. Откатить.

- [ ] **Step 8: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/ui/theme.py src/onecstarter/ui/settings_view.py tests/ui/test_settings_view.py
git commit -m "feat: настройки — сворачиваемые группы, свёрнуты по умолчанию"
```

---

## Task 3: широкое поле «Корень каталогов серверов»

**Files:**
- Modify: `src/onecstarter/ui/settings_view.py` (`_add_row`, вызов для строки
  «Корень каталогов серверов»)
- Modify: `tests/ui/test_settings_view.py`

**Interfaces:**
- Consumes: `SettingsView.expand_group` из задачи 2.
- Produces: `_add_row(..., wide_control: bool = False)`.

- [ ] **Step 1: Написать падающий тест**

```python
def test_servers_root_field_grows_with_the_section(
    application: QApplication, tmp_path: Path
) -> None:
    """Поле пути тянется вместе с формой (замечание заказчика 1.1, спека §2)."""
    view, _ = _view(application, tmp_path)
    view.expand_group("СЕРВЕРЫ")
    view.resize(700, 600)
    view.layout().activate()
    narrow = view.servers_root_edit().width()

    view.resize(1200, 600)
    view.layout().activate()

    assert view.servers_root_edit().width() > narrow


def test_only_the_path_row_gets_a_wide_control(
    application: QApplication, tmp_path: Path
) -> None:
    """Растянутый сегментный переключатель выглядел бы сломанным (спека §2)."""
    view, _ = _view(application, tmp_path)
    view.expand_group("ВНЕШНИЙ ВИД")
    view.resize(700, 600)
    view.layout().activate()
    narrow = view.row_control("Тема").width()

    view.resize(1200, 600)
    view.layout().activate()

    assert view.row_control("Тема").width() == narrow
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/ui/test_settings_view.py -k "grows_with_the_section" -v`
Expected: FAIL — ширина поля не меняется, `narrow == wide`.

- [ ] **Step 3: Реализация**

В `_add_row` добавить параметр и ветку:

```python
def _add_row(
    self,
    title: str,
    note: str,
    control: QWidget,
    *,
    extra: QWidget | None = None,
    wide_control: bool = False,
) -> None:
    ...
    row = QHBoxLayout()
    if wide_control:
        # Заголовок и подпись — колонкой известной ширины, орган забирает
        # остаток и тянется с формой (спека §2). Флаг только у строки пути:
        # растянутый сегментный переключатель выглядел бы сломанным.
        holder = QWidget()
        holder.setLayout(body)
        holder.setFixedWidth(WIDE_ROW_LABEL_WIDTH)
        row.addWidget(holder)
        row.addWidget(control, stretch=1)
    else:
        row.addLayout(body, stretch=1)
        row.addWidget(control, alignment=Qt.AlignmentFlag.AlignTop)
    self._target_layout().addLayout(row)
```

Константа рядом с `SERVERS_ROOT_ROW_NOTE`:

```python
# Ширина колонки заголовка у строк с широким органом управления
# (спека §2). Значение — из утверждённого мокапа.
WIDE_ROW_LABEL_WIDTH = 330
```

Вызов для строки пути:

```python
self._add_row(
    "Корень каталогов серверов",
    SERVERS_ROOT_ROW_NOTE,
    self._build_servers_root_control(store.settings.servers_root),
    wide_control=True,
)
```

- [ ] **Step 4: Прогнать и убедиться, что проходит**

Run: `uv run pytest tests/ui/test_settings_view.py -v`
Expected: PASS.

- [ ] **Step 5: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/ui/settings_view.py tests/ui/test_settings_view.py
git commit -m "feat: поле «Корень каталогов серверов» тянется вместе с формой"
```

---

## Task 4: `version_options` переезжает в `services`

**Files:**
- Modify: `src/onecstarter/services/display.py` (добавить `version_options`)
- Modify: `src/onecstarter/ui/dialogs/infobase.py` (убрать `_version_options`,
  импортировать новую)
- Test: `tests/unit/test_display.py`

**Interfaces:**
- Consumes: `InfobaseItem`, `Installation`, `VersionCell` (существуют).
- Produces:
  `version_options(installations: Sequence[Installation], item: InfobaseItem | None = None,
  cell: VersionCell | None = None) -> list[tuple[str, str | None]]`

**Почему переезд, а не импорт из `ui`.** Потребителей становится два —
диалог (задача 7) и подменю (задача 5). Оставь список в `ui/dialogs/infobase.py`,
и меню импортировало бы его из соседнего модуля UI: работало бы, но развело бы
два источника правды при первой правке, и пользователь увидел бы в двух местах
разный набор версий для одной записи (спека §6). Qt в функции нет и не было.

- [ ] **Step 1: Написать падающий тест**

Дописать в `tests/unit/test_display.py`:

```python
def test_version_options_without_an_item_offers_installed_versions() -> None:
    """Диалог добавления: записи ещё нет, «как установлено» без уточнения."""
    options = version_options(INSTALLED)

    assert options[0] == ("как установлено", None)
    assert [value for _text, value in options[1:]] == [
        str(installation.version) for installation in INSTALLED
    ]


def test_version_options_keeps_a_requested_version_that_is_not_installed() -> None:
    """Иначе нетронутый диалог откатился бы на первый пункт и снял Version."""
    item = _item(requested_version="8.3.9")
    cell = version_cell(item, INSTALLED, [])

    options = version_options(INSTALLED, item, cell)

    assert "8.3.9" in [value for _text, value in options]


def test_version_options_does_not_duplicate_an_installed_request() -> None:
    item = _item(requested_version=str(INSTALLED[0].version))
    cell = version_cell(item, INSTALLED, [])

    values = [value for _text, value in version_options(INSTALLED, item, cell)]

    assert values.count(str(INSTALLED[0].version)) == 1
```

`INSTALLED` и `_item` — уже существующие помощники файла; если их нет,
завести локально по образцу соседних тестов модуля.

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/unit/test_display.py -k version_options -v`
Expected: FAIL — `NameError: name 'version_options' is not defined`

- [ ] **Step 3: Перенести функцию**

Вырезать `_version_options` из `src/onecstarter/ui/dialogs/infobase.py`
и вставить в `src/onecstarter/services/display.py` под именем
`version_options` с новой сигнатурой:

```python
def version_options(
    installations: Sequence[Installation],
    item: InfobaseItem | None = None,
    cell: VersionCell | None = None,
) -> list[tuple[str, str | None]]:
    """Пункты выбора версии: «как установлено» + установленные.

    Общий источник для диалога записи и подменю списка баз (спека §6):
    разойдись они, пользователь увидел бы в двух местах разный набор
    версий для одной записи.

    `item is None` — новая запись (диалог добавления): показывать нечего,
    кроме установленных, и уточнять «как установлено» нечем.

    Для существующей записи сохраняется прежнее поведение: действующая
    версия в скобках у первого пункта, когда `Version` у записи нет вовсе
    ([Ф] T-05.5), и отдельный пункт для запрошенной версии, которой нет
    среди установленных, — без него нетронутый диалог не нашёл бы себе
    пункта, откатился бы к первому, и `changes()` восприняла бы это как
    решение пользователя снять `Version`.

    Кроме WEB: там `version_cell` всегда отдаёт «веб» (Version на запуск
    веб-базы не влияет), и подстановка читалась бы как «как установлено (веб)».
    """  # noqa: RUF002
    display_text = ""
    if item is not None and cell is not None and item.kind is not ConnectKind.WEB:
        display_text = cell.text
    default_label = "как установлено"
    if item is not None and item.requested_version is None and display_text:
        default_label = f"{default_label} ({display_text})"
    options: list[tuple[str, str | None]] = [(default_label, None)]
    seen: set[str | None] = {None}
    for installation in installations:
        value = str(installation.version)
        if value in seen:
            continue
        seen.add(value)
        options.append((value, value))
    if item is not None and item.requested_version is not None:
        if item.requested_version not in seen:
            options.append((display_text or item.requested_version, item.requested_version))
    return options
```

В `ui/dialogs/infobase.py` заменить вызов:

```python
from onecstarter.services.display import VersionCell, version_cell, version_options
...
for text, data in version_options(installations, item, cell):
    self._version.addItem(text, data)
```

- [ ] **Step 4: Прогнать и убедиться, что проходит**

Run: `uv run pytest tests/unit/test_display.py tests/ui/test_infobase_dialog.py -v`
Expected: PASS. Существующие тесты диалога обязаны пройти без правок —
это и есть доказательство, что переезд ничего не изменил в поведении.

- [ ] **Step 5: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/services/display.py src/onecstarter/ui/dialogs/infobase.py tests/unit/test_display.py
git commit -m "refactor: version_options в services — общий список версий для диалога и меню"
```

---

## Task 5: подменю «Версия платформы» в списке баз

**Files:**
- Modify: `src/onecstarter/ui/bases/view.py` (`_build_menu`, новый `_add_version_menu`)
- Modify: `tests/ui/test_bases_view.py`

**Interfaces:**
- Consumes: `version_options` из задачи 4; `Workspace.update_infobase(key, changes)`;
  `_is_read_only(item)`; `COMMON_NOTE`.
- Produces: `BasesView._add_version_menu(menu: QMenu, item: InfobaseItem, key: str) -> None`,
  `BasesView.set_version(key: str, version: str | None) -> None`.

**Обязательный приём.** Подменю создаётся как `QMenu("Версия платформы", menu)`
с явным родителем, а не однострочником `menu.addMenu("…")`: последний давал
«Internal C++ object already deleted» при чтении состава подменю после возврата
`_build_menu`. Причина не установлена, обход воспроизводим — всё записано
в докстринге `_add_cache_menu`, повторять опыт не нужно.

- [ ] **Step 1: Написать падающий тест**

```python
def _version_submenu(menu: QMenu) -> QMenu | None:
    for action in menu.actions():
        if action.text() == "Версия платформы":
            return action.menu()
    return None


def test_version_submenu_offers_installed_versions(assembled) -> None:
    view, item, key = _base_row(assembled)
    menu = view._build_menu(item, key)

    submenu = _version_submenu(menu)

    assert submenu is not None
    labels = [action.text() for action in submenu.actions() if not action.isSeparator()]
    assert labels[0].startswith("как установлено")
    assert str(INSTALLED[0].version) in labels


def test_version_submenu_writes_the_chosen_version(assembled) -> None:
    """Пишет сразу, как Ctrl+D для избранного (спека §4), прежним путём."""
    view, item, key = _base_row(assembled)
    view.set_version(key, "8.3.25.1633")

    changed = next(i for i in view._workspace.items() if i.key == key)
    assert changed.requested_version == "8.3.25.1633"


def test_version_submenu_clears_version_for_as_installed(assembled) -> None:
    view, item, key = _base_row(assembled)
    view.set_version(key, "8.3.25.1633")

    view.set_version(key, None)

    changed = next(i for i in view._workspace.items() if i.key == key)
    assert changed.requested_version is None


def test_version_submenu_is_disabled_for_a_common_list_record(assembled_common) -> None:
    """Отказ показывается до действия — тем же приёмом, что «Свойства…»."""
    view, item, key = _common_row(assembled_common)
    menu = view._build_menu(item, key)

    action = next(a for a in menu.actions() if a.text() == "Версия платформы")
    assert action.isEnabled() is False
    assert COMMON_NOTE in action.toolTip()


def test_group_menu_has_no_version_submenu(assembled) -> None:
    view, item, key = _group_row(assembled)
    menu = view._group_menu_for(item, key)

    assert _version_submenu(menu) is None
```

Помощники `_base_row`, `_common_row`, `_group_row` — по образцу
существующих тестов меню в файле; если их нет, завести локально.

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/ui/test_bases_view.py -k version_submenu -v`
Expected: FAIL — `assert submenu is not None` не выполняется, подменю нет.

- [ ] **Step 3: Реализация**

В `_build_menu`, сразу после строки с `properties` и **перед**
`self._add_cache_menu(menu, item)`:

```python
version_action = self._add_version_menu(menu, item, key)
```

Новый метод рядом с `_add_cache_menu`:

```python
def _add_version_menu(self, menu: QMenu, item: InfobaseItem, key: str) -> QAction:
    """Подменю «Версия платформы» — ускоритель для того, что лежит в свойствах.

    Пункты — та же `version_options`, что у диалога записи (спека §6):
    разойдись они, пользователь увидел бы в двух местах разный набор
    версий для одной записи.

    `QMenu(title, menu)` с явным родителем, а не однострочный
    `menu.addMenu("…")`: у последнего наблюдалось «Internal C++ object
    already deleted» при чтении состава подменю после возврата
    `_build_menu` — подробности в докстринге `_add_cache_menu`.

    Выбор пишет сразу, без подтверждения, — тем же правилом, что `Ctrl+D`
    для избранного. Записи общего списка подменю получают неактивным:
    отказ показывается до действия (спека §3.2). `update_infobase`
    всё равно зовёт `_reject_common` — это второй рубеж, не замена первому.
    """  # noqa: RUF002
    submenu = QMenu("Версия платформы", menu)
    action = menu.addMenu(submenu)
    submenu.setToolTipsVisible(True)
    cell = version_cell(item, self._installations or [], self._cfg_rules)
    for index, (text, value) in enumerate(
        version_options(self._installations or [], item, cell)
    ):
        entry = submenu.addAction(text)
        entry.setCheckable(True)
        entry.setChecked(value == item.requested_version)
        entry.triggered.connect(
            lambda _checked=False, v=value: self.set_version(key, v)
        )
        if index == 0:
            submenu.addSeparator()
    if _is_read_only(item):
        menu.setToolTipsVisible(True)
        action.setEnabled(False)
        action.setToolTip(COMMON_NOTE)
    return action

def set_version(self, key: str, version: str | None) -> None:
    """Записать выбранную в подменю версию. `None` — «как установлено».

    Отдельный метод, а не лямбда внутри меню: `QMenu.exec` блокирует
    офскрин-тесты, и путь записи обязан быть достижим напрямую — тот же
    приём, что у `_apply_properties`/`show_properties`.
    """  # noqa: RUF002
    try:
        self._workspace.update_infobase(key, {"Version": version})
    except ServicesError as error:
        self._on_error(error)
        return
    self.rebuild()
```

Импорты: `version_options` из `onecstarter.services.display`, `QAction`
из `PySide6.QtGui`.

- [ ] **Step 4: Прогнать и убедиться, что проходит**

Run: `uv run pytest tests/ui/test_bases_view.py -v`
Expected: PASS.

- [ ] **Step 5: Мутационная проверка**

Убрать `action.setEnabled(False)` в ветке `_is_read_only`.
Expected: FAIL в `test_version_submenu_is_disabled_for_a_common_list_record`.

Заменить `{"Version": version}` на `{"Version": version or ""}`.
Expected: FAIL в `test_version_submenu_clears_version_for_as_installed` —
пустая строка не то же самое, что снятый ключ.

Оба отката — правкой файла, результат записать в `docs/tasks.md`.

- [ ] **Step 6: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/ui/bases/view.py tests/ui/test_bases_view.py
git commit -m "feat: подменю «Версия платформы» в контекстном меню списка баз"
```

---

## Task 6: `add_infobase` принимает версию и клиента

**Files:**
- Modify: `src/onecstarter/services/model.py` (добавить `NewInfobase`)
- Modify: `src/onecstarter/services/workspace.py:227-241`
- Modify: `tests/unit/test_workspace.py`

**Interfaces:**
- Produces:
  - `NewInfobase(name: str, connect: str, folder: str, version: str | None = None,
    app: str | None = None)` — frozen dataclass
  - `Workspace.add_infobase(name: str, connect: str, folder: str | None = None,
    version: str | None = None, app: str | None = None) -> str`

- [ ] **Step 1: Написать падающий тест**

```python
def test_add_infobase_writes_version_and_app_when_given(tmp_path: Path) -> None:
    workspace = _workspace(tmp_path)

    key = workspace.add_infobase(
        "Новая", 'File="D:\\Bases\\New";', "/", version="8.3.25.1633", app="ThinClient"
    )

    item = next(i for i in workspace.items() if i.key == key)
    assert item.requested_version == "8.3.25.1633"
    assert item.app == "ThinClient"


def test_add_infobase_writes_neither_key_by_default(tmp_path: Path) -> None:
    """Обычное добавление не меняется: «как установлено» и «Авто» молчат."""
    workspace = _workspace(tmp_path)

    key = workspace.add_infobase("Новая", 'File="D:\\Bases\\New";', "/")

    section = _raw_section(tmp_path, "Новая")
    assert "Version" not in section
    assert "App" not in section
```

`_workspace` и `_raw_section` — существующие помощники файла.

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/unit/test_workspace.py -k add_infobase -v`
Expected: FAIL — `TypeError: add_infobase() got an unexpected keyword argument 'version'`

- [ ] **Step 3: Реализация**

В `services/model.py`:

```python
@dataclass(frozen=True)
class NewInfobase:
    """Данные новой записи — вход `Workspace.add_infobase`.

    Датакласс, а не кортеж из пяти значений: позиционные `name`, `connect`,
    `folder`, `version`, `app` — все строки или `None`, и перепутать их
    местами не помешал бы ни один тип.
    """  # noqa: RUF002

    name: str
    connect: str
    folder: str
    version: str | None = None
    app: str | None = None
```

В `services/workspace.py`:

```python
def add_infobase(
    self,
    name: str,
    connect: str,
    folder: str | None = None,
    version: str | None = None,
    app: str | None = None,
) -> str:
    """Добавить запись и вернуть её ключ привязки.

    Ключ возвращается, потому что искать новую запись по имени нельзя:
    имена в списке не уникальны (дизайн плана 3, §5 — `ADD` с дублем
    разрешён).

    `version` и `app` попадают в секцию, только когда заданы: «как
    установлено» и «Авто» в диалоге добавления означают отсутствие ключа,
    а не пустое значение (спека §3).
    """  # noqa: RUF002
    changes: dict[str, str | None] = {"Connect": connect}
    if folder:
        changes["Folder"] = folder
    if version:
        changes["Version"] = version
    if app:
        changes["App"] = app
    result = self._write(SectionPatch(PatchKind.ADD, name=name, changes=changes))
    if result.key is None:
        # Недостижимо: ADD всегда создаёт секцию и возвращает её ключ.
        raise ServicesError("Запись добавлена, но её ключ неизвестен")
    return result.key
```

- [ ] **Step 4: Прогнать и убедиться, что проходит**

Run: `uv run pytest tests/unit/test_workspace.py -v`
Expected: PASS.

- [ ] **Step 5: Мутационная проверка**

Заменить `if version:` на безусловное `changes["Version"] = version`.
Expected: FAIL в `test_add_infobase_writes_neither_key_by_default` —
обычное добавление начало бы писать в чужой файл ключ, которого
пользователь не задавал. Откатить правкой файла.

- [ ] **Step 6: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/services/model.py src/onecstarter/services/workspace.py tests/unit/test_workspace.py
git commit -m "feat: add_infobase принимает версию платформы и клиента"
```

---

## Task 7: «Версия» и «Клиент» в диалоге добавления

**Files:**
- Modify: `src/onecstarter/ui/dialogs/infobase.py` (`__init__`, `new_record`)
- Modify: `src/onecstarter/ui/bases/view.py:1260-1290` (`_apply_new_infobase`)
- Modify: `tests/ui/test_infobase_dialog.py`, `tests/ui/test_bases_view.py`

**Interfaces:**
- Consumes: `version_options` (задача 4), `NewInfobase` и
  `add_infobase(..., version, app)` (задача 6).
- Produces: `InfobaseDialog.new_record() -> NewInfobase` (было
  `tuple[str, str, str]` — все вызывающие обязаны перейти).

- [ ] **Step 1: Написать падающий тест**

```python
def test_add_dialog_offers_version_and_client(application: QApplication) -> None:
    dialog = InfobaseDialog.for_new(
        groups=["/"], installations=INSTALLED, cfg_rules=[]
    )
    dialog.set_name("Новая")
    dialog.set_file_path(r"D:\Bases\New")

    dialog.set_version("8.3.25.1633")
    dialog.set_app("ThinClient")
    record = dialog.new_record()

    assert record.version == "8.3.25.1633"
    assert record.app == "ThinClient"


def test_untouched_add_dialog_asks_for_neither_version_nor_client(
    application: QApplication,
) -> None:
    """По умолчанию «как установлено» и «Авто» — в файл не пишется ничего."""
    dialog = InfobaseDialog.for_new(
        groups=["/"], installations=INSTALLED, cfg_rules=[]
    )
    dialog.set_name("Новая")
    dialog.set_file_path(r"D:\Bases\New")

    record = dialog.new_record()

    assert record.version is None
    assert record.app is None
```

- [ ] **Step 2: Прогнать и убедиться, что падает**

Run: `uv run pytest tests/ui/test_infobase_dialog.py -k add_dialog -v`
Expected: FAIL — `ValueError: диалог не предлагает версию «8.3.25.1633»`
(для новой записи `self._version` сейчас не создаётся вовсе).

- [ ] **Step 3: Реализация**

В `InfobaseDialog.__init__` вынести построение «Версии» и «Клиента»
из ветки `if item is not None:` — они строятся всегда:

```python
cell = version_cell(item, installations, cfg_rules) if item is not None else None
self._version = QComboBox()
for text, data in version_options(installations, item, cell):
    self._version.addItem(text, data)
version_index = (
    self._version.findData(item.requested_version) if item is not None else 0
)
self._version.setCurrentIndex(version_index if version_index >= 0 else 0)

self._app = QComboBox()
for text, data in _APP_ITEMS:
    self._app.addItem(text, data)
app_index = self._app.findData(_app_key(item.app)) if item is not None else 0
if app_index < 0:
    self._app.addItem(item.app or "", item.app)
    app_index = self._app.count() - 1
self._app.setCurrentIndex(app_index)
self._initial_app = self._app.currentData()

form.addRow("Версия", self._version)
form.addRow("Клиент", self._app)
```

Внутри `if item is not None:` остаются только `self._version_hint`,
`self._os_auth` со своей строкой формы и таблица прочих ключей:
у записи, которой ещё нет, показывать нечего.

`new_record` переписать:

```python
def new_record(self) -> NewInfobase:
    """Данные новой записи — вход `Workspace.add_infobase`.

    Только для диалога добавления (`for_new`): диалог правки существующей
    записи новых не создаёт, `changes()` — его собственный путь записи.

    «Версия» и «Клиент» необязательны: `None` означает «как установлено»
    и «Авто», то есть отсутствие ключа в секции, а не пустое значение.
    """  # noqa: RUF002
    connect = build_connect(
        self._kind_box.currentData(),
        file_path=self._file_path.text(),
        server=self._server.text(),
        ref=self._ref.text(),
        url=self._url.text(),
    )
    return NewInfobase(
        name=self._name.text().strip(),
        connect=connect,
        folder=self._folder.current_path(),
        version=self._version.currentData(),
        app=self._app.currentData(),
    )
```

В `ui/bases/view.py::_apply_new_infobase`:

```python
try:
    record = dialog.new_record()
except ValueError as error:
    self._on_error(
        InvalidRequestError(f"Не удалось прочитать данные диалога: {error}")  # noqa: RUF001
    )
    return
try:
    self._workspace.add_infobase(
        record.name,
        record.connect,
        record.folder,
        version=record.version,
        app=record.app,
    )
except ServicesError as error:
    ...
```

- [ ] **Step 4: Прогнать и убедиться, что проходит**

Run: `uv run pytest tests/ui/test_infobase_dialog.py tests/ui/test_bases_view.py -v`
Expected: PASS. Тесты правки существующей записи обязаны пройти
без изменений — «Версия» и «Клиент» там уже были.

- [ ] **Step 5: Мутационная проверка**

В `new_record` заменить `version=self._version.currentData()` на
`version=str(self._version.currentData() or "")`.
Expected: FAIL в `test_untouched_add_dialog_asks_for_neither_version_nor_client`.
Откатить правкой файла.

- [ ] **Step 6: Линт, типы, коммит**

```powershell
uv run ruff check .
uv run mypy
git add src/onecstarter/ui/dialogs/infobase.py src/onecstarter/ui/bases/view.py tests/ui/test_infobase_dialog.py tests/ui/test_bases_view.py
git commit -m "feat: диалог добавления базы получает «Версию» и «Клиента»"
```

---

## Task 8: закрытие вехи

**Files:**
- Modify: `pyproject.toml` (версия)
- Modify: `docs/tasks.md` (закрытие, результаты мутационных проверок)

- [ ] **Step 1: Полный прогон в файл**

```powershell
$env:PYTHONFAULTHANDLER=1
uv run pytest -rA --tb=long -p no:cacheprovider > "$env:TEMP\v21.log" 2>&1
$LASTEXITCODE
```

Expected: код 0. Код 139 — известный долг T-12 п. 15, повторить прогон;
повтор обязан пройти. Любой другой ненулевой код — регресс, разбирать.

- [ ] **Step 2: Поднять версию**

В `pyproject.toml`: `version = "2.0.0"` → `version = "2.1.0"`.
Единственное место — правило `CLAUDE.md`, «Сборка».

- [ ] **Step 3: Записать веху в `docs/tasks.md`**

Дописать раздел вехи: четыре замечания, ссылки на спеку и мокап,
результаты **всех** мутационных проверок из задач 1, 2, 5, 6, 7 — что именно
ломали и в каком тесте это отозвалось.

**Файл в дереве CRLF, в индексе LF** — править побайтово скриптом
с проверкой единственности вхождения; `git diff --stat` обязан показать
изменённые строки, а не файл целиком.

- [ ] **Step 4: Smoke-сборка**

```powershell
.\build.ps1
```

Expected: сборка проходит и заканчивается smoke-тестом собранного
экземпляра. GUI собранного приложения и установщик **не запускать** —
только по явному согласию заказчика.

- [ ] **Step 5: Коммит**

```powershell
git add pyproject.toml docs/tasks.md
git commit -m "release: версия 2.1.0 — интерфейсные правки вехи"
```

---

## Самопроверка плана

**Покрытие спеки.**

| Раздел спеки | Задача |
| --- | --- |
| §1.1 типографика заголовка, отбивка групп | 2 (шаг 3, 4) |
| §1.2 `CollapsibleGroup` | 1 |
| §1.3 шеврон | 1 |
| §1.4 свёрнуто по умолчанию, состояние не хранится | 1 (умолчание), 2 (тест) |
| §1.5 сборка раздела, второй уровень свёртки, аксессоры | 2 |
| §1.6 тестовый долг | 2 (шаги 5, 7) |
| §2 широкое поле | 3 |
| §3 «Версия» и «Клиент» в диалоге | 6 (services), 7 (ui) |
| §4 подменю версии | 5 |
| §5 отклонённые альтернативы | — записаны в спеке, кода не требуют |
| §6 инварианты | 4 (переезд `version_options`) |
| §7 тесты и мутации | по задачам, сведение — 8 (шаг 3) |
| §8 порядок работ | порядок задач 1→8 |

Разночтение §8 снято: спека говорит «§1 и §2 вместе», имея в виду
соседство в очереди, а не единый коммит. Задачи 2 и 3 идут подряд
и обе трогают `_add_row`; разделены потому, что ревьюер может принять
свёртку и отклонить реализацию ширины независимо.

**Заглушки.** Не найдено: каждый шаг несёт либо код, либо точную команду
с ожидаемым результатом.

**Согласованность имён.** `version_options` (задачи 4, 5, 7),
`NewInfobase` (6, 7), `set_version` (5), `expand_group`/`is_group_expanded`/
`group` (2, 3), `body_layout`/`set_palette`/`set_expanded`/`is_expanded`/
`chevron`/`button` (1, 2) — совпадают во всех местах употребления.

**Найдено и исправлено при самопроверке.** В задаче 1 шаг 5 первая
редакция предлагала мутацию `chevron_pixmap(False, colour)` и утверждала,
что упадёт `test_chevron_follows_the_palette`, — неверно: этот тест
проверяет цвет, а не направление. Мутация заменена на снятие
`self._body.setVisible(checked)`, которое отзывается в тесте свёртки.
