# Запуск веб- и серверных баз — план реализации v2.3

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** веб-база запускается тонким клиентом или браузером по выбору, а версию платформы мы перестаём навязывать там, где её диктует сервер.

**Architecture:** решение «браузер или клиент» переезжает из `services/launch.py` (где оно принималось по виду строки соединения) в чистую функцию `choose_launch_plan` в `domain/launch.py`, которая ветвится по `App` и возвращает размеченный результат `LaunchPlan | LaunchRefusal`. Разовый выбор пользователя типизируется новым `LaunchTarget` (три клиента + браузер) вместо `ClientKind`, у которого браузера нет. Полярность `/AppAutoCheckVersion` определяется видом базы: пиним только файловые и `UNKNOWN`.

**Tech Stack:** Python 3.12, PySide6, pytest, pytest-qt, ruff, mypy strict (кроме `onecstarter.ui.*`), uv.

**Спека:** [2026-09-08-v23-web-launch-design.md](../specs/2026-09-08-v23-web-launch-design.md). **Замеры:** [t05-16-web-server-launch.md](../../research/t05-16-web-server-launch.md).

## Global Constraints

- **Qt только в `src/onecstarter/ui/`.** `domain`, `config`, `platform_1c`, `security`, `services` не импортируют `PySide6` ни прямо, ни транзитивно.
- **Выбор версии и выбор цели запуска — чистые функции.** Без обращений к ФС и процессам внутри; всё окружение подаётся аргументами; покрываются табличными тестами.
- **`domain` не импортирует `services`.** Настройка приходит в домен булевым значением, а не своим enum — тот же приём, что `DefaultClient.default_app`.
- **Форматы 1С читаются и пишутся без потерь.** `InfobaseDialog.changes()` возвращает **пару** `(правки, новое имя)` ([`infobase.py:1150`](../../../src/onecstarter/ui/dialogs/infobase.py#L1150)); для нетронутого диалога она обязана быть `({}, None)` при **любом** виде размещения.
- **Каждый коммит зелёный.** Задача, меняющая сигнатуру или тип на границе слоёв, обязана менять и все её вызовы в том же коммите, а перед `git commit` прогонять весь suite и `uv run ruff check . && uv run mypy`. Узкий прогон одного файла — гейт TDD внутри задачи, а не доказательство готовности.
- **Секреты — только через `security/`.** Пароль не попадает ни в сообщения об ошибках, ни в `LaunchOutcome.command_line` иначе как через `redact_arguments`.
- **Схема `settings.json` остаётся `1`.** Новый ключ необязателен, старый файл читается без миграции, незнакомое значение мягко уходит в дефолт.
- **Мутационная проверка обязательна** для тестов, проверяющих отказ операции или сохранность пользовательского файла. На табличных тестах чистых функций не требуется.
- **Полные прогоны pytest — в файл, не в `tail`:** `uv run pytest -q > e:\tmp\<имя>.log 2>&1; type e:\tmp\<имя>.log`. Интермиттентный access violation (T-12 п. 15) иначе теряет блок падения.
- **Ветка:** `feat/2026-09-08-v23-web-launch` от `docs/2026-09-08-v23-web-launch` (`288c9d6`).

## Структура файлов

| Файл | Ответственность | Что меняется | Задача |
| --- | --- | --- | --- |
| `src/onecstarter/domain/launch.py` | сборка командной строки, выбор клиента | **+** `LaunchTarget`, `RefusalReason`, `LaunchPlan`, `LaunchRefusal`, `choose_launch_plan` | 1 |
| `src/onecstarter/services/settings.py` | модель настроек | **+** `WebLaunch`, поле `web_launch` | 2 |
| `src/onecstarter/services/launch.py` | сценарий запуска: план → процесс или браузер | ветвление по плану, полярность `/AppAutoCheckVersion`, `version=None` для WEB/SERVER | 3 |
| `src/onecstarter/services/workspace.py` | состояние и проводка | тип канала `forced`, `set_web_launch` | 3 |
| `src/onecstarter/ui/app.py` | сборка runtime | проводка `web_launch` в `Workspace` | 3 |
| `src/onecstarter/ui/bases/view.py` | дерево, меню, хоткеи | `LaunchTarget` в `launch_key`, меню веб-базы, `_launch_current` | 3 |
| `src/onecstarter/services/workspace.py` | проверка дублей имени | `_reject_ambiguous_name` перестаёт пропускать веб-базы | 4 |
| `src/onecstarter/services/display.py` | тексты колонок и пунктов | ветка `SERVER` в `version_cell`, фильтр в `version_options` | 5 |
| `src/onecstarter/ui/dialogs/infobase.py` | свойства записи | пункт «Веб-клиент (браузер)» для WEB | 6 |
| `src/onecstarter/ui/settings_view.py` | раздел «Настройки» | строка «Веб-базы открывать», сужение подсказки | 7 |

---

### Task 0: снять тесты, которые сломаются молча или повесят прогон

Первой и отдельно. Два разных повода, оба обязаны быть закрыты **до** смены поведения.

**Повод 1 — зависание.** `test_f4_does_nothing_for_web_base` конструирует `BasesView` без `on_error`, а умолчание ([`view.py:400`](../../../src/onecstarter/ui/bases/view.py#L400)) поднимает настоящий модальный `QMessageBox`. Как только `DESIGNER` на веб-базе станет `LaunchError` (Task 3), тест повесит suite в offscreen — класс отказа T-12 п. 15, который мы уже ловим пять сессий.

**Повод 2 — тест с противоположным утверждением.** [`tests/unit/test_workspace.py:490`](../../../tests/unit/test_workspace.py#L490) `test_duplicate_name_does_not_block_web_base` утверждает обратное тому, что делает Task 4, и его докстринг («веб-база открывается браузером по адресу из `ws`, а не по `/IBName`») перестаёт быть верным уже на Task 3 (**[Ф]** T-05.16 № 1). Оставленный, он падает дважды: сначала на новом отказе по дублю, потом на `assert ....url` — потому что веб-база без `App` теперь идёт тонким клиентом и `url is None`.

**Files:**
- Modify: `tests/ui/test_bases_view.py:1195-1225`
- Modify: `tests/unit/test_workspace.py:490-496`

**Interfaces:**
- Consumes: ничего.
- Produces: ни один тест веб-базы не поднимает модальное окно; ни один тест не утверждает, что веб-база открывается браузером безусловно.

- [ ] **Step 1: найти конструкторы `BasesView` без `on_error`**

Run: `uv run python -c "import pathlib; p=pathlib.Path('tests/ui/test_bases_view.py'); [print(i+1, l.rstrip()) for i,l in enumerate(p.read_text(encoding='utf-8').splitlines()) if 'BasesView(' in l]"`

Ожидание: список строк. Цель — те, где `on_error` не передан, и рядом упоминается веб-база.

- [ ] **Step 2: перевести найденные тесты на явный сборщик ошибок**

```python
def test_f4_does_nothing_for_web_base(qtbot: QtBot) -> None:
    errors: list[Exception] = []
    view = BasesView(workspace, on_error=errors.append)
    qtbot.addWidget(view)
    ...
    assert errors == []
```

`qtbot.addWidget` обязателен: без регистрации виджет удаляется сборщиком мусора Qt в непредсказуемый момент — та же причина, по которой T-12 п. 15 воспроизводится интермиттентно.

- [ ] **Step 3: удалить `test_duplicate_name_does_not_block_web_base`**

Удалить целиком тест `tests/unit/test_workspace.py:490-496`. Его замена появится в Task 4 с противоположным утверждением и новым обоснованием — сохранять оба нельзя, они взаимоисключающи.

- [ ] **Step 4: полный прогон**

Run: `uv run pytest -q > e:\tmp\t0.log 2>&1; type e:\tmp\t0.log`
Ожидание: PASS. Поведение продукта не изменилось — менялись только тесты.

- [ ] **Step 5: commit**

```bash
git add tests/ui/test_bases_view.py tests/unit/test_workspace.py
git commit -m "test: снять тесты, которые сломаются молча или повесят прогон на смене канала"
```

---

### Task 1: `LaunchTarget`, `LaunchPlan`, `LaunchRefusal` и чистая функция выбора

**Files:**
- Modify: `src/onecstarter/domain/launch.py`
- Test: `tests/unit/test_launch.py`

**Interfaces:**
- Consumes: `ClientKind`, `choose_client`, `is_web_client_app` (существуют), `ConnectKind` из `domain/connect.py`.
- Produces:
  - `LaunchTarget` — enum `THIN`/`THICK`/`DESIGNER`/`BROWSER`, свойство `client: ClientKind | None`;
  - `RefusalReason` — enum `THICK_TO_WEB`/`WEB_APP_THICK`/`BROWSER_FOR_SERVER`/`BROWSER_FOR_FILE`/`BROWSER_FOR_UNKNOWN`;
  - `LaunchPlan(client: ClientKind | None, auto_check_mode: bool, auto_check_version: bool)`;
  - `LaunchRefusal(reason: RefusalReason)`;
  - `choose_launch_plan(kind, app, default_app, *, web_default_is_browser, forced) -> LaunchPlan | LaunchRefusal`.

- [ ] **Step 1: написать падающий табличный тест**

```python
import pytest

from onecstarter.domain.connect import ConnectKind
from onecstarter.domain.launch import (
    ClientKind,
    LaunchPlan,
    LaunchRefusal,
    LaunchTarget,
    RefusalReason,
    choose_launch_plan,
)

WEB = ConnectKind.WEB
SRV = ConnectKind.SERVER
FILE = ConnectKind.FILE
UNK = ConnectKind.UNKNOWN


@pytest.mark.parametrize(
    ("kind", "app", "forced", "browser_default", "expected"),
    [
        # Веб-база: решает App, потом настройка
        (WEB, "WebClient", None, False, LaunchPlan(None, False, False)),
        (WEB, "ThinClient", None, False, LaunchPlan(ClientKind.THIN, False, True)),
        (WEB, "ThickClient", None, False, LaunchRefusal(RefusalReason.WEB_APP_THICK)),
        (WEB, None, None, False, LaunchPlan(ClientKind.THIN, True, True)),
        (WEB, "Auto", None, False, LaunchPlan(ClientKind.THIN, True, True)),
        (WEB, "Мусор", None, False, LaunchPlan(ClientKind.THIN, True, True)),
        (WEB, None, None, True, LaunchPlan(None, False, False)),
        (WEB, "Auto", None, True, LaunchPlan(None, False, False)),
        # Веб-база: разовый выбор сильнее и App, и настройки
        (WEB, "WebClient", LaunchTarget.THIN, True, LaunchPlan(ClientKind.THIN, False, True)),
        (WEB, "ThinClient", LaunchTarget.BROWSER, False, LaunchPlan(None, False, False)),
        (WEB, None, LaunchTarget.THICK, False, LaunchRefusal(RefusalReason.THICK_TO_WEB)),
        (WEB, None, LaunchTarget.DESIGNER, False, LaunchRefusal(RefusalReason.THICK_TO_WEB)),
        # Серверная: клиент как раньше, версию не пиним
        (SRV, None, None, False, LaunchPlan(ClientKind.THIN, True, True)),
        (SRV, "ThickClient", None, False, LaunchPlan(ClientKind.THICK, False, True)),
        (SRV, None, LaunchTarget.DESIGNER, False, LaunchPlan(ClientKind.DESIGNER, False, True)),
        (SRV, None, LaunchTarget.BROWSER, False, LaunchRefusal(RefusalReason.BROWSER_FOR_SERVER)),
        # Файловая: всё как было, версию пиним
        (FILE, None, None, False, LaunchPlan(ClientKind.THIN, True, False)),
        (FILE, "ThinClient", None, False, LaunchPlan(ClientKind.THIN, False, False)),
        (FILE, None, LaunchTarget.DESIGNER, False, LaunchPlan(ClientKind.DESIGNER, False, False)),
        (FILE, None, LaunchTarget.BROWSER, False, LaunchRefusal(RefusalReason.BROWSER_FOR_FILE)),
        # Неразобранная строка соединения идёт файловым путём — маршрут не меняем
        (UNK, None, None, False, LaunchPlan(ClientKind.THIN, True, False)),
        (UNK, None, LaunchTarget.BROWSER, False, LaunchRefusal(RefusalReason.BROWSER_FOR_UNKNOWN)),
    ],
)
def test_choose_launch_plan(
    kind: ConnectKind,
    app: str | None,
    forced: LaunchTarget | None,
    browser_default: bool,
    expected: LaunchPlan | LaunchRefusal,
) -> None:
    assert (
        choose_launch_plan(
            kind, app, None, web_default_is_browser=browser_default, forced=forced
        )
        == expected
    )


def test_web_plan_ignores_default_client_setting() -> None:
    """«Клиент по умолчанию» = толстый не должен запрещать запуск веб-баз.

    До вехи настройка на веб-базы не влияла вовсе (короткое замыкание стояло
    раньше `_choose_client`), и это сохраняется намеренно: толстый клиент
    через `ws=` не подключается, и «толстый по умолчанию» означал бы запрет
    запуска всех веб-баз.
    """
    plan = choose_launch_plan(
        WEB, None, "ThickClient", web_default_is_browser=False, forced=None
    )
    assert plan == LaunchPlan(ClientKind.THIN, True, True)


def test_launch_target_browser_has_no_client() -> None:
    assert LaunchTarget.BROWSER.client is None
    assert LaunchTarget.THIN.client is ClientKind.THIN


def test_web_client_app_on_non_web_still_raises() -> None:
    """Прежний отказ не тронут вехой: у не-ws записи App=WebClient — ValueError
    из `choose_client`, а не `LaunchRefusal`. Два механизма сосуществуют
    намеренно (спека §3): новые причины типизированы, старая ветка не трогается.
    """
    with pytest.raises(ValueError):
        choose_launch_plan(
            FILE, "WebClient", None, web_default_is_browser=False, forced=None
        )
```

- [ ] **Step 2: прогнать тест, убедиться, что падает**

Run: `uv run pytest tests/unit/test_launch.py -k "launch_plan or launch_target or web_client_app_on_non_web" -q`
Ожидание: FAIL с `ImportError: cannot import name 'LaunchTarget'`.

- [ ] **Step 3: реализовать в `domain/launch.py`**

Дописать импорт:

```python
from onecstarter.domain.connect import ConnectKind, parse_connect
```

`domain/connect.py` ничего из `domain/launch.py` не импортирует — цикла не возникает.

После `choose_client` добавить:

```python
class LaunchTarget(Enum):
    """Что пользователь запросил разово: клиент или браузер.

    Отдельный тип, а не член `ClientKind`: тот про исполняемый файл
    (`ClientConvention.executables`), а браузеру файла не соответствует.
    До v2.3 канал `forced` был типизирован `ClientKind`, выразить в нём
    «открыть браузером» было нечем, и пункт меню держался на коротком
    замыкании в `services/launch.py` (спека v2.3, §2).
    """

    THIN = "thin"
    THICK = "thick"
    DESIGNER = "designer"
    BROWSER = "browser"

    @property
    def client(self) -> ClientKind | None:
        """`None` — только у `BROWSER`."""
        return _TARGET_CLIENTS.get(self)


_TARGET_CLIENTS = {
    LaunchTarget.THIN: ClientKind.THIN,
    LaunchTarget.THICK: ClientKind.THICK,
    LaunchTarget.DESIGNER: ClientKind.DESIGNER,
}


class RefusalReason(Enum):
    """Почему запуск невозможен. Текст сообщения строит `services`."""

    THICK_TO_WEB = "thick-to-web"
    WEB_APP_THICK = "web-app-thick"
    BROWSER_FOR_SERVER = "browser-for-server"
    BROWSER_FOR_FILE = "browser-for-file"
    BROWSER_FOR_UNKNOWN = "browser-for-unknown"


@dataclass(frozen=True)
class LaunchRefusal:
    reason: RefusalReason


@dataclass(frozen=True)
class LaunchPlan:
    """`client is None` — открыть браузером; тогда оба флага не значат ничего."""

    client: ClientKind | None
    auto_check_mode: bool
    auto_check_version: bool


_BROWSER_REFUSALS = {
    ConnectKind.SERVER: RefusalReason.BROWSER_FOR_SERVER,
    ConnectKind.FILE: RefusalReason.BROWSER_FOR_FILE,
    ConnectKind.UNKNOWN: RefusalReason.BROWSER_FOR_UNKNOWN,
}

_BROWSER_PLAN = LaunchPlan(client=None, auto_check_mode=False, auto_check_version=False)


def choose_launch_plan(
    kind: ConnectKind,
    app: str | None,
    default_app: str | None,
    *,
    web_default_is_browser: bool,
    forced: LaunchTarget | None,
) -> LaunchPlan | LaunchRefusal:
    """Чем и как запускать запись. Чистая функция (спека v2.3, §3).

    Полярность `/AppAutoCheckVersion` определяется ВИДОМ базы, а не желанием:
    **[Ф]** T-05.16 — у веб- и серверных баз версию диктует сервер, и пин
    чужой версии даёт молчаливый повис. Условие написано перечислением
    (`FILE or UNKNOWN`), а не отрицанием `not FILE`: второе молча сменило бы
    полярность у достижимого входа `UNKNOWN`.

    Настройка приходит булевым значением: `domain` не импортирует `services`.
    """  # noqa: RUF002
    pins_version = kind is ConnectKind.FILE or kind is ConnectKind.UNKNOWN
    auto_check_version = not pins_version

    if forced is LaunchTarget.BROWSER:
        if kind is not ConnectKind.WEB:
            return LaunchRefusal(_BROWSER_REFUSALS[kind])
        return _BROWSER_PLAN

    if forced is not None:
        client = forced.client
        if client is None:  # pragma: no cover — BROWSER обработан веткой выше
            raise ValueError("BROWSER должен был быть обработан раньше")
        if kind is ConnectKind.WEB and client is not ClientKind.THIN:
            return LaunchRefusal(RefusalReason.THICK_TO_WEB)
        return LaunchPlan(client, auto_check_mode=False, auto_check_version=auto_check_version)

    if kind is ConnectKind.WEB:
        return _web_plan(app, web_default_is_browser=web_default_is_browser)

    # Не веб-база: прежний путь без изменений, включая отказ `ValueError`
    # на `App=WebClient` у не-ws записи (его ловит и переводит `services`).
    choice = choose_client(app, default_app)
    return LaunchPlan(choice.client, choice.auto_check_mode, auto_check_version)


def _web_plan(app: str | None, *, web_default_is_browser: bool) -> LaunchPlan | LaunchRefusal:
    """Веб-база: решает `App`, а при `Auto`/пусто/нераспознанном — настройка.

    `/AppAutoCheckMode` снимается тогда, и только тогда, когда пользователь
    выбрал КЛИЕНТА. Настройка выбирает КАНАЛ (браузер против тонкого), поэтому
    её значение «тонкий» флаг сохраняет — и командная строка совпадает
    с эталоном **[Ф]** T-05.16 № 8. Нераспознанное значение `App` клиента
    не даёт (`_client_from_app` вернул бы `None`) и ведёт себя как `Auto`.

    `default_app` здесь не участвует намеренно: «толстый по умолчанию»
    означал бы запрет запуска всех веб-баз. До v2.3 настройка на них тоже
    не влияла — короткое замыкание стояло раньше `_choose_client`.
    """  # noqa: RUF002
    if is_web_client_app(app):
        return _BROWSER_PLAN
    if app is not None and app.casefold() == "thinclient":
        return LaunchPlan(ClientKind.THIN, auto_check_mode=False, auto_check_version=True)
    if app is not None and app.casefold() == "thickclient":
        return LaunchRefusal(RefusalReason.WEB_APP_THICK)
    if web_default_is_browser:
        return _BROWSER_PLAN
    return LaunchPlan(ClientKind.THIN, auto_check_mode=True, auto_check_version=True)
```

- [ ] **Step 4: прогнать тесты домена**

Run: `uv run pytest tests/unit/test_launch.py -q`
Ожидание: PASS, включая существующие тесты `choose_client` (функция не тронута).

- [ ] **Step 5: гейт перед коммитом**

Run: `uv run pytest -q > e:\tmp\t1.log 2>&1; type e:\tmp\t1.log`
Run: `uv run ruff check . && uv run mypy`
Ожидание: PASS и чисто. Новый код ещё никем не вызывается — весь suite обязан быть зелёным.

- [ ] **Step 6: commit**

```bash
git add src/onecstarter/domain/launch.py tests/unit/test_launch.py
git commit -m "feat(domain): choose_launch_plan — цель запуска и полярность версии по виду базы"
```

---

### Task 2: настройка `web_launch`

**Files:**
- Modify: `src/onecstarter/services/settings.py`
- Test: `tests/unit/test_settings.py`

**Interfaces:**
- Consumes: ничего из Task 1.
- Produces: `WebLaunch` (`THIN = "thin"`, `BROWSER = "browser"`), свойство `WebLaunch.is_browser: bool`, поле `Settings.web_launch: WebLaunch = WebLaunch.THIN`.

- [ ] **Step 1: написать падающие тесты**

```python
def test_web_launch_defaults_to_thin(tmp_path: Path) -> None:
    assert load_settings(tmp_path / "нет.json").web_launch is WebLaunch.THIN


def test_web_launch_roundtrip(tmp_path: Path) -> None:
    path = tmp_path / "settings.json"
    save_settings(path, Settings(web_launch=WebLaunch.BROWSER))
    assert load_settings(path).web_launch is WebLaunch.BROWSER


def test_unknown_web_launch_is_not_corruption(tmp_path: Path) -> None:
    """Незнакомое значение — более новая версия могла записать своё."""
    path = tmp_path / "settings.json"
    save_settings(path, Settings())
    payload = json.loads(path.read_text(encoding="utf-8"))
    payload["web_launch"] = "квантовый"
    path.write_text(json.dumps(payload, ensure_ascii=False), encoding="utf-8")
    assert load_settings(path).web_launch is WebLaunch.THIN
    assert not (path.parent / (path.name + ".bad")).exists()


def test_settings_file_without_web_launch_reads_without_migration(tmp_path: Path) -> None:
    """Файл прежней установки: ключа нет, схема та же, дефолт подставляется."""
    path = tmp_path / "settings.json"
    path.write_text(
        json.dumps({"schema": SCHEMA_VERSION, "theme": "dark"}, ensure_ascii=False),
        encoding="utf-8",
    )
    settings = load_settings(path)
    assert settings.web_launch is WebLaunch.THIN
    assert settings.theme is ThemeMode.DARK
```

- [ ] **Step 2: прогнать, убедиться в падении**

Run: `uv run pytest tests/unit/test_settings.py -k web_launch -q`
Ожидание: FAIL с `ImportError: cannot import name 'WebLaunch'`.

- [ ] **Step 3: реализовать**

В `__all__` добавить `"WebLaunch"`, затем:

```python
class WebLaunch(Enum):
    """Чем открывать веб-базу, когда запись не сказала явно (спека v2.3, §5).

    Дефолт `THIN` совпадает со штатным стартером: **[Ф]** T-05.16 № 8 —
    веб-база с `App=Auto` запускается тонким клиентом, браузер при этом
    не открывается. Что браузер открывается при `App=WebClient` — **[Д]**,
    от противного к № 8: записи с таким `App` в замерах не встретилось,
    отдельный замер назначен (спека §10). Значение настройки выбирает
    КАНАЛ, а не клиента, поэтому `/AppAutoCheckMode` при нём сохраняется
    (спека §3).
    """  # noqa: RUF002

    THIN = "thin"
    BROWSER = "browser"

    @property
    def is_browser(self) -> bool:
        return self is WebLaunch.BROWSER
```

В `Settings`:

```python
    # Спека v2.3, §5. Дефолт — поведение штатного стартера ([Ф] T-05.16 № 8),
    # а не прежних установок: до вехи веб-базы всегда открывались браузером,
    # и это изменение видимое — потому и вынесено в настройку.
    web_launch: WebLaunch = WebLaunch.THIN
```

В `load_settings` — `web_launch=_web_launch_of(payload.get("web_launch"))`; в `save_settings` — `"web_launch": settings.web_launch.value`; рядом с `_client_of`:

```python
def _web_launch_of(value: Any) -> WebLaunch:
    """Незнакомое значение — не порча: более новая версия могла записать своё."""
    try:
        return WebLaunch(value)
    except ValueError:
        return WebLaunch.THIN
```

- [ ] **Step 4: гейт перед коммитом**

Run: `uv run pytest -q > e:\tmp\t2.log 2>&1; type e:\tmp\t2.log`
Run: `uv run ruff check . && uv run mypy`
Ожидание: PASS и чисто.

- [ ] **Step 5: commit**

```bash
git add src/onecstarter/services/settings.py tests/unit/test_settings.py
git commit -m "feat(settings): web_launch — чем открывать веб-базу по умолчанию"
```

---

### Task 3: сквозная смена канала запуска — `launch_infobase`, `Workspace`, `BasesView`

**Files:**
- Modify: `src/onecstarter/services/launch.py`
- Modify: `src/onecstarter/services/workspace.py:114`, `:165-172`, `:437-460`
- Modify: `src/onecstarter/ui/app.py:1044` и место первичной сборки `Workspace`
- Modify: `src/onecstarter/ui/bases/view.py:475-482`, `:746-752`, `:915-931`, `:1002-1030`
- Test: `tests/unit/test_services_launch.py`, `tests/unit/test_workspace.py`, `tests/ui/test_bases_view.py`

**Interfaces:**
- Consumes: `choose_launch_plan`, `LaunchPlan`, `LaunchRefusal`, `RefusalReason`, `LaunchTarget` (Task 1); `WebLaunch` (Task 2).
- Produces: `launch_infobase(..., web_default_is_browser: bool = False, forced_target: LaunchTarget | None = None)`; `Workspace.launch(key, forced: LaunchTarget | None = None)`; `Workspace.set_web_launch(*, is_browser: bool)`; `BasesView.launch_key(key, forced: LaunchTarget | None = None)`; `LaunchOutcome.version is None` для WEB и SERVER.

**Почему одной задачей.** `forced` — сквозной канал через три слоя: `BasesView.launch_key` ([`view.py:746`](../../../src/onecstarter/ui/bases/view.py#L746)) → `Workspace.launch` ([`workspace.py:437`](../../../src/onecstarter/services/workspace.py#L437)) → `launch_infobase`. Смена `ClientKind` на `LaunchTarget` неделима: коммит, трогающий только `services/launch.py`, оставляет `workspace.py:457` с `forced_client=…` — `TypeError` на любом запуске и `mypy call-arg`; коммит, трогающий только `workspace.py`, оставляет `view.py` передающим `ClientKind` туда, где ждут `LaunchTarget`, — `choose_launch_plan` дойдёт до `forced.client`, которого у `ClientKind` нет. Зелёных промежуточных состояний у этой правки не существует. Псевдоним `forced_client` не спасает: шим пришлось бы держать на **двух** границах, и его потом не забыть снять.

- [ ] **Step 1: написать падающие тесты слоя запуска**

```python
WEB_SECTION = '[Портал]\r\nConnect=ws="http://web-server/resource/";\r\nApp=ThinClient\r\nVersion=8.3\r\n'
SRV_SECTION = '[Учёт]\r\nConnect=Srvr="srv";Ref="acc";\r\nVersion=8.3.99.1\r\n'


def _launch(section: str, **kwargs: object) -> tuple[LaunchOutcome, list[LaunchCommand]]:
    calls: list[LaunchCommand] = []
    outcome = launch_infobase(
        _item(section),
        installations=INSTALLED,
        cfg_rules=[],
        conventions=CONVENTIONS,
        default_app=None,
        spawn=lambda command: calls.append(command) or 4242,  # type: ignore[func-returns-value]
        open_url=lambda url: pytest.fail("браузер не должен открываться"),
        **kwargs,  # type: ignore[arg-type]
    )
    return outcome, calls


def test_web_base_with_thin_app_launches_client_not_browser() -> None:
    outcome, calls = _launch(WEB_SECTION)
    assert outcome.kind is LaunchKind.PROCESS
    assert calls[0].executable.name == "1cv8c.exe"
    assert calls[0].arguments == 'ENTERPRISE /IBName"Портал" /AppAutoCheckVersion'


def test_web_base_does_not_pin_version() -> None:
    """[Ф] T-05.16: версию веб-базы диктует публикация; пин чужой версии
    даёт молчаливый повис на «Загрузка конфигурационной информации…»."""
    outcome, calls = _launch(WEB_SECTION)
    assert "/AppAutoCheckVersion-" not in calls[0].arguments
    assert outcome.version is None


def test_server_base_does_not_pin_version_even_with_unknown_version() -> None:
    """Version=8.3.99.1 не установлена, но отказа быть не должно: версию
    выбирает кластер ([Ф] T-05.16 № 5), наш резолв к серверной неприменим."""
    outcome, calls = _launch(SRV_SECTION)
    assert "/AppAutoCheckVersion-" not in calls[0].arguments
    assert outcome.version is None


def test_file_base_still_pins_version() -> None:
    _, calls = _launch('[Демо]\r\nConnect=File="C:\\Bases\\Demo";\r\nVersion=8.3.25.1633\r\n')
    assert "/AppAutoCheckVersion-" in calls[0].arguments


def test_web_base_with_auto_app_and_thin_setting_keeps_auto_check_mode() -> None:
    """[Ф] T-05.16 № 8: эталон штатного стартера при App=Auto несёт
    /AppAutoCheckMode. Настройка выбирает канал, а не клиента, и флаг снимать
    не должна."""
    _, calls = _launch('[Портал]\r\nConnect=ws="http://web-server/resource/";\r\n')
    assert calls[0].arguments == (
        'ENTERPRISE /IBName"Портал" /AppAutoCheckVersion /AppAutoCheckMode'
    )


def test_web_base_with_web_client_app_opens_browser() -> None:
    opened: list[str] = []
    outcome = launch_infobase(
        _item('[Портал]\r\nConnect=ws="http://web-server/resource/";\r\nApp=WebClient\r\n'),
        installations=INSTALLED,
        cfg_rules=[],
        conventions=CONVENTIONS,
        default_app=None,
        spawn=lambda command: pytest.fail("процесс не должен порождаться"),
        open_url=lambda url: opened.append(url) or True,  # type: ignore[func-returns-value]
    )
    assert outcome.kind is LaunchKind.BROWSER
    assert opened == ["http://web-server/resource/"]


def test_web_base_with_auto_app_follows_setting() -> None:
    opened: list[str] = []
    outcome = launch_infobase(
        _item('[Портал]\r\nConnect=ws="http://web-server/resource/";\r\n'),
        installations=INSTALLED,
        cfg_rules=[],
        conventions=CONVENTIONS,
        default_app=None,
        web_default_is_browser=True,
        spawn=lambda command: pytest.fail("процесс не должен порождаться"),
        open_url=lambda url: opened.append(url) or True,  # type: ignore[func-returns-value]
    )
    assert outcome.kind is LaunchKind.BROWSER


def test_thick_client_forced_on_web_base_refuses_and_does_not_open_browser() -> None:
    with pytest.raises(LaunchError) as error:
        launch_infobase(
            _item('[Портал]\r\nConnect=ws="http://web-server/resource/";\r\n'),
            installations=INSTALLED,
            cfg_rules=[],
            conventions=CONVENTIONS,
            default_app=None,
            forced_target=LaunchTarget.THICK,
            spawn=lambda command: pytest.fail("процесс не должен порождаться"),
            open_url=lambda url: pytest.fail("браузер не должен открываться"),
        )
    assert "ws=" in str(error.value)


def test_browser_forced_on_file_base_refuses() -> None:
    with pytest.raises(LaunchError) as error:
        launch_infobase(
            _item('[Демо]\r\nConnect=File="C:\\Bases\\Demo";\r\n'),
            installations=INSTALLED,
            cfg_rules=[],
            conventions=CONVENTIONS,
            default_app=None,
            forced_target=LaunchTarget.BROWSER,
            spawn=lambda command: pytest.fail("процесс не должен порождаться"),
            open_url=lambda url: pytest.fail("браузер не должен открываться"),
        )
    assert "браузер" in str(error.value).casefold()


def test_web_base_credentials_reach_command_line() -> None:
    """Требование заказчика: авторизация v2.2 обязана работать и на веб-базе.
    До вехи `_launch_web` командной строки не собирал вовсе."""
    _, calls = _launch(WEB_SECTION, credentials=Credentials("Иванов", "секрет"))
    assert '/N"Иванов"' in calls[0].arguments
    assert '/P"секрет"' in calls[0].arguments


def test_auto_version_path_without_installations_fails_before_spawn() -> None:
    with pytest.raises(LaunchError) as error:
        launch_infobase(
            _item(WEB_SECTION),
            installations=[],
            cfg_rules=[],
            conventions=CONVENTIONS,
            default_app=None,
            spawn=lambda command: pytest.fail("процесс не должен порождаться"),
            open_url=lambda url: pytest.fail("браузер не должен открываться"),
        )
    assert "не найдено" in str(error.value)
```

- [ ] **Step 2: прогнать, убедиться в падении**

Run: `uv run pytest tests/unit/test_services_launch.py -q > e:\tmp\t3.log 2>&1; type e:\tmp\t3.log`
Ожидание: FAIL. Часть — `TypeError: unexpected keyword argument 'web_default_is_browser'`, часть — старое поведение (браузер вместо клиента).

- [ ] **Step 3: заменить голову `launch_infobase`**

```python
def launch_infobase(
    item: InfobaseItem,
    *,
    installations: Sequence[Installation],
    cfg_rules: Sequence[DefaultVersionRule],
    conventions: Sequence[ClientConvention],
    default_app: str | None,
    web_default_is_browser: bool = False,
    forced_target: LaunchTarget | None = None,
    spawn: Callable[[LaunchCommand], int] = spawn_process,
    open_url: Callable[[str], bool] = webbrowser.open,
    credentials: Credentials | None = None,
) -> LaunchOutcome:
    if item.is_group or item.connect is None:
        raise LaunchError(f"«{item.name}» — группа, а не информационная база")  # noqa: RUF001

    plan = _plan(item, default_app, web_default_is_browser, forced_target)
    if plan.client is None:
        return _launch_web(item, open_url)

    installation, version = _installation_for(item, plan, installations, cfg_rules)
    convention = convention_for(installation.version, conventions)
    if convention is None:
        raise LaunchError(
            f"Для версии {installation.version} нет соглашения раскладки в реестре версий"
        )
    arguments = build_arguments(
        plan.client,
        ib_name=item.name,
        auto_check_version=plan.auto_check_version,
        auto_check_mode=plan.auto_check_mode,
        credentials=credentials,
    )
    command = build_launch_command(installation, convention, plan.client, arguments)
    try:
        pid = spawn(command)
    except OSError as error:
        raise LaunchError(
            f"Не удалось запустить клиента для «{item.name}»: {error}.\n"  # noqa: RUF001
            f"Команда: \"{command.executable}\" {redact_arguments(command.arguments)}"
        ) from error
    return LaunchOutcome(
        kind=LaunchKind.PROCESS,
        client=plan.client,
        command_line=f'"{command.executable}" {redact_arguments(command.arguments)}',
        url=None,
        pid=pid,
        version=version,
    )
```

- [ ] **Step 4: добавить `_plan` и `_installation_for`, убрать `_choose_client`**

```python
_REFUSAL_TEXTS = {
    RefusalReason.THICK_TO_WEB: (
        "«{name}» опубликована на веб-сервере (ws=): её открывает тонкий клиент "
        "или браузер, толстый клиент и Конфигуратор к ней не подключаются"
    ),
    RefusalReason.WEB_APP_THICK: (
        "Для «{name}» в записи задан App=ThickClient, но база опубликована "
        "на веб-сервере (ws=) — толстый клиент к ней не подключается"
    ),
    RefusalReason.BROWSER_FOR_SERVER: (
        "«{name}» — серверная база, браузером её не открыть: в браузере "
        "работают только базы, опубликованные на веб-сервере"
    ),
    RefusalReason.BROWSER_FOR_FILE: (
        "«{name}» — файловая база, браузером её не открыть: в браузере "
        "работают только базы, опубликованные на веб-сервере"
    ),
    RefusalReason.BROWSER_FOR_UNKNOWN: (
        "У «{name}» не разобран вид размещения, браузером её не открыть: "
        "в браузере работают только базы, опубликованные на веб-сервере"
    ),
}


def _plan(
    item: InfobaseItem,
    default_app: str | None,
    web_default_is_browser: bool,
    forced_target: LaunchTarget | None,
) -> LaunchPlan:
    """План запуска; оба вида отказа переводятся в `LaunchError` здесь.

    Отказов два происхождения. Типизированный `LaunchRefusal` — новые причины
    вехи v2.3 (спека §3): их пять, и различить их вызывающему можно только
    по `reason`. `ValueError` из `choose_client` — прежний путь `App=WebClient`
    у не-ws записи, он не тронут вехой и сохраняет своё сообщение.
    """  # noqa: RUF002
    try:
        outcome = choose_launch_plan(
            item.kind,
            item.app,
            default_app,
            web_default_is_browser=web_default_is_browser,
            forced=forced_target,
        )
    except ValueError as error:
        source = "в записи" if is_web_client_app(item.app) else "умолчанием машины"
        raise LaunchError(
            f"Для «{item.name}» {source} задан App=WebClient, но строка соединения "
            "не ws= — веб-клиент запускать нечем"
        ) from error
    if isinstance(outcome, LaunchRefusal):
        raise LaunchError(_REFUSAL_TEXTS[outcome.reason].format(name=item.name))
    return outcome


def _installation_for(
    item: InfobaseItem,
    plan: LaunchPlan,
    installations: Sequence[Installation],
    cfg_rules: Sequence[DefaultVersionRule],
) -> tuple[Installation, VersionNumber | None]:
    """Установка, из которой порождать процесс, и версия для `LaunchOutcome`.

    При `auto_check_version` версию выбирает платформа по ответу сервера
    (**[Ф]** T-05.16 № 4, 7): порождаем максимальную установленную, наш
    процесс завершается за секунды, платформа поднимает нужную. Возвращаемая
    версия при этом `None` — какая запустится, мы не знаем, и врать в UI нельзя.
    """  # noqa: RUF002
    if plan.auto_check_version:
        if not installations:
            raise LaunchError(
                f"Для «{item.name}» не найдено ни одной установленной версии платформы"  # noqa: RUF001
            )
        return max(installations, key=lambda installation: installation.version), None

    resolution = resolve_version(
        item.requested_version,
        item.section_default_version,
        cfg_rules,
        [installation.version for installation in installations],
    )
    if resolution.version is None:
        raise LaunchError(_version_problem(item, resolution.source))
    installation = next(
        installation
        for installation in installations
        if installation.version == resolution.version
    )
    return installation, resolution.version
```

Импорты привести в порядок: `choose_client` здесь больше не нужен, нужны `LaunchPlan`, `LaunchRefusal`, `LaunchTarget`, `RefusalReason`, `choose_launch_plan`, `VersionNumber`.

- [ ] **Step 5: починить три существующих теста файла**

Правим **до** прогона, иначе он гарантированно красный:

1. `test_forced_designer_uses_thick_executable` ([`:131`](../../../tests/unit/test_services_launch.py#L131)) — `forced_client=ClientKind.DESIGNER` → `forced_target=LaunchTarget.DESIGNER`, импорт `LaunchTarget` в шапку файла.
2. `test_web_base_opens_browser` ([`:139`](../../../tests/unit/test_services_launch.py#L139)) — **удалить**: без `App` веб-база теперь идёт тонким клиентом. Его роль делят `test_web_base_with_web_client_app_opens_browser` и `test_web_base_with_auto_app_follows_setting` из шага 1.
3. `test_web_base_reports_failed_browser` ([`:229`](../../../tests/unit/test_services_launch.py#L229)) — **сохранить**, дав записи `App=WebClient`. Это единственная в дереве проверка ветки `open_url() is False`: без неё неоткрывшаяся база молча попадёт в историю запусков как успешная.

```python
def test_web_base_reports_failed_browser() -> None:
    """`webbrowser.open` вернул False — база не открылась и в историю запусков
    попасть не должна. С v2.3 канал задаётся явно (`App=WebClient`): у записи
    без `App` умолчание — тонкий клиент, и до `_launch_web` дело не дошло бы.
    """
    with pytest.raises(LaunchError) as error:
        launch_infobase(
            _item('[Портал]\r\nConnect=ws="http://web-server/resource/";\r\nApp=WebClient\r\n'),
            installations=INSTALLED,
            cfg_rules=[],
            conventions=CONVENTIONS,
            default_app=None,
            spawn=lambda command: pytest.fail("процесс не должен порождаться"),
            open_url=lambda url: False,
        )
    assert "http://web-server" not in str(error.value)
```

Run: `uv run pytest tests/unit/test_services_launch.py -q > e:\tmp\t3.log 2>&1; type e:\tmp\t3.log`
Ожидание: PASS.

- [ ] **Step 6: мутационная проверка полярности версии**

Временно заменить в `choose_launch_plan` строку `pins_version = kind is ConnectKind.FILE or kind is ConnectKind.UNKNOWN` на `pins_version = True`.

Run: `uv run pytest tests/unit/test_services_launch.py -q`
Ожидание: падают `test_web_base_does_not_pin_version` и `test_server_base_does_not_pin_version_even_with_unknown_version` — **и падают именно на `/AppAutoCheckVersion-` в аргументах**. Записать фактический вывод в `docs/tasks.md`. Откатить.

- [ ] **Step 7: мутационная проверка отказа**

Временно заменить в `_plan` `raise LaunchError(_REFUSAL_TEXTS[...])` на `return LaunchPlan(ClientKind.THIN, False, True)`.

Run: `uv run pytest tests/unit/test_services_launch.py -q`
Ожидание: падают `test_thick_client_forced_on_web_base_refuses_and_does_not_open_browser` и `test_browser_forced_on_file_base_refuses`. Откатить.

- [ ] **Step 8: `Workspace` — тип канала и проводка настройки**

Падающие тесты:

```python
def test_workspace_passes_web_setting_to_launch(tmp_path: Path) -> None:
    workspace = _workspace_with_web_base(tmp_path)
    workspace.set_web_launch(is_browser=True)
    assert workspace.launch(_web_key(workspace)).kind is LaunchKind.BROWSER


def test_workspace_forced_browser_beats_thin_app(tmp_path: Path) -> None:
    """«Открыть в браузере» — разовый выбор, сильнее и App, и настройки."""
    workspace = _workspace_with_web_base(tmp_path)  # у записи App=ThinClient
    outcome = workspace.launch(_web_key(workspace), LaunchTarget.BROWSER)
    assert outcome.kind is LaunchKind.BROWSER
```

Реализация: в `__init__` — параметр `web_default_is_browser: bool = False` и поле `self._web_default_is_browser`; рядом с `set_default_app`:

```python
    def set_web_launch(self, *, is_browser: bool) -> None:
        """Настройка «Веб-базы открывать». Слабее записи: `App` решает
        раньше (спека v2.3, §3)."""
        self._web_default_is_browser = is_browser
```

Сигнатура `launch(self, key: str, forced: LaunchTarget | None = None)`, вызов:

```python
        outcome = launch_infobase(
            item,
            installations=self._installations,
            cfg_rules=self._cfg_rules,
            conventions=self._conventions,
            default_app=self._default_app,
            web_default_is_browser=self._web_default_is_browser,
            forced_target=forced,
            spawn=self._spawn,
            open_url=self._open_url,
            credentials=credentials,
        )
```

В `ui/app.py` рядом с `runtime.workspace.set_default_app(...)` (строка 1044) и в месте первичной сборки `Workspace`:

```python
        runtime.workspace.set_web_launch(is_browser=store.settings.web_launch.is_browser)
```

Run: `uv run pytest tests/unit/test_workspace.py -q`
Ожидание: PASS.

- [ ] **Step 9: `BasesView` — тип канала, хоткеи, `_launch_current`, меню**

Падающие тесты:

```python
def test_ctrl_1_launches_thin_client_on_web_base(qtbot: QtBot) -> None:
    """Заменяет test_ctrl_1_on_web_base_does_nothing: бездействие было обходом
    того, что браузер невыразим в ClientKind."""
    view, errors, launched = _view_with_web_base(qtbot)
    view.launch_key(_web_key(view), LaunchTarget.THIN)
    assert launched == [(_web_key(view), LaunchTarget.THIN)]
    assert errors == []


def test_designer_on_web_base_refuses_and_opens_nothing(qtbot: QtBot) -> None:
    """Дыра, которую закрывало прежнее бездействие («выдать браузер
    за Конфигуратор»), теперь закрывается отказом — и это надо сторожить."""
    view, errors, opened = _view_with_web_base_and_real_workspace(qtbot)
    view.launch_key(_web_key(view), LaunchTarget.DESIGNER)
    assert opened == []
    assert len(errors) == 1


def test_web_menu_offers_thin_and_browser(qtbot: QtBot) -> None:
    view, _, _ = _view_with_web_base(qtbot)
    labels = [action.text() for action in _menu_actions(view, _web_key(view))]
    assert any("Тонкий клиент" in label for label in labels)
    assert any("Открыть в браузере" in label for label in labels)
```

Реализация:

```python
    def launch_key(self, key: str, forced: LaunchTarget | None = None) -> None:
```

```python
        QShortcut(QKeySequence("Ctrl+1"), self, lambda: self._launch_current(LaunchTarget.THIN))
        QShortcut(QKeySequence("Ctrl+2"), self, lambda: self._launch_current(LaunchTarget.THICK))
        QShortcut(QKeySequence("Ctrl+3"), self, lambda: self._launch_current(LaunchTarget.DESIGNER))
        QShortcut(QKeySequence("F4"), self, lambda: self._launch_current(LaunchTarget.DESIGNER))
```

```python
    def _launch_current(self, forced: LaunchTarget | None) -> None:
        """Веб-база больше не особый случай: клиент для неё выразим планом
        (спека v2.3, §3), а невозможные сочетания дают отказ с объяснением,
        а не тишину. Прежнее бездействие было обходом того, что «браузер»
        не выражался в `ClientKind`.
        """  # noqa: RUF002
        key = self._current_base_key()
        if key:
            self.launch_key(key, forced)
```

Меню веб-базы:

```python
        if item.kind is ConnectKind.WEB:
            menu.addAction("Запустить\tF3", lambda: self.launch_key(key))
            menu.addAction(
                "Тонкий клиент\tCtrl+1", lambda: self.launch_key(key, LaunchTarget.THIN)
            )
            # Разовый выбор канала: сильнее и App записи, и настройки.
            menu.addAction(
                "Открыть в браузере", lambda: self.launch_key(key, LaunchTarget.BROWSER)
            )
        else:
            menu.addAction("Запустить\tF3", lambda: self.launch_key(key))
            menu.addAction(
                "Тонкий клиент\tCtrl+1", lambda: self.launch_key(key, LaunchTarget.THIN)
            )
            menu.addAction(
                "Толстый клиент\tCtrl+2", lambda: self.launch_key(key, LaunchTarget.THICK)
            )
            menu.addAction(
                "Конфигуратор\tF4", lambda: self.launch_key(key, LaunchTarget.DESIGNER)
            )
```

Пункт «Тонкий клиент» есть в обеих ветках, поэтому запись с `App=WebClient` получает обратный разовый выбор без отдельного условия.

Удалить `test_ctrl_1_on_web_base_does_nothing` и `test_f4_does_nothing_for_web_base`; переписать докстринг теста на `:3613-3618` и комментарий-обоснование на строках 906–914 (история «явно затребованный клиент для веб-базы — бездействие»), объяснив, почему бездействие сменилось отказом.

Run: `uv run pytest tests/ui/test_bases_view.py -q > e:\tmp\t3-ui.log 2>&1; type e:\tmp\t3-ui.log`
Ожидание: PASS.

- [ ] **Step 10: гейт перед коммитом**

Run: `uv run pytest -q > e:\tmp\t3-full.log 2>&1; type e:\tmp\t3-full.log`
Ожидание: PASS. Красное здесь — недоведённая смена канала внутри **этой** задачи, а не наследство соседней: канал целиком тут.

Run: `uv run ruff check . && uv run mypy`
Ожидание: чисто. `call-arg` в `workspace.py` или `arg-type` в `ui/bases/view.py` — признак того, что один из трёх слоёв остался на старом типе.

- [ ] **Step 11: commit — один на всю смену канала**

```bash
git add src/onecstarter/services/launch.py src/onecstarter/services/workspace.py src/onecstarter/ui/app.py src/onecstarter/ui/bases/view.py tests/unit/test_services_launch.py tests/unit/test_workspace.py tests/ui/test_bases_view.py
git commit -m "feat(launch): канал запуска — LaunchTarget сквозь три слоя, веб-база идёт по App"
```

---

### Task 4: `_reject_ambiguous_name` распространяется на веб-базы

**Files:**
- Modify: `src/onecstarter/services/workspace.py:560-600`
- Test: `tests/unit/test_workspace.py`

**Interfaces:**
- Consumes: `Workspace.launch` (Task 3).
- Produces: отказ «Не уникальное имя» для веб-баз.

- [ ] **Step 1: написать падающий тест**

```python
def test_duplicate_web_names_are_rejected_before_launch(tmp_path: Path) -> None:
    """Замена удалённого в Task 0 test_duplicate_name_does_not_block_web_base.

    Запуск веб-базы идёт по /IBName ([Ф] T-05.16 № 1), а платформа при дублях
    имени прекращает запуск с «Не уникальное имя информационной базы»
    ([Ф] T-05.3). Прежнее обоснование («открывается браузером по адресу
    из ws, имя в пути не участвует») умерло вместе с браузерным путём.
    """
    workspace = _workspace(tmp_path)
    workspace.add_infobase("Портал", 'File="C:\\Bases\\Dup";')
    with pytest.raises(LaunchError) as error:
        workspace.launch("id:77777777-7777-7777-7777-777777777777")
    assert "уникальн" in str(error.value).casefold()
```

Ключ и имя взяты из удалённого теста, чтобы фикстура не переписывалась.

- [ ] **Step 2: прогнать, убедиться в падении**

Run: `uv run pytest tests/unit/test_workspace.py -k duplicate_web -q`
Ожидание: FAIL — исключение не поднято.

- [ ] **Step 3: убрать исключение веб-баз и переписать обоснование**

Удалить из `_reject_ambiguous_name`:

```python
        if item.kind is ConnectKind.WEB:
            return
```

Абзац докстринга про веб-базы заменить на:

```
        Веб-базы проверяются наравне с остальными: с вехи v2.3 они
        запускаются тонким клиентом по `/IBName` (**[Ф]** T-05.16 № 1), и имя
        в этом пути участвует. Прежнее обоснование («открывается браузером
        по адресу из `ws`») перестало быть верным вместе со сменой пути
        запуска. Запись, которую пользователь всё же откроет браузером
        (`App=WebClient` или настройка), проверку тоже проходит — отказ
        по дублю имени тогда излишен, но безвреден, а ветвление проверки
        по каналу запуска развело бы её с местом, где канал выбирается.
```

- [ ] **Step 4: мутационная проверка**

Вернуть `return` для WEB временно.

Run: `uv run pytest tests/unit/test_workspace.py -k duplicate_web -q`
Ожидание: новый тест падает на непроизошедшем `LaunchError`. Записать вывод. Откатить.

- [ ] **Step 5: гейт и commit**

Run: `uv run pytest -q > e:\tmp\t4.log 2>&1; type e:\tmp\t4.log` — PASS
Run: `uv run ruff check . && uv run mypy` — чисто

```bash
git add src/onecstarter/services/workspace.py tests/unit/test_workspace.py
git commit -m "fix(workspace): дубль имени веб-базы отвергается — запуск идёт по /IBName"
```

---

### Task 5: колонка и подсказка версии для серверных баз

**Files:**
- Modify: `src/onecstarter/services/display.py:253-256`, `:336`
- Test: `tests/unit/test_display.py`

**Interfaces:**
- Consumes: ничего из предыдущих задач.
- Produces: `version_cell(...)` для `ConnectKind.SERVER` → `VersionCell("сервер", False, <подсказка>)`; для WEB подсказка перестаёт быть `None`.

- [ ] **Step 1: написать падающие тесты**

```python
def test_server_version_cell_says_cluster_decides() -> None:
    cell = version_cell(_server_item(), INSTALLED, [])
    assert cell.text == "сервер"
    assert not cell.problem
    assert "кластер" in (cell.hint or "").casefold()


def test_web_version_cell_explains_publication() -> None:
    cell = version_cell(_web_item(), INSTALLED, [])
    assert cell.text == "веб"
    assert "публикац" in (cell.hint or "").casefold()


def test_server_cell_does_not_wait_for_discovery() -> None:
    """«Версию определяет кластер» не зависит от того, нашли ли мы установки:
    показывать «…» вместо верного утверждения — прятать факт за неизвестным.
    Ветка SERVER стоит до проверки discovery_pending, как и ветка WEB."""
    cell = version_cell(_server_item(), [], [], discovery_pending=True)
    assert cell.text == "сервер"


def test_version_options_do_not_paste_server_placeholder() -> None:
    """«как установлено (сервер)» — бессмыслица: фильтр должен пропускать
    только FILE, где в ячейке настоящая версия."""
    item = _server_item()
    options = version_options(INSTALLED, item, version_cell(item, INSTALLED, []))
    assert options[0][0] == "как установлено"
```

- [ ] **Step 2: прогнать, убедиться в падении**

Run: `uv run pytest tests/unit/test_display.py -q`
Ожидание: FAIL — для SERVER возвращается разрешённая версия.

- [ ] **Step 3: реализовать**

В `version_cell` после ветки WEB и **до** `discovery_pending`:

```python
    if item.kind is ConnectKind.WEB:
        return VersionCell("веб", False, _PUBLICATION_HINT)
    if item.kind is ConnectKind.SERVER:
        return VersionCell("сервер", False, _CLUSTER_HINT)
```

Рядом с `_ARCH_LABEL`:

```python
# Спека v2.3, §4: выбор версии у этих баз остаётся и продолжает писать
# `Version` в `.v8i` (ключ читает штатный стартер), но на НАШ запуск он
# не влияет — и молчать об этом нельзя.
_PUBLICATION_HINT = (
    "Версию определяет публикация на веб-сервере — ключ Version "
    "на запуск не влияет ([Ф] 08.09.2026)"
)
_CLUSTER_HINT = (
    "Версию определяет кластер — ключ Version на запуск не влияет "
    "([Ф] 08.09.2026)"
)
```

В `version_options` фильтр переписать перечислением разрешённого:

```python
    if item is not None and cell is not None and item.kind is ConnectKind.FILE:
        display_text = cell.text
```

Список видов, у которых `cell.text` — настоящая версия, свёлся к одному, и новый вид не начнёт молча подставлять слово-заглушку. Докстринг обновить: «Кроме WEB» → «Только для FILE».

- [ ] **Step 4: прогнать и починить существующий тест**

Run: `uv run pytest tests/unit/test_display.py -q`
Ожидание: PASS. Тест про «…» при `discovery_pending` для серверной базы упадёт — перевести его на файловую базу и **дописать в докстринг** новое обоснование (для SERVER ожидание изменилось осознанно, спека §4).

- [ ] **Step 5: гейт и commit**

Run: `uv run pytest -q > e:\tmp\t5.log 2>&1; type e:\tmp\t5.log` — PASS
Run: `uv run ruff check . && uv run mypy` — чисто

```bash
git add src/onecstarter/services/display.py tests/unit/test_display.py
git commit -m "feat(display): у серверных и веб-баз колонка версии честно говорит, кто её выбирает"
```

---

### Task 6: диалог свойств — «Веб-клиент (браузер)» и охранный инвариант

**Files:**
- Modify: `src/onecstarter/ui/dialogs/infobase.py:353`, `:501-511`
- Test: `tests/ui/test_infobase_dialog.py`

**Interfaces:**
- Consumes: `ConnectKind` (существует).
- Produces: пункт «Веб-клиент (браузер)» → `App=WebClient`, предлагаемый только у записей с `kind is WEB`.

**Что уже есть и что нет.** Охранный тест `test_untouched_dialog_reports_no_changes` ([`:130`](../../../tests/ui/test_infobase_dialog.py#L130)) построен на **серверной** записи (`Srvr="s";Ref="r";Usr="admin";` + неизвестный `External`), а параметризованный `test_untouched_dialog_writes_nothing_for_edge_case_connect_strings` ([`:174`](../../../tests/ui/test_infobase_dialog.py#L174)) покрывает одиннадцать краевых строк соединения. Оба переписывать не нужно. Настоящая дыра другая: **ни один охранный тест не даёт записи `requested_version`** — а именно этот путь веха ставит под удар. `findData(item.requested_version)` при `-1` откатывает комбобокс на нулевой пункт с данными `None`, и `changes()` отдаёт `Version: None`, что **удаляет ключ** из файла, который делится со штатным стартером ([`infobase.py:495-499`](../../../src/onecstarter/ui/dialogs/infobase.py#L495), [`:1164-1166`](../../../src/onecstarter/ui/dialogs/infobase.py#L1164)).

Публичный доступ к комбобоксу клиента — `set_app` ([`:1121`](../../../src/onecstarter/ui/dialogs/infobase.py#L1121)), он бросает `ValueError` на значении, которого диалог не предлагает; на этом и строятся тесты. Существующий `test_set_app_rejects_a_value_the_dialog_never_offered` ([`:381`](../../../tests/ui/test_infobase_dialog.py#L381)) построен на **файловой** записи и после правки остаётся верным.

- [ ] **Step 1: написать падающие тесты**

```python
def test_web_record_accepts_web_client_app(qtbot: Any) -> None:
    item = _item('ws="http://srv/base";', ())
    dialog = InfobaseDialog(item, groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    dialog.set_app("WebClient")
    assert dialog.changes() == ({"App": "WebClient"}, None)


@pytest.mark.parametrize(
    ("connect", "kind_hint"),
    [
        ('File="D:\\b";', "file"),
        ('Srvr="s";Ref="r";', "server"),
        ('ws="http://srv/base";', "web"),
    ],
)
@pytest.mark.parametrize("version", ["8.3.25.1633", "8.3.99.1", "8.3"])
def test_untouched_dialog_with_version_writes_nothing(
    qtbot: Any, connect: str, kind_hint: str, version: str
) -> None:
    """Открыл свойства, нажал ОК — ключ Version остался в секции.

    Дыра, которую закрывает тест: ни один прежний охранный тест не давал
    записи `requested_version`, а именно на нём стоит риск вехи. Три значения
    покрывают три ветки `version_options`: установленная, не установленная
    и маска — у двух последних пункт добавляется отдельно, и без него
    `findData` вернул бы -1.
    """
    item = _item(connect, (), requested_version=version)
    dialog = InfobaseDialog(item, groups=["/"], installations=INSTALLED, cfg_rules=[])
    qtbot.addWidget(dialog)
    assert dialog.changes() == ({}, None), f"{kind_hint} / {version}"
```

`changes()` возвращает **пару** `(правки, новое имя)`, поэтому сверка идёт с `({}, None)` — как во всех существующих тестах модуля.

- [ ] **Step 2: прогнать, убедиться в падении**

Run: `uv run pytest tests/ui/test_infobase_dialog.py -q > e:\tmp\t6.log 2>&1; type e:\tmp\t6.log`
Ожидание: FAIL на `test_web_record_accepts_web_client_app` (`ValueError: WebClient`). Охранный `test_untouched_dialog_with_version_writes_nothing` должен пройти **уже сейчас**, на всех девяти сочетаниях. Если он падает — дефект в диалоге реален; разбирать его, а не подгонять ассерт.

- [ ] **Step 3: реализовать**

```python
_APP_ITEMS = (("Авто", None), ("Тонкий клиент", "ThinClient"), ("Толстый клиент", "ThickClient"))
_WEB_APP_ITEM = ("Веб-клиент (браузер)", "WebClient")
```

```python
        self._app = QComboBox()
        app_items = _APP_ITEMS
        if item is not None and item.kind is ConnectKind.WEB:
            # Пункт осмыслен только у ws-записи: у остальных `App=WebClient`
            # отвергается на запуске, и предлагать его — приглашать в отказ.
            app_items = (*_APP_ITEMS, _WEB_APP_ITEM)
        for text, data in app_items:
            self._app.addItem(text, data)
```

Ветка «незнакомое значение `App` сохраняем отдельным пунктом» не трогается: запись с `App=WebClient` и не-ws строкой соединения по-прежнему получит свой пункт через неё.

- [ ] **Step 4: прогнать**

Run: `uv run pytest tests/ui/test_infobase_dialog.py -q > e:\tmp\t6.log 2>&1; type e:\tmp\t6.log`
Ожидание: PASS.

- [ ] **Step 5: мутационная проверка охранного инварианта**

Временно заменить `self._version.setCurrentIndex(version_index if version_index >= 0 else 0)` на `self._version.setCurrentIndex(0)`.

Run: `uv run pytest tests/ui/test_infobase_dialog.py -k version_writes_nothing -q`
Ожидание: падают параметры с `8.3.99.1` и `8.3` (у `8.3.25.1633` нулевой пункт может совпасть). Записать фактические node id упавших параметров — это и есть доказательство, что охранный тест не пустышка. Откатить.

- [ ] **Step 6: гейт и commit**

Run: `uv run pytest -q > e:\tmp\t6-full.log 2>&1; type e:\tmp\t6-full.log` — PASS
Run: `uv run ruff check . && uv run mypy` — чисто

```bash
git add src/onecstarter/ui/dialogs/infobase.py tests/ui/test_infobase_dialog.py
git commit -m "feat(ui): в свойствах веб-записи появляется «Веб-клиент (браузер)»"
```

---

### Task 7: строка «Веб-базы открывать» в «Настройках»

**Files:**
- Modify: `src/onecstarter/ui/settings_view.py:80-88`, `:224-229`, `:446-452`, `:704-711`
- Test: `tests/ui/test_settings_view.py`

**Interfaces:**
- Consumes: `WebLaunch` (Task 2), `Workspace.set_web_launch` (Task 3).
- Produces: сегмент выбора, `SettingsView.web_launch_buttons`, `SettingsView._choose_web_launch`.

- [ ] **Step 1: написать падающий тест**

```python
def test_web_launch_row_writes_setting(qtbot: QtBot, tmp_path: Path) -> None:
    view, store = _settings_view(qtbot, tmp_path)
    qtbot.addWidget(view)
    view.web_launch_buttons[1].click()   # «В браузере»
    assert store.settings.web_launch is WebLaunch.BROWSER
    assert load_settings(store.path).web_launch is WebLaunch.BROWSER
```

`qtbot.addWidget` обязателен: `test_settings_view.py` фигурирует в T-12 п. 15 как место интермиттентного краха именно из-за незарегистрированных виджетов.

- [ ] **Step 2: прогнать, убедиться в падении**

Run: `uv run pytest tests/ui/test_settings_view.py -k web_launch -q`
Ожидание: FAIL, `AttributeError: web_launch_buttons`.

- [ ] **Step 3: реализовать**

```python
WEB_LAUNCH_CHOICES = (
    (WebLaunch.THIN, "Тонким клиентом"),
    (WebLaunch.BROWSER, "В браузере"),
)
```

Строка сразу после «Клиент по умолчанию»:

```python
        self._add_row(
            "Веб-базы открывать",
            "Для баз, опубликованных на веб-сервере. Выбор в записи (App) "
            "и «Открыть в браузере» главнее",
            self._build_web_launch_segment(),
        )
```

Подсказку «Клиент по умолчанию» сузить, чтобы две настройки не спорили:

```python
            "Чем запускать не-веб-базу, где клиент не указан. Выбор в записи (App) "
            "и Ctrl+1/Ctrl+2 главнее",
```

`_build_web_launch_segment`, `_choose_web_launch` и синхронизацию в `_sync` написать по образцу `_build_client_segment` / `_choose_client` ([`:446-452`](../../../src/onecstarter/ui/settings_view.py#L446), [`:704-711`](../../../src/onecstarter/ui/settings_view.py#L704)). `_choose_web_launch` обязан вызвать `set_web_launch` у `Workspace`, иначе настройка запомнится, но не подействует до перезапуска.

- [ ] **Step 4: гейт и commit**

Run: `uv run pytest -q > e:\tmp\t7.log 2>&1; type e:\tmp\t7.log` — PASS
Run: `uv run ruff check . && uv run mypy` — чисто

```bash
git add src/onecstarter/ui/settings_view.py tests/ui/test_settings_view.py
git commit -m "feat(ui): настройка «Веб-базы открывать»"
```

---

### Task 8: сборка вехи

**Files:**
- Modify: `docs/tasks.md` (раздел вехи: гейты, мутации, долг)
- Modify: `pyproject.toml` (версия `2.3.0`)

- [ ] **Step 1: полный прогон в файл**

Run: `uv run pytest -q > e:\tmp\v23-full.log 2>&1; type e:\tmp\v23-full.log`
Ожидание: PASS. При интермиттентном access violation (T-12 п. 15) — повторить и записать проявление в `docs/tasks.md`, не подгоняя вывод.

- [ ] **Step 2: линт и типы**

Run: `uv run ruff check . && uv run mypy`
Ожидание: чисто.

- [ ] **Step 3: записать результаты мутационных проверок**

В `docs/tasks.md`, раздел вехи: для каждой из четырёх мутаций (Task 3 шаги 6–7, Task 4 шаг 4, Task 6 шаг 5) — что ломали, какой тест упал и **на чём именно**. Тест, зелёный на пустышке, хуже отсутствующего.

- [ ] **Step 4: поднять версию**

`pyproject.toml`: `version = "2.3.0"` — единственный источник правды о версии.

- [ ] **Step 5: commit**

```bash
git add docs/tasks.md pyproject.toml
git commit -m "chore: веха v2.3 — мутационные проверки записаны, версия 2.3.0"
```

---

### Task 9: замеры, которые закрывают метки [Д]

Не кодовая: выполняет заказчик, агент готовит протокол и записывает результат. Без неё веха закрывается с четырьмя непроверенными местами — и это должно быть видно, а не забыто.

**Files:**
- Modify: `docs/research/t05-16-web-server-launch.md` (раздел «Осталось непроверенным»)
- Modify: `.claude/skills/platform-launch/SKILL.md` (факт 14 и раздел о `/N`/`/P`)

- [ ] **Step 1: подготовить протокол четырёх замеров**

1. **Вход** по перенесённым `/N`/`/P` при удвоенном `/N` — на реальном пользователе серверной базы. Главный: на нём стоит работоспособность авторизации v2.2 для веб- и серверных баз. Пароль в файл результатов не пишется (образец — `t05-15-launch-matrix.ps1`).
2. `App=ThickClient` на веб-базе — наш отказ построен на **[Д]**.
3. `App=WebClient` на веб-базе у штатного стартера.
4. Материализация **ws**-записи из общего списка (T-05.2 снят на файловой).

- [ ] **Step 2: снять замеры и записать результат**

Подтверждённое повышается до **[Ф]**, опровергнутое исправляется в скиле, спеке и коде.

- [ ] **Step 3: commit**

```bash
git add docs/research/t05-16-web-server-launch.md .claude/skills/platform-launch/SKILL.md
git commit -m "docs: замеры T-05.16, закрывающие метки [Д] вехи v2.3"
```

---

## Самопроверка плана

**Покрытие спеки.** §1 → Task 3. §2 → Task 1 (`LaunchTarget`) + Task 3 (протяжка по трём слоям одним коммитом). §3 → Task 1. §4 → Task 3 (полярность), Task 5 (честная пометка в UI). §5 → Task 2, 3, 7. §6 → Task 6 (свойства), Task 3 (меню и хоткеи). §7 → Task 3 (`version=None`); показ недостоверных `pid`/`command_line` отдельного пункта не требует — наружу они не выводятся, а единственное место, где мы показывали версию, закрывает Task 5. §8 → отражён в спеке как принятое поведение, кода не требует. §9 → Task 0 и шаги «удалить/переписать» в Task 3, 5. §10 → мутации в Task 3, 4, 6 и сборка в Task 8; замеры — Task 9. §11 → выполнено коммитом `288c9d6`. §12 → Task 9.

**Промежуточных красных состояний нет.** Сквозной канал `forced` меняется целиком внутри Task 3, и её коммит — единственный, где несогласованность типов вообще возможна; гейт Step 10 (весь suite + `ruff` + `mypy`) стоит до него. Остальные задачи трогают по одному слою и не меняют ни одной сигнатуры, видимой другому слою: Task 4 — тело `_reject_ambiguous_name`, Task 5 — `display.py`, Task 6 — `dialogs/infobase.py`, Task 7 — `settings_view.py`. Дерево зелёное после каждого коммита, и ревьюер может принять любую задачу отдельно.

**Заглушек нет:** каждый шаг несёт либо код, либо точную команду с ожидаемым результатом.

**Согласованность имён:** `LaunchTarget`, `RefusalReason`, `LaunchPlan`, `LaunchRefusal`, `choose_launch_plan`, `web_default_is_browser`, `forced_target`, `set_web_launch`, `WebLaunch.is_browser` определены в Task 1–2 и употребляются дальше в том же написании. Параметр `Workspace.launch` называется `forced`, параметр `launch_infobase` — `forced_target`: имена разные, **тип один** — `LaunchTarget | None`.

**Существующие тесты, названные поимённо:** `test_f4_does_nothing_for_web_base`, `test_duplicate_name_does_not_block_web_base` (Task 0); `test_forced_designer_uses_thick_executable`, `test_web_base_opens_browser`, `test_web_base_reports_failed_browser`, `test_ctrl_1_on_web_base_does_nothing`, комментарий `test_bases_view.py:906-914`, докстринг `:3613-3618` (Task 3); тест «…» при `discovery_pending` (Task 5). Не трогаются: `test_untouched_dialog_reports_no_changes` (:130, серверная запись), `test_untouched_dialog_writes_nothing_for_edge_case_connect_strings` (:174), `test_set_app_rejects_a_value_the_dialog_never_offered` (:381, файловая запись).
